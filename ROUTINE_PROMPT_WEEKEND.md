# luckyon 브리핑 — 주말 절차서 (토: 주간 결산 / 일: 다음 주 일정)

> **루틴 설정 방법**: claude.ai/code/routines 에 아래 **두 개**를 등록합니다. Instructions 칸에는 두 줄만 넣습니다.
> 각 루틴이 **한국어·영어 카드를 모두 만들어 각각 발행**하므로 주말 게시물은 총 4건이지만, 세션은 2번입니다.
>
> **① 토요일 주간 결산** — 매주 토요일 **07:35 KST** (금요일 미국장 마감 직후, 발행 약 08:00)
> ```
> 저장소의 ROUTINE_PROMPT_WEEKEND.md 파일을 읽고, 그 절차를 순서대로 그대로 수행하세요.
> SESSION = sat
> ```
>
> **② 일요일 다음 주 일정** — 매주 일요일 **20:30 KST** (발행 약 21:00, 월요일 대비)
> ```
> 저장소의 ROUTINE_PROMPT_WEEKEND.md 파일을 읽고, 그 절차를 순서대로 그대로 수행하세요.
> SESSION = sun
> ```
>
> **`LANG` 을 지정하지 않습니다.** 예전에는 한국어·영어 루틴을 따로 돌렸지만, 그 방식은 나중 언어가 먼저 언어의 검증을 신뢰하고 재검증을 건너뛰는 실패 모드가 있었습니다(평일에서 2026-07-21 실제 사고). 지금은 세션 하나가 리서치·검증을 한 번만 하고 두 언어 카드를 모두 만듭니다.

---

## 이 문서의 범위

**이 문서는 주말 브리핑(sat·sun)이 "무엇을 조사하고 무엇을 쓸지"만 정한다.** 나머지는 아래 문서들에 있고, 이 세션은 **넷을 모두 따른다.**

| 문서 | 담당 |
|---|---|
| `ROUTINE_COMMON.md` | 준비·검증·커밋·발행·알림·정정 — **모든 축 공통 절차** |
| `FORMAT_BRIEFING.md` | 카드 9장 구성·섹션 유형·훅 카드·JSON 스키마·분량 — **평일·주말 공용 양식** |
| `ROUTINE_PROMPT.md` | **시세 조회 방법**(타일별 도구·티커·함정)·**섹터 ETF 티커 목록**·**정기 발표 체크리스트** — 평일 절차서의 리서치 A절과 예비 지표절을 주말도 그대로 공유한다 |
| 이 문서 | 주말 브리핑의 리서치와 콘텐츠 구성 |

### 0단계 (필수)

**리서치를 시작하기 전에 `ROUTINE_COMMON.md`·`FORMAT_BRIEFING.md`·`ROUTINE_PROMPT.md` 를 먼저 읽는다.** 세션이 아는 것은 문서뿐이라, 안 읽으면 그 안의 규칙은 없는 것과 같다. 실제로 2026-08-04 아침 세션이 양식 문서를 못 본 채 발행해 데이터 카드가 한 장도 안 들어간 사고가 있었다.

그다음 `ROUTINE_COMMON.md` §1 의 준비(의존성·날짜·중복 실행 가드)를 수행하고, 루틴 지시문의 `SESSION`(sat|sun)을 확인한다.

**주말은 평일과 같은 렌더러·같은 경로·같은 스키마를 쓴다.** 카드 9장 구성도 같고, **각 칸에 채우는 내용만 다르다.** 예전에는 주말 절차서가 `econ`·`ai` 6+6 구버전 구성을 그대로 안내하고 있었는데, 그 필드는 하위 호환으로만 남아 있으므로 **쓰지 않는다.**

---

## 세션별 커버 범위

| | 토요일 (sat) — 주간 결산 | 일요일 (sun) — 다음 주 일정 |
|---|---|---|
| 발행 목표 시각 | 08:00 KST | 21:00 KST |
| 커버 범위 | 지난 월요일 ~ 금요일 (미국장은 금요일 마감까지) | 다음 주 월요일 ~ 금요일 예정 일정 |
| 성격 | "이번 주 무슨 일이 있었나" | "다음 주 무엇을 봐야 하나" |
| 훅 카드 | `summary` (`FORMAT_BRIEFING.md` §3.1) | `hook_bull`/`hook_bear` (§3.2) |

- **sat 이 `summary` 를 쓰는 이유**: 호재·악재는 앞으로 벌어질 일의 '재료'를 가리키는 말이라, 이미 끝난 한 주의 결과를 그렇게 가르면 어색하다. 평일 am·pm 과 같은 이유다 (2026-08-04 결정).
- **sun 만 `hook_bull`/`hook_bear` 를 쓴다.** 아직 결과가 없는 회차라 기대/경계로 가르는 것이 자연스럽다. 렌더러가 `sun` 에서 라벨을 '기대 요인 / 경계 요인'으로 바꿔 출력한다.

아래를 순서대로 수행하세요.

---

## 리서치

이 리서치 한 번으로 한국어·영어 카드를 모두 만든다.
**시세·지표·실적 일정은 데이터 소스에서 직접 조회하고, WebSearch·뉴스 검색은 데이터로 얻을 수 없는 것(사건 배경, 발표문 찾기, Fear&Greed)에만 보조로 쓴다.** 지난 한 주의 평일 콘텐츠(`content/` 의 이번 주 am/pm JSON)를 먼저 읽으면 흐름을 빠르게 잡을 수 있지만, **주간 등락률과 일정은 반드시 다시 조회한다.**

### A. 시장 지표 (markets 10개 타일)

**평일과 완전히 동일한 10개를 같은 순서로 쓴다** — `나스닥` · `S&P 500` · `코스피` · `VIX` · `Fear&Greed` · `미 10년물 금리` · `원/달러(달러인덱스 DXY 병기)` · `금` · `WTI 원유` · `비트코인`.

축마다 타일을 바꾸면 독자가 매번 다시 읽어야 하고, 주 5일과 주말이 다른 계정처럼 보인다. 조회 방법·주의사항(Yahoo 종가 함정, 순차 호출, 근사치 금지, 휴장 처리)은 **`ROUTINE_PROMPT.md` 의 리서치 A 절을 그대로 따른다.** 주말 전용 차이는 아래뿐이다.

- **`value` 는 금요일 마감가**다. sat·sun 모두 같은 값이며, sun 은 `market_note` 에 "금요일 마감 기준"을 밝힌다.
- **`delta` 는 주간 등락률**이다. `▲ +2.1%` 처럼 기호+숫자로만 쓰고(언어 무관 필드다 — `FORMAT_BRIEFING.md` §4), "주간"이라는 말은 `note_ko`/`note_en` 이나 `market_note` 에 넣는다. `dir` 도 주간 방향으로 맞춘다.
- **주간 등락률은 직접 계산한다.** `get_historical_stock_prices` 를 `period:"1mo"`, `interval:"1d"` 로 불러 **이번 주 금요일 종가 ÷ 지난주 금요일(직전 주 마지막 거래일) 종가**로 구한다. `period:"5d"` 는 휴장이 낀 주에 기준일이 밀리므로 쓰지 않는다.
- 비트코인·금·WTI 처럼 주말에도 값이 움직이는 자산은 **금요일 마감 시점 값으로 통일한다.** 토·일 사이 변동을 섞으면 다른 타일과 기준이 달라진다. 주말 사이 큰 변동이 있었다면 그 사실은 `note` 한 줄로 적는다.

### B. 본문 카드 구성 (`sections` 5장)

평일과 같은 원칙이다 — **뉴스 기사를 옮겨 적지 않는다.** 시세·지표·공시·1차 발표문에서 **계산된 관찰**을 뽑는다. 주말은 한 주(또는 다음 주) 단위라 평일보다 이런 재료가 오히려 많다.

> 나스닥 주간 +1.2%로 3주 연속 상승. 3주 누적 +5.8%는 4월 이후 최대.
> 12개 섹터 중 9개가 올랐는데 에너지만 -3.4%로, 지수 상승과 반대로 움직인 유일한 축이었다.

**`type` 은 아래 표대로 쓴다. 비워 두지 않는다** — 유형별 필드는 `FORMAT_BRIEFING.md` §2 에 있다.

| | `sat` (주간 결산) | `sun` (다음 주 일정) |
|---|---|---|
| ③ | **이번 주 지수 기록** `type:"stats"` `cols:1` `#e66767` — 나스닥·S&P 500·코스피 주간 등락 큰 숫자 + 주간 고저·연속 주 기록을 `sub` 로 | **다음 주 경제 일정** `type:"stats"` `cols:2` `#e66767` — 발표 예정 지표 4~6개, `value`=컨센서스, `sub`=이전치·발표 시각(KST) |
| ④ | **주간 섹터 등락** `type:"bars"` `#e0a94f` — 미국 코어 섹터 ETF 12개의 주간 등락 | **다음 주 실적 발표** `type:"stats"` `cols:2` `#3987e5` — 대형주 4~6개, `value`=예상 EPS, `sub`=발표일·장전/장후 |
| ⑤ | **이번 주 승자·패자** `type:"rank"` `#3987e5` — 주간 등락률 또는 주간 거래대금 상위 (아래 참고) | **다음 주 출발선** `type:"bars"` `#e0a94f` — 금요일 마감 기준 섹터별 주간 등락. 다음 주를 어디서 출발하는지 보여준다 |
| ⑥ | **AI · 반도체 기술** (`type` 없음) `#9085e9` — 이번 주 AI 소식 | **AI · 반도체 기술** (`type` 없음) `#9085e9` — 다음 주 예정 이벤트 + 주말 사이 나온 소식 |
| ⑦ | **이번 주 주요 소식** (`type` 없음) `#5aa469` — 한 주를 움직인 사건 | **놓치면 안 될 것** (`type` 없음) `#5aa469` — 정책·지정학 일정, 만기·수급 이벤트 |

- **⑥⑦만 글 카드다** (`items` 배열). ③④⑤는 수치 카드이며, **수치 카드가 최소 두 장은 되어야 한다.** 그 주 재료가 표의 성격과 안 맞으면 유형을 바꿔도 되지만 전부 글 카드가 되면 안 된다.
- **`sun` 도 수치 카드를 쓸 수 있다.** 앞으로 벌어질 일이라 숫자가 없다고 생각하기 쉽지만, **컨센서스·이전치·예상 EPS 는 이미 확정된 숫자**다. 아직 안 나온 실측치 자리를 비워두고 `sub` 에 이전치를 넣으면 그대로 데이터 카드가 된다. 이때 `delta` 는 비운다 — 실측치가 없는데 등락을 적으면 이미 발표된 것처럼 읽힌다.

#### ③ 이번 주 지수 기록 (sat)

- `value` 는 금요일 종가, `delta` 는 주간 등락률, `sub` 에 계산된 관찰을 넣는다 (예: `"주간 고점 6,712 · 3주 연속 상승"`).
- 한국 지수의 주간 기록은 `node scripts/fetch-krx.mjs <금요일 날짜>` 의 `indexes.*.records.weekToDate` · `.streakDays` · `.range20d` 로 바로 나온다.
- `breadth` 로 **주간 상승/하락 종목 수**를 함께 보여주면 지수만으로 안 보이는 것이 드러난다.

#### ④ 주간 섹터 등락 (sat) / ⑤ 다음 주 출발선 (sun)

- 미국 코어 섹터 ETF 12개(`SMH` `XLK` `XLC` `XLF` `XLY` `XLV` `XLI` `XLE` `XLP` `XLB` `XLU` `XLRE`)를 `get_historical_stock_prices` `period:"1mo"` 로 받아 **주간 등락률을 직접 계산한다.** 티커 목록과 테마 ETF, `FMP sector-performance-snapshot` 을 쓰지 않는 이유는 `ROUTINE_PROMPT.md` 의 예비 지표 절에 있다.
- 한국 업종으로 바꿔도 된다 — `fetch-krx.mjs` 의 `industry` 가 주력 15개 업종을 시총 가중으로 준다. 다만 **한 카드에 두 나라를 섞지 않는다.** 섞으면 기준 통화·거래일이 달라 막대 길이 비교가 무의미해진다.
- `bars` 라벨은 **한글 8자·영문 15자 안쪽**이다. 넘으면 잘린다 (`FORMAT_BRIEFING.md` §6).

#### ⑤ 이번 주 승자·패자 (sat)

`rank` 는 `rows:[{name_ko/en, value, pct}]` 다. 아래 중 그 주에 이야깃거리가 있는 쪽을 고른다.

- **주간 등락률 상위** — `value` 에 금요일 종가, `pct` 에 주간 등락률. `pct` 는 부호에 따라 자동으로 색이 붙는다.
- **주간 거래대금 상위** — `value` 에 금액(`8.6조`), `pct` 에 주간 등락률. `share` 로 상위 종목이 시장 전체에서 차지한 비중 띠를 함께 넣으면 쏠림이 보인다.
- 상승만 5개 늘어놓지 말고 **오른 쪽과 내린 쪽을 함께** 담는다. 한 주의 승자만 보여주면 그 주가 실제로 어땠는지가 안 보인다.

#### ⑥ AI · 반도체 기술 (양 세션 공통)

이 계정은 브랜드에서 "경제와 AI"를 약속하므로 AI 축이 매 회차 한 장 있어야 한다. **주가 나열이 아니라 AI 자체의 소식을 담는다** — 모델·툴 출시와 버전, HBM·GPU·TPU 등 반도체 기술과 표준, AI 인프라·데이터센터 투자, AI 규제·정책. AI 종목 등락만 순위로 늘어놓는 카드는 ③④와 겹치고 AI에 대해 아무것도 말해주지 않으므로 만들지 않는다.

- `sat`: 이번 주에 실제로 나온 것. 주중 평일 브리핑에서 이미 다룬 항목은 **한 주 관점으로 다시 묶을 때만** 넣는다 (같은 문장을 그대로 옮기지 않는다).
- `sun`: 다음 주 예정된 AI·반도체 이벤트(컨퍼런스, 출시 예정, 반도체 기업 실적)와 **주말 사이 나온 소식**. 주말은 평일 브리핑이 없어 토요일 낮~일요일 저녁 사이 소식이 통째로 비는 구간이다 — 이 카드와 ⑦이 그 자리를 메운다.
- 뉴스 기반이라 `ROUTINE_COMMON.md` §3 검증의 B(정확성)·C(최신성)를 특히 엄격히 적용한다.

#### ⑦ 주요 소식 (sat) / 놓치면 안 될 것 (sun)

- **지역으로 못박지 않는다.** 한국을 우선하되 지역 구분 없이 그 범위에서 나온 굵직한 것을 모은다. 제목을 지역으로 적으면 그 제목이 곧 수집 범위를 가둬 다른 좋은 재료를 놓치게 된다 (2026-08-04 평일에서 확인된 문제).
- **채우기 전에 반드시 실제로 검색한다. 최소 두 번** — 한 번은 한국 쪽(`"주간 증시"`·`"코스피 주간"`), 한 번은 해외(`"뉴욕증시 주간"`·`"연준"`·`"중동"` 등). `mcp__PlayMCP__NaverSearch-search_news` 에 `sort:"date"` 로 훑고, 여러 매체가 반복해 언급하는 그 주의 동인을 찾는다.
- **시장을 움직인 원인은 결과보다 먼저 싣는다.** markets 타일이 어떤 사건 때문에 움직였다면 그 사건이 본문 어딘가에 반드시 있어야 한다.
- `sat` 은 `time` 에 발생 시점을 적는다. **`time` 은 언어 무관 필드라 두 언어 카드에 그대로 박히므로** `"화요일"`·`"8/5(화)"` 같은 한국어를 쓰지 않는다. `8/5` 처럼 숫자만 쓰거나, 요일이 꼭 필요하면 `8/5 Tue` 로 쓴다.
- `sun` 은 다음 주 확정 일정 중 ③④에 안 들어간 것을 담는다 — 옵션·선물 만기(쿼드러플 위칭), 지수 정기변경, 대선·의회 일정, 국제회의, 주요국 휴장일.
- **한국은행 기준금리 결정(금통위)은 절대 놓치지 않는다.** 그 주에 있었으면 sat ⑦ 첫 항목, 다음 주에 예정돼 있으면 sun ⑦ 첫 항목에 넣는다 (③은 수치 카드라 문장 항목을 담지 못한다).

### C. 일정 리서치 (schedule 4~6건)

**두 세션 모두 다음 주 일정을 담는다.** 다만 깊이가 다르다.

- `sat`: **가장 굵직한 것만** 미리 보기로. 상세는 일요일 브리핑이 다루므로 여기서 다 쓰지 않는다.
- `sun`: 다음 주 일정 중 **시장 영향이 가장 큰 TOP 픽**. ③④ 카드와 겹쳐도 된다 — 이 카드는 "이것만은 캘린더에 적어두세요" 역할이다.

- **`time` 은 요일+시각(KST)** — `월 22:30` 이 아니라 **언어 무관 필드이므로 숫자로** `8/10 22:30` 처럼 쓴다. 요일을 넣고 싶으면 `8/10 Mon 22:30`.
- 시장 영향이 큰 일정은 `"importance": "high"`(카드에 "주목" 배지), 나머지는 `"mid"`.
- **`schedule_title` 은 `"주요 일정"` / `"Key Schedule"` 로 고정이다.** 세션별로 바꾸지 않는다 (`FORMAT_BRIEFING.md` §5). 예전 주말 절차서의 "다음 주 미리 보기"·"다음 주 TOP 픽"은 쓰지 않는다 — "다음 주"라는 맥락은 `market_hours` 박스와 각 항목의 날짜가 이미 알려준다.

**데이터 소스** (평일과 동일):

| 무엇 | 조회 |
|---|---|
| 경제지표 일정 | `git fetch origin main -q && git show origin/main:data/econ-calendar.json > /tmp/econ.json` — 없거나 낡으면 `node scripts/fetch-econ-calendar.mjs 1 10 > /tmp/econ.json` |
| 실적 일정·컨센서스 | FMP `calendar` → `earnings-calendar`, `from_date`/`to_date` 로 다음 주 지정. `epsEstimated`·`revenueEstimated` |
| 이번 주 실적 실제치 (sat) | 같은 도구, 지난 주 날짜로. `epsActual`·`revenueActual` |
| 한국 지수·업종·거래대금 | `node scripts/fetch-krx.mjs <금요일 날짜>` |
| 미국 지수·ETF·환율·원자재 | PlayMCP `UsStockInfo-get_historical_stock_prices` |
| 미 국채금리 | FMP `economics` → `treasury-rates` |

- `econ-calendar.json` 의 `at` 은 **UTC** 다. KST 로 환산해 쓴다(+9시간). `impact` 가 `HIGH` 인 것을 우선하고 `LOW`(국채 입찰·주간 재고)는 걸러 쓴다. **반드시 `origin/main` 에서 읽는다** — 워크플로가 main 에 커밋하므로 작업트리 파일은 낡았을 수 있다.
- **캘린더 워크플로는 주말 세션 직전에도 돈다** (토 07:20 / 일 20:10 KST). 그러니 평일과 똑같이 `fetchedAt` 이 12시간을 넘으면 낡은 것으로 보고 2순위로 내려간다. 다만 2순위(세션 내 스크립트)는 **미국 지표만 나오고 이전치가 없어** ③ `stats` 카드의 `sub` 를 채울 수 없으므로, 1순위가 비었으면 그 사실을 마무리 보고에 적는다 — 워크플로가 안 돌았다는 신호다.
- 미국장 마감 후 발표되는 실적은 그 **미국 날짜**로 조회한다 (예: 8/12 06:00 KST 발표 = 8/11 미국 날짜).
- 캘린더가 실패하면 `ROUTINE_PROMPT.md` 의 정기 발표 체크리스트로 채운다. **같은 날 비슷한 지표가 둘 이상 나오는 경우가 많으니(S&P 글로벌 PMI 와 ISM PMI) 하나만 넣고 끝내지 않는다.**

### D. 훅 카드 작성 (sun 전용 주의사항)

`sat` 은 `summary` 를 쓴다 — 작성 요령은 `FORMAT_BRIEFING.md` §3.1 에 있고, 한 주 관점으로 쓰는 것만 다르다 (결과 → 무엇이 그렇게 만들었나 → 수급 → 반대로 움직인 것).

`sun` 의 `hook_bull`/`hook_bear` 는 **아직 일어나지 않은 일**을 다루므로 "확실한 사실만"이라는 기준을 그대로 적용할 수 없다. 대신 셋을 지킨다:

1. **일정 자체가 확정된 것만 쓴다** — 발표일이 공시된 지표, 확정된 실적 발표일. "~할 것으로 보인다" 수준의 미확정 이벤트는 넣지 않는다.
2. **기대·경계의 근거가 구체적이어야 한다** — 컨센서스 전망치, 직전 발표 수치, 회사 가이던스. "시장이 기대하고 있습니다" 같은 근거 없는 서술은 쓰지 않는다.
3. **단정적 예측을 하지 않는다.** 결과를 미리 규정하지 말고 무엇이 관건인지를 쓴다.
   - ✕ "수요일 CPI가 둔화되며 금리 인하 기대가 커질 것입니다"
   - ○ "수요일 CPI — 직전 3.1%에서 둔화되는지가 다음 주 금리 기대의 분기점입니다"

한쪽이 뚜렷하지 않으면 그쪽은 비워둔다 — 한쪽만 있으면 렌더러가 `headline_sub` 를 함께 노출한다.

**`ROUTINE_COMMON.md` §3 검증에 훅 카드도 함께 넘긴다.** 첫 카드라 오류 시 임팩트가 가장 크다. 다만 판정 범위를 지정한다:
- `sat`: 인용한 **사실(수치·날짜·사건)** 이 맞는지만. 인과 해석의 타당성은 판정시키지 않는다.
- `sun`: ⓐ 그 일정이 **실제로 그 날짜에 예정돼 있는지**(공식 발표 일정·기업 IR 공지 기준), ⓑ 인용한 **직전 수치·컨센서스·가이던스가 맞는지** 두 가지만. **전망 자체의 타당성은 검증 대상이 아니다** — 정답이 없는 판정을 시키면 검증 결과 전체가 흐려진다.

---

## 리서치가 끝나면

1. **`ROUTINE_COMMON.md` §3 의 발행 전 통합 검증(A~E)** 을 반드시 거친다. 통과 전에는 콘텐츠 JSON 을 확정하지 않는다. sun 세션은 C(최신성) 판정에 **"다음 주 ~예정"이 검증 시점에도 사실인지**가 포함된다 — 일정은 변경·연기가 잦다.
2. **`FORMAT_BRIEFING.md` §5 스키마**로 `content/<DATE>-<SESSION>.json` 을 저장한다. `content/` 의 가장 최근 파일을 예시로 참고하되 반드시 이번 세션 내용으로 새로 만든다. **`econ`·`ai` 필드는 쓰지 않는다** (렌더러가 하위 호환으로 남겨둔 것일 뿐).
   - **주말 전용 고정 값**(`market_hours`·`next_brief`·`outro_tagline`)은 `FORMAT_BRIEFING.md` §5 의 고정 문구 절에 있다.
   - `headline`: sat = 이번 주 최대 이슈 요약 / sun = 다음 주 최대 관전 포인트.
   - `caption`: sat 은 "한 주 결산"임을, sun 은 "다음 주 일정"임을 첫 줄에 밝힌다.
   - `sun` 세션은 먼저 같은 주 `sat` 파일을 읽고 **토요일에 이미 다룬 항목을 그대로 반복하지 않는다.**
3. **`FORMAT_BRIEFING.md` §6** 대로 ko·en 카드를 렌더링하고 9장 전부 분량 초과를 눈으로 확인한다. 훅 카드는 sat 이면 `summary` 불릿이, sun 이면 기대/경계 블록이 보이는 것이 정상이다.
4. **`ROUTINE_COMMON.md` §4~§7** 대로 커밋·푸시 → 토큰 점검 → Instagram 발행 → 카카오톡 알림 → 마무리 보고.
