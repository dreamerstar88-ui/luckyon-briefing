# luckyon 브리핑 — 주말 절차서 (토: 주간 결산 / 일: 다음 주 일정)

> **루틴 설정 방법**: claude.ai/code/routines 에 아래 **두 개**를 등록합니다. Instructions 칸에는 두 줄만 넣습니다.
> 각 루틴이 **한국어·영어 카드를 모두 만들어 각각 발행**하므로 주말 게시물은 총 4건이지만, 세션은 2번입니다.
>
> **① 토요일 주간 결산** — 매주 토요일 **07:35 KST** (금요일 미국장 마감 직후, 발행 약 08:00)
> ```
> 저장소의 ROUTINE_PROMPT_WEEKEND.md 파일을 읽고, 그 절차를 순서대로 그대로 수행하세요.
> SESSION = sat
> ```
>
> **② 일요일 다음 주 일정** — 매주 일요일 **20:30 KST** (발행 약 21:00, 월요일 대비)
> ```
> 저장소의 ROUTINE_PROMPT_WEEKEND.md 파일을 읽고, 그 절차를 순서대로 그대로 수행하세요.
> SESSION = sun
> ```
>
> **`LANG` 을 지정하지 않습니다.** 예전에는 한국어·영어 루틴을 따로 돌렸지만, 그 방식은 나중 언어가 먼저 언어의 검증을 신뢰하고 재검증을 건너뛰는 실패 모드가 있었습니다(평일에서 2026-07-21 실제 사고). 지금은 세션 하나가 리서치·검증을 한 번만 하고 두 언어 카드를 모두 만듭니다.

---

## 이 문서의 범위

**이 문서는 주말 브리핑(sat·sun)이 "무엇을 조사하고 무엇을 쓸지"만 정한다.** 나머지는 아래 문서들에 있고, 이 세션은 **넷을 모두 따른다.**

| 문서 | 담당 |
|---|---|
| `ROUTINE_COMMON.md` | 준비·검증·커밋·발행·알림·정정 — **모든 축 공통 절차** |
| `FORMAT_BRIEFING.md` | 카드 구성·JSON 스키마·분량. **§2-A 가 sat, §2-B 가 sun 전용**이고 §4·§6·§7 은 전 세션 공용 |
| `ROUTINE_PROMPT.md` | **시세 조회 방법**(타일별 도구·티커·함정)·**섹터 ETF 티커 목록**·**정기 발표 체크리스트** — 평일 절차서의 리서치 A절과 예비 지표절을 주말도 그대로 공유한다 |
| `DATA_SOURCES.md` | **어떤 데이터 소스가 되고 안 되는지의 기록** — egress 함정, 세션↔러너 비대칭, 지수 PER/PBR 결론 |
| 이 문서 | 주말 브리핑의 리서치와 콘텐츠 구성 |

### 0단계 (필수)

**리서치를 시작하기 전에 `ROUTINE_COMMON.md`·`FORMAT_BRIEFING.md`·`ROUTINE_PROMPT.md` 를 먼저 읽는다.** 데이터 조회가 막히면 `DATA_SOURCES.md` 를 본다 — 이미 시도해 실패한 경로를 다시 파지 않도록. 세션이 아는 것은 문서뿐이라, 안 읽으면 그 안의 규칙은 없는 것과 같다. 실제로 2026-08-04 아침 세션이 양식 문서를 못 본 채 발행해 데이터 카드가 한 장도 안 들어간 사고가 있었다.

그다음 `ROUTINE_COMMON.md` §1 의 준비(의존성·날짜·중복 실행 가드)를 수행하고, 루틴 지시문의 `SESSION`(sat|sun)을 확인한다.

**주말은 sat·sun 둘 다 고정 10장 편성에 전용 렌더러를 쓴다** — 토요일이 2026-08-08 에
(`FORMAT_BRIEFING.md` §2-A), 일요일이 2026-08-09 에 (`§2-B`) 평일 편성에서 갈라져 나왔다.
실행 명령은 셋 다 같다 (`render-cards.mjs` 가 sat·sun 을 알아서 넘긴다).

**주말에서 쓰지 않는 필드** — `sections` · `summary` · `hook_bull`/`hook_bear` · `markets` ·
`schedule` · `market_hours` · 구버전 `econ`/`ai` 6+6 최상위 배열. 넣어도 주말 렌더러가 읽지 않아
**오류 없이 조용히 사라진다** (반대로 `cover` 를 빠뜨리면 멈춘다). (일요일의 `econ`·`ai` 는 §2-B 의 카드 ④·⑦ 전용 «객체»로 이름만 같다.)

---

## 세션별 커버 범위

| | 토요일 (sat) — 주간 결산 | 일요일 (sun) — 다음 주 일정 |
|---|---|---|
| 발행 목표 시각 | 08:00 KST | 21:00 KST |
| 커버 범위 | 지난 월요일 ~ 금요일 (미국장은 금요일 마감까지) | 다음 주 월요일 ~ 금요일 예정 일정 |
| 성격 | "이번 주 무슨 일이 있었나" | "다음 주 무엇을 봐야 하나" |
| 카드 편성 | **고정 10장** (`FORMAT_BRIEFING.md` §2-A) | **고정 10장** (§2-B) |
| 첫 카드 | 표지 — `cover{}` | 표지 — `cover{}` |
| 렌더러 | `render-cards-sat.mjs` (자동 위임) | `render-cards-sun.mjs` (자동 위임) |
| 강조색 | 딥그린 `#2f5d50` | 딥인디고 `#4b4180` |

- **주말은 평일 편성을 쓰지 않는다.** 한 주 정리(sat)와 다음 주 예고(sun)를 평일과 똑같이 내면
  그 회차의 의미가 사라진다는 판단이었다. 토요일은 달력형 발표 결과·봉차트·대형주 2단을,
  일요일은 요일 그리드 캘린더·컨센서스 대비를 갖는다 — 둘 다 «주 단위»에서만 성립하는 카드다.
- **바탕은 같고 강조색만 다르다.** 주말이 한 가족으로 읽히되 토·일은 구분된다.
- **화살표(→)는 두 회차를 잇는 시각 언어다.** 토요일 ⑤가 «예상 → 실제»로 결과를 보여주고,
  일요일 ④⑤가 «직전치 → 컨센서스»·«전년 동기 → 컨센서스»로 그 앞면을 보여준다.


**두 세션은 내용을 공유하지 않는다.** 커버 범위가 겹치지 않으니(지난 주 결과 vs 다음 주 예정)
같은 항목이 양쪽에 실릴 일이 없고, 편성·렌더러·스키마도 위 표대로 갈린다.
**공유하는 것은 «조회·집필 기법» 뿐이다** — AI 카드에 무엇을 담는지, 뉴스를 어떻게 수집하는지 같은
방법론. 이것들은 §B2 에 한 번만 적어 두고 양쪽이 참조한다.
방법이 같다고 실리는 내용이 같아지는 것은 아니다.

아래를 순서대로 수행하세요.

---

## 리서치

이 리서치 한 번으로 한국어·영어 카드를 모두 만든다.
**시세·지표·실적 일정은 데이터 소스에서 직접 조회하고, WebSearch·뉴스 검색은 데이터로 얻을 수 없는 것(사건 배경, 발표문 찾기, Fear&Greed)에만 보조로 쓴다.** 지난 한 주의 평일 콘텐츠(`content/` 의 이번 주 am/pm JSON)를 먼저 읽으면 흐름을 빠르게 잡을 수 있지만, **주간 등락률과 일정은 반드시 다시 조회한다.**

### A0. 토요일(sat) 재료 지도 — **sat 이면 여기부터 읽는다**

토요일은 카드 10장이 각자 전용 필드를 읽는다. 필드 이름과 스키마는 `FORMAT_BRIEFING.md` §2-A 에 있고,
여기서는 **각 칸을 무엇으로 채우는지**만 정한다. 표의 **§B2 기법 N** 은 «조회 방법»을 공용한다는 뜻이고, 실을 내용은 이 표가 정한다.

| 카드 | 필드 | 무엇으로 채우나 |
|---|---|---|
| ① 표지 | `cover` | 큰 수치 하나(그 주 최대 이슈의 실측치) + 요약 3줄 + 지수 3칸. **요약 3줄은 뒤 카드에 실제로 있는 수치만 쓴다** — 표지에만 있는 숫자는 검증을 빠져나간다 |
| ② 지수 등락 | `indexes`, `valuation` | **`node scripts/fetch-sat-indexes.mjs` 출력의 `indexes` 를 그대로 복사**하고 `note_ko`/`note_en` 만 쓴다 (**§B2 기법 1**). 종가·주간 등락률·이동평균·고점·RSI 가 다 계산돼 있다 |
| ③ 주간 차트 | `indexes[].ohlc` | ②와 **같은 배열을 렌더러가 다시 읽는다.** 따로 준비할 것이 없다 |
| ④ 지수 외 지표 | `metrics` | 10칸 고정. 아래 A절 목록에서 지수 3개를 빼고 **2년물·이더리움을 더한** 10개 |
| ⑤ 발표 결과 | `calendar` | 지난 월~금 발표된 경제지표·실적의 **예상치와 실제치 쌍**. C절 데이터 소스 표의 `epsActual`/`revenueActual` 과 `econ-calendar.json` |
| ⑥ 한 주를 움직인 것 | `news` | 3~4건. **§B2 기법 5** |
| ⑦ 섹터 등락 | `sectors` | 미국 코어 섹터 ETF 12개. **§B2 기법 2** |
| ⑧ 대형주 | `movers` | 한국 3 + 미국 3. **상승만 늘어놓지 않는다** — 오른 쪽과 내린 쪽을 함께 담는다(**§B2 기법 3**). `seq` 는 전주 금요일 종가부터 5거래일 종가 6개. 미국 종목은 `logo` 에 티커를 적으면 캐시된 로고가 붙는다 |
| ⑨ AI · 반도체 | `ai` | 3건. **§B2 기법 4**, `icon:"chip"` 사용 |
| ⑩ 아웃트로 | `outro` | 고정 문구. 다음 브리핑 안내는 "내일 밤 9시, 다음 주 일정" |

**토요일에만 해당하는 주의사항**

- **지수 PER(`valuation`)은 `data/index-valuation.json` 에서 가져온다.**
  `index-valuation` 워크플로가 토요일 05:45 KST 에 WSJ «P/E Ratios & Earnings Yields» 표에서
  미국 3개 지수의 현재 PER·1년 전 PER·추정 PER 을 받아 커밋한다(러너에서만 열린다 — 세션은 403).

  ```
  git fetch origin main -q && git show origin/main:data/index-valuation.json > /tmp/val.json
  ```

  - `rows` 를 복사하고 `note_ko`/`note_en` 에 **출처와 기준을 반드시 적는다.** 같은 날 S&P 500 PER 이
    WSJ 26.02 / multpl 29.88 / iShares 30.65 로 최대 18% 벌어진다 — 이익 계열이 기관마다 달라서다.
    무출처로 쓰면 특정 벤더의 방법론을 사실처럼 내보내게 된다.
  - **표시 이름은 `name_ko` 를 그대로 쓴다.** WSJ 표에는 나스닥 «종합»이 없어 «100» 이 잡히는데,
    카드 ③은 종합지수 봉차트를 그린다. 이름을 '나스닥'으로 뭉개면 독자는 같은 지수의 PER 로 읽는다.
  - **EPS 는 넣지 않는다.** 표에 없고, 종가 ÷ PER 로 되짚으면 반올림 오차가 '지수 EPS' 처럼 보인다.
  - **코스피·코스닥 PER/PBR 은 확보 경로가 없다. 다시 찾지 않는다.** 무엇을 시도해 왜 실패했는지는
    `DATA_SOURCES.md` §4 에 전부 적혀 있다(KRX 는 브라우저로도 IP 차단, 공식 OpenAPI 는 유료라
    2026-08-08 에 채택하지 않기로 결정). 한국 행을 넣지 말고 `note` 에 미국 기준임을 밝힌다.
  - `rows` 가 비면 렌더러가 안내 문구로 대체한다. **틀린 숫자보다 빈칸이 낫다.**
- **`calendar` 의 색은 방향만 뜻한다** — `dir:1` = 실제치가 예상보다 **높음**, `-1` = 낮음, `0` = 같음.
  좋고 나쁨으로 정하지 않는다. 실업수당·감원처럼 '낮으면 좋은' 지표도 낮으면 `-1` 이다.
  (해석과 방향을 섞어 쓰다 기업 지표와 경제 지표의 색 의미가 반대가 된 적이 있다.)
- **`⑧ movers` 는 대형주만 담는다.** 주간 등락률 상위를 그대로 쓰면 소형주 급등주가 올라온다.
  시가총액 상위권에서 그 주에 가장 크게 오른 2개와 가장 크게 내린 1개를 각 시장에서 고른다.
- **토요일은 `schedule` 을 쓰지 않는다** (§C 참고). 다만 §C 의 «데이터 소스» 표는 ⑤ 발표 결과를
  채우는 데 그대로 쓴다.

### A1. 일요일(sun) 재료 지도 — **sun 이면 여기부터 읽는다**

일요일도 카드 10장이 각자 전용 필드를 읽는다. 필드 이름과 스키마는 `FORMAT_BRIEFING.md` §2-B 에 있고,
여기서는 **각 칸을 무엇으로 채우는지**만 정한다.

**순서는 «먼저 주말 사이 있었던 일부터, 그럼 다음 주는»** 이라는 시간 흐름을 따른다.
②만 지난 주말이고 ③~⑧ 이 다음 주다. ⑨ 출발선은 뉴스가 아니라 기준값이라 뒤에 참조로 붙는다.

| 카드 | 필드 | 무엇으로 채우나 |
|---|---|---|
| ① 표지 | `cover` | **다음 주 최대 관전 포인트**의 컨센서스 수치 하나 + 요약 3줄 + 지난주 지수 3칸. 선정 기준은 아래 «표지 고르는 법» |
| ② 주말 사이 소식 | `weekend` | 토요일 낮 ~ 일요일 저녁에 **이미 일어난** 일 3건. **§B2 기법 5** |
| ③ 캘린더 | `week` | 다음 주 월~금 확정 일정을 **요일당 3~4건**. C절 데이터 소스 표의 `econ-calendar.json` + 실적 일정. `time` 은 시각만(`21:30`), 날짜는 요일 헤더에 |
| ④ 경제 지표 | `econ` | 미국·글로벌 지표 4~5건. **`prev`(직전치)와 `est`(컨센서스)를 쌍으로** 반드시 함께 넣는다 |
| ⑤ 실적 | `earnings` | 미국 대형주 6건. `epsPrev`(전년 동기 실적) → `eps`(컨센서스). `logo` 에 티커를 적으면 캐시 로고가 붙는다 |
| ⑥ 한국 다음 주 | `korea` | 국내 지표·실적·이벤트 3건. **금통위가 다음 주면 반드시 여기 포함** |
| ⑦ AI · 반도체 | `ai` | 3건. **다음 주에 예정된 것만.** **§B2 기법 4**, `icon:"chip"` 사용 |
| ⑧ 놓치면 안 될 것 | `watch` | 3건. ③~⑤에 안 들어간 확정 일정 — 만기·지수 정기변경·국제회의·**휴장일** |
| ⑨ 출발선 | `start` | 지수 3개 + 지표 6개, 금요일 마감 기준. **A절 목록에서 추려 쓴다** |
| ⑩ 아웃트로 | `outro` | 고정 문구. 다음 브리핑 안내는 "내일 아침 8시, 미국장 마감 정리" |

**② 와 ⑦ 이 겹치지 않게 시점으로 자른다.** 둘 다 AI 소식을 담을 수 있어 방치하면 반드시 겹친다.

| | ② 주말 소식 | ⑦ AI · 반도체 |
|---|---|---|
| 시점 | 토 낮 ~ 일 저녁, **이미 일어난** 일 | 다음 주에 **예정된** 일 |
| AI 소식이면 | 주말에 나온 모델·칩·정책 소식은 **여기** | 다음 주 발표·시행 예정인 것만 |

- **한 항목은 한 카드에만 싣는다.** 주말에 나온 AI 소식을 ⑦로 올리기로 했으면 ②에서 뺀다.
- **⑤ 실적과 ⑦ 도 각을 나눈다** — ⑤는 «숫자»(컨센서스 EPS·발표 시각), ⑦은 «무엇을 볼 것인가»
  (가이던스·기술 쟁점). 같은 기업이 양쪽에 나와도 되지만 같은 말을 반복하면 안 된다.

**표지 고르는 법 (`cover.hero`)** — 판단이 흔들리지 않게 순서를 못박는다.

1. **발표일이 공시된 확정 일정**에서만 고른다. "~할 것으로 보인다" 수준의 미확정 이벤트는 후보가 아니다.
2. **영향 범위 순으로 고른다**: 통화정책(FOMC·금통위) > 물가(CPI·PCE) > 고용(고용보고서) >
   성장(GDP·ISM) > 개별 기업 실적.
3. **컨센서스 숫자가 있어야 한다.** `hero.value` 에 숫자를 박아야 하므로 컨센서스가 없는 이벤트는 못 쓴다.
4. 동률이면 **미국 시장 영향이 큰 쪽**. 전 세계 독자를 대상으로 하는 계정이고,
   글로벌 지수 방향을 정하는 것은 미국장이다. 한국 일정은 ⑥ 한국 카드가 따로 다룬다.

- **라벨에 «컨센서스»임을 반드시 밝힌다** (`美 7월 CPI 컨센서스`). 라벨을 `美 7월 CPI` 로만 적으면
  독자는 이미 발표된 값으로 읽는다. `sub` 에 직전치와 발표 시각(KST)을 붙여 무엇과 비교하는 숫자인지 드러낸다.
- **`points` 3줄은 뒤 카드에 실제로 있는 일정만 쓴다** — 표지에만 있는 항목은 검증을 빠져나간다.
- **단정적 예측을 하지 않는다.** 결과를 미리 규정하지 말고 무엇이 관건인지를 쓴다.
  - ✕ "수요일 CPI가 둔화되며 금리 인하 기대가 커질 것입니다"
  - ○ "직전 3.1%에서 3%를 밑도는지가 9월 인하 기대의 분기점입니다"

**일요일에만 해당하는 주의사항**

- **`econ.rows` 는 `prev` 없이 내지 않는다.** 컨센서스만 있으면 화살표 카드가 «무엇에서» 왔는지를
  잃어 절반이 빈다. 직전치를 못 구한 지표는 그 자리에 다른 지표를 넣는다.
- **`dir` 은 방향만 뜻한다** — `1` = 컨센서스가 직전치보다 **높음**, `-1` = 낮음, `0` = 같음.
  좋고 나쁨으로 정하지 않는다. 실업수당처럼 '낮으면 좋은' 지표도 컨센서스가 낮으면 `-1` 이다.
- **`week`(③) 는 요일당 3~4건이 기본이다.** 2건 이하면 카드가 눈에 띄게 빈다. 지표만으로 모자라면
  실적·만기·해외 지표를 함께 넣는다. `importance:"high"` 는 **요일당 최대 1건** — 전부 high 면
  아무것도 강조되지 않는다.
- **⑨ 출발선은 회고를 여기서 끝낸다.** 지난주 지수 상세·섹터·대형주는 토요일 회차가 통째로 다루므로,
  일요일이 되풀이하면 하루 만에 같은 내용이 다시 나간다. **섹터 카드는 일요일에 없다.**
- **토요일 회차를 먼저 읽는다.** 같은 주 `content/<토요일>-sat.json` 을 읽고 **거기서 이미 다룬 항목을
  그대로 반복하지 않는다.** 특히 ⑥ 주말 소식은 토요일이 금요일 마감까지만 다룬 뒤의 구간이다.

### A. 시장 지표 — `metrics` 10칸 (sat) / `start.metrics` 6칸 (sun)

> sat 은 아래 10개에서 지수 3개(나스닥·S&P 500·코스피)를 빼고 — 그것들은 ②③이 이미 다룬다 —
> `미 2년물 금리`·`이더리움`을 더해 10개를 맞춘다.
> **sun 은 `start.indexes` 에 지수 3개(S&P 500·나스닥·코스피), `start.metrics` 에 나머지 중
> 6개**(VIX·미 10년물·원/달러·금·WTI·비트코인)를 쓴다. 조회 방법은 셋 다 동일하다.

**평일과 완전히 동일한 10개를 같은 순서로 쓴다** — `나스닥` · `S&P 500` · `코스피` · `VIX` · `Fear&Greed` · `미 10년물 금리` · `원/달러(달러인덱스 DXY 병기)` · `금` · `WTI 원유` · `비트코인`.

축마다 타일을 바꾸면 독자가 매번 다시 읽어야 하고, 주 5일과 주말이 다른 계정처럼 보인다. 조회 방법·주의사항(Yahoo 종가 함정, 순차 호출, 근사치 금지, 휴장 처리)은 **`ROUTINE_PROMPT.md` 의 리서치 A 절을 그대로 따른다.** 주말 전용 차이는 아래뿐이다.

- **`value` 는 금요일 마감가**다. sat·sun 모두 같은 값이며, sun 은 `start.note` 에 "금요일 마감 기준"을 밝힌다 (카드 부제에도 이미 나온다).
- **`delta` 는 주간 등락률**이다. `▲ +2.1%` 처럼 기호+숫자로만 쓴다 (언어 무관 필드 — `FORMAT_BRIEFING.md` §4). **sun 의 `start.indexes[].wk` 는 문자열이 아니라 숫자**이며 렌더러가 부호와 색을 붙인다.
- **주간 등락률은 직접 계산한다.** `get_historical_stock_prices` 를 `period:"1mo"`, `interval:"1d"` 로 불러 **이번 주 금요일 종가 ÷ 지난주 금요일(직전 주 마지막 거래일) 종가**로 구한다. `period:"5d"` 는 휴장이 낀 주에 기준일이 밀리므로 쓰지 않는다.
- 비트코인·금·WTI 처럼 주말에도 값이 움직이는 자산은 **금요일 마감 시점 값으로 통일한다.** 토·일 사이 변동을 섞으면 다른 타일과 기준이 달라진다. 주말 사이 큰 변동이 있었다면 그 사실은 `note` 한 줄로 적는다.

### B. 본문 카드에 무엇을 담나 — 두 세션 공통 원칙

> **카드 배정은 §A0(sat)·§A1(sun) 표가 정한다.** 여기는 그 칸을 채울 때의 공통 원칙이다.

평일과 같다 — **뉴스 기사를 옮겨 적지 않는다.** 시세·지표·공시·1차 발표문에서 **계산된 관찰**을 뽑는다.
주말은 한 주(또는 다음 주) 단위라 평일보다 이런 재료가 오히려 많다.

> (sat) 나스닥 주간 +1.2%로 3주 연속 상승. 3주 누적 +5.8%는 4월 이후 최대.
> (sun) 컨센서스 3.0%는 직전 3.1%보다 낮다. 3%를 밑돌면 2024년 3월 이후 처음이다.

**«앞으로 벌어질 일»에도 숫자는 있다.** 일요일이 다루는 것은 아직 안 나온 결과지만
**컨센서스·직전치·전년 동기 실적은 이미 확정된 숫자**다. 그래서 일요일도 수치 카드(③④⑦)를 갖는다.
없는 것은 «실측치»뿐이고, 그 자리는 비워 두는 것이 아니라 **화살표의 오른쪽(컨센서스)** 이 채운다.

### B2. 조회·집필 기법 (두 세션 공용)

**여기 있는 것은 «어떻게» 뿐이다.** 무엇을 실을지는 sat 은 §A0, sun 은 §B 가 정한다.
방법을 한 곳에 모아 둔 이유는, 예전에 같은 계산법이 양쪽 절에 따로 적혀 있다가 한쪽만 고쳐져
어긋난 적이 있어서다. **기법이 같다고 두 회차의 내용이 같아지는 것은 아니다.**

#### 기법 1. 지수 데이터 — sat ②③ / sun 미사용

**한 줄로 5개 지수를 다 만든다. 손으로 모으지 않는다.**

```
node scripts/fetch-sat-indexes.mjs <금요일 날짜> > /tmp/sat-idx.json
```

일봉 70개 + 이동평균 20/50/100/200 + 기간 고점 + 주간 등락률 + RSI(14)가 한 번에 나온다.
`indexes` 배열을 **그대로 복사**해 콘텐츠 JSON 에 넣고 `note_ko`/`note_en` 만 채운다
(`_check` 는 대조용이라 옮기지 않는다).

- **`5/5개 지수` 가 나오는지 확인한다.** 하나라도 빠지면 그만큼 카드 ②③이 빈다.
  미국 쪽은 Yahoo 가 첫 요청에 429 를 주고 스크립트의 재시도가 뚫는 구조라, 실패하면
  **한 번 더 돌려 본다.** 그래도 안 되면 마무리 보고에 적는다.
- **`data/sat-indexes.json` 은 예비용이다.** `sat-indexes` 워크플로가 토요일 06:30 KST 에
  커밋해 두지만, **러너에서는 Yahoo 가 클라우드 IP 를 막아 미국 3개가 통째로 빈다**
  (2026-08-08 실측: 세션 5/5 vs 러너 2/5). 한국 지수만 신뢰하고, **미국 지수의 근거로 쓰지 않는다.**
  세션 쪽 조회가 아예 안 되는 날의 최후 수단이며, 읽을 때 `missing` 을 반드시 확인한다.
- `note` 한 줄("7주 연속 주간 하락")의 재료는 `node scripts/fetch-krx.mjs <금요일 날짜>` 에 있다 —
  `indexes.*.records.weekToDate` · `.streakDays` · `.range20d`, 그리고 주간 상승/하락 종목 수.

#### 기법 2. 섹터 등락 계산 — sat ⑦ 전용

- 미국 코어 섹터 ETF 12개(`SMH` `XLK` `XLC` `XLF` `XLY` `XLV` `XLI` `XLE` `XLP` `XLB` `XLU` `XLRE`)를 `get_historical_stock_prices` `period:"1mo"` 로 받아 **주간 등락률을 직접 계산한다.** 티커 목록과 테마 ETF, `FMP sector-performance-snapshot` 을 쓰지 않는 이유는 `ROUTINE_PROMPT.md` 의 예비 지표 절에 있다.
- 한국 업종으로 바꿔도 된다 — `fetch-krx.mjs` 의 `industry` 가 주력 15개 업종을 시총 가중으로 준다. 다만 **한 카드에 두 나라를 섞지 않는다.** 섞으면 기준 통화·거래일이 달라 막대 길이 비교가 무의미해진다.
- `bars` 라벨은 **한글 8자·영문 15자 안쪽**이다. 넘으면 잘린다 (`FORMAT_BRIEFING.md` §6).

#### 기법 3. 등락 상·하위 종목 고르기 — sat ⑧ / sun 미사용

- 상승만 늘어놓지 말고 **오른 쪽과 내린 쪽을 함께** 담는다. 한 주의 승자만 보여주면
  그 주가 실제로 어땠는지가 안 보인다.
- **대형주에서만 고른다.** 주간 등락률 상위를 그대로 쓰면 소형주 급등주가 올라온다.
  시가총액 상위권에서 가장 크게 오른 2개 + 가장 크게 내린 1개가 기본 배분이다.
- 한국·미국을 **각각** 고른다. 한 열에 두 나라를 섞으면 통화·거래일이 달라 비교가 안 된다.

#### 기법 4. AI · 반도체 카드 — sat ⑨ / sun ⑧

이 계정은 브랜드에서 "경제와 AI"를 약속하므로 AI 축이 매 회차 한 장 있어야 한다. **주가 나열이 아니라 AI 자체의 소식을 담는다** — 모델·툴 출시와 버전, HBM·GPU·TPU 등 반도체 기술과 표준, AI 인프라·데이터센터 투자, AI 규제·정책. AI 종목 등락만 순위로 늘어놓는 카드는 지수·섹터 카드와 겹치고 AI에 대해 아무것도 말해주지 않으므로 만들지 않는다.

- `sat`: 이번 주에 실제로 나온 것. 주중 평일 브리핑에서 이미 다룬 항목은 **한 주 관점으로 다시 묶을 때만** 넣는다 (같은 문장을 그대로 옮기지 않는다).
- `sun ⑦`: **다음 주에 예정된 것만 담는다** — 컨퍼런스, 출시·시행 예정일, 반도체 기업 실적의 관전 포인트.
  **주말 사이 나온 AI 소식은 여기가 아니라 ② 주말 소식으로 보낸다** (§A1 의 시점 구분표).
  실적을 다룰 때는 ⑤와 각을 나눈다 — ⑤가 컨센서스 EPS 숫자를 보여줬으면, ⑦은 그 발표에서
  «무엇을 볼 것인가»(AI 가속기 가이던스·장비 수주 흐름 같은 기술 쟁점)를 쓴다.
- 뉴스 기반이라 `ROUTINE_COMMON.md` §3 검증의 B(정확성)·C(최신성)를 특히 엄격히 적용한다.

#### 기법 5. 뉴스 카드 수집 — sat ⑥ / sun ②⑥⑧

- **지역으로 못박지 않는다.** 한국을 우선하되 지역 구분 없이 그 범위에서 나온 굵직한 것을 모은다. 제목을 지역으로 적으면 그 제목이 곧 수집 범위를 가둬 다른 좋은 재료를 놓치게 된다 (2026-08-04 평일에서 확인된 문제).
- **채우기 전에 반드시 실제로 검색한다. 최소 두 번** — 한 번은 한국 쪽(`"주간 증시"`·`"코스피 주간"`), 한 번은 해외(`"뉴욕증시 주간"`·`"연준"`·`"중동"` 등). `mcp__PlayMCP__NaverSearch-search_news` 에 `sort:"date"` 로 훑고, 여러 매체가 반복해 언급하는 그 주의 동인을 찾는다.
- **시장을 움직인 원인은 결과보다 먼저 싣는다.** 지표 타일(sat `metrics`·`indexes` / sun `start`)이
  어떤 사건 때문에 움직였다면 그 사건이 본문 어딘가에 반드시 있어야 한다.
- **발생 시점은 언어 무관 필드에 들어가므로 숫자로만 쓴다.** 두 세션 모두 글 카드의 `src` 에 넣는다
  (`KRX · 8/6`, `8/13`). `"화요일"`·`"8/5(화)"` 같은 한국어를 쓰지 않는다 — 요일이 꼭 필요하면 `8/5 Tue`.
- `sun ⑧` 은 다음 주 확정 일정 중 ③④⑤에 안 들어간 것을 담는다 — 옵션·선물 만기(쿼드러플 위칭), 지수 정기변경, 대선·의회 일정, 국제회의, **주요국 휴장일**(주말 렌더러에 개장시간 박스가 없으므로 휴장은 여기서만 알린다).
- **한국은행 기준금리 결정(금통위)은 절대 «빠뜨리지» 않는다.** 그 주에 있었으면 **sat ⑥**,
  다음 주에 예정돼 있으면 **sun ⑥(한국 다음 주)** 에 반드시 싣는다 (수치 카드는 문장 항목을 담지 못한다).
  **순서까지 강제하지는 않는다** — 그 주 무게에 따라 정한다. 원래 이 규칙은 금통위가 통째로 누락된
  적이 있어 «반드시 포함»을 못박은 것이지, 항상 맨 앞에 두라는 뜻이 아니었다.

### C. 일정 데이터 — **두 세션 공용**

> **`schedule` 필드는 어느 주말 세션도 쓰지 않는다.** 예전 sun 이 쓰던 «TOP 픽» 카드는
> 2026-08-09 개편에서 ② 캘린더로 대체됐다 — 요일 그리드가 같은 일을 더 잘 한다.
> 아래 표는 **sat ⑤ 발표 결과**와 **sun ②③④** 를 채우는 공용 조회처다.

- **일요일 ③ 캘린더**: 다음 주 월~금 확정 일정을 요일당 3~4건. `importance:"high"` 는 요일당 최대 1건.
- **일요일 ④ 경제 지표**: 그중 미국·글로벌 지표 4~5건을 **직전치와 컨센서스 쌍**으로 뽑아 다시 싣는다.
  ③과 겹치는 것이 정상이다 — ③은 «언제»를, ④는 «무엇에서 무엇으로»를 보여준다.
- **일요일 ⑤ 실적**: 미국 대형주 6건. `epsEstimated` 를 `eps` 로, 전년 동기 실제치를 `epsPrev` 로.

**데이터 소스** (평일과 동일):

| 무엇 | 조회 |
|---|---|
| 경제지표 일정 | `git fetch origin main -q && git show origin/main:data/econ-calendar.json > /tmp/econ.json` — 없거나 낡으면 `node scripts/fetch-econ-calendar.mjs 1 10 > /tmp/econ.json` |
| 실적 일정·컨센서스 | FMP `calendar` → `earnings-calendar`, `from_date`/`to_date` 로 다음 주 지정. `epsEstimated`·`revenueEstimated` |
| 이번 주 실적 실제치 (sat) | 같은 도구, 지난 주 날짜로. `epsActual`·`revenueActual` |
| 한국 지수·업종·거래대금 | `node scripts/fetch-krx.mjs <금요일 날짜>` |
| 미국 지수·ETF·환율·원자재 | PlayMCP `UsStockInfo-get_historical_stock_prices` |
| 미 국채금리 | FMP `economics` → `treasury-rates` |

- `econ-calendar.json` 의 `at` 은 **UTC** 다. KST 로 환산해 쓴다(+9시간). `impact` 가 `HIGH` 인 것을 우선하고 `LOW`(국채 입찰·주간 재고)는 걸러 쓴다. **반드시 `origin/main` 에서 읽는다** — 워크플로가 main 에 커밋하므로 작업트리 파일은 낡았을 수 있다.
- **캘린더 워크플로는 주말 세션 직전에도 돈다** (토 07:20 / 일 20:10 KST). 그러니 평일과 똑같이 `fetchedAt` 이 12시간을 넘으면 낡은 것으로 보고 2순위로 내려간다. 다만 2순위(세션 내 스크립트)는 **미국 지표만 나오고 이전치가 없어** 일요일 ④의 `prev` 를 채울 수 없으므로, 1순위가 비었으면 그 사실을 마무리 보고에 적는다 — 워크플로가 안 돌았다는 신호다.
  **`prev` 를 못 구한 지표는 ④에 넣지 않는다** — 화살표 왼쪽이 비면 카드 절반이 빈다. 그 자리에 직전치가 있는 다른 지표를 넣고, 원래 지표는 ③ 캘린더에만 남긴다.
- 미국장 마감 후 발표되는 실적은 그 **미국 날짜**로 조회한다 (예: 8/12 06:00 KST 발표 = 8/11 미국 날짜).
- 캘린더가 실패하면 `ROUTINE_PROMPT.md` 의 정기 발표 체크리스트로 채운다. **같은 날 비슷한 지표가 둘 이상 나오는 경우가 많으니(S&P 글로벌 PMI 와 ISM PMI) 하나만 넣고 끝내지 않는다.**

### D. 표지 카드 작성 — **두 세션 모두 `cover`**

순서는 같다 — 그 회차를 한 문장으로 압축한 `headline` → 판단의 근거가 되는 큰 수치 하나(`hero`) →
3줄 요약(`points`) → 지수 3칸(`tiles`).

- **`sat`**: `hero` 는 **그 주 최대 이슈의 실측치**를 고르고 `sub` 에 예상치를 붙여 낙차를 보여준다.
- **`sun`**: `hero` 는 **다음 주 최대 관전 지표의 컨센서스**다. 아직 결과가 없으므로 «확실한 사실만»을
  그대로 적용할 수 없고, 대신 §A1 의 «표지 고르는 법» 네 단계를 따른다. 라벨에 «컨센서스»임을
  반드시 밝히고, 단정적 예측 대신 무엇이 관건인지를 쓴다.

**`ROUTINE_COMMON.md` §3 검증에 표지 카드도 함께 넘긴다.** 첫 카드라 오류 시 임팩트가 가장 크다. 다만 판정 범위를 지정한다:
- `sat`: 인용한 **사실(수치·날짜·사건)** 이 맞는지만. 인과 해석의 타당성은 판정시키지 않는다.
- `sun`: ⓐ 그 일정이 **실제로 그 날짜에 예정돼 있는지**(공식 발표 일정·기업 IR 공지 기준), ⓑ 인용한 **직전 수치·컨센서스·가이던스가 맞는지** 두 가지만. **전망 자체의 타당성은 검증 대상이 아니다** — 정답이 없는 판정을 시키면 검증 결과 전체가 흐려진다.

---

## 리서치가 끝나면

1. **`ROUTINE_COMMON.md` §3 의 발행 전 통합 검증(A~E)** 을 반드시 거친다. 통과 전에는 콘텐츠 JSON 을 확정하지 않는다. sun 세션은 C(최신성) 판정에 **"다음 주 ~예정"이 검증 시점에도 사실인지**가 포함된다 — 일정은 변경·연기가 잦다.
2. **스키마대로 `content/<DATE>-<SESSION>.json` 을 저장한다** — sat 은 `FORMAT_BRIEFING.md` **§2-A**,
   sun 은 **§2-B**. 예시 파일도 갈린다: sat 은 `content/example-sat.json`, sun 은 **`content/example-sun.json`**.
   어느 쪽이든 반드시 이번 세션 내용으로 새로 만든다.
   - **`content/` 의 지난 sun 파일을 예시로 삼지 않는다.** `2026-08-02-sun.json` 은 개편 전
     `econ`/`ai` 6+6 스키마라 그대로 베끼면 **렌더러가 «cover 가 없습니다» 로 멈춘다.** 견본은 `example-sun.json` 뿐이다.
   - **주말 고정 문구**(아웃트로 큰 문구·다음 브리핑 안내)는 `outro{}` 안에 넣는다 —
     최상위 `next_brief_*`·`outro_tagline_*` 는 평일 전용이라 주말 렌더러가 읽지 않는다.
   - `headline`: 두 세션 모두 `cover.headline` 이다. 최상위 `headline_*` 는 주말에서 쓰지 않는다.
   - `caption`: sat 은 "한 주 결산"임을, sun 은 "다음 주 일정"임을 첫 줄에 밝힌다.
   - `sun` 세션은 먼저 같은 주 `sat` 파일을 읽고 **토요일에 이미 다룬 항목을 그대로 반복하지 않는다.**
3. **`FORMAT_BRIEFING.md` §6** 대로 ko·en 카드를 렌더링하고 **전부 분량 초과를 눈으로 확인한다**
   (**sat·sun 모두 10장**). 명령은 세 세션 모두 `node scripts/render-cards.mjs <DATE> <lang> <SESSION>` 이며
   주말이면 렌더러가 알아서 전용 렌더러로 넘긴다.
   - 주말 첫 카드는 라벨칩 + 굵은 두 줄 제목 + 큰 수치 카드가 보이는 것이 정상이다.
   - **sat 은 영어 카드 ⑤를 특히 본다.** `억`·`만` 이 그대로 남아 있으면 `est_en`/`act_en` 을 안 넣은 것이다
     (`FORMAT_BRIEFING.md` §4). 실제로 그렇게 새어 나간 적이 있다.
   - **sun 은 ③ 캘린더와 ⑤ 실적을 특히 본다.** 요일당 2건 이하면 ②가 비어 보이고,
     기업명이 길면 ④ 타일이 밀린다. `slot_ko`/`slot_en` 을 한쪽만 채우면 다른 언어가 빈칸이 된다.
   - **대체텍스트 개수가 카드 수와 같은지 확인한다.** 주말은 10개여야 한다:
     ```
     node -e "import('./scripts/lib/alt-text.mjs').then(async m=>{const fs=await import('node:fs');
       const c=JSON.parse(fs.readFileSync('content/<DATE>-<SESSION>.json','utf8'));
       for(const l of ['ko','en']) console.log(l, m.buildAltTexts(c,l).length)})"
     ```
     어긋나면 대체텍스트가 엉뚱한 슬라이드에 붙는다. 카드 편성을 바꿨다면
     `scripts/lib/alt-text.mjs` 의 `satAltTexts()`·`sunAltTexts()` 도 같이 고쳐야 한다.
4. **`ROUTINE_COMMON.md` §4~§7** 대로 커밋·푸시 → 토큰 점검 → Instagram 발행 → 카카오톡 알림 → 마무리 보고.
