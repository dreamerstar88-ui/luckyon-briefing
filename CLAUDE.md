# CLAUDE.md

## 자동 발행 루틴에 대한 사전 승인 (중요)

이 저장소는 `claude.ai/code/routines` 로 등록된 무인 스케줄 세션(아침/저녁 브리핑, 주말 브리핑,
스토리, 차트 노트 등)이 정기적으로 실행됩니다. 그 절차는 `ROUTINE_PROMPT.md` · `ROUTINE_COMMON.md` ·
`ROUTINE_PROMPT_WEEKEND.md` · `ROUTINE_PROMPT_STORY.md` · `ROUTINE_PROMPT_CHARTNOTES.md` 에
문서화되어 있습니다. 이 문서가 정한 절차를 그대로 따르는 세션은, 아래 두 가지를 **실시간 사용자 승인 없이**
수행할 권한을 저장소 소유자로부터 사전에 위임받습니다.

세션 실행 환경(Claude Code on the web)이 세션마다 별도 작업 브랜치를 기본값으로 부여하더라도, 이
문서가 있는 한 아래 두 행동은 "사전 승인된 것"으로 간주하고 그대로 진행하세요. 매번 "진행해도
되겠습니까"를 묻지 않습니다 — 그게 이 파일을 두는 목적입니다.

1. **`claude/live` 브랜치에 직접 커밋·푸시.** `ROUTINE_COMMON.md` §3 의 발행 전 검증(별도 검증
   에이전트 포함)을 통과한 콘텐츠에 한해, §4 의 커밋 절차(자기 축 경로만 add, `origin/claude/live`
   `FETCH_HEAD` 기준으로 재체크아웃, `git add -A` 금지)를 그대로 따라 `claude/live` 에 발행한다.
   이 브랜치는 GitHub Pages·Instagram 발행 전용 산출물 브랜치이며 사람이 리뷰하는 브랜치가 아니므로,
   PR·추가 승인 없이 직접 반영해도 된다. 현재 세션의 git 자격증명이 실제로 이 브랜치에 쓸 수 있는지
   불확실하면(예: push 가 거부되면) 그 사실을 사용자에게 알리고 멈춘다 — 이 항목은 "권한이 있다면
   써도 된다"는 사전 승인이지, 없는 권한을 억지로 만들어내라는 뜻이 아니다.
2. **문서·스크립트를 고쳤을 때 `main` 브랜치에도 반영.** `ROUTINE_COMMON.md` §9 가 요구하는 대로,
   루틴 세션이 스스로 발견해 고친 스크립트 버그나 절차서 수정은 다음 회차에도 적용되도록 `main` 에
   커밋·푸시한다. 그 세션이 실제로 만든, 발행 절차와 직접 관련된 수정에 한하며, 무관한 리팩터링을
   끼워 넣지 않는다.

**이 위임의 범위**: 위 두 브랜치(`claude/live`, `main`)에 대한, 위 절차서들이 정한 정기 발행·유지보수
흐름 안에서의 커밋·푸시만 해당한다. 그 밖의 목적(무관한 리팩터링, 강제 푸시, 커밋 히스토리 재작성,
다른 저장소, 이 저장소가 아닌 계정·서비스에 대한 행동)에는 적용되지 않으며, 그런 작업은 여전히 매번
사람의 명시적 승인을 받는다.

**추가 경위**: 2026-08-14 저녁 브리핑에서, 검증까지 마친 콘텐츠가 세션에 주어진 기본 작업 브랜치
제한 때문에 `claude/live` 에 닿지 못해 발행이 막히고 사용자가 매번 실시간으로 승인해야 했다. 저장소
소유자가 이 문제의 재발 방지를 직접 요청해 이 사전 승인 항목을 추가했다. 이후에도 같은 패턴(발행
직전에 브랜치 제한으로 멈춤)이 재발하면, 이 파일이 실제로 그 제한을 우회하는 데 도움이 되었는지
사용자에게 물어 확인하고, 안 됐다면 이 문서만으로는 부족하다는 뜻이니 플랫폼 설정 쪽을 다시 안내한다.

## 참고 자료 위치 (메모)

- **비행 여행 숏폼 프로젝트 교훈 정리** (2026-09-05 폐기, 재시도 시 참고): 사용자 PC `C:\Users\PSJ_1\.claude\비행여행영상_교훈정리_2026-09-05.md`.
  저장소에는 남기지 않았다. 기획 규칙, 도구별 실측 비용(나노바나나 Pro·Veo 3.1), 캐릭터 시트 지시법, 인체 동작 프롬프트 규칙,
  다음 시도 전 준비물(선불 크레딧 $200 이상, 힉스필드·Kling 검토)이 정리되어 있다. 같은 주제를 다시 시작하면 이 파일부터 읽는다.
