# 커넥터 커버리지 점검 절차서 (1회용)

> **용도**: FMP·Alpha Vantage 커넥터가 루틴 세션에서 실제로 작동하는지, 무료 티어로 브리핑에 필요한
> 데이터를 어디까지 받을 수 있는지 검사합니다. **카드 생성과 Instagram 발행은 하지 않습니다.**
>
> **실행 방법**: 루틴 Instructions 를 아래 내용으로 바꾸고 "지금 실행"을 누르세요. 점검이 끝나면
> Instructions 를 원래 내용(ROUTINE_PROMPT.md 참조)으로 되돌리세요.
> ```
> git fetch origin claude/gifted-clarke-aueujz 를 실행한 뒤,
> git show origin/claude/gifted-clarke-aueujz:PROBE_CONNECTORS.md 로 절차서를 읽고
> 그 절차를 순서대로 그대로 수행하세요.
> ```
> (이 파일이 main 에 머지된 뒤에는 "저장소의 PROBE_CONNECTORS.md 파일을 읽고 수행하세요" 한 줄이면 됩니다.)

당신은 luckyon 브리핑의 데이터 파이프라인 점검 담당입니다. 아래를 순서대로 수행하세요.

1. **도구 확인**: ToolSearch로 이 세션에 로드된 FMP·Alpha Vantage MCP 도구를 검색해 전체 목록을 확보한다
   (예: "fmp", "alpha vantage", "quote", "index" 등으로 검색). 발견된 도구 이름을 모두 기록한다.
   둘 중 하나라도 도구가 없으면 그 사실을 기록하고 5단계로 건너뛴다.

2. **시세 커버리지 테스트**: 브리핑 카드②의 타일에 필요한 아래 항목을 하나씩 실제로 호출해 본다.
   각 항목에 대해 ①성공/실패 ②받은 값과 기준 시각 ③사용한 도구·심볼을 기록한다.
   - 미국 지수: S&P 500, Dow Jones, Nasdaq Composite (지수 자체가 안 되면 SPY·DIA·QQQ ETF로 대체 시도)
   - 지수 선물: S&P 500 선물, 나스닥 선물 (안 되면 "불가"로 기록)
   - 한국: KOSPI, KOSDAQ (^KS11, ^KQ11 등 여러 심볼 시도)
   - 일본: 닛케이 225
   - 변동성: VIX
   - 금리: 미 10년물 국채 수익률
   - 환율: 원/달러 (USD/KRW)
   - 원자재: 금 현물 또는 선물, WTI 원유
   - 암호화폐: 비트코인(USD)

3. **분석 기능 테스트**: Alpha Vantage 기술적 지표를 시험한다 — S&P 500(또는 SPY)의 RSI(14일),
   SMA(20일)를 호출해 성공 여부와 값을 기록한다. FMP의 실적 캘린더·경제지표 캘린더 도구가 있으면
   "오늘~+3일" 범위로 호출해 몇 건이 오는지 기록한다.

4. **레이트 리밋 체감 기록**: 위 호출 과정에서 발생한 오류(429, 프리미엄 전용 안내 등)를 그대로 기록한다.

5. **보고서 저장·푸시**: 결과를 `data/probe-report.md` 로 저장한다. 형식: 항목별 표(성공/실패/값/도구명/비고)
   + 결론("무료 티어로 10개 타일 중 N개 확보 가능, 대체 필요 항목: …"). 세션에 지정된 작업 브랜치에
   커밋·푸시한다 (없으면 `claude/probe-report` 브랜치를 만들어 푸시한다). Instagram 발행, claude/live
   푸시, 카드 렌더는 절대 하지 않는다.

6. **알림**: PushNotification으로 핵심 결과를 보낸다 — 커넥터 작동 여부, 10개 타일 중 확보 가능 개수,
   실패 항목 목록을 한 줄씩. 보고서 파일 경로도 함께 적는다.
