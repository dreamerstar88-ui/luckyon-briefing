# 백테스트 릴스 제작 루틴

> 이 문서는 클라우드 루틴이 매주 읽고 그대로 수행하는 절차서다.
> 차트노트와 같은 **승인제**다. 제작까지만 하고, 발행은 사람이 확인한 뒤에 한다.

---

## 0. 준비

먼저 아래 문서를 **반드시 읽는다**. 규칙을 모르면 없는 것과 같다.

- `ROUTINE_COMMON.md` — 공통 규칙(브랜치·커밋·알림·검증)
- `scripts/backtest/_series.json` — 소재 백로그와 발행 이력

그다음 이번 회차 소재를 정한다.

1. `_series.json`의 `queue` 배열에서 **맨 앞 항목**을 꺼낸다.
2. 그 항목의 `stamp`가 `content/backtest/`에 이미 있으면 **중복 실행이므로 즉시 중단**하고 그대로 보고한다.
3. 큐가 비어 있으면 제작하지 말고 "소재 큐가 비었습니다"라고 알린 뒤 종료한다.

---

## 1. 데이터 수집

```bash
node scripts/backtest/fetch-prices.mjs <TICKER...> "KRW=X"
```

- 출처는 Yahoo Finance chart API. **웹검색으로 얻은 시세를 쓰지 않는다.**
- `KRW=X`는 마지막 요약 화면의 원화 환산에 쓰인다. 빠뜨리면 원화 표기가 생략된다.
- 응답의 `first_trade_date`와 `INCEPTION` 표가 어긋나면 경고가 뜬다. **경고가 뜨면 중단하고 보고한다.**

### 상장일 주의

시작일이 상장일보다 앞서면 그 구간은 실제 데이터가 없다. 큐 항목의 `start`가 상장일보다 빠르면 **상장일로 자동 조정**되며, 이 사실을 캡션과 화면에 반드시 밝힌다.

| 티커 | 상장일 |
|---|---|
| QQQ | 1999-03-10 |
| SPY | 1993-01-29 |
| QLD | 2006-06-21 |
| UPRO | 2009-06-25 |
| TQQQ | 2010-02-11 |

---

## 2. 백테스트 실행

```bash
node scripts/backtest/run-backtest.mjs \
  --tickers <TICKER> --amount <금액> --mode <dca|lump> --stamp <STAMP>
```

- `--mode dca` (적립식) → 비교군은 **적금**(1년 만기, 만기 원리금 재예치로 연복리)
- `--mode lump` (일시불) → 비교군은 **예금**(연복리)
- `--rate` 기본 0.03. 큐 항목에 값이 있으면 그것을 쓴다.

결과는 `content/<STAMP>.json`에 저장된다.

### 수치 검증 (건너뛰지 않는다)

저장된 JSON을 열어 아래를 직접 확인한다. 하나라도 어긋나면 **중단하고 보고**한다.

- `stats.invested` ≈ 거래일수 × 1회 납입액
- `stats.final_value`가 `series` 마지막 점의 `v`와 일치
- `stats.mdd_pct`가 -100% ~ 0% 범위
- `stats.benchmark_value`가 `invested`보다 크다 (이자가 붙었으므로)
- `stats.irr_pct`가 상식 범위 (-50% ~ +100%)

---

## 3. 사건 마커 점검

`data/events.json`에 이번 백테스트 구간에 걸치는 사건이 **3개 이상** 있는지 확인한다.

- 부족하면 사건을 추가한다. **단, 사실 확인된 것만 넣는다.**
- 추측·기억에 의존한 날짜는 절대 넣지 않는다. 확신이 없으면 넣지 않는 쪽을 택한다.
- `label`은 12자 이내. 길면 세로 라벨이 차트를 침범한다.

---

## 4. 렌더링

```bash
node scripts/backtest/render-backtest-reel.mjs --stamp <STAMP> --lang ko
```

산출물 (`cards/backtest/<STAMP>/ko/`):

| 파일 | 용도 |
|---|---|
| `reel.mp4` | 발행용 (1080×1920 / 30fps / H.264 yuv420p) |
| `cover.png` | 커버 |
| `preview.gif` | 사람 검토용 축소판 |

### 렌더링 후 눈 검증

`preview.gif`를 확인하고 아래를 점검한다.

- 사건 라벨이 서로 겹치지 않는가 (겹치면 `label`을 줄인다)
- 마지막 요약표의 6개 값이 JSON과 일치하는가
- 원화 환산이 마지막 화면에 **한 번만** 나오는가
- 그래프가 끊겨 보이지 않는가 (`series` 길이가 4,000 이상이어야 정상)

---

## 5. 캡션 생성과 검사

```bash
node scripts/backtest/write-caption.mjs --stamp <STAMP>
node scripts/backtest/lint-caption.mjs --stamp <STAMP>
```

**검사가 실패하면 발행 준비를 중단한다.** 금지 표현(매수 권유·수익 단정·위험 부정)이 잡히면 문구를 고치고 다시 검사한다.

캡션 숫자는 전부 JSON에서 자동으로 들어간다. **손으로 고쳐 쓰지 않는다.**

---

## 6. 커밋과 검토 요청

`ROUTINE_COMMON.md`의 커밋·푸시 절차를 따른다. 그다음:

1. 검토 페이지에 이번 회차를 추가한다 (차트노트와 같은 방식)
2. 카카오톡으로 검토 링크를 보낸다
3. **여기서 멈춘다.** 인스타그램 발행은 승인 후 별도 절차(`ROUTINE_PROMPT_BACKTEST_APPROVE.md`)에서 한다

알림 문구는 다음 형식으로 쓴다.

```
[백테스트 EP.<N>] <티커> <조건>
투입 $<X> → 평가 $<Y> (<N>배)
검토: <링크>
```

---

## 7. 마무리 보고

다음을 보고한다.

- 이번 회차 stamp와 소재
- 핵심 수치 3개 (투입 / 평가 / 배수)
- 검증에서 걸린 것이 있으면 그 내용
- `_series.json`의 `queue`에서 처리한 항목을 `done`으로 옮겼는지 여부

---

## 하지 말아야 할 것

- **투자 권유 표현** — "지금 사", "무조건", "보장", "추천". `lint-caption.mjs`가 잡지만 애초에 쓰지 않는다.
- **미래 단정** — "앞으로도 오릅니다" 같은 문장.
- **승인 없는 발행** — 이 루틴은 제작까지만이다.
- **수치 손대기** — 캡션·화면의 숫자는 전부 JSON에서 나온다. 보기 좋게 반올림하거나 고치지 않는다.
- **기간 편향 무시** — 레버리지 종목은 시작 시점에 따라 결과가 크게 달라진다. 상승장 초입에서 시작한 회차라면 캡션에 그 사실을 한 줄 넣는다.
