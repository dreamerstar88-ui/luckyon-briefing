# 주간 쇼츠 — 작업 매뉴얼 (기획부터 발행까지)

한 주의 나스닥100 선물 5분봉을 35초에 다시 돌려보는 쇼츠다. 유튜브 쇼츠와 인스타그램
릴스에 같은 영상을 올린다. **1회차(2026-08-31~09-04)를 처음부터 끝까지 한 번 다 해 보고
그 과정을 정리한 문서다.** 다음 회차는 이 순서대로 하면 된다.

- 공통 규칙은 `ROUTINE_COMMON.md` 를 따른다. 이 문서는 그 위에 주간 쇼츠만의 절차를 얹는다.
- 산출물 표본: `docs/samples/weekly-shorts/v7-test-video/`
- 매니페스트 표본: `content/weekly-shorts/2026-08-31.json`

---

## 0. 전체 흐름 한눈에

| 단계 | 하는 일 | 산출물 | 걸리는 시간 |
|---|---|---|---|
| 1 | 대상 주 확정 | — | 1분 |
| 2 | 5분봉 수집 | `us5m_nqf.json` | 1분 |
| 3 | 경제 캘린더 수집 | 캘린더 HTML | 2분 |
| 4 | 사건 5개 선정 | 매니페스트 초안 | 10분 |
| 5 | 문구 작성 | 매니페스트 완성 | 15분 |
| 6 | 영상 렌더링 | 프레임 1,050장 → mp4 | 3분 |
| 7 | 배경음악 붙이기 | 완성본 mp4 | 2분 |
| 8 | 자막 만들기 | ko·en srt | 1분 |
| **9** | **독립 검증 (필수)** | 검증 보고서 | 10분 |
| 10 | 공개 URL 만들기 | Pages 배포 | 5분 |
| 11 | 유튜브 발행 | 영상 + 자막 2벌 | 3분 |
| 12 | 인스타그램 발행 | 릴스 | 3분 |
| 13 | 기록 남기기 | 커밋 | 2분 |

**9단계를 건너뛰지 않는다.** 검증 없이 10단계로 넘어가지 않는다.

---

## 1. 대상 주 확정

지난주 **월요일 미국 정규장 개장 ~ 금요일 정규장 마감**이다. 그 사이 야간 선물 거래도
모두 포함한다. 선물이냐 현물이냐가 아니라 "일주일"이 기준이다.

야후 5분봉은 약 1개월치만 준다. **2주 넘게 지난 주는 이 파이프라인으로 만들 수 없다.**
지나간 주를 다루려면 다른 데이터 소스를 먼저 구해야 한다.

## 2. 5분봉 수집

```js
// query1.finance.yahoo.com/v8/finance/chart/NQ%3DF?interval=5m&range=1mo
// 타임스탬프는 UTC. 미 동부(EDT)는 -4시간, 겨울(EST)은 -5시간.
```

- 심볼은 `NQ=F` (나스닥100 선물). 야후는 지수 자체(^NDX)도 주지만 야간이 비어 5분봉 되감기에 못 쓴다.
- **11월 첫 일요일 이후 회차는 ET 오프셋을 -5 로 바꾼다.** 검증 스크립트 상단 `ET_OFFSET` 도 같이 고친다.
- 1주일이면 봉 약 1,180개가 나온다. 크게 다르면 데이터가 빈 것이니 멈추고 확인한다.

## 3. 경제 캘린더 수집

```
https://tradingeconomics.com/united-states/calendar?d1=YYYY-MM-DD&d2=YYYY-MM-DD
```

- 중요도는 HTML 의 CSS 클래스 `calendar-date-1|2|3` 에 들어 있다. 1=★, 2=★★, 3=★★★.
- 발표 시각은 그 안의 `<span>` 에 **UTC** 로 찍힌다. ET = UTC−4, KST = UTC+9.
- 실제값 `id='actual'`, 시장 예상 `id='consensus'`, 직전값 `id='previous'`.
- 파서: `docs/samples/weekly-shorts/v7-test-video/te-calendar-parse.mjs`
- 같은 발표 시각의 세부 지표들은 5분봉에서 한 봉이므로 **하나로 묶는다.**
  묶을 때 등급이 가장 높은 지표 이름을 대표로 쓴다.

> **캘린더 HTML 은 그 주가 끝난 직후 받아 `content/weekly-shorts/<날짜>.calendar.html` 로 저장한다.**
> 트레이딩이코노믹스는 GET 의 `d1`/`d2` 를 **무시하고 현재 주부터 돌려줄 때가 있다.**
> 지난 주 구간을 나중에 다시 받을 방법이 없으므로, 제작 시점의 원본을 남겨 두지 않으면
> 검증 단계에서 별표·실제값·예상값을 대조할 수 없다. 검증 스크립트는 실시간 조회가
> 대상 주를 주지 않으면 이 스냅샷으로 넘어가고, 그 사실을 화면에 남긴다.

> **`node` 로 받을 때는 `NODE_USE_ENV_PROXY=1` 이 필요하다.**
> node 의 전역 `fetch` 는 `HTTPS_PROXY` 를 스스로 타지 않아 방화벽에 직접 부딪히고,
> **허용 목록과 아무 상관 없는 `403 Host not in allowlist` 를 돌려준다.**
> 2026-09-08 에 이 메시지를 보고 "환경설정에서 도메인이 지워졌다"고 오진한 적이 있다.
> `curl` 은 프록시를 타므로 정상이다. **같은 URL 이 curl 로는 되고 node 로는 403 이면 프록시 문제다.**
> `verify-shorts.mjs` 는 이 환경변수를 켜고 자기 자신을 다시 띄우게 해 뒀다.

> **인베스팅닷컴은 못 쓴다.** 허용 목록에 넣어도 사이트가 봇 차단(403)을 돌려준다.
> 포렉스팩토리는 프록시 단계에서 막힌다. 트레이딩이코노믹스만 된다.
> 그마저 **환경의 접속 허용 목록에 `tradingeconomics.com` 이 있어야 한다.**
> 없으면 클로드 환경 편집 > 접속 가능 사이트에 추가한다.

## 4. 사건 5개 선정 — 규칙

대표가 정한 규칙이다. 작업자가 바꾸거나 덧붙이지 않는다.

1. **하루에 최소 1개.**
2. 거시지표는 **★★★ 우선. 그날 ★★★ 가 없으면 ★★** 로 내려간다.
3. **같은 조건이면 장중(미 동부 09:30~16:00) 우선.** 장중끼리 겹치면 그 다음에 등락률을 본다.
   프리장·애프터장은 거래량이 얕아 등락률을 그대로 비교하기 어렵다.
   그날 후보가 하나뿐이면 프리장이어도 그대로 쓴다(매니페스트에 `session_exception` 으로 사유를 적는다).
4. 기업 실적이 같은 날 여럿이면 **시가총액이 큰 기업** 우선.
5. 뉴스는 **주가 변동에 크게 영향을 준 것** 우선, **최초 보도 시각** 기준.
6. **시각이 정해진 사건만.** "인플레 우려로 FOMC 부담" 같은 기간 전체의 분위기는 5분봉에 찍을 시각이 없다.
7. **주간 최고·최저 같은 구조점 제외.** 한 주가 끝나야 알 수 있는 값이라 그 시점의 사건이 아니다.
   **정규장 개장·마감도 사건이 아니다.**

### 자주 틀리는 자리 (1회차에서 실제로 틀렸다)

- **별표는 우리가 매기는 게 아니다.** 시장 참여자들이 중요하다고 본 것을 캘린더가 표기해 둔 것이다.
  기억으로 쓰지 말고 반드시 캘린더에서 뽑는다.
- **예정된 지표라고 다 사건은 아니다.** 반대로, 발표됐는데 시장이 꿈쩍도 안 한 것 역시 그 주에 대한
  정보다. 등락률이 0에 가까워도 ★★★ 이면 목록에 남긴다. **움직임의 크기로 사건 여부를 판정하지 않는다.**
- 큰 움직임에 그럴듯한 뉴스를 갖다 붙이지 않는다. 최초 보도 시각을 확정하지 못하면 라벨을 붙이지 않는다.

자세한 적용 예는 `docs/samples/weekly-shorts/v7-test-video/사건선정기준.md`.

## 5. 문구 작성

사건 문구는 **발표된 값과 비교값만** 적는다. 두 줄로 나눈다.

| | 내용 | 색 |
|---|---|---|
| 첫 줄 | 실제 발표값 (주인공) | 사건 색 (노랑·초록·빨강) |
| 둘째 줄 | 예상값 또는 직전값 (비교 기준) | 회색 `#8f9aad` |

"지수는 무반응", "예상을 넘겼는데 하락" 같은 **반응 해석은 넣지 않는다.** 그건 그래프가 보여 주는
일이고, 글로 한 번 더 말하면 보는 사람의 판단을 앞지른다.

다섯 개가 "실제값 / 비교값" 한 형태로 통일되면 넘어갈 때 리듬이 생긴다.

사건 색은 그 봉의 반응을 따른다 — |등락률| 0.05% 미만이면 노랑, 상승 초록, 하락 빨강.
번호 배지와 그래프 세로선도 같은 색이라 글 묶음과 그래프가 묶인다.

**여기까지 정리한 것을 매니페스트(`content/weekly-shorts/<날짜>.json`)에 적는다.**
검증 스크립트가 이 파일을 기준으로 대조하므로, 매니페스트가 없으면 검증도 없다.

## 6. 영상 렌더링

`docs/samples/weekly-shorts/v7-test-video/` 의 `scene.js`(그리기)와 `render.mjs`(구동)를 복사해
사건 배열만 바꾼다.

```bash
node render.mjs --out=frames          # 프레임 1,050장, 약 70초
node render.mjs --out=sv --sample=3.5,20.0,28.0   # 특정 시각만 확인
```

- Playwright 헤드리스 크로미움이 `<canvas>` 에 그리고 프레임을 JPEG(q92)로 찍는다.
- 폰트는 base64 로 HTML 에 심는다. 외부 폰트를 부르면 렌더가 깨진다.
- 구성 (35초): 훅 0~5.0 / 되감기 5.0~26.2 / 정답 26.2~30.3 / 요약·루프 30.3~35.0
- **훅은 문구를 3.05초에 다 띄우고 1.95초 세워 둔 뒤 5.0초에 넘어간다.**
  `HOOK_HOLD` 값 하나로 조절된다. 훅 길이를 바꿔도 정지 시간은 유지된다.
- 그리는 순서는 **세로 라벨 → 주가 선**이다. 주가 선이 라벨 위를 지나가야 한다.
- 숫자는 `numT()` 로 자릿수 폭을 고정해 그린다. 캔버스에는 `tabular-nums` 가 안 먹는다.

```bash
FF=node_modules/ffmpeg-static/ffmpeg
$FF -y -framerate 30 -i frames/f%05d.jpg -c:v libx264 -preset slow -crf 18 \
   -pix_fmt yuv420p -movflags +faststart silent.mp4
```

## 7. 배경음악

```bash
# loudnorm 을 페이드보다 먼저 걸어야 끝이 제대로 무음이 된다.
# 순서를 바꾸면 loudnorm 이 잦아드는 구간을 도로 끌어올려 끝이 -28dBFS 에 머문다.
$FF -y -ss <시작초> -i 곡.mp3 -t 35 \
   -af "loudnorm=I=-14:TP=-1.5:LRA=11,afade=t=in:st=0:d=3.0,afade=t=out:st=30.3:d=4.7:curve=ipar" \
   -ar 48000 -ac 2 bgm.wav
$FF -y -i silent.mp4 -i bgm.wav -map 0:v:0 -map 1:a:0 -c:v copy \
   -c:a aac -b:a 192k -ar 48000 -ac 2 -shortest -movflags +faststart reel.mp4
```

- `<시작초>` 는 곡에서 가장 힘이 붙는 지점이 **정답 공개 시각(26.2초)** 에 오도록 잡는다.
- 페이드아웃 곡선은 `ipar`. 앞부분 음량을 유지하다 뒤에서 미끄러진다.
  직선(`tri`)이나 `esin` 은 페이드 시작하자마자 8~9dB 떨어져 "끊긴다"는 인상을 준다.
- 원곡이 자연히 잦아드는 지점과 페이드가 겹치지 않게 시작점을 잡는다. 겹치면 너무 일찍 사라진다.
- 완성본 목표: **-14 LUFS · 트루피크 -1.5 dBTP 이하.**
- 음원은 대표가 구글 드라이브에 올려 주신 것을 커넥터로 받는다. **11MB WAV 는 한도 초과로
  실패하고 2.4MB mp3 는 된다.** 다음에도 mp3 로 올려 주시면 된다.

## 8. 자막

`docs/samples/weekly-shorts/v7-test-video/srt.mjs` 가 영상 타임라인 값에서 구간을 계산해
ko·en 두 벌을 만든다. 사건 정지 시각과 자막 구간이 정확히 맞는다.

이 영상에는 말소리가 없다. 자막은 "들리는 말"이 아니라 화면에서 벌어지는 일을 글로 옮긴 것이다.
자막 버튼을 켜는 시청자는 많지 않겠지만, 유튜브가 글자로 읽을 수 있어야 검색·추천에 걸린다.

---

## 9. 독립 검증 — 여기서 멈추고 반드시 한다

`ROUTINE_COMMON.md` §3 의 단일 QA 게이트가 이 축에도 그대로 적용된다.
**만든 세션이 스스로 재검토하면 확증편향으로 놓친다.** 별도 에이전트를 띄운다.

### 9-1. 기계 검사

```bash
node scripts/weekly-shorts/verify-shorts.mjs content/weekly-shorts/<날짜>.json \
     --srt docs/samples/weekly-shorts/<폴더>
```

원본 5분봉과 캘린더를 **새로 받아** 독립적으로 재계산해 매니페스트와 대조한다.
만든 세션의 중간 산출물은 쓰지 않는다. 검사 항목:

| 번호 | 검사 |
|---|---|
| [2] | 봉 개수 · 주간 등락률 · 최대낙폭 · 고점 · 저점 · 주간 최고/최저 · 퀴즈 정답 보기 |
| [3] | 사건별 5분봉 등락률과 그 주 순위 |
| [4] | 요일당 최소 1개 · 장중 발표 여부(장 밖이면 사유 필수) |
| [5] | 캘린더의 별표 · 실제값 · 예상값 |
| [6] | 화면 문구가 그 숫자를 실제로 담고 있는가 (162K ↔ 16.2만 단위 환산 포함) |
| [7] | 구간 연속성 · 전체 길이 · 훅 정지 시간 · 되감기 시간이 양수인가 |
| [8] | 자막 구간이 사건 정지 시각과 맞는가 |

종료코드 0 이 아니면 **발행 금지.** `⛔` 는 네트워크 차단으로 확인 자체를 못 한 것이며,
통과가 아니라 **미검증**이다.

### 9-2. 판단 검사 (에이전트)

`shorts-verifier` 에이전트를 띄운다(`.claude/agents/shorts-verifier.md`).
기계가 못 보는 것을 사람 눈으로 본다.

> **에이전트 타입은 세션이 시작될 때 등록된다.** 이 정의 파일을 방금 만들었거나 고쳤다면
> 그 세션에서는 `shorts-verifier` 를 이름으로 부를 수 없다("Agent type not found").
> 그럴 때는 범용 에이전트를 띄우고 **"`.claude/agents/shorts-verifier.md` 를 읽고 그 지시서를
> 그대로 따르라"** 고 시킨다. 다음 세션부터는 이름으로 바로 불린다.

- 영상 프레임을 실제로 뽑아 **읽는다.** 오타·조사·영어 철자, 글자 잘림, 쇼츠 UI 가림, 검은 프레임
- 배색 규칙(첫 줄 사건 색 / 둘째 줄 회색), 세로 라벨 위로 주가 선이 지나가는지
- 화면·자막·설명란이 **서로 같은 사실을 말하는지**. 하나만 고치고 나머지를 안 고치는 사고가 잦다
- 정답 숫자가 제목·표지에 새지 않았는지
- 금지 표현(매수 권유·수익 단정·위험 부정), 면책 문구 유무
- 그 주에 **더 높은 등급의 후보가 있었는데 빠지지 않았는지** — 캘린더를 다시 받아 요일별로 비교

> 백그라운드로 띄운 뒤에는 `ScheduleWakeup` 류를 같이 호출하지 않는다.
> 2026-08-13 세션에서 그렇게 했다가 에이전트가 통째로 중단된 사고가 있었다.
> 완료 알림이 올 때까지 다른 작업을 섞지 말고 기다린다.

### 9-3. 판정

- FAIL 이면 고치고 **처음부터 다시 검증한다.** 고친 항목만 재검사하지 않는다.
- BLOCKED(미검증 있음)이면 그 항목을 사람이 직접 확인하거나 환경을 고친 뒤 다시 돌린다.
- PASS 여야 10단계로 넘어간다.

---

## 10. 공개 URL 만들기

**유튜브도 인스타그램도 로컬 파일을 받지 않는다.** 공개 URL 이 먼저 있어야 한다.

```bash
git worktree add /tmp/live FETCH_HEAD --detach   # origin/claude/live 기준
mkdir -p /tmp/live/cards/reels/<stamp>/ko
# reel.mp4 · cover.png · caption.txt · subtitles.ko.srt · subtitles.en.srt 를 넣는다
cd /tmp/live && git add cards/reels/<stamp> && git commit && git push origin HEAD:claude/live
```

- `caption.txt` 가 없으면 인스타 발행 스크립트가 멈춘다. 선택이 아니라 필수 산출물이다.
- **`git add -A` 를 쓰지 않는다.** 자기 경로만 add 한다(`ROUTINE_COMMON.md` §4).
- Pages 배포는 `pages-deploy.yml` 워크플로다. 보통 3분 안쪽.

> **이 세션은 `github.io` 로 직접 접속이 막혀 있다**(프록시 403).
> 배포 완료 여부는 **GitHub Actions 워크플로 실행 상태**로 확인한다.
> 유튜브·메타 서버가 직접 URL 을 받아 가므로 세션의 접속 차단은 발행에 문제가 되지 않는다.

## 11. 유튜브 발행

Zapier MCP 의 YouTube 커넥션을 쓴다.

**먼저 채널을 확인한다.** 연결이 여러 개라 엉뚱한 채널에 올릴 수 있다.

```
youtube_make_api_get_request
  url = https://www.googleapis.com/youtube/v3/channels
  querystring = { part: snippet, mine: true }
→ id 가 UCdqD5SpqpXZJUzCZILyeRAA (luckyon 넘버뷰) 인 연결을 쓴다
```

`youtube_upload_video` 에 공개 URL 을 그대로 넣는다. 채널 관행을 따른다:

| 항목 | 값 |
|---|---|
| 카테고리 | 25 (News & Politics) |
| 언어 | `default_language` · `default_audio_language` 모두 `ko` |
| 아동용 | false |
| 공개 | public |
| 제목 끝 | `[시리즈 nn] #Shorts` |
| 태그 | 채널 고정 태그 + 그 회차 어휘, 40개 안팎 |
| 구독자 알림 | 기본 켜짐. **업로드 순간에만 적용되고 나중에 못 바꾼다** |

**자막**은 Zapier 의 YouTube 액션 목록에 업로드가 없다. 원시 요청으로 직접 보낸다.

```
youtube_make_api_mutating_request
  url = https://www.googleapis.com/upload/youtube/v3/captions
  querystring = { part: snippet, uploadType: multipart }
  headers = { Content-Type: multipart/related; boundary=<경계문자열> }
  body = 경계로 나눈 두 파트 — JSON 스니펫 + SRT 본문
```

SRT 는 텍스트라 이 경로로 올라간다. `status: serving` 이 나오면 성공이다. ko·en 각각 한 번씩.

## 12. 인스타그램 발행

```bash
SKIP_PAGES_WAIT=1 node scripts/reels/publish-reel.mjs <stamp> ko
```

- `IG_ACCESS_TOKEN` · `IG_USER_ID` · `PAGES_BASE_URL` 환경변수가 있어야 한다.
- 토큰은 60일짜리다. `IG_TOKEN_EXPIRES_AT` 를 먼저 본다.
- 컨테이너 인코딩에 1분 안쪽 걸린다. `FINISHED` 가 되면 발행된다.
- `SKIP_PAGES_WAIT=1` 은 이 세션이 `github.io` 에 못 닿기 때문이다. Pages 배포는 이미 10단계에서 확인했다.

## 13. 기록 남기기

매니페스트에 `youtube_video_id` · `instagram_media_id` · `instagram_permalink` 를 채우고
작업 브랜치에 커밋한다. 다음 회차가 앞 회차를 참조할 수 있어야 한다.

---

## 1회차 검증에서 실제로 걸린 것 (같은 실수를 반복하지 않기 위해)

검증 에이전트를 처음 돌렸더니 발행본에서 이만큼 나왔다. **검증이 없었으면 전부 그대로 나갔다.**

| 무엇 | 왜 생겼나 | 어떻게 막았나 |
|---|---|---|
| 훅 영어 줄이 화면 밖으로 잘림 | `enBand()` 에 폭 제한이 없었다 | 968px 안에 들어오게 글자를 줄이고, 그래도 넘치면 말줄임 |
| "하루 반 만에" 가 실제 29시간을 36시간으로 부풀림 | 어림말을 손으로 적었다 | 봉 간격에서 계산해 "1.2일" 처럼 자동 생성 |
| 요약·정답·루프의 숫자가 전부 문자열 상수 | 다음 회차에 옛 숫자가 남는다 | 전부 `BARS`·`STATS` 에서 계산 |
| 루프 전환에서 글자 위에 글자가 1초 겹침 | 요약과 루프 카드가 동시에 반투명 | 요약이 먼저 빠지고 그 뒤에 루프가 들어오게 분리 |
| 검증 스크립트의 훅 정지 시간 검사가 항등식 | `(a-b)-((a-b)-c)` 는 언제나 `c` | `scene.js` 의 실제 상수를 읽어 대조 |
| 오차 범위 통과가 값 차이를 숨김 | 29,705 vs 29,704 가 ✅ 뒤에 가려짐 | 오차 내여도 두 값을 함께 출력 |
| 캘린더 대조가 다른 주 값을 긁어옴 | 행 날짜를 확인하지 않았다 | 대상 주 밖의 행은 버리고, 하나도 없으면 실패 처리 |
| 문서가 매니페스트와 모순 | 사건5가 프리장인데 "다섯 개 모두 장중" | 문서 수정 + 스크립트가 `session_exception` 을 요구 |

아직 남은 것(다음 회차에 판단):
- 세로 라벨 ④⑤ 의 일부 글자가 주가 선에 덮여 잘 안 읽힌다. 라벨 y 를 선에서 비키게 옮기는 안.
- "지난주"와 "이번 주"가 한 영상 안에 섞여 있다. 표현을 통일할 것.
- 되감기 헤더 날짜는 KST, x축 요일은 ET 거래일이라 "09/04 금" 칸 위에 "9/5 토"가 뜬다.
- 사건1만 비교 기준이 직전값(previous)이고 나머지는 예상값(consensus)이다.

## 발행 뒤에 볼 것

첫 회차 기준 두 가지만 본다.

- **4~9초 구간 이탈률** — 훅을 5초로 늘린 게 이번 판의 큰 변경이다. 예전에 7.6초짜리 정적
  인트로가 이 구간에 이탈 절벽을 만든 기록이 있다. 무너지면 훅을 4초로 줄인다.
- **26초 전후 유지율** — 정답이 공개되는 지점. 여기까지 남은 사람이 많으면
  "절반 공개, 절반 은폐" 구조가 통한 것이다.

## 이미 발행한 뒤 오류를 발견했다면

`ROUTINE_COMMON.md` §8 을 따른다. 영상 자체를 고쳐야 하면 두 플랫폼 모두 삭제하고 다시 올려야
하며, 조회수와 초반 노출이 초기화된다. **그래서 9단계 검증이 있는 것이다.**
