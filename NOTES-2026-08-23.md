# 2026-08-23 평일 카드 개편 — 다음 회차에서 확인할 것

> **성격**: 지침서가 아니라 일회성 인수인계 메모다. 규칙은 이미 `FORMAT_BRIEFING.md` §1·§5 와
> `ROUTINE_PROMPT.md` 에 들어가 있다. 확인이 끝나면 지워도 된다.
>
> **브랜치**: `main`. 코드·지침서는 `main` 이 본류이고 `claude/live` 는 발행물 전용이다.

---

## 0. 무엇이 바뀌었나

**평일(am·pm) 카드가 9장에서 10장 고정 편성으로 바뀌었고, 그리는 방식이 통째로 달라졌다.**

예전에는 `sections[].type` 이 카드 모양을 정했고 카드가 전부 비슷한 표·타일이었다.
지금은 **장마다 모양이 다르다** — 선 차트, 0 기준 발산 막대, 순위 가로 막대, 등락 비율 띠,
타임라인. 그리고 **카드마다 그날 내용으로 만든 사진**이 배경에 깔린다.

`scripts/render-cards.mjs` 는 이제 라우터다. `am`·`pm` 이면 `render-cards-day.mjs` 로,
`sat`·`sun` 이면 각각의 렌더러로 위임한다. **호출 명령은 그대로다.**

```
node scripts/render-cards.mjs <DATE> <ko|en> <am|pm>
```

---

## 1. 세션이 새로 해야 하는 일

### 1-1. 배경 사진을 먼저 만든다

콘텐츠 JSON 에 `card_photos` 를 넣고, **렌더 전에** 이것을 돌린다.

```
node scripts/gen-card-photos.mjs <DATE> <SESSION>
```

```json
"card_photos": {
  "1": "The NYSE trading floor moments after the closing bell, traders in silhouette against a wall of glowing screens, warm amber light raking across the room.",
  "3": "A dark trading floor at night, index numbers streaming across a curved wall of screens."
}
```

- **영문으로 쓰는 편이 잘 나온다.**
- 사진은 배경에서 **크게 흐려지고 어두워진다.** 세부 묘사보다 **구도·명암·색조**를 적는다.
- **표지(1번)만 흐리지 않고 그대로 쓰인다.** 여기에 가장 신경 쓴다.
- 전부 적을 필요는 없다. 적지 않은 장은 저장소에 든 사진으로 내려간다.
- 키(`GEMINI_API_KEY`)가 없거나 생성이 실패해도 **브리핑을 멈추지 않는다.** 그 장만 번들 사진을 쓴다.

### 1-2. 차트 데이터를 넣는다

**이걸 빠뜨리면 3장의 차트가 통째로 비고 2장의 큰 타일도 밋밋해진다.**

```json
"chart_series_values": [26460.2, 26588.4, 26803.0, 26742.1, 26680.5, 26312.4, 26370.9, 26067.2, 26180.5],
"chart_series_labels": ["8.11","8.12","8.13","8.14","8.17","8.18","8.19","8.20","8.21"]
```

최근 **9거래일 종가**. 한국은 `fetch-krx.mjs`, 미국은 야후 차트 API 에서 받는다.
두 배열의 길이는 같아야 한다. 8~10점이 적당하다 — 그보다 촘촘하면 x축 라벨이 겹친다.

### 1-3. `sections` 는 제목으로 찾는다

`type` 은 더 이상 보지 않는다. **아래 제목을 글자 그대로** 써야 그 카드가 그려진다.

| 장 | `title_ko` | 필요한 필드 |
|---|---|---|
| 3 | `미국장 기록` (pm 은 `코스피 · 코스닥 기록`) | `stats` 2개 · `breadth` · `flows` |
| 4 | `섹터별 등락` | `bars` 10~15개 |
| 5 | `거래대금 상위` | `rows` 5개 (`short` 넣으면 공매도 열) · `share` |
| 6 | `실적 · 지표 발표` | `stats` 최대 6개 |
| 7 | `AI · 반도체 기술` | `items` 4건 |
| 8 | `주요 소식` | `items` 4건 |

1·2·9·10 장(표지·시장 한눈에·일정·아웃트로)은 `sections` 가 아니라 **최상위 필드**로 그린다.

### 1-4. 표지 3줄

```json
"cover_kicker": "간밤 미국장 · 8/21 마감",
"cover_facts": [
  ["shield", "다우 +0.98% · S&P 500 +0.43%", "53,277.01 / 7,674.37 · 3대 지수 모두 상승"],
  ["bond",   "그런데 주간으로는 S&P −1.4%",   "7월 이후 첫 주간 하락 · 30년물 금리 2주 연속 상승"],
  ["chip",   "돈은 오른 종목으로 갔다 · 57.7%", "상승 $204.7B 대 하락 $149.8B"]
]
```

아이콘은 `shield · bond · chip · rate · ai · globe · clock · target` 중에서 고른다.

**전체 스키마는 `FORMAT_BRIEFING.md` §5 에 있다.** `content/example-am.json` 이 실제로 도는 예제다.

---

## 2. 확인표

- [ ] 카드가 **10장** 나왔는가
- [ ] 1장 표지에 사진이 깔리고 아이콘 3줄이 보이는가
- [ ] 2장에 지표 **10개 전부**와 큰 타일 안의 작은 차트가 있는가
- [ ] 3장에 **선 차트**가 그려졌는가 (비어 있으면 `chart_series_values` 누락)
- [ ] 3장 등락 종목수 띠와 투자자별 순매수 막대가 라벨과 겹치지 않는가
- [ ] 4장 섹터 막대가 0선을 기준으로 좌우로 뻗는가
- [ ] 5장 순위 막대 + 등락률(+미국장이면 공매도) 열이 보이는가
- [ ] 6장 실적·지표 타일이 2열로 채워졌는가
- [ ] 7·8장 글 카드가 각각 4건인가
- [ ] 9장 타임라인과 운영시간 상자가 보이는가
- [ ] 모든 카드 머리 로고의 `o` 가 앰버 원판 + 흰 클로버인가
- [ ] 어느 카드도 본문이 푸터에 닿지 않는가

---

## 3. 증상별 대처

| 증상 | 원인 | 대처 |
|---|---|---|
| 카드가 9장만 나옴 | 옛 렌더러 | `git pull origin main` |
| 3장 차트가 빔 | `chart_series_values` 누락 | 9거래일 종가를 넣는다 |
| 어떤 카드가 통째로 빔 | `sections` 제목이 표와 다름 | 제목을 글자 그대로 맞춘다 |
| 배경이 전부 같은 사진 | `card_photos` 없음 또는 생성 실패 | 로그를 본다. 실패해도 발행은 진행 |
| 글자가 시스템 폰트처럼 보임 | 폰트 파일 누락 | `assets/fonts/Pretendard-*.woff2` 확인 |
| 사진이 너무 밝아 글자가 안 읽힘 | 프롬프트가 밝은 장면 | "deep shadows, low key" 를 넣어 다시 만든다 |

---

## 4. 손댄 파일

| 파일 | 무엇 |
|---|---|
| `scripts/render-cards-day.mjs` | **신규** — 평일 10장 렌더러 |
| `scripts/lib/cardkit.mjs` | **신규** — 선 차트·발산 막대·순위 막대·타임라인·아이콘 |
| `scripts/gen-card-photos.mjs` | **신규** — 내용에 맞는 배경 사진 생성 |
| `scripts/render-cards.mjs` | am·pm 위임 추가 (sat·sun 과 같은 방식) |
| `assets/fonts/Pretendard-*.woff2` | **신규** — 5종 |
| `assets/photos/*.jpg` | **신규** — 생성 실패 시 쓰는 예비 사진 10장 |
| `content/example-am.json` | **신규** — 실제로 도는 예제 |
| `FORMAT_BRIEFING.md` §1·§5 | 편성·스키마 교체 |
| `ROUTINE_PROMPT.md` | 섹션 구성표를 10장 편성으로 · 사진 생성 단계 |

**절 구성은 어느 문서도 건드리지 않았다.** 제목 목록을 원본과 대조해 확인했다.
