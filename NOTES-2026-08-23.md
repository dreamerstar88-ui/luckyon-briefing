# 2026-08-23 개편 — 다음 회차에서 확인할 것

> **성격**: 지침서가 아니라 일회성 인수인계 메모다. 규칙 자체는 이미
> `FORMAT_BRIEFING.md` §2 와 `ROUTINE_PROMPT.md` 에 들어가 있다. 확인이 끝나면 지워도 된다.
>
> **브랜치**: 이 파일과 아래 변경은 전부 **`main`** 에 있다. 코드·지침서는 `main` 이 본류이고
> `claude/live` 는 발행물 전용이다.

---

## 1. 세션이 아무것도 안 해도 되는 것

### 브랜드 로고

머리·꼬리·아웃트로의 `luckyon 브리핑` 이 **글자에서 확정 로고 파일로** 바뀌었다.
로고의 `o` 자리는 **앰버 원판 안의 흰 네잎클로버**라 글자로는 재현할 수 없었다.

배경 밝기에 따라 파일이 다르다.

| 렌더러 | 종이색 | 파일 |
|---|---|---|
| 평일 `render-cards.mjs` | 어두움 `#0d0d0d` | `wordmark-briefing.png` (통짜) |
| 주말 `render-cards-sat/sun.mjs` | 크림 `#f2efe6` | `wordmark-luckyon-ink.png` (먹색, luckyon 까지만) |

주말은 `브리핑`/`Briefing` 을 **글자로 남겼다** — 그 축은 영문 카드에서 번역하고 색도
`P.accent`(sat 초록·sun 보라)를 따르기 때문이다. 통짜 로고를 넣으면 번역도 색도 사라진다.

### 수급 파서 · 윈도우 경로

- `fetch-krx-flows.mjs` 가 **고쳐졌다.** 조회 주소의 `sosok` 파라미터가 표를 비우고 있었다.
  단위도 백만원 → **억원**으로 바로잡았고 **기타법인**을 넣어 네 주체 합이 0 이 되는지 검산한다.
- 경로 계산이 깨지던 스크립트 **12개**를 고쳤다 (`new URL(...).pathname` → `fileURLToPath`).
  공백이 든 경로에서 파일을 못 찾던 문제다.

---

## 2. 세션이 JSON 에 넣어야 나오는 것

### 2-1. 거래대금 상위 카드의 공매도 비중 열 (미국장)

`rank` 카드의 행에 `short` 를 넣으면 열이 자동으로 생긴다. 값은
`node scripts/fetch-us-flows.mjs <YYYY-MM-DD>` 의 `topTurnover[].shortPct`. **키가 필요 없다.**

```json
{ "name_ko": "테슬라", "name_en": "Tesla", "value": "$20.98B", "pct": 5.14, "short": 47.5 }
```

### 2-2. 공매도 막대는 반드시 `mode: "compare"`

```json
"share": {
  "mode": "compare",
  "label_ko": "종목별 공매도 비중", "label_en": "Short volume share by name",
  "baseline": 46.8,
  "baseline_label_ko": "표본 평균", "baseline_label_en": "Sample average",
  "segments": [ { "label_ko": "테슬라", "label_en": "Tesla", "pct": 47.5, "color": "#e66767" } ]
}
```

`mode` 를 빼면 `nested` 가 되어 조각처럼 이어 붙고 **"합쳐서 107%"** 로 읽힌다.
`baseline` 에는 `fetch-us-flows.mjs` 의 `shortVolume.sampleAvgPct` 를 넣는다.

거래대금 비중처럼 **진짜 조각인 값**(상위 2종목 52.4% / 나머지 47.6%)은 `nested` 가 맞다.

### 2-3. 실적 · 지표 발표는 `type: "econ"`

전에는 `type:"stats"` 로 만들던 자리다. 이제 전용 카드가 있다.

```json
{
  "type": "econ", "color": "#5b8dd6",
  "title_ko": "실적 · 지표 발표", "title_en": "Earnings & Data",
  "col_delta_ko": "예상·전년 대비", "col_delta_en": "vs Est./YoY",
  "rows": [
    { "region": "KR", "name_ko": "한국 7월 수출", "name_en": "Korea July exports",
      "sub_ko": "통관 기준 · 전년동월 대비", "sub_en": "Customs basis, YoY",
      "actual": "989.6억$", "estimate": null, "surprise": 63.0 },
    { "region": "US", "name_ko": "월마트 2분기", "name_en": "Walmart Q2",
      "sub_ko": "주당순이익 · 8/20 발표", "sub_en": "EPS · reported 8/20",
      "actual": "$0.81", "estimate": "$0.73", "surprise": 10.96 }
  ]
}
```

- `region:"KR"` 행을 **렌더러가 위로 올린다.** JSON 순서는 신경 쓰지 않아도 된다.
- 행은 **6건까지**, `col_delta_ko` 는 **8자 안쪽** (넘으면 열 제목이 두 줄로 접힌다).
- 미국 실적 실측치는 기존대로 FMP `earnings-calendar` 의 `epsActual` 을 쓰면 된다
  (`ROUTINE_PROMPT.md` 의 공시·실적 조회 경로 표). 한국 기업 실적은 OpenDART.

---

## 3. 한국 수출입 — `node scripts/fetch-kr-trade.mjs`

관세청을 먼저 두드리고 안 열리면 한국은행 ECOS 로 내려간다.
**어느 쪽을 썼는지 출력 `source` 에 남으니 카드 각주에 그대로 옮긴다.**

2026-08-23 로컬 실행 확인값 (2026년 7월분):

| 항목 | 값 |
|---|---|
| 수출 | 989.6억 달러 (전년동월 **+63.0%**) |
| 수입 | 685.7억 달러 |
| 무역수지 | **+303.9억 달러** |
| 중화학공업품 | 872.0억 달러 = 수출의 **88.1%** (+69.7%) |
| 수입 원자재/자본재/소비재 | 46.1% / 40.8% / 13.1% |

### 키 — 이번 회차에 확인이 필요한 유일한 항목

`ECOS_API_KEY` 를 쓴다. **저장소 시크릿에 넣은 것은 워크플로만 본다** — 세션이 직접 돌리는
이 스크립트는 **루틴 환경 설정**의 값을 읽는다. 없으면 이렇게 말하고 멈춘다:

```
ECOS_API_KEY 가 없습니다. 클라우드에서 돌 때는 루틴 환경 설정에 ECOS_API_KEY 를
넣어야 합니다 … 키가 없으면 이 카드의 한국 지표 자리는 비우고 진행하십시오.
```

**이때 브리핑을 멈추지 않는다.** 한국 수출입만 빼고 나머지로 `econ` 카드를 채우고,
키가 없었다는 사실을 마무리 보고에 적는다.

---

## 4. 확인표

- [ ] 모든 카드 머리 로고의 `o` 가 앰버 원판 + 흰 클로버인가
- [ ] 주말판이면 `luckyon` 이 **먹색**인가 (흰색이면 크림 종이에 묻힌다)
- [ ] 주말 영문판에서 `Briefing` 이 번역돼 있고 accent 색인가
- [ ] 꼬리·아웃트로 로고도 같은 모양인가
- [ ] 거래대금 상위 표에 열 제목이 붙었는가
- [ ] 미국 거래대금 카드에 공매도 비중 열이 있는가
- [ ] 공매도 막대가 종목마다 **따로** 그려지고 회색 기준선이 서 있는가
- [ ] 실적·지표 카드가 `econ` 으로 나왔는가. 한국 항목이 위에 있는가
- [ ] `econ` 마지막 열 제목이 두 줄로 접히지 않았는가
- [ ] 수급이 `data/krx-flows.json` 에서 값을 갖고 왔는가 (`checksum` 이 0 인가)
- [ ] 어느 카드도 본문이 푸터에 닿지 않는가

---

## 5. 증상별 대처

| 증상 | 원인 | 대처 |
|---|---|---|
| 로고가 파란 `브리핑` 글자 | 렌더러가 옛 코드 | `git pull` 후 다시 렌더 |
| 로고 자리가 빈 네모 | `assets/brand/` 없음 | `git pull` |
| 주말판 `luckyon` 이 안 보임 | 어두운 배경용 로고를 씀 | `wordmark-luckyon-ink.png` 인지 확인 |
| 공매도 열 없음 | `rows[]` 에 `short` 가 하나도 없음 | `fetch-us-flows.mjs` 의 `shortPct` 를 넣는다 |
| 공매도 막대가 한 줄로 이어짐 | `share.mode` 누락 | `"mode": "compare"` 추가 |
| 실적·지표가 글 카드로 나옴 | `type` 누락 | `"type": "econ"` 추가 |
| `econ` 열 제목이 두 줄 | `col_delta_ko` 8자 초과 | 줄인다 |
| 한국 수출입 없음 | `ECOS_API_KEY` 미설정 | 그 항목만 빼고 진행 · 보고에 기록 |

---

## 6. 손댄 파일

| 파일 | 무엇 |
|---|---|
| `scripts/render-cards.mjs` | 로고 · rank 열 제목/공매도 열 · `share.mode` · `econ` 카드 |
| `scripts/render-cards-sat.mjs` · `-sun.mjs` | 먹색 로고 (번역·accent 색 유지) |
| `scripts/fetch-krx-flows.mjs` | `sosok` 제거 · 억원 · 기타법인 · 검산 |
| `scripts/fetch-us-flows.mjs` | **신규** — 미국 거래대금 · 공매도 · 상승/하락 거래대금 |
| `scripts/fetch-kr-trade.mjs` | **신규** — 한국 수출입 (관세청 → ECOS 자동 전환) |
| 스크립트 12개 | 윈도우·공백 경로 수정 |
| `assets/brand/` | **신규** — 확정 로고 3종 |
| `FORMAT_BRIEFING.md` · `ROUTINE_PROMPT.md` · `ROUTINE_COMMON.md` | 표·목록 안에만 반영 |

**절 구성은 세 문서 모두 건드리지 않았다.** 원본과 제목 목록을 대조해 확인했다.
