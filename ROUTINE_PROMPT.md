# luckyon 브리핑 루틴 절차서

> **루틴 설정 방법**: claude.ai/code/routines 에 아래처럼 루틴을 등록하세요. Instructions 칸에는 두 줄만 넣습니다.
> 하루 총 **2개** 루틴(아침/저녁)을 등록해 운영합니다. 각 루틴이 한국어·영어 카드를 모두 만들어 각각 발행합니다.
>
> **트리거는 요일 기준 "주중(weekdays, 월~금)"으로 지정합니다.** 루틴 등록 화면의 Select a trigger 에서 "weekdays" 프리셋을 고르면 되고, 더 세밀한 조정이 필요하면 프리셋으로 만든 뒤 CLI에서 `/schedule update` 로 cron 표현식(예: `35 7 * * 1-5`)을 지정할 수 있습니다. 시각은 사용자의 로컬 시간대 기준으로 입력하면 자동 변환됩니다.
> **공휴일에는 트리거를 끄지 않습니다.** 제헌절·설날처럼 한국 증시가 휴장하는 평일에도 브리핑은 정상 발행하되, 시장 카드·일정 카드에 "오늘 휴장" 안내를 담는 방식으로 처리합니다 (별도 휴일 캘린더 관리가 필요 없고, 미국장·글로벌 뉴스는 휴장일에도 유효하기 때문입니다).
>
> **① 아침 브리핑** — 매주 월~금 07:35 KST 실행 (발행이 약 08:00에 이뤄지도록 여유를 둔 시각)
> ```
> 저장소의 ROUTINE_PROMPT.md 파일을 읽고, 그 절차를 순서대로 그대로 수행하세요.
> SESSION = am
> ```
>
> **② 저녁 브리핑** — 매주 월~금 20:35 KST 실행 (발행이 미 정규장 개장 전인 약 21:00에 이뤄지도록)
> ```
> 저장소의 ROUTINE_PROMPT.md 파일을 읽고, 그 절차를 순서대로 그대로 수행하세요.
> SESSION = pm
> ```
>
> 이렇게 하면 절차 수정이 필요할 때 이 파일만 고치면 되고, 루틴 설정은 다시 손댈 필요가 없습니다.
> `SESSION` 이 지정되지 않은 구버전 루틴은 `am` 으로 간주합니다.
> **(변경 이력)** 예전에는 같은 `DATE`+`SESSION` 을 한국어·영어 루틴이 5분 간격으로 나눠 실행하며 나중 언어가 먼저 언어의 `content/<DATE>-<SESSION>.json` 을 재사용하는 방식이었습니다. 이 방식은 "나중 언어가 먼저 언어의 검증을 신뢰하고 재검증을 건너뛰는" 실패 모드가 있었고(실제로 2026-07-21에 2주 전 기사가 "오늘 발표"로 오발행되는 사고가 있었음), 세션이 하루 4번 필요해 관리 부담도 컸습니다. 지금은 세션 하나가 리서치 1회로 ko·en 카드를 모두 만들고 검증도 한 번만 하므로 이 문제가 구조적으로 사라집니다.

---

당신은 "luckyon 브리핑"의 자동 발행 담당입니다. 저장소 `luckyon-briefing`이 클론된 상태이며, 목표는 시장 기록·지표·공시 기반 카드뉴스를 생성해 Instagram에 캐러셀로 발행하는 것입니다.

> **이 계정은 여러 콘텐츠 축을 함께 운영합니다.** 이 문서는 그중 **평일 브리핑**만 담당합니다.
> 축마다 파일 경로·발행 스크립트가 분리돼 있으므로, **이 문서의 절차를 수행하면서 다른 축의 파일을 건드리지 마세요.**
>
> | 축 | 절차서 | 콘텐츠 경로 | 카드 경로 | 발행 스크립트 |
> |---|---|---|---|---|
> | 평일 브리핑 | `ROUTINE_PROMPT.md` (이 문서) | `content/<DATE>-<SESSION>.json` | `cards/<DATE>/<SESSION>/<LANG>/` | `scripts/publish-instagram.mjs` |
> | 주말 브리핑 | `ROUTINE_PROMPT_WEEKEND.md` | `content/<DATE>-<SESSION>.json` | `cards/<DATE>/<SESSION>/<LANG>/` | `scripts/publish-instagram.mjs` |
> | 스토리 | `ROUTINE_PROMPT_STORY.md` | `data/stories/<STAMP>.json` | `cards/stories/<STAMP>/<LANG>/` | `scripts/stories/publish-story.mjs` |
> | 릴스 | (문서 없음) | `data/reels/<STAMP>.json` | `cards/reels/<STAMP>/<LANG>/` | `scripts/reels/publish-reel.mjs` |
> | 차트 노트 | `ROUTINE_PROMPT_CHARTNOTES.md` | `content/chart-notes/<STAMP>.json` | `cards/chart-notes/<STAMP>/<LANG>/` | `scripts/chart-notes/publish-chartnotes.mjs` |
>
> `content/` **루트**의 json 은 브리핑 전용입니다 (`content/2026-07-14.json` 같은 세션 없는 구버전 파일 포함).
> 다른 축의 콘텐츠를 여기에 두면 렌더러·발행 스크립트가 서로의 파일을 집어 들 수 있으니, 반드시 하위 디렉터리로 분리하세요.
> 모든 축이 `claude/live` 브랜치 하나와 GitHub Pages 를 공유하므로, 커밋할 때 **자기 축의 경로만** `git add` 하세요.

브리핑은 하루 2회(아침/저녁)이며, 각 회차는 **"직전 브리핑 이후 있었던 일" + "다음 브리핑 전까지 예정된 일정"** 을 담습니다. 각 회차는 한국어·영어로 각각 발행되므로, 실제 Instagram 게시물은 하루 총 4건(am-ko, am-en, pm-ko, pm-en)이지만, 이를 만드는 **세션(=이 루틴의 실행)은 하루 2번(am, pm)** 입니다. 리서치와 사실검증은 세션당 한 번만 하고, 그 결과로 한국어·영어 카드를 모두 만들어 각각 발행합니다 — 두 언어가 서로 다른 사실관계로 발행될 위험이 원천적으로 없습니다.

| | 아침 브리핑 (am) | 저녁 브리핑 (pm) |
|---|---|---|
| 발행 목표 시각 | 08:00 KST | 21:00 KST (미 정규장 개장 전) |
| 커버 범위 | 전날 21:00 이후 밤사이 (미국장 마감 결과 중심) | 오늘 08:00 이후 (한국장 마감, 아시아·중동·유럽, 미 프리마켓) |
| 일정 카드 범위 | 지금 ~ 오늘 21:00 (한국 낮 시간대) | 지금 ~ 내일 08:00 (미국장 밤 시간대) |

아래를 순서대로 수행하세요.

1. **의존성 확인**: `npm ls playwright` 로 playwright가 있는지 확인하고, 없으면 `npm install` 을 실행한다. (setup 스크립트에서 이미 설치됐다면 건너뛴다.)
   - `npm install` 이 403 등으로 실패하는 환경이면, 전역 설치본을 심볼릭 링크로 연결한다:
     `mkdir -p node_modules && ln -sf /opt/node22/lib/node_modules/playwright node_modules/playwright`

2. **오늘 날짜·세션 확인**: 한국시간(KST) 기준 오늘 날짜를 `YYYY-MM-DD` 형식으로 정하고 `DATE` 로 사용한다. 루틴 지시문의 `SESSION`(am|pm)을 확인한다 (지정이 없으면 am).
   - **중복 실행 가드**: `claude/live` 브랜치에 `content/<DATE>-<SESSION>.json` 이 이미 있다면(예: 같은 세션을 사람이 수동으로 다시 돌린 경우), 그 파일이 이미 Instagram에 발행됐을 가능성이 있다. 3~9단계를 새로 진행하기 전에 이 사실을 인지하고, 8단계(Instagram 발행) 직전 판단이 애매하면 무리해서 추측하지 말고 PushNotification 으로 "같은 세션 파일이 이미 존재 — 중복 발행 여부 확인 필요"를 알린 뒤 진행 여부를 신중히 결정한다. (정상적인 스케줄 실행이라면 파일이 없는 것이 기본값이며, 이 가드는 예외 상황 대비용이다.)

3. **리서치**: 아래 3-a~3-e 를 수행한다. 이 리서치 한 번으로 한국어·영어 카드를 모두 만든다.
   **시세·지표·공시는 데이터 소스에서 직접 조회하고, WebSearch 는 데이터로 얻을 수 없는 것(Fear&Greed, 1차 발표문 찾기, 사건 배경 확인)에만 보조로 쓴다.**

   **3-a. 시장 지표 (markets 10개 타일)** — **아침·저녁 동일한 10개**로 통일한다 (순서 고정):
   `나스닥` · `S&P 500` · `코스피` · `VIX` · `Fear&Greed` · `미 10년물 금리` · `원/달러(달러인덱스 DXY 병기)` · `금` · `WTI 원유` · `비트코인`.

   - **값은 반드시 아래 데이터 소스에서 새로 조회한다. 웹검색으로 얻은 숫자나 직전 세션 JSON의 값을 쓰지 않는다 (중요).**

     | 타일 | 조회 방법 |
     |---|---|
     | 나스닥 / S&P 500 (am·종가) | PlayMCP `UsStockInfo-get_historical_stock_prices` — `^IXIC` / `^GSPC` |
     | 나스닥 / S&P 500 (pm·선물) | 같은 도구 — `NQ=F` / `ES=F` |
     | 코스피 | 같은 도구 — `^KS11` |
     | VIX | 같은 도구 — `^VIX` |
     | 원/달러 · 달러인덱스 | 같은 도구 — `KRW=X` / `DX-Y.NYB` |
     | 금 · WTI | 같은 도구 — `GC=F` / `CL=F` |
     | 비트코인 | 같은 도구 — `BTC-USD` |
     | 미 10년물 금리 | FMP `economics` 도구, `endpoint: "treasury-rates"`, `from_date`/`to_date` 로 해당 거래일 지정 → `year10` 값 |
     | Fear & Greed | API가 없으므로 WebSearch (CNN Fear & Greed Index) |
     | **한국 증시 하루치 기록** (본문 카드용) | `node scripts/fetch-krx.mjs <YYYY-MM-DD>` — 코스피·코스닥 종가·등락률·거래대금·거래량, 상승/하락/보합 종목 수, 등락률·거래대금 상위 종목을 JSON 으로 준다 |
     | 한국 개별 종목 시세·52주 고저 | `https://query1.finance.yahoo.com/v8/finance/chart/<종목코드>.KS` (코스닥은 `.KQ`) — 종가는 `meta.regularMarketPrice` 를 쓴다 |

     - **`fetch-krx.mjs` 는 KRX 원천 데이터를 미러링한 CSV 를 읽는다.** KRX 정보데이터시스템(`data.krx.co.kr`)과 네이버 금융은 이 실행 환경의 네트워크 정책에서 차단되므로 직접 조회할 수 없다 — 대신 이 스크립트를 쓴다. 휴장일이면 exit 2 로 조용히 빠지므로, **그때는 브리핑을 멈추지 말고** 한국 항목을 빼고 진행한다.
     - **Yahoo 차트 API 의 함정 2가지** (실측 확인): ① 응답의 `indicators.quote[0].close` 배열은 **당일 마지막 값이 `null`** 이다. 종가는 반드시 `meta.regularMarketPrice` 에서 읽는다. ② `meta.chartPreviousClose` 가 틀린 값을 주는 경우가 있다(2026-08-03 `^KS11` 기준 8088.34 로 나왔으나 실제 7/31 종가는 6595.45). **전일 대비는 직접 계산한다.**
     - **한국 수급(외국인·기관·개인 순매수)은 현재 조회할 수 없다. 다시 찾아보지 말 것.** 확인한 내용:
       - `data.krx.co.kr`(KRX 정보데이터시스템)·`finance.naver.com`·`apis.data.go.kr`·증권사 오픈API 는 이 환경의 네트워크 정책에서 차단된다. `pykrx` 도 같은 이유로 실패하며, 1.2.8 부터는 KRX 계정까지 필요하다.
       - `openapi.krx.co.kr`(KRX 공식 OPEN API)은 접속되지만 **서비스 목록에 투자자별 매매동향 API 자체가 없다** — 지수·일별매매정보(OHLCV)·종목기본정보·채권·파생뿐이다. 인증키를 받아도 이 데이터는 나오지 않는다.
       - `fetch-krx.mjs` 가 쓰는 FDR 캐시에도 수급 데이터셋이 없다 (listing·index·snap 만 수집한다).
       - ECOS·OpenDART·Yahoo·FMP·Alpha Vantage 어디에도 일별 투자자별 순매수가 없다.
       - 개별 종목의 매매 주체별 순매수도 같은 이유로 불가능하다.
       **추정하거나 기사에서 옮겨 적지 말고, 수급 항목 자체를 빼고 쓴다.** 대신 거래대금 상위·상승/하락 종목 수·업종별 등락으로 그날의 자금 흐름을 보여준다. (풀려면 환경의 네트워크 허용목록에 `data.krx.co.kr` 또는 `openapi.koreainvestment.com` 을 추가하고 각각 KRX 계정 / 한국투자증권 앱키가 필요하다 — 사용자 결정 사항이다.)

     - `get_historical_stock_prices` 는 `period: "5d"`, `interval: "1d"` 로 부르면 최근 5거래일이 나온다. **등락률(delta)은 직전 거래일 종가와 당일 값으로 직접 계산한다** — 기사에 적힌 등락률을 옮겨 적지 않는다.
     - 반환되는 `Date` 는 UTC 기준이라 한국 지수(`^KS11`)는 하루 앞 날짜로 찍힌다 (예: `2026-08-02T15:00:00Z` = 8월 3일 KST 장). 날짜를 KST로 환산해 확인한다.
     - 미 10년물은 **확정된 과거 종가**다. 추정하거나 직전 카드 값에서 더하고 빼지 말고 반드시 조회한다. (2026-08-03 세션에서 am `4.71%` → pm `4.73%` 로 직전 카드 값에 임의로 더해 발행했으나 실제 7/31 종가는 `4.75%` 였다.)
     - 도구 호출은 **순차로** 한다. 같은 순간에 여러 건을 병렬로 던지면 일부 데이터 제공자의 초당 요청 제한에 걸려 실패한다.

   - **직전 세션 대조 (필수)**: JSON 저장 전, 직전 세션 파일(`content/` 의 가장 최근 파일)을 열어 markets 10개 타일을 비교한다. **값과 등락률이 소수점까지 똑같은 타일이 있으면 조회를 빠뜨린 것으로 간주하고 다시 조회한다.** (직전 거래일 종가를 그대로 쓰는 게 맞는 경우 — 예: 시장이 열리기 전 — 는 예외지만, 그때도 note 에 기준일을 밝힌다.)

   - **세션별로 다른 것은 "타일 종류"가 아니라 "값의 성격"뿐이며, 이는 한 줄 해석(note)으로 밝힌다:**
     - `am`(아침, 미국장 마감 후): S&P 500·나스닥은 **미국장 마감 종가**. 코스피는 전 거래일 종가(장 열리기 전).
     - `pm`(저녁, 미국장 개장 전): S&P 500·나스닥은 **선물(futures)** 로 표기하고 note 에 "선물"임을 밝힌다. 값은 위 표대로 PlayMCP `UsStockInfo-get_historical_stock_prices` 에 `NQ=F`·`ES=F` 를 넣어 조회한다. 코스피는 당일 종가.
       - **선물 조회 실패 시 대체 (중요)**: 위 도구가 값을 못 주면 **발행을 멈추지 말고** S&P 500·나스닥을 직전 미국장 **종가**로 표기하고 note 에 "전일 종가·개장 전"이라고 밝힌다.
       - (구버전 경로) `scripts/fetch-futures.mjs` + Apify + `data/futures-cache.json` 캐시는 위 도구를 못 쓸 때를 위한 예비 수단으로만 남겨 둔다. 이 캐시는 GitHub Actions가 **main 브랜치**에 커밋하므로, 읽기 전에 반드시 최신본을 가져와야 한다 — 6단계의 `git merge origin/main` 은 이 시점보다 한참 뒤라서, 그냥 읽으면 **직전 세션 때 병합된 낡은 캐시**를 쓰게 된다. (2026-08-03 pm 세션까지 이 이유로 당일 선물이 한 번도 반영되지 않았다. 지금은 `fetch-futures.mjs` 가 작업트리 캐시가 낡았으면 `origin/main` 에서 직접 읽는다.)
       - **이 경로를 쓸 때는 `delta` 값을 그대로 믿지 말고 반드시 직접 계산한다** — Apify 액터가 주는 `changePercent` 가 실제와 다른 사례가 확인됐다 (2026-08-03 캐시: `NASDAQ Fut 28,504.5 ▼3.56%` 로 기록됐으나 직전 종가 28,404.25 대비 실제로는 `+0.35%` 로 부호까지 반대였다).
       - **금지 표현 (중요)**: note_ko/note_en 에 "선물 조회 불가/실패/건너뜀", "Futures feed unavailable" 등 **내부 수집 과정이 실패했다는 사실을 독자에게 노출하는 문구를 절대 쓰지 않는다.** 독자는 이게 선물이었는지 아닌지, 조회가 됐는지 안 됐는지 알 필요가 없다 — 그냥 "종가" 데이터 하나로 보이게 자연스럽게 쓴다 (예: `"7/17 미국장 마감 종가"`). 조회 성공/실패 여부는 이 문서의 절차(스크립트 실행)에서만 판단하고, 카드 문구는 항상 독자 입장에서 자연스러운 결과만 담는다.
   - **원/달러 타일**: 원/달러 환율을 메인 값(`value`)으로, 달러인덱스(DXY)를 `value_sub` 로 병기한다 (예: `"value": "1,487"`, `"value_sub": "· DXY 100.8"`).
   - **휴장 처리 (중요)**: 한국·미국 등 해당 시장이 공휴일·주말로 휴장이어도 **타일을 절대 빼지 않는다.** 타일 개수·배치를 항상 10개로 일정하게 유지한다. 휴장 타일은 `value` 에 직전 종가(또는 마지막 고시가)를 그대로 두고, `delta` 는 `"휴장"`(또는 마지막 거래일 등락률), `dir` 은 상황에 맞게, 한 줄 해석(`note_ko`/`note_en`)에 기준일·사유를 적는다 (예: `"서울 제헌절 휴장 · 달러인덱스 +0.06%"` / `"Seoul holiday · Dollar index +0.06%"`). 지수 선물처럼 휴장 중에도 거래되는 항목은 정상 표기한다.

   **3-b. 본문 카드 구성 (`sections` 4장)** — 이 브리핑은 **뉴스 기사를 옮겨 적지 않는다.** 시세·지표·공시·1차 발표문에서 그날의 **기록**을 뽑아 쓴다. 뉴스 기반 계정은 이미 많고, 기사를 옮기는 과정에서 보도일 오기·선행 인용·단정 같은 오류가 반복적으로 발생했다 (2026-08-03 사고 참조).

   - **핵심 원칙 — 시세 나열이 아니라 "계산된 관찰"을 쓴다.** `코스피 6,257.45 ▼5.12%` 는 시장 카드(2장)가 이미 보여준다. 본문 카드는 거기서 한 발 더 나간 것을 담는다:
     > 역대 최대 상승(+17.91%) 다음 거래일 -5.12%. 이틀 합산 +11.9%로 7월 고점은 지켰다.
     > 삼성전자·SK하이닉스 동반 8%대 하락은 7월 들어 네 번째.

     이런 문장은 조회한 데이터에서만 나오고, 기사에는 없으며, **검증도 다시 계산하면 끝난다.** 매 회차 이런 성격의 항목(신기록·연속 기록·N개월래 최고/최저·순위 변동)을 **최소 1건** 넣는다.

     특히 **지수와 시장 내부가 엇갈리는 날**은 좋은 재료다. 예를 들어 2026-08-03 코스피는 5.12% 급락했지만 상승 종목이 455개로 하락 419개보다 많았고, 거래대금 상위 2종목(SK하이닉스 8.64조·삼성전자 6.74조)이 시장 전체 26.33조의 58%를 차지했다 — "대형주만 빠진 하락"이라는 사실은 지수만 봐서는 보이지 않는다. `fetch-krx.mjs` 출력에서 이런 어긋남을 먼저 찾아본다.

   - **섹션 구성** (각 3~4건, `sections` 배열 순서대로 카드 ③④⑤⑥):

     | | `am` (미국장 마감 후) | `pm` (한국장 마감 후) |
     |---|---|---|
     | ③ | **미국장 기록** `#e66767` — 지수 마감, 섹터 등락, 신고가/신저가 | **코스피 · 코스닥 기록** `#e66767` — 지수, 거래대금, 상승/하락 종목 수 |
     | ④ | **실적 발표** `#e0a94f` — 전날 밤 어닝 결과(예상 대비) | **종목 기록** `#e0a94f` — 등락 상위, 거래대금 상위, 신고가 |
     | ⑤ | **금리 · 지표 · 원자재** `#3987e5` — 국채 커브, 발표된 지표 실제치, 유가·금·환율 | **오늘의 공시** `#5aa469` — DART 에서 시장에 의미 있는 건 |
     | ⑥ | **한국 · 아시아** `#5aa469` — 전 거래일 한국장, 아시아 증시, 주요 공시 | **글로벌 · 미 선물** `#3987e5` — 아시아 마감, 유가·환율, 미 선물 |

   - **각 항목은 반드시 조회한 데이터에서 나와야 한다.** 기사에 적힌 숫자를 옮기지 않는다. `src` 에는 언론사가 아니라 **데이터 출처**를 적는다 (예: `KRX`, `DART`, `美 재무부`, `FMP`, `Yahoo Finance`).
   - **사건성 항목은 1차 발표문을 쓴다.** 금통위·FOMC·고용지표·규제 시행 등은 기사 대신 한국은행·연준·통계청·집행위의 **발표 원문**을 근거로 삼는다. 이러면 "누가 주장했는지"가 흐려지지 않는다.
   - **한국은행 기준금리 결정(금통위)은 절대 놓치지 않는다.** 개최일(연 8회, 통상 1·2·4·5·7·8·10·11월)에 해당 세션이 발행되면 결과(동결/인상/인하, 표결, 배경)를 ③ 섹션 첫 항목으로 반드시 넣는다.
   - **공시 선별 (pm ⑤)**: DART 는 하루 코스피만 200건이 넘고 대부분 정기·형식 공시다. **시장에 의미 있는 유형만** 고른다 — 단일판매·공급계약체결, 유상증자/전환사채, 자기주식 취득·처분, 최대주주 변경, 대량보유상황보고(5% 룰), 매출액·손익구조 30% 이상 변동, 생산중단, 소송. 정기보고서·기재정정·임원변경 등은 넣지 않는다. 금액이 큰 순, 시총이 큰 기업 순으로 본다.
   - **pm 세션은 먼저 `content/<DATE>-am.json` 을 읽고** 아침에 이미 다룬 항목은 제외한다. (파일이 없으면 `content/` 의 가장 최근 파일을 참고하되, 없는 것 때문에 중단하지 않는다.)
   - 각 항목은 결론부터 1~2문장으로, 한국어와 영어 두 버전을 모두 작성한다.
   - **재료가 부족한 날은 아래 예비 지표를 순서대로 투입한다.** 그날의 1차 재료가 먼저이고, 모자라는 만큼만 위에서부터 가져다 쓴다. 전부 데이터에서 바로 나오는 것들이라 어느 날이든 반드시 채울 수 있다.

     | 순위 | 예비 지표 | 조회 |
     |---|---|---|
     | 1 | **지수와 시장 내부의 어긋남** — 지수는 빠졌는데 상승 종목이 더 많은지, 거래대금이 소수 종목에 쏠렸는지 | `fetch-krx.mjs` → `markets.*.counts`, `.concentration` |
     | 2 | **연속 기록·주간/월간 누적** — N일 연속 상승/하락, 이번 주·이번 달 누적 등락 | 같은 스크립트 → `indexes.*.records.streakDays`, `.weekToDate`, `.monthToDate` |
     | 3 | **N일 최고/최저** — 20일·60일 기준 신고가/신저가 여부와 그 구간 폭 | 같은 스크립트 → `.records.range20d`, `.range60d` |
     | 4 | **업종별 등락** — 오른 업종 수 vs 내린 업종 수, 최상·최하 업종 | 한국: 같은 스크립트 → `industry` / 미국: 아래 섹터 ETF |
     | 5 | **52주 신고가/신저가 종목 수** | Yahoo 차트 API 를 종목코드별로 돌려 `meta.fiftyTwoWeekHigh/Low` 집계 (전종목이면 ~45초) |
     | 6 | **미 국채 커브** — 2년-10년 스프레드, 30년물 수준과 그 의미 | FMP `economics` → `treasury-rates` |
     | 7 | **미국 섹터 성과** | 아래 섹터 ETF 표 (FMP `sector-performance-snapshot` 은 쓰지 않는다 — 아래 주의 참고) |
     | 8 | **이번 주 실적 일정 밀도** — 남은 주에 어떤 대형주가 몰려 있는지 | FMP `calendar` → `earnings-calendar` |
     | 9 | **원자재·환율 주간 변동** | 3-a 의 시세 도구로 5일치를 받아 계산 |
     | 10 | **오늘 공시 통계** — 유형별 건수, 계약 공시 총액 | DART (`opendart` 도구) |

     - **업종은 그 나라의 주력 업종을 우선한다.** 세분류를 그대로 쓰면 "시멘트·석회", "수산물 가공" 같은 것이 상위에 올라와 시장을 설명하지 못한다. 등락률은 **시가총액 가중**으로 본다 — 단순 평균은 소형주가 결과를 좌우해 지수와 어긋난다.
       - **나라별로 방식이 다르다: 한국은 상장 종목을 묶은 업종 집계, 미국은 섹터 ETF.** 한국은 섹터 ETF 가 미국만큼 갖춰져 있지 않아 종목을 직접 묶는 쪽이 정확하고, 미국은 ETF 가 이미 시총 가중이라 그대로 쓰면 된다. **한국 업종을 ETF 로 대체하지 않는다.**
       - **한국**: `fetch-krx.mjs` 가 KSIC 세분류를 주력 15개 업종으로 묶어 시총 가중으로 계산해 준다 (`industry.all` 이 우선순위 순). 코스피 시총의 약 93%를 덮는다. 삼성전자는 KSIC 상 '통신 및 방송 장비 제조업'이라 그대로 두면 반도체에서 빠지므로 스크립트가 예외로 반도체에 넣는다.
       - **미국**: 아래 ETF 를 3-a 의 시세 도구(`UsStockInfo-get_historical_stock_prices`, `period: "5d"`)로 조회해 직접 계산한다. ETF 는 시총 가중이라 한국 쪽 계산과 성격이 같다. **아래 티커는 전부 실제 조회를 확인했다.**

         **코어 섹터** — 그날의 섹터 지형을 보려면 이것부터 본다 (우선순위 순):

         | 섹터 | 티커 | | 섹터 | 티커 |
         |---|---|---|---|---|
         | 반도체 | `SMH` | | 산업재 | `XLI` |
         | 기술 | `XLK` | | 에너지 | `XLE` |
         | 커뮤니케이션 | `XLC` | | 필수소비재 | `XLP` |
         | 금융 | `XLF` | | 소재 | `XLB` |
         | 경기소비재 | `XLY` | | 유틸리티 | `XLU` |
         | 헬스케어 | `XLV` | | 부동산 | `XLRE` |

         **테마** — 그날 크게 움직인 것만 골라 쓴다. 매일 다 보지 않는다:
         `SOXX`(반도체) · `IGV`(소프트웨어) · `LIT`(2차전지) · `ITA` `PPA` `XAR`(방산·항공우주) · `BOTZ` `ROBO`(로봇·AI) · `URA`(원자력) · `XBI`(바이오텍) · `JETS`(항공) · `IYT`(운송) · `KWEB`(중국인터넷) · `GDX`(금광) · `XME`(금속·광산) · `CIBR` `HACK`(사이버보안) · `FINX`(핀테크) · `TAN` `ICLN`(태양광·신재생) · `ARKK`(혁신성장) · `QQQ` `SPY`(지수 대조용)

       - **한국 업종과 짝지으면 좋은 재료가 나온다.** 같은 테마가 두 시장에서 다르게 움직인 날은 그 자체가 관찰거리다 — 반도체 `SMH`↔한국 반도체, 2차전지 `LIT`↔한국 2차전지, 방산 `ITA`↔한국 기계·방산, 원자력 `URA`↔한국 전력·에너지, 로봇 `BOTZ`↔코스닥 로봇주, 바이오 `XBI`↔한국 바이오·제약, 소프트웨어 `IGV`↔한국 인터넷·소프트웨어.

       - **FMP `sector-performance-snapshot` 은 쓰지 않는다.** 거래소에 상장된 전 종목의 **단순 평균**이라 소형주에 휘둘린다 — 2026-07-31 나스닥이 +1.00% 마감한 날 이 API 의 Technology 는 -1.77% 로 나왔다. 같은 날 `XLK` 는 -0.22% 였다.

     - **직전 2회 세션에서 쓴 예비 지표는 건너뛴다.** `content/` 의 최근 파일 2개를 열어 어떤 예비 항목을 썼는지 보고, 같은 것이 사흘 연속 나가지 않게 한다. 매번 "연속 N일 상승"만 나오면 금방 식상해진다.
     - **예비 지표도 3-d 검증을 똑같이 거친다.** 예비라서 급히 넣은 것이라는 이유로 검증을 건너뛰지 않는다.
     - **예비까지 동원해도 부족하면 그때는 줄인다.** 섹션 3장, 한 섹션 2건도 정상이다(카드 수가 줄 뿐 레이아웃은 정상). 빈칸을 메우려고 확인 안 된 내용을 넣는 것이 훨씬 나쁘다 — 이건 최후 수단이지 첫 번째 선택지가 아니다.

   **3-b-2. 훅 카드의 호재·악재 (`hook_bull` / `hook_bear`)**

   1번 카드(훅)는 헤드라인 아래에 **초록 "▲ 호재" 블록과 빨강 "▼ 악재" 블록**을 세운다.
   오늘 시장을 한 문장씩으로 양쪽에서 보여주는 칸이며, 이 시리즈의 첫 화면을 만드는 핵심 요소다.
   본문 항목을 확정한 **직후 여기서** 고른다 — 4단계에서 고르면 3-d 검증을 못 거친다.

   - **역할**: headline 이 "무슨 일이 있었는지"(결론)를 말하므로, 이 두 블록은 **그 이면의 균형점**을 보여준다.
     headline 과 중복되지 않게 쓴다 — headline 이 결론이면 이 둘은 근거이자 반대편 시각이다.
   - 각 1~2문장. **구체적인 종목·수치·사건**을 담는다. "투자심리가 개선됐습니다" 같은 뭉뚱그린 말은 쓰지 않는다.
     각각 시장 전체에 가장 크게 영향을 준 **단일 사실 하나씩**만 담고, 여러 개를 나열하지 않는다.
   - **확실한 것만 넣는다 — 한쪽만 뚜렷한 날은 그쪽만 넣는다.** 악재 일색인 날에도 호재 칸을 억지 낙관으로
     채우지 않는다. 먼저 **"덜 나쁜 것"이나 반등 근거**를 사실로 찾아보고(예: 낙폭 과대, 실적 자체는 견조,
     특정 섹터만 선방), 그런 사실이 정말 없으면 **그 칸을 비워둔다.** 애매한 것을 억지로 채우면 독자에게
     혼동만 준다. 호재만 있는 날·악재만 있는 날 모두 정상이다.
   - **재료**: 위 `sections` 항목에서 찾는 것을 우선하되, 거기에 확실한 호재·악재가 없으면 **조회한 다른
     데이터에서 가져와도 된다.** 이때 섹션 구성은 억지로 교체하지 않는다 (금통위 항목 등이 밀려날 수 있다).
     다만 섹션 밖에서 그날의 핵심 호재·악재를 발견했다면 선별이 잘못됐다는 신호이므로 한 번 재검토한다.
   - **렌더 동작**: 둘 다 있으면 헤드라인이 78px→58px 로 줄고 두 블록이 들어간다. **한쪽만 있으면**
     헤드라인은 78px 을 유지하고 `headline_sub` 가 그 한 블록과 함께 노출된다(카드가 비어 보이지 않게).
     **둘 다 없으면** `headline_sub` 로 폴백된다 — 이때는 "호재/악재로 나눠 보여주는" 이 시리즈의 컨셉이
     사라지므로, 뚜렷한 재료가 있는데도 빠뜨리는 일은 없어야 한다.

   실제 예시 (2026-07-18 아침):
   ```json
   "hook_bull": {
     "body_ko": "TSMC가 2분기 사상 최대 실적에 美 1,000억달러 증설까지 발표 — AI 반도체 수요 자체는 여전히 견조합니다.",
     "body_en": "TSMC posts a record Q2 and unveils a $100B US expansion — underlying AI-chip demand still looks solid."
   },
   "hook_bear": {
     "body_ko": "넷플릭스가 실적 호조에도 가이던스 실망으로 10%↓, 반도체주 급락까지 겹치며 나스닥이 1.4% 밀렸습니다.",
     "body_en": "Netflix drops 10% on soft guidance despite a beat; a chip selloff drags the Nasdaq down 1.4%."
   }
   ```

   **3-c. 일정 리서치 (schedule 4~6건)** — 다음 브리핑 전까지 예정된 일정을 조사한다. 모든 시각은 KST로 표기한다.
   - `am`: 오늘 21:00 전까지 — 국내 지표 발표, 중국·일본 지표, 국내 주요 기업 실적 발표, 유럽 장 초반 주요 이벤트 등.
   - `pm`: 내일 08:00 전까지 — 미국 지표 발표(CPI·PPI·고용 등, KST 시각 병기), 미국 주요 기업 실적(장전/장후 구분), FOMC·연준 인사 발언 등.
   - 시장에 영향이 큰 일정은 `"importance": "high"` (카드에 "주목" 배지), 나머지는 `"mid"`.
   - 해당 시간대에 예정된 일정이 정말 없으면 "특별한 일정 없음"을 한 건으로 넣지 말고, 다음 날 이후의 굵직한 일정(FOMC, 빅테크 실적 등)을 "미리 보기"로 채운다.

   **실적 일정은 데이터로 받는다** — FMP `calendar` 도구, `endpoint: "earnings-calendar"`, `from_date`/`to_date` 로 해당 날짜를 지정하면 종목별 EPS·매출 컨센서스가 함께 나온다. 미국장 마감 후 발표는 그 **미국 날짜**로 조회한다 (예: 8/4 06:00 KST 발표 = 8/3 미국 날짜). 배당·IPO·액면분할은 같은 도구의 다른 endpoint 로 받는다.

   **경제지표는 정기 발표 체크리스트로 빠짐을 막는다.** 경제 캘린더 사이트(Investing.com·세이브티커·TradingView 등)는 이 환경의 네트워크 정책에서 차단돼 조회할 수 없고, FMP 의 `economics-calendar` 도 상위 플랜 전용이다. 그래서 **아래 정기 일정을 먼저 훑고, 해당하는 것을 WebSearch 로 시각·컨센서스만 확인해 채운다.** 같은 날 비슷한 지표가 둘 이상 나오는 경우가 많으니(예: S&P 글로벌 PMI 와 ISM PMI) 하나만 넣고 끝내지 않는다.

     | 시기 | 미국 | 한국 |
     |---|---|---|
     | 매월 1영업일 | S&P 글로벌 제조업 PMI(22:45), ISM 제조업 PMI(23:00) | 수출입 동향(09:00), 소비자물가(08:00) |
     | 매월 3영업일 | S&P 글로벌 서비스업 PMI, ISM 서비스업 PMI | — |
     | 매월 첫 금요일 | 비농업고용·실업률(21:30) | — |
     | 매주 목요일 | 신규 실업수당청구(21:30) | — |
     | 매월 중순 | CPI·PPI·소매판매·산업생산(21:30) | 고용동향, 금통위(연 8회) |
     | 매월 말 | PCE 물가, 내구재주문, GDP 잠정치 | 산업활동동향, 기업경기실사지수 |
     | 분기 | FOMC(연 8회, 새벽 03:00 발표·03:30 회견), 잭슨홀(8월 말) | 한은 경제전망(2·5·8·11월) |

     시각은 서머타임 여부에 따라 1시간 달라지므로 KST 환산 결과를 발행 전에 다시 확인한다.

   **3-d. 뉴스 검증 (필수 · 통과 못 하면 발행 금지)** — 이 단계는 건너뛰거나 줄일 수 있는 단계가 아니다. **검증 에이전트를 실제로 띄워 결과를 받기 전에는 4단계(JSON 저장)로 넘어가지 않는다.** 세션이 시간·자원에 쫓기더라도 뉴스 건수를 줄일지언정 이 검증은 생략하지 않는다 (건수를 줄이는 것은 독자가 알아채지 못하지만, 검증을 건너뛰면 틀린 사실이 발행된다).

   **기록 기반으로 바뀌면서 검증의 성격도 바뀐다.** 예전에는 "기사 원문과 대조"였지만, 이제 항목 대부분은 우리가 직접 계산한 값이라 **대조할 원문이 없다.** 따라서 검증 에이전트에게 **원본 데이터를 다시 조회해 독립적으로 재계산**시킨다 — "이 값이 맞는지 기사에서 확인해라"가 아니라 "같은 소스에서 다시 뽑아 같은 숫자가 나오는지 확인해라"로 지시한다. 계산이 들어간 항목("N일 연속", "N개월래 최고", "올해 N번째")은 특히 기간·기준을 명시해 재계산시킨다.

   3-b에서 추린 본문 항목 전부를 최종 JSON에 넣기 전에, 반드시 **별도의 검증 에이전트**(`Agent` 도구, `subagent_type: general-purpose` 또는 `Explore`, `run_in_background: false`로 결과를 받고 이어서 진행)를 최소 1개 띄워 아래를 점검시킨다. 뉴스를 고른 세션이 스스로 재검토하면 확증편향으로 놓치기 쉽다 — 실제로 2026-07-21 아침 브리핑에서 2주 전(7/7) 발표된 삼성전자 실적 기사를 "7/21 발표"로 잘못 표기한 채 그대로 발행한 사고가 있었다. (이 검증은 세션당 한 번만 하면 ko·en 카드 모두에 적용된다 — 언어별로 따로 돌릴 필요 없다.)
   - 검증 에이전트 프롬프트에 `sections` 본문 항목 전체(각 항목의 headline/body/src/time 초안 + 근거로 삼은 데이터 출처·조회 조건, 계산이 들어갔다면 그 산식)를 통째로 넘기고, 항목별로 아래를 판정해 pass/fail과 사유를 표로 보고하게 한다:
     1. **최초 게시일** — 기사 URL의 날짜 패턴(예: `.../202607071439...` → 7월 7일), 바이라인, 본문의 입력·수정 시각 등으로 실제 최초 보도 시점을 확인하고, 이 세션의 뉴스 범위·최신성 원칙(24시간 이내, 예외는 시장을 움직이는 진행 중인 사건)을 지키는지 판정한다. "오늘 ~한다/발표/공개" 류로 프레이밍된 항목은 특히 꼼꼼히 본다.
     2. **사실 일치** — 헤드라인·본문의 핵심 수치·인용구가 원문 기사와 일치하는지. 수치(가격·%·날짜)는 어림값 말고 원문과 자릿수까지 대조한다.
     3. **미래형 표현 검증** — "오늘 ~한다/예정" 류 문장이 검증 시점에도 여전히 사실인지.
     4. **선행 인용 금지** — 카드에 적은 수치가 **발행 시점 이후에야 나온 값**이 아닌지. 진행 중인 사건(유가 급락, 지수 급등락 등)은 시간이 갈수록 수치가 커지는데, 나중 시점의 더 극적인 숫자를 끌어다 쓰면 발행 시점 기준으로는 사실이 아니게 된다. (2026-08-03 am 발행분에서 "국제유가 7% 급락"으로 썼으나 발행 시점 낙폭은 4.6~5.7%였고, 6~7%대는 그날 밤 22시 이후에야 나온 수치였다.)
     5. **단정 금지** — 한쪽 당사자의 주장을 양측 합의로 단정하지 않았는지. (같은 발행분에서 트럼프의 주장을 근거로 "이란과 호르무즈 합의"라고 단정했으나, 이란은 이를 부인하고 오만을 통한 협의만 진행 중이었다.) 주장 주체를 밝혀 `트럼프 "이란과 합의"` 처럼 쓴다.
   - **3-b-2 에서 고른 `hook_bull`·`hook_bear` 도 같은 프롬프트에 함께 넘겨 검증한다.** 첫 카드에 나가는 문구라 오류 시 임팩트가 가장 크다. 단 검증 범위는 그 문장이 **인용한 사실(수치·날짜·사건)** 에 한정한다 — "AI 수요가 정말 견조한가" 같은 인과 해석의 타당성까지 판정시키지 않는다 (정답이 없는 판정을 시키면 검증 결과 전체가 흐려진다).
   - fail로 판정된 항목은 그대로 쓰지 않는다. 다른 뉴스로 교체하거나, 여전히 넣을 가치가 있는 오래된 뉴스라면(예: 진행 중인 사건의 배경 설명) `headline`/`time`을 실제 게시일 기준으로 정정해 "오늘 뉴스"처럼 보이지 않게 고친다 (예: `"time": "7/21 발표"` → `"time": "7/7 발표"`, 헤드라인에서도 "오늘/방금" 뉘앙스를 제거).
   - 검증 결과(항목별 pass/fail·사유)를 짧게 로그에 남긴다.

   **3-e. 과거에 이미 발행된 브리핑의 오류를 발견했다면** — 리서치·검증 도중 이전 세션(오늘 이전 포함)이 이미 Instagram에 발행한 내용에 사실 오류가 있음을 우연히 발견하는 경우가 있다(예: 위 3-d의 삼성전자 사례, 또는 2026-07-21 pm 세션에서 발견된 유가 수치 오류(브렌트 88.87→88.26달러)·미확인 중재국(이집트) 언급 사례). 발견 즉시 무시하지 말고 처리한다:
   - `content/<DATE>-<SESSION>.json`(오류가 있는 과거 파일)을 정정 커밋한다.
   - **Instagram 캡션은 발행 후 API로 수정이 불가능하다** (`POST /{media-id}` 에 `caption` 파라미터를 보내면 거부됨 — 확인됨). 이미 나간 게시물의 카드 이미지 자체(예: econ 카드에 박힌 잘못된 날짜)도 재업로드로는 고칠 수 없다. 대신 정정 댓글을 단다: `POST /{media-id}/comments`, `message` 파라미터. **ko·en 게시물 모두에** 남긴다 — 한쪽 언어만 정정하고 다른 언어를 빠뜨리는 사고가 실제로 있었다(2026-07-21 pm: ko 게시물엔 정정 댓글을 달았지만 en 게시물은 빠뜨렸다가 뒤늦게 발견해 추가함).
   - PushNotification 으로 무엇을 왜 정정했는지 알린다.
   - **예시를 문서에 남길 때는 실제 커밋·댓글로 재확인한 내용만 적는다.** 이 절차서 자체도 한 번 부정확한 예시(실제로 없었던 "IREN 발표일 오류"를 있었던 것처럼 적은 것)가 섞여 들어간 적이 있다 — 정정 사례를 적을 때 실제 diff나 게시된 댓글을 대조하지 않고 요약만으로 적으면 이런 사고가 재발한다.

4. **콘텐츠 JSON 작성**: `content/<DATE>-<SESSION>.json` 파일을 아래 스키마로 저장한다. (기존 `content/` 의 가장 최근 파일을 예시로 참고하되, 반드시 이번 세션 내용으로 새로 만든다. sections 3~4장, markets 10개, schedule 4~6건을 채운다. **`econ`·`ai` 필드는 더 이상 쓰지 않는다** — 렌더러는 하위 호환을 위해 남겨두었을 뿐이다.)

   **주의 — 언어 무관(invariant) 필드**: `markets[].label`/`delta`/`value`, `sections[].items[]` 의 `src`/`time`, `schedule[].time` 은 렌더 스크립트(`scripts/render-cards.mjs`)가 `${LANG}` 분기 없이 **base 필드 이름 그대로**(`m.value`, `m.delta`, `n.src`, `n.time`, `s.time`) 출력한다 — `_ko`/`_en` 접미사가 붙은 필드는 렌더러가 아예 읽지 않으므로, 거기에 값을 넣어도 카드에는 나타나지 않는다(빈 칸이 된다). 이 JSON 하나로 ko·en 카드를 모두 렌더링하므로, 처음 작성할 때부터 두 언어 모두에서 자연스러운 중립 표기로 써야 한다 (예: 라벨은 "S&P 500 Fut" 처럼 영문 약어로, delta 는 "▼6.37%"처럼 기호+숫자 위주로, time 은 "7/16 15:30 KST"처럼, src 는 "KRX", "DART", "FMP" 처럼 데이터 출처 표기 위주로). 한국어 조사·서술형 어미가 꼭 필요한 문구는 이 필드들에 넣지 않는다.

   ```
   {
     "date", "session",                          // session: "am" | "pm"
     "dateLabel_ko","dateLabel_en",
     "headline_ko","headline_sub_ko","headline_en","headline_sub_en",
     "hook_bull":{"body_ko","body_en"},        // ▲ 호재 — 뚜렷하면 넣는다 (3-b-2)
     "hook_bear":{"body_ko","body_en"},        // ▼ 악재 — 한쪽만 넣어도 된다 (3-b-2)
     "markets":[{"label","value","delta","dir(up|down|flat)"} x10],
     "market_note_ko","market_note_en",
     "sections":[                                 // 본문 카드 3~4장 (3-b). 순서대로 카드 ③④⑤⑥
       {
         "title_ko","title_en",                   // 예: "코스피 · 코스닥 기록" / "Kospi & Kosdaq Records"
         "color",                                 // 3-b 표의 섹션별 색
         "items":[{"headline_ko","headline_en","body_ko","body_en","src(선택)","time(선택)"} x3~4]
       } x3~4
     ],
     "schedule_title_ko","schedule_title_en",     // 예: "오늘 낮 주요 일정" / "Today's daytime schedule"
     "market_hours": {
       "title_ko","title_en",                     // am: "한국 증시 운영시간" / pm: "미국 증시 운영시간"
       "lines_ko":[...], "lines_en":[...]
     },
     "schedule":[{"time","title_ko","title_en","detail_ko","detail_en","importance(high|mid)"} x4~6],
     "next_brief_ko","next_brief_en",
     "caption_ko","caption_en","sources":[...]
   }
   ```
   - `market_hours` 고정 문구:
     - `am`: 제목 "한국 증시 운영시간", 내용 "정규장 09:00 ~ 15:30 · 동시호가 08:30~09:00 / 15:20~15:30"
     - **휴장일 처리**: 해당 세션의 시장(am=한국, pm=미국)이 공휴일로 휴장이면 브리핑을 건너뛰지 않는다. 대신 `lines` 에 "※ 오늘(M/D)은 ○○로 하루 휴장" 한 줄을 추가하고, `schedule` 첫 항목에 휴장 안내를 `importance: "high"` 로 넣고, 헤드라인·market_note 에도 휴장 사실을 반영한다.
     - `pm`: 제목 "미국 증시 운영시간", 내용 "정규장 22:30 ~ 익일 05:00 (서머타임 기준)" — 11월 초~3월 초(서머타임 해제 기간)에는 "23:30 ~ 익일 06:00" 으로 쓴다. 프리마켓/애프터마켓 시간을 한 줄 더 넣어도 좋다.
   - `next_brief` 고정 문구:
     - `am`: "🌙 다음 브리핑 — 오늘 밤 9시, 미국장 개장 전" / "🌙 Next brief — 9 PM KST, before the US open"
     - `pm`: "☀️ 다음 브리핑 — 내일 아침 8시, 미국장 마감 정리" / "☀️ Next brief — 8 AM KST, US close wrap-up"
     - 단, **금요일 `pm`** 은 다음 발행이 토요일 아침 주간 결산이므로 "📊 다음 브리핑 — 내일 아침 8시, 한 주 결산" / "📊 Next brief — 8 AM KST, the week in review" 로 쓴다. (주말 루틴(ROUTINE_PROMPT_WEEKEND.md)이 활성화된 상태 기준. 주말 루틴을 끄면 "☀️ 다음 브리핑 — 월요일 아침 8시" 로 되돌린다.)
   - `time` 은 한국시간(KST) 기준으로 표기한다.
   - caption 에는 요약 + 팔로우/저장 유도 문구 + 해시태그 15개 내외를 포함하고, 끝에 다음 브리핑 예고 한 줄(`next_brief` 내용)을 넣는다. pm 캡션에는 "저녁 브리핑" 임을 밝힌다.
   - **한국어 맞춤법·띄어쓰기 검증**: JSON 저장 전에 모든 한국어 텍스트(특히 markets 의 delta, note, headline, schedule)를 다시 읽으며 맞춤법과 띄어쓰기를 점검한다. 예: "두달래 최고"(X) → "두 달 내 최고"(O). 지표 변동 표현은 자연스러운 한국어로 쓰되, 주가지수·금리 등은 "두 달 내 최고", "52주 신고가", "20일 이동평균선 상회" 처럼 시장에서 통용되는 표현을 우선한다.
   - **수치는 정확하게 (근사치 금지)**: markets 의 value 는 일의 자리까지 정확한 수치로 쓴다. 비트코인 "$62K대"(X) → "$64,940"(O), WTI "$79대"(X) → "$79.75"(O). delta 도 "▲ 7%대" 같은 어림 표현 대신 확인된 정확한 %("▲ 1.1%")를 우선하고, 정확한 값을 못 구했을 때만 방향+정성 표현을 쓴다.
   - **영어판 100% 영어 검증**: `_en` 필드는 물론, 위에서 언급한 언어 무관 공용 필드(markets의 value/delta/label, sections[].items 의 src/time)에도 한글이 한 글자도 남지 않았는지 저장 전에 점검한다. 한국어 표기가 필요해 보이면 그 필드에 넣지 말고 문구 자체를 언어 중립적으로 다시 쓴다. (**예외 — 브랜드 로고·푸터**: 영어 카드에도 상단 로고와 하단 푸터가 `luckyon 브리핑` 으로, 즉 **한글 그대로** 나간다. 이는 버그가 아니라 브랜드 정체성을 위한 의도된 선택이므로 **영어로 바꾸지 않는다.** `render-cards.mjs` 에 하드코딩돼 있고, 이 검증에서 "영어 카드에 한글이 있다"고 잡아내 고치는 일이 없어야 한다.)

5. **카드 이미지 생성**: 같은 JSON으로 한국어·영어 카드를 모두 만든다.
   - `node scripts/render-cards.mjs <DATE> ko <SESSION>`
   - `node scripts/render-cards.mjs <DATE> en <SESSION>`
   - `cards/<DATE>/<SESSION>/ko/`, `cards/<DATE>/<SESSION>/en/` 각각에 card1~8.png (8장: 훅, 시장, 경제×2, AI×2, 일정, 아웃트로)가 생겼는지 확인한다.
   - **훅 카드(1번) 확인**: JSON 에 넣은 호재·악재 블록이 그대로 보이는지 확인한다. 한쪽만 넣은 날은
     그 블록과 `headline_sub` 가 함께 나오는 것이 정상이다. 넣었는데 안 보이거나, 뚜렷한 재료가 있었는데
     둘 다 없이 `headline_sub` 만 보이면 3-b-2 를 다시 확인한다.
   - **카드 분량 초과 확인 (매번 필수)**: 특히 영어는 같은 내용도 한국어보다 줄바꿈이 많아져 카드 높이(1350px)를 넘기기 쉽다. 두 언어 모두 3~7번 카드(경제·AI 뉴스, 일정)를 열어 하단 텍스트가 푸터("luckyon 브리핑"·페이지 번호)와 겹치는 항목이 있는지 확인한다. 호재·악재를 넣은 날은 **1번 카드도 함께 확인한다** (헤드라인이 길고 둘 다 있는 날이 가장 빡빡하다). 겹치면 해당 항목의 `body_ko`/`body_en`/`detail_ko`/`detail_en` 등 **그 언어 텍스트만** 줄여 재렌더링한다 (사실관계나 다른 언어 필드는 건드리지 않는다). 겹침이 없어질 때까지 반복한다.

6. **고정 브랜치 `claude/live` 에 커밋 & 푸시**: 반드시 아래 순서대로, 항상 같은 브랜치 이름을 재사용한다 (새 브랜치를 매번 만들지 않는다). GitHub Pages가 이 브랜치를 소스로 보고 있으므로, 여기에 푸시해야 이미지가 공개된다. ko·en 카드와 content json을 한 번에 커밋한다.
   ```
   git fetch origin claude/live
   git checkout -B claude/live FETCH_HEAD          # 항상 원격 최신 기준 (아래 주의 참고)
   git add content/<DATE>-<SESSION>.json cards/<DATE>/<SESSION>
   git commit -m "brief: <DATE> <SESSION> (ko+en)"
   git push origin claude/live
   git rev-parse HEAD origin/claude/live           # 두 해시가 같은지 확인
   ```
   (main 브랜치는 건드리지 않는다. `claude/`로 시작하는 브랜치는 별도 권한 설정 없이도 기본적으로 푸시가 허용된다.)

   **주의 ① — `git add -A` 를 쓰지 않는다.** 이 계정은 여러 콘텐츠 축을 함께 운영하고, 축마다 별도 세션이 돈다.
   `-A` 를 쓰면 다른 축이 만들다 만 파일(아직 커밋 전인 카드 이미지 등)까지 이 커밋에 끌려 들어와,
   검증도 안 된 남의 콘텐츠가 브리핑 커밋으로 발행될 수 있다. **위처럼 자기 축 경로만 명시해서 add 한다.**

   **주의 ② — 반드시 `FETCH_HEAD` 기준으로 체크아웃한다.** 다른 축이 먼저 푸시했을 수 있으므로
   원격 최신을 기준으로 삼아야 push 가 거부되지 않는다. (`git checkout claude/live` 만 하면 로컬에 남아 있던
   오래된 브랜치 위에 커밋하게 된다.) 루틴은 매번 새 컨테이너에서 도므로 이 방식으로 잃을 로컬 작업은 없다.
   push 가 거부되면 `--force` 를 쓰지 말고 **`git fetch` 부터 다시** 한다 — 남의 커밋을 덮어쓰지 않는다.

7. **토큰 만료 점검**: 환경변수 `IG_TOKEN_EXPIRES_AT`(YYYY-MM-DD)를 읽어, 오늘로부터 10일 이내면 PushNotification으로 "인스타 토큰 갱신 필요 (만료 임박)" 알림을 보낸다. (ko·en 공통이므로 세션당 한 번만 확인한다.)

8. **Instagram 발행**: 한국어·영어를 각각 발행한다. 스크립트가 GitHub Pages 반영을 스스로 기다린 뒤 캐러셀을 발행한다.
   - `PAGES_BASE_URL` 환경변수가 설정돼 있어야 한다 (예: https://dreamerstar88-ui.github.io/luckyon-briefing).
   - `node scripts/publish-instagram.mjs <DATE> ko <SESSION>`
   - `node scripts/publish-instagram.mjs <DATE> en <SESSION>`
   - 두 언어는 **서로 독립적으로** 시도한다 — 한쪽이 실패해도 다른 쪽 발행을 건너뛰지 않는다. 각각 성공하면 media id 를, 실패하면 에러 메시지를 그대로 로그에 남기고 토큰/권한/URL 중 무엇이 원인인지 판단한다.
   - **슬라이드별 `alt_text`** 는 콘텐츠 JSON 에서 자동 생성돼 함께 전달된다 (로직: `scripts/lib/alt-text.mjs`). 넘기지 않으면 인스타가 이미지 속 숫자를 OCR로 읽어 자동 대체텍스트를 만들고, 구글이 그걸 색인해 검색 스니펫에 원시 숫자만 노출된다. **따로 할 일은 없다.**
   - 적용 여부가 의심되면 발행 후 `node scripts/verify-alt-text.mjs <DATE> <LANG> <SESSION>` 로 대조한다 (게시물을 캡션으로 찾아 슬라이드별 alt_text 를 읽어와 비교). Graph API 는 모르는 파라미터를 오류 없이 무시하기도 해서 **"발행 성공"이 곧 "적용됨"은 아니다.** 매 회차 돌릴 필요는 없고, 발행 방식이나 이 로직을 바꾼 직후에만 한 번 확인하면 된다.

9. **카카오톡 발행 알림 (PlayMCP)**: 두 언어 발행 시도가 모두 끝나면 — **결과가 무엇이든** — PlayMCP 의 카카오톡 도구로 결과 메시지를 보낸다 (사용자가 "발행 여부"를 카톡으로 바로 확인하기 위함). ko·en 결과를 한 메시지에 합쳐 보낸다.
   - 이 세션에 로드된 PlayMCP 카카오톡 전송 도구를 찾아 사용한다 (도구 그룹명 `KakaotalkChat`; "나에게 보내기 / 나와의 채팅"으로 전송). 정확한 도구 이름·파라미터는 로드된 스키마를 보고 맞춘다.
   - **메시지 형식** (200자 제한 고려, 핵심만 압축):
     - 둘 다 성공: `✅ [luckyon] <DATE> <SESSION> 발행 완료 — KO 카드N장·media <id> / EN 카드N장·media <id>`
     - 일부만 성공: `⚠️ [luckyon] <DATE> <SESSION> 발행 일부 성공 — KO: <성공/실패 요약> · EN: <성공/실패 요약>. 수동 확인 필요`
     - 둘 다 실패: `❌ [luckyon] <DATE> <SESSION> 발행 실패 — <사유 한 줄>. 수동 확인 필요`
   - **폴백**: 이 세션에서 PlayMCP 카카오톡 도구를 쓸 수 없으면(스케줄/헤드리스 세션에서 미로드일 수 있음), 건너뛰지 말고 **PushNotification 으로 동일 내용을 보낸다.**
   - **중요(한계)**: 이 알림은 에이전트가 실제로 실행됐을 때만 나간다. Claude 사용량 한도 소진 등으로 루틴 자체가 안 뜨면 카톡도 못 간다. 따라서 이 메시지는 "성공 확인 하트비트"이며, **약속된 시각까지 카톡이 오지 않으면 그 자체가 누락 신호**임을 사용자는 알고 있어야 한다.

10. **마무리 보고**: 무엇을 발행했는지(세션, 언어별 media id, 카드 수)와 실패가 있었다면 그 내용을 요약한다.

### 최초 1회 준비사항 (사람이 직접)
이 루틴을 처음 한 번 "지금 실행"으로 돌려 `claude/live` 브랜치가 저장소에 생기게 한 뒤,
GitHub 저장소 → Settings → Pages → Branch 드롭다운에서 `claude/live` 를 선택하고 Save 하세요.
(브랜치가 아직 없으면 드롭다운에 나타나지 않습니다. 최초 1회만 이 순서가 필요합니다.)
