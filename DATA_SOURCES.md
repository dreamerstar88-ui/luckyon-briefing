# 데이터 소스 조사 기록 (SOURCES)

> **이 문서가 있는 이유**: 어떤 소스가 **되고 안 되는지**를 세션마다 다시 알아내면 매번 몇십 분씩
> 같은 벽에 부딪힌다. 더 나쁜 것은 **틀린 결론이 주석에 남아 다음 사람이 시도조차 안 하게 되는 것**이다.
> 실제로 `fetch-krx.mjs` 주석이 "KRX·네이버는 차단"이라고 단정하고 있었는데 사실이 아니었고,
> 그 문장 하나 때문에 여러 세션이 그 경로를 아예 검토하지 않았다.
>
> **모든 축의 세션이 이 문서를 읽는다.** 새 소스를 시험했으면 결과를 여기 적는다 —
> **성공만이 아니라 실패도 적는다.** 실패 기록이 이 문서의 절반이다.
>
> 측정값에는 날짜를 붙인다. 소스 정책은 바뀐다.

---

## 1. 이 환경의 egress 사실 (가장 자주 헛짚는 지점)

**프록시는 HTTPS CONNECT 터널만 받는다. 평문 `http://` 는 무조건 실패한다.**

```
✗ http://data.krx.co.kr/...     → 프록시가 거부. 응답 본문에 그 이유를 적어 준다
✓ https://data.krx.co.kr/...    → 200
```

- **`http://` 로 받은 4xx 를 «차단» 으로 읽지 않는다.** 2026-08-08 에 지수 PER 탐침이 KRX 에서
  400/403 을 받고 "egress 차단"으로 결론 냈는데, 원인은 스킴 하나였다. **먼저 https 로 바꿔 본다.**
- 응답이 서버가 만든 페이지(한국어 오류 안내 등)면 **차단이 아니라 도달**이다. 프록시 차단은
  프록시 자신의 영어 메시지로 온다.
- 브라우저 자동화(Playwright)는 세션에서 프록시를 넘지 못한다 — `proxy: {server: HTTPS_PROXY}` 를
  줘도 Chromium 이 `ERR_CONNECTION_RESET` 으로 끊긴다. **세션에서 브라우저로 크롤링하려는 시도는
  접는다.** 러너에서는 된다(아래 §3).

## 2. 세션 ↔ 러너 비대칭 (2026-08-08 실측)

**"세션에서 막히면 러너로" 가 항상 맞는 게 아니다. 반대 방향도 있다.** 소스마다 다르니 표를 본다.

| 소스 | 세션 | Actions 러너 | 비고 |
|---|---|---|---|
| `raw.githubusercontent.com` (FDR KRX 미러) | ✓ | ✓ | 인증 문턱 없음. 한국 지수 OHLC 의 표준 경로 |
| Yahoo chart API (`query1`) | **✓ 5/5** | **✗ 0/3** | **러너가 막힌다** — Yahoo 가 클라우드 IP 차단. 세션은 첫 요청 429 후 재시도로 뚫림 |
| WSJ `market-data/stocks/peyields` | ✗ 403 | **✓ 200** | 지수 PER 의 유일한 실사용 경로 |
| `cdn.alphavantage.co/logos` | ✗ 000 | ✓ | 로고 캐시 |
| iShares IVV 페이지 / multpl / Nasdaq NDX | ✗ | ✓ | 대조용으로만 씀 (§4) |
| S&P DJI xlsx · Invesco · SSGA · KRX API | ✗ | ✗ | 403 / 406 / 400 |
| `data.krx.co.kr` | 도달하나 `LOGOUT` | **앱 레벨 차단** | §4 참고 |
| stooq.com | 200 이지만 봇 검증 JS 페이지 | — | 자동 조회 불가 |
| `*.github.io` (이 저장소의 GitHub Pages, 발행 카드 호스팅) | ✗ 403 | (미확인) | 프록시가 `CONNECT` 자체를 403 으로 거부 — 스킴 문제(§1)가 아니라 도메인 자체가 허용목록 밖이다(2026-08-18 확인). **세션이 자체적으로 Pages 반영을 확인할 방법이 없다는 뜻**이지만 문제되지 않는다 — 인스타그램 발행은 Meta 서버가 이미지를 직접 가져가므로 세션의 도달 가능 여부와 무관하다. **세션이 이 도메인을 상대로 대기/폴링 루프를 만들지 않는다** — 만들면 절대 조건이 충족되지 않아 무한 대기한다(`ROUTINE_COMMON.md` §6 의 5분 상한 규칙 참고, 2026-08-18 am 에 실제로 약 1시간 정체된 사고가 있었다). |
| `tradingview.com` 전체(`www.`·`symbol-search.`·`s3.`·`data.`·`scanner.`) | ✗ 403 | **✓ 200/301** (2026-08-18 재확인, 아래 §8) | 스토리 축의 야후 지연(§7 참고) 대안으로 나스닥 미니 선물 위젯을 시도(2026-08-18) — 세션에서는 다섯 서브도메인 전부 프록시가 `CONNECT` 자체를 403 거부. 같은 시점 `www.google.com` 도 동일하게 막혀 있어 트레이딩뷰만의 문제가 아니라 **이 세션 환경의 일반 웹 접속 자체가 허용목록 밖은 전부 차단**되는 정책임을 확인(§1 의 스킴 문제나 §2 의 재시도 문제와는 다른, 도메인 자체 차단). **러너에서는 실제로 도달한다**(§8에 실측). 그래도 진행하지 않기로 함 — 트레이딩뷰 위젯은 데이터 추출·재사용을 이용약관으로 금지한다. |
| `openapi.kiwoom.com` · `www.kiwoom.com` | ✗ 403 | ✓ 302 (2026-08-18) | 키움 신규 REST API. 세션은 막히지만 러너는 도달. 다만 해외선물(나스닥) 지원은 아직 "확대 예정" 단계라 §8 참고 |
| `hist.databento.com`·`databento.com`·`api.polygon.io`·`finnhub.io` | ✗ 403 | ✓ 200/404 (2026-08-18) | §8 참고 |
| `openapi.koreainvestment.com:9443`(KIS 실전) | 미확인 | ✓ 200 (2026-08-18) | §8 참고. `openapivts.koreainvestment.com:29443`(모의투자)는 같은 시도에서 000 — 포트 자체 특성일 수 있어 재확인 필요 |

- **`curl` 한 방으로 판단하지 않는다.** Yahoo 는 첫 요청에 429 를 주고 재시도·백오프로 뚫린다.
  `curl` 로 429/000 을 보고 "막혔다"고 결론 내면 실제로 되는 경로를 버린다.
- 러너에서만 되는 것은 워크플로로 받아 `data/` 에 커밋하고 세션은 그 파일을 읽는다
  (`econ-calendar` · `logos-cache` · `index-valuation` 이 그 패턴).

## 3. 브라우저 자동화

- **크롬 «확장 프로그램»은 이 파이프라인에 쓸 수 없다.** 확장은 사용자 브라우저 안에서 돌기 때문에,
  매주 정해진 시각에 무인으로 도는 워크플로가 호출할 대상이 없다. 사람이 브라우저를 열어둬야 한다.
- 그 아이디어의 등가물은 **Playwright + Chromium** 이고 이미 설치돼 있다
  (`CHROMIUM_PATH=/opt/pw-browsers/chromium`). 카드 렌더가 이걸 쓴다.
- 다만 **크롤링 용도로는 양쪽이 다 막혔다**: 세션은 프록시(§1), 러너는 대상 사이트의 IP 차단(§4).
  **JS 세션·봇방어를 브라우저로 넘으려는 시도는 이 두 벽을 먼저 확인하고 시작한다.**

## 4. 지수 PER / PBR — 결론 (2026-08-08 확정)

### 미국 3개 지수 → **해결**

`index-valuation` 워크플로가 WSJ 표에서 받아 `data/index-valuation.json` 에 커밋한다.
현재 PER · 1년 전 PER · 추정 PER · 배당수익률.

- **출처를 카드에 반드시 적는다.** 같은 날 S&P 500 PER 이 **WSJ 26.02 / multpl 29.88 /
  iShares IVV 30.65** 로 최대 18% 벌어진다. 셋 다 "as-reported 기준"이라 말하지만 쓰는 이익 계열이
  다르다(Birinyi / S&P 공식 / 보유종목 가중). 하나를 골라 무출처로 쓰면 특정 벤더의 방법론을
  사실처럼 내보내게 된다. **세 지수를 한 소스에서 받아 같은 방법론으로 비교하는 것이 이 칸의 규칙이다.**
- **EPS 는 쓰지 않는다.** 표에 없고, 종가 ÷ PER 로 되짚으면 반올림 오차가 '지수 EPS' 처럼 보인다.
- WSJ 표에 나스닥 **종합**이 없어 **100** 이 잡힌다. 카드 ③은 종합지수 봉차트를 그리므로
  **이름을 '나스닥 100' 으로 정확히 적는다.** 스크립트가 읽은 행 이름에서 표시명을 만든다.

### 코스피 · 코스닥 → **경로 없음. 다시 파지 않는다**

PER 이든 PBR 이든 같다. 문제는 지표 종류가 아니라 **단위**다 — 개별 종목 값은 구해도 **지수 단위**
값은 KRX 가 자체 가중 방식으로 산출·발표하므로, 종목 데이터로 되짚으면 공식 수치와 어긋난다.

시도하고 실패한 것 전부:

| 시도 | 결과 |
|---|---|
| KRX `getJsonData.cmd` (https + 쿠키 자, 여러 `bld`) | `LOGOUT` — 화면이 JS 로 트는 세션을 요구 |
| KRX 를 **실제 브라우저로** (러너 Playwright) | `서비스 제공 불가능` — KRX 가 데이터센터 IP 를 앱 레벨 차단 |
| KRX 를 브라우저로 (세션) | `ERR_CONNECTION_RESET` — 프록시(§1) |
| **KRX 공식 OpenAPI** | 열리지만 **유료** → 채택 안 함 (2026-08-08 사용자 결정) |
| 네이버 금융 지수 페이지 (데스크톱) | 200 이지만 **PER/PBR 항목 자체가 없음** |
| 네이버 모바일 API (`m.stock.naver.com/api/index/...`) | 000 |
| FinanceDataReader 미러 `listing/krx` | 가격·시총만(`Close`·`Marcap`·`Stocks`). 이익 계열 없어 산출 불가 |
| KOSIS · FnGuide · investing.com | 000 |

**그래서 한국 지수 PER 칸은 비운다.** `valuation.rows` 에 한국을 넣지 않고, 카드 `note` 에 미국
기준임을 밝힌다. 렌더러는 있는 행만 그리므로 카드는 정상이다.

**다시 시도해도 되는 조건은 하나뿐이다** — KRX 가 무료 정책으로 바뀌거나, 지수 단위 PER/PBR 을
공표하는 다른 무료 공식 소스가 생겼을 때. 그 밖의 경우 위 표를 다시 훑지 말고 빈칸을 유지한다.
**자체 산출로 공식 수치를 흉내내지 않는다 — 틀린 숫자보다 빈칸이 낫다.**

## 5. 그 밖의 확보 못 한 항목

| 항목 | 상태 |
|---|---|
| 한국 투자자별 매매동향 (외국인·기관·개인 순매수) | 미러에 없음. `ROUTINE_PROMPT.md` 리서치 A절 참고 |
| 지수 선물 (ES=F·NQ=F) | Apify 액터 경유 (`futures-cache` 워크플로, `APIFY_TOKEN` 필요) |
| ARM 로고 | `logos-cache` 에서 유일하게 실패. 모노그램으로 나감 |
| FMP `calendar`→`earnings-calendar` (다음 주 실적) | **표본이 작다.** 2026-08-09 sun 세션 확인: `from_date`/`to_date`/`limit:1000` 을 어떻게 바꿔도 8월 한 달 전체가 티커 약 20개로 고정됐다(다른 날짜 범위로 반복해도 같은 집합이 나온다). 코어위브·슈퍼마이크로·어플라이드 머티어리얼즈처럼 언론에 이미 보도된 실적 일정이 이 20개 밖이면 아예 나오지 않는다 — "그 주에 실적이 없다"가 아니라 "이 API 표본 밖이다". **뉴스로 날짜만 다수 매체 대조되면 캘린더 카드(예: sun ③)엔 써도 되지만, 컨센서스 EPS 숫자가 필요한 카드(sun ⑤)엔 이 표본 안의 티커만 쓴다.** |
| FMP `calendar`→`earnings-company`, `statements`→`income-statement` (개별 종목 과거 실적) | **대부분 ACCESS DENIED.** AAPL·HD·AMAT·DE·CSCO 로 시도 — WMT·BIDU 는 성공, 대부분은 플랜 제한 오류. 성공/실패가 종목별로 일관되지 않아 예측 불가. 과거 분기 실적(예: 전년 동기 EPS)이 필요하면 이 경로에 의존하지 말고 기업 발표 보도자료를 WebSearch 로 찾는 편이 낫다(2026-08-09 sun 에서 시스코 전년 동기 EPS 를 이렇게 확보). |

## 6. 탐침 기록 (재실행 불필요)

아래는 결론이 난 조사다. **같은 걸 다시 돌리지 않는다.** 보고서는 `data/` 에 남아 있다.

| 워크플로 | 무엇을 확인했나 | 리포트 |
|---|---|---|
| `probe-index-valuation` | 지수 PER 소스 10곳의 접근 가능 여부 → 러너에서 5곳 | `data/probe-index-valuation.json` |
| `probe-valuation-extract` | 그중 값이 실제로 뽑히는지 + WSJ 표 칼럼 헤더 확보 | `data/probe-valuation-extract.json` |
| `probe-krx-browser` | KRX 를 러너 브라우저로 열 수 있는지 → 앱 레벨 차단 | `data/probe-krx-browser.json` |

---

## 7. 세이브티커(saveticker) — 미국장 마감 리포트 (2026-08-14 확인)

매 미국장 마감 후 **07:12~08:00 KST** 사이에 그날의 마감 리포트를 올린다. 밤사이 미국장 요약 +
발표된 경제지표(예상·이전·실제) + **다음 날 주요 일정**이 한 글에 정리돼 있다.

**호스트 주의 — `www.` 가 붙으면 차단된다.**

```
✗ https://www.saveticker.com/...     → 프록시 403 (CONNECT tunnel failed)
✓ https://saveticker.com/...         → 200
✗ https://media.saveticker.com/...   → 403. 이미지판 리포트를 읽을 수 없는 이유
```

**받는 법 — `node scripts/fetch-saveticker.mjs [YYYY-MM-DD]` 를 쓴다.** 인자는 리포트 제목에 붙는
날짜(= 미국 거래일)이고, 생략하면 어제(KST)다. 아직 안 올라왔으면 **exit 2** 로 조용히 빠지므로
그때는 건너뛴다. 아래는 그 스크립트가 하는 일이자, 스크립트가 깨졌을 때 고치기 위한 기록이다.

1. 목록 검색 → 그날 글 ID 찾기. **`?search=` 없이는 최신 20건만 나와 리포트가 안 잡힌다.**
   ```
   https://saveticker.com/api/news/list?search=마감%20리포트
   ```
   `news_list[].title` 이 `SAVE 마감 리포트｜ 26년 MM월 DD일 (요일) - 텍스트` 인 것을 고른다.
   **제목에 `- 텍스트` 가 붙은 판만 쓴다** — 안 붙은 쌍둥이 글은 본문이 전부 이미지라 읽을 수 없다.
2. 목록 API 의 `content` 는 83자로 잘려 있다. 본문은 상세 페이지에서 받는다:
   `https://saveticker.com/news/<id>` → Next.js flight 페이로드(`self.__next_f.push([1,"..."])`)를
   이어붙여 `unicode_escape` 디코드하면 `{"type":"text","content":"..."}` 배열로 본문이 나온다.
   (SSR HTML 에는 본문이 없다 — `<body>` 만 긁으면 빈손이다.)

**발행 시각이 우리 루틴과 겹친다 (중요).** 10회 표본에서 07:12~08:00, 중앙값 ~07:26.
am 루틴 발화 시각 07:35 기준으로 **10일 중 3일은 아직 안 올라와 있었다**(07:43·07:52·08:00).
그러니 **세션 시작 시점에 받지 말고, 리서치를 마친 뒤 `ROUTINE_COMMON.md` §3 검증 직전에 받는다** —
그 무렵이면 거의 확실히 올라와 있고, 쓰임새(누락 점검)도 딱 그 자리다. 없으면 그냥 건너뛴다.

**쓰임새 — 숫자 출처가 아니라 누락 점검용이다.** 이건 매체가 정리한 **2차 자료**이므로
`ROUTINE_PROMPT.md` 의 "시세·지표·공시는 데이터 소스에서 직접 조회" 원칙을 대체하지 않는다.
- ✅ 검증 D(완전성) 체크리스트 — 그 시간대에 발표된 지표를 ★중요도와 함께 나열해 주므로,
  우리가 빠뜨린 게 있는지 대조하기 좋다.
- ✅ 다음 날 일정 카드(⑧)의 후보 목록.
- ✅ 우리가 따로 못 구하는 메타데이터 — 연준 인사의 **성향(매파/비둘기/중립)과 투표권 유무**, 배당락 종목.
- ❌ 카드에 싣는 수치의 근거로 쓰지 않는다. 여기서 발견한 항목도 BLS·EIA·재무부 등 1차 소스로 확인한 뒤 쓴다.

---

## 8. 나스닥·S&P 선물 실시간 대체 소스 조사 (2026-08-18, 스토리 축)

야후의 상시 ~10분 지연(§7 관련)을 줄여보려고 무료·저가 대체 소스를 훑었다. **결론: 이 세션 예산으로
야후보다 확실히 나은 무료 경로는 없다.** 아래는 시도 순서대로.

- **트레이딩뷰·키움 REST·Databento·Polygon·Finnhub 도메인**: 세션에서는 전부 프록시 403(§2 표
  참고 — `www.google.com`도 막혀 있어 이 세션 환경의 허용목록 정책 때문). **GitHub Actions
  러너에서는 전부 200대로 도달한다** — `probe-market-data.yml`(workflow_dispatch)로 실측
  (2026-08-18). 즉 "세션이 막으니 못 쓴다"가 아니라 "러너 워크플로로 받아 캐시하면 된다"가 맞는
  결론 — `futures-cache.yml`(Apify)과 같은 패턴.
- **Finnhub·Alpaca**: CME 선물(NQ/ES) 자체를 취급하지 않는다. **탈락.**
- **Barchart OnDemand**: 계좌+API 키 필요. 공식 문서에 **"인트라데이 데이터가 최대 20분 캐시
  지연"**이라고 명시돼 있다 — 야후(10분)보다 **더 나쁘다.** **탈락.**
- **Databento** (`hist.databento.com`, GLBX.MDP3 데이터셋): 가입 자체는 무료지만 카드 등록이
  필요하고 $125 크레딧이 지급된다. `probe-databento.yml`로 실제 키를 받아 테스트:
  - 인증은 정상(`metadata.list_datasets` → 200).
  - `timeseries.get_range` 로 당일 데이터를 요청하면 **`dataset_unavailable_range` 에러** —
    "구독·라이선스가 필요하며, 요청 시각 기준 **8시간 이전** 데이터까지만 무료 크레딧으로 접근
    가능"(실측: 요청 15:54 UTC 기준 접근 가능 최신 시각이 07:54 UTC). **당일 실시간은 커녕 그날
    정규장 개장 전 데이터까지만 열려 있다.** 진짜 실시간을 받으려면 **CME 데이터 구독을 별도로
    월 $179 결제**해야 한다(2026-08 기준, `databento.com/pricing`). **사용자 판단으로 결제는
    보류 — 이 소스는 여기서 멈춘다.**
- **Polygon(현 Massive)**: CME 지원은 있으나 비즈니스 플랜이 월 $2,499(확인됨), 개인 실시간
  선물 티어의 구체적 가격은 못 찾음 — 비쌀 가능성이 높아 후순위.
- **Interactive Brokers(IBKR)**: API는 Docker로 헤드리스 실행 가능(`gnzsnz/ib-gateway-docker`
  등 실사용 사례 다수, 리눅스 OK). 다만 CME 실시간 데이터 번들에 **월 최소 $30 상당의 커미션
  발생 조건**이 붙고, 실제 브로커 계좌+선물 거래권한 신청이 선행돼야 한다. 무거움.
- **한국투자증권(KIS) Open API** — **가장 유력한 후보로 보류 중**:
  - API 이용료 자체는 없음("API 사용에 따른 별도 이용료는 없으며, 매매수수료는 HTS 사용수수료와
    동일") — Databento식 월 구독이 없다.
  - `openapi.koreainvestment.com:9443`(실전), `apiportal.koreainvestment.com` 모두 러너에서
    도달 확인(200/302). `openapivts.koreainvestment.com:29443`(모의투자)만 000 — 재확인 필요.
  - **그런데 해외선물옵션 계좌 개설 자체가 무겁다**: 사전교육 최소 1시간 + 모의거래 최소 3시간
    이수 + 기본예탁금(구체적 금액 미확인, 지점 문의 필요) 이 선행돼야 앱키를 받을 수 있다. 단순
    데이터 조회 목적으로는 진입장벽이 크다.
  - 해외선물 실시간 웹소켓의 실제 지연 정도는 **앱키를 받아야 실측 가능** — 문서 검색만으로는
    확인 못 함. 사용자가 계좌 개설을 진행해 앱키를 확보하면 그때 재개.

**다음에 이어갈 때**: `.github/workflows/probe-databento.yml`·`probe-market-data.yml` 은 이
조사에 쓴 일회성 워크플로로 남아 있다(재실행 가능, `DATABENTO_API_KEY` 시크릿 필요). KIS 앱키를
확보하면 같은 패턴으로 `probe-kis.yml` 을 만들어 웹소켓 지연을 실측하면 된다.

---

## 9. 美 지수·선물·원자재·환율·코인 조회 — `UsStockInfo` 도구 소실, 대체 경로 확정 (2026-08-20)

`ROUTINE_PROMPT.md` A절은 나스닥·S&P·코스피·VIX·원달러·금·WTI·비트코인 조회를 **PlayMCP
`UsStockInfo-get_historical_stock_prices`** 하나로 지정하는데, **2026-08-20 am 세션 시점에
이 도구가 세션에 로드된 PlayMCP 도구 목록에 없었다** (`ToolSearch` 로 "UsStockInfo"·
"get_historical_stock_prices"·"PlayMCP stock" 등 여러 키워드로 찾아도 안 잡힘 — 배포에서
빠졌거나 이름이 바뀐 것으로 보이나 원인은 미확인). 이 세션은 아래 대체 경로로 전량 대체해
문제없이 완료했다 — **다음 세션은 `UsStockInfo` 를 먼저 찾아보되, 없으면 바로 이 경로로 넘어가고
새로 헤매지 않는다.**

- **미 지수 종가(나스닥 종합·S&P500·VIX)**: `mcp__FMP__indexes`, `endpoint: "index-quote-short"`,
  `symbol: "^IXIC"`/`"^GSPC"`/`"^VIX"` — 정상 동작. 전일 대비 delta 는 `index-historical-price-eod-light`
  로 `from_date`/`to_date` 지정해 직접 계산한다(`change` 필드도 오지만 검산 겸 둘 다 본다).
  **단, 코스피(`^KS11`)는 `index-quote-short` 가 "ACCESS DENIED: 요금제 상향 필요"를 반환한다**
  (2026-08-20 확인) — 이 심볼은 FMP 로 못 받는다.
- **코스피·미 선물(NQ=F·ES=F)·상품(GC=F·CL=F)·달러인덱스(DX-Y.NYB)·원달러(KRW=X)·비트코인(BTC-USD)**:
  Yahoo chart API 를 세션에서 **직접 curl** 한다 — §2 표가 이미 "세션 5/5 성공"으로 확인해 둔
  경로를 그대로 쓰면 된다. **`period=` 파라미터가 아니라 `range=` 를 써야 한다** — `period=5d` 를
  주면 하루치(가장 최근 값)만 오고 과거 종가가 비어 delta 계산이 안 된다(2026-08-20 에 실제로
  이 실수로 SMH 등 섹터 ETF 5일치가 하루치로 잘려 나왔다). 반드시 이렇게 호출한다:
  ```
  curl -s -A "Mozilla/5.0" "https://query1.finance.yahoo.com/v8/finance/chart/<SYM>?range=5d&interval=1d"
  ```
  `meta.regularMarketPrice` 가 최신값, `indicators.quote[0].close` 배열(당일 마지막 값 `null`
  주의, `FORMAT_BRIEFING.md`·`ROUTINE_PROMPT.md` 기존 경고와 동일)에서 전일 종가를 골라 delta 를
  직접 계산한다. `meta.chartPreviousClose` 는 기존 경고대로 신뢰하지 않는다(원/달러에서도
  1,412.26 같은 오답이 재현됨, 2026-08-20).
- **美 섹터 ETF(SMH·XLK·XLC·XLF·XLY·XLV·XLI·XLE·XLP·XLB·XLU·XLRE) 5일 시계열**: 위와 같은 Yahoo
  `range=5d` 호출로 전부 정상 수신(2026-08-20, 12개 전량 확인). `ROUTINE_PROMPT.md` §B 의 섹터
  ETF 표는 그대로 유효하다 — 조회 도구만 이걸로 바꾸면 된다.
- **`mcp__FMP__forex`·`mcp__FMP__commodity` 는 이 계정 요금제에서 대부분 막힌다.** `forex-quote`
  (USDKRW·DXY 등)는 호출 즉시 "ACCESS DENIED: 요금제 상향 필요". `commodity` 의
  `commodities-quote` 는 **`GCUSD`(금)는 한 번 성공**했으나 같은 세션의 `CLUSD`(WTI)는
  거부됐다 — 심볼별로 다르게 막히는지, 일시적 제한인지 불확실하니 **이 도구들에 시간 쓰지 말고
  바로 위 Yahoo 경로로 간다.**
- **Fear & Greed**: 여전히 API 없음. WebSearch 로 여러 매체를 대조하면 같은 날짜에도 수치가
  갈린다(2026-08-20 에 54~59 사이로 소스마다 달랐다) — 가장 반복해 언급되는 값을 쓰고,
  CNN 공식 페이지(`cnn.com/markets/fear-and-greed`)는 이 환경에서 `EGRESS_BLOCKED`로
  WebFetch 가 막힌다(직접 조회 시도할 필요 없음).

---

## 이 문서를 고칠 때

- **측정값에는 날짜를 붙인다.** 소스 정책은 바뀐다. 날짜 없는 "안 된다"는 반년 뒤에 쓸모가 없다.
- **실패를 지우지 않는다.** 실패 기록이 이 문서의 값이다. 다시 되기 시작했으면 줄을 지우지 말고
  "언제부터 다시 됨"을 덧붙인다.
- **`fetch` 결과만으로 «차단» 이라고 쓰지 않는다.** 스킴(§1)·재시도(§2)·세션vs러너(§2)를 확인한 뒤 적는다.
