# 2026-08-23 개편 — 브리핑 세션이 확인할 것

> **이 문서의 성격**: 지침서가 아니다. 2026-08-23 에 무엇을 고쳤고 다음 회차에서 무엇을
> 눈으로 확인해야 하는지만 적은 **일회성 인수인계 메모**다. 확인이 끝나면 지워도 된다.
> 규칙 자체는 `FORMAT_BRIEFING.md` 와 `ROUTINE_PROMPT.md` 에 이미 들어가 있다.

---

## 0. 한 줄 요약

렌더러(`scripts/render-cards.mjs`)에 **카드 두 가지가 새로 생겼고 브랜드 로고가 바뀌었다.**
로고는 세션이 아무것도 안 해도 적용된다. **나머지는 세션이 JSON 에 필드를 넣어야 나온다.**

---

## 1. 세션이 아무것도 안 해도 되는 것

### 브랜드 로고

머리·꼬리·아웃트로의 `luckyon 브리핑` 표기가 **글자에서 확정 로고 파일로** 바뀌었다.
`assets/brand/wordmark-briefing.png` 를 렌더러가 직접 읽는다.

**왜 바꿨나**: 예전에는 `luckyon` + 파란 `브리핑` 을 글자로 그렸다. 확정 로고는
`o` 자리가 **앰버 원판 안의 흰 네잎클로버**이고 `브리핑` 도 앰버다. 글자로는 클로버를
그릴 수 없고 색까지 달랐다.

**확인**: 아무 카드나 열어 머리 왼쪽 위를 본다. `o` 가 노란 동그라미 안에 흰 클로버면 맞다.
`브리핑` 이 파란색이면 렌더러가 옛 코드인 것이니 보고할 것.

---

## 2. 세션이 JSON 에 넣어야 나오는 것

### 2-1. 거래대금 상위 카드의 공매도 비중 열

`rank` 카드의 각 행에 `short` 를 넣으면 **공매도 비중 열이 자동으로 생긴다.**
한 행이라도 `short` 가 있으면 열이 생기고, 값이 없는 행은 `—` 로 나온다.

```json
{
  "type": "rank",
  "title_ko": "미국 거래대금 상위", "title_en": "US Top Value Traded",
  "color": "#3987e5",
  "col_value_ko": "거래대금", "col_value_en": "Turnover",
  "rows": [
    { "name_ko": "테슬라", "name_en": "Tesla", "value": "$20.98B", "pct": 5.14, "short": 47.5 }
  ]
}
```

값은 `node scripts/fetch-us-flows.mjs <YYYY-MM-DD>` 의 `topTurnover[].shortPct` 를 그대로 쓴다.
**이 스크립트는 키가 필요 없다** (야후 스크리너 + FINRA 공개 파일).

### 2-2. 공매도 막대는 반드시 `mode: "compare"`

```json
"share": {
  "mode": "compare",
  "label_ko": "종목별 공매도 비중", "label_en": "Short volume share by name",
  "baseline": 46.8,
  "baseline_label_ko": "표본 평균", "baseline_label_en": "Sample average",
  "segments": [
    { "label_ko": "테슬라", "label_en": "Tesla", "pct": 47.5, "color": "#e66767" }
  ]
}
```

**`mode` 를 빼면 `nested` 가 되어 틀린 그림이 나온다.** `nested` 는 "합이 100 인 조각" 을
한 줄에 이어 붙이는 방식이다. 공매도 비중처럼 **서로 독립인 값**을 이렇게 그리면
47.5% 와 60.1% 가 붙어 "합쳐서 107%" 로 읽힌다. 2026-08-22 에 실제로 이 사고가 났다.

`baseline` 에는 `fetch-us-flows.mjs` 의 `shortVolume.sampleAvgPct` 를 넣는다.
기준선이 서야 독자가 "평균보다 높은가 낮은가" 로 읽을 수 있다.

**거래대금 비중처럼 진짜 조각인 값**(상위 2종목 52.4% / 나머지 47.6%)은 `mode` 를 빼거나
`"nested"` 로 둔다. 그쪽이 맞다.

### 2-3. 실적 · 지표 발표 카드 (`type: "econ"`)

새 카드 유형이다. **실측치를 예상치와 나란히** 놓는다.

```json
{
  "type": "econ",
  "title_ko": "실적 · 지표 발표", "title_en": "Earnings & Data",
  "color": "#5b8dd6",
  "col_delta_ko": "예상·전년 대비", "col_delta_en": "vs Est./YoY",
  "rows": [
    { "region": "KR", "name_ko": "한국 7월 수출", "name_en": "Korea July exports",
      "sub_ko": "통관 기준 · 전년동월 대비", "sub_en": "Customs basis, YoY",
      "actual": "989.6억$", "estimate": null, "surprise": 63.0 },
    { "region": "US", "name_ko": "월마트 2분기", "name_en": "Walmart Q2",
      "sub_ko": "주당순이익 · 8/20 발표", "sub_en": "EPS · reported 8/20",
      "actual": "$0.81", "estimate": "$0.73", "surprise": 10.96 }
  ]
}
```

- `region` 이 `"KR"` 인 행을 **렌더러가 위로 올린다.** JSON 순서는 신경 쓰지 않아도 된다.
- 예상치가 없는 지표는 `estimate: null` 로 두고 `surprise` 에 전년동월 대비 증감률을 넣는다.
  그럴 때는 **`col_delta_ko` 로 열 제목을 바꾸고** 각 행 `sub` 에 무엇과 견준 값인지 적는다.
  열 제목이 "예상 대비" 인 채로 전년 대비 숫자를 넣으면 독자가 오해한다.
- 행은 **6건까지**. 그 이상이면 푸터에 닿는다.
- `col_delta_ko` 는 **8자 안쪽**. 길면 열 제목이 두 줄로 접힌다.

**어디서 받나**
- 미국 실적 실측치 — Alpha Vantage `EARNINGS` 의 `quarterlyEarnings[]` 에
  `reportedEPS` · `estimatedEPS` · `surprisePercentage` 가 있다.
  `EARNINGS_CALENDAR` 는 **예정만** 주므로 실측에는 못 쓴다.
- 한국 수출입 — 아래 3번.
- 한국 기업 실적 — 공시(OpenDART).

---

## 3. 한국 수출입 — `node scripts/fetch-kr-trade.mjs`

새로 만든 수집기다. **관세청을 먼저 두드리고 안 열리면 한국은행 ECOS 로 내려간다.**
어느 쪽을 썼는지는 출력 `source` 에 남으니 **카드 각주(`note_ko`)에 그대로 옮겨 적는다.**

2026-08-23 로컬 실행 확인값 (2026년 7월분):

| 항목 | 값 |
|---|---|
| 수출 | 989.6억 달러 (전년동월 **+63.0%**) |
| 수입 | 685.7억 달러 |
| 무역수지 | **+303.9억 달러** |
| 중화학공업품 수출 | 872.0억 달러 = 수출의 **88.1%** (+69.7%) |
| 경공업품 | 37.8억 달러 (3.8%) |
| 수입 원자재 / 자본재 / 소비재 | 46.1% / 40.8% / 13.1% |

### 키가 필요하다 — 클라우드에서는 확인 필요

`ECOS_API_KEY` 를 쓴다. 로컬에서는 `api-keys/keys.env` 를 읽지만
**클라우드에는 그 파일이 없다.** 루틴 환경 설정에 `ECOS_API_KEY` 가 있어야 한다.

키가 없으면 스크립트가 이렇게 말하고 멈춘다:

```
ECOS_API_KEY 가 없습니다. 클라우드에서 돌 때는 루틴 환경 설정에 ECOS_API_KEY 를
넣어야 합니다 (로컬에서는 api-keys/keys.env 를 읽습니다).
키가 없으면 이 카드의 한국 지표 자리는 비우고 진행하십시오.
```

**이 경우 브리핑을 멈추지 않는다.** 한국 수출입 항목만 빼고 나머지로 `econ` 카드를 채운다.
그리고 **키가 없었다는 사실을 보고에 남긴다.**

### 관세청은 아직 안 열려 있다

`1220000/Itemtrade/getItemtradeList` 는 서비스가 살아 있지만 이 계정 인증키가
등록돼 있지 않아 `등록되지 않은 서비스키`(코드 30) 가 온다. 같은 키가 국토부
실거래가에는 `resultCode 000` 을 주므로 **키는 정상이고 이 서비스에만 안 붙어 있다.**

**승인을 기다릴 필요는 없다.** 위 스크립트가 알아서 ECOS 로 내려간다.
승인되면 그날부터 자동으로 관세청 HS 품목별을 쓰고 `source` 가 바뀐다.

---

## 4. 이번 회차 확인표

렌더링이 끝나면 카드를 열어 아래를 눈으로 본다.

- [ ] **모든 카드** 머리 왼쪽 로고의 `o` 가 앰버 원판 + 흰 클로버인가
- [ ] **모든 카드** 꼬리 로고도 같은 모양인가 (반투명하게 나온다)
- [ ] **아웃트로(마지막 장)** 가운데 로고가 크게 들어갔는가
- [ ] **거래대금 상위 카드** 표에 열 제목(종목 / 거래대금 / 등락률)이 붙었는가
- [ ] **미국 거래대금 카드** 공매도 비중 열이 있는가
- [ ] **공매도 막대** 종목마다 막대가 따로 그려지고 회색 기준선이 서 있는가
      — 한 줄로 이어 붙어 있으면 `mode:"compare"` 를 빠뜨린 것
- [ ] **실적·지표 카드** 가 나왔는가. 한국 항목이 위에 있는가
- [ ] **실적·지표 카드** 마지막 열 제목이 두 줄로 접히지 않았는가
- [ ] `fetch-kr-trade.mjs` 가 값을 냈는가. 못 냈으면 `ECOS_API_KEY` 문제인지 확인
- [ ] 어느 카드도 본문이 푸터(`luckyon 브리핑` 줄)에 닿지 않는가

---

## 5. 문제가 생기면

| 증상 | 원인 | 대처 |
|---|---|---|
| 로고가 파란 `브리핑` 글자 | 렌더러가 옛 코드 | `git pull` 후 다시 렌더 |
| 로고 자리가 빈 네모 | `assets/brand/wordmark-briefing.png` 없음 | `git pull` |
| 공매도 열이 안 보임 | `rows[]` 에 `short` 가 하나도 없음 | `fetch-us-flows.mjs` 출력의 `shortPct` 를 넣는다 |
| 공매도 막대가 한 줄로 이어짐 | `share.mode` 누락 | `"mode": "compare"` 추가 |
| 실적·지표 카드가 글 카드로 나옴 | `type` 누락 | `"type": "econ"` 추가 |
| `econ` 마지막 열 제목이 두 줄 | `col_delta_ko` 가 8자 초과 | 줄인다 |
| 한국 수출입이 안 나옴 | `ECOS_API_KEY` 미설정 | 그 항목만 빼고 진행 · 보고에 기록 |

---

## 6. 이번에 손댄 파일

| 파일 | 무엇 |
|---|---|
| `scripts/render-cards.mjs` | 로고 적용 · rank 열 제목/공매도 열 · `share.mode` · `econ` 카드 |
| `scripts/fetch-kr-trade.mjs` | **신규** — 한국 수출입 (관세청 → ECOS 자동 전환) |
| `scripts/fetch-us-flows.mjs` | (8/22 추가) 미국 거래대금 · 공매도 · 상승/하락 거래대금 |
| `assets/brand/wordmark-briefing.png` | **신규** — 확정 로고. 앰버 원판이 빠져 있던 것을 다시 합성 |
| `FORMAT_BRIEFING.md` | 유형 표에 `econ` 추가 · `rank` 필드 확장 · 브랜드 표기 규칙 |
| `ROUTINE_PROMPT.md` | 자료 수집 표에 한국 수출입 경로 · 관세청 상태 |

**절 구성은 어느 문서도 건드리지 않았다.** 표와 목록 안에만 채웠다.
