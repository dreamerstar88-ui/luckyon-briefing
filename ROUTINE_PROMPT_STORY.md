# luckyon 스토리 루틴 절차서 (미국장 개장 33분)

> **루틴 설정 방법**: claude.ai/code/routines 에서 아래처럼 등록합니다. Instructions 칸에는 한 줄만 넣습니다.
>
> **트리거**: 매주 월~금 **23:01 KST** (발행이 23:03 무렵 이뤄지도록 2분 여유를 둡니다)
> ```
> 저장소의 ROUTINE_PROMPT_STORY.md 파일을 읽고, 그 절차를 순서대로 그대로 수행하세요.
> ```
>
> **서머타임 주의 (중요)** — 미국 정규장 개장은 서머타임 기간(3월 둘째 일요일~11월 첫째 일요일)엔 22:30 KST, 해제 기간엔 23:30 KST 입니다.
> 이 절차서는 "개장 후 33분"을 담으므로 루틴 시각도 함께 옮겨야 합니다.
> - **3월~10월**: 23:01 KST
> - **11월~2월**: 00:01 KST
>
> 시각을 안 옮기면 2단계에서 "아직 개장 전"으로 조용히 종료되므로 **잘못 발행되지는 않습니다.** 대신 그 기간 내내 아무것도 안 올라갑니다.
>
> **왜 30분이 아니라 33분, 왜 컷오프가 10분이 아니라 13분인가 (2026-08-18)** — 야후의 무료 선물
> 1분봉은 상시 ~10분 지연되는 피드다. 예전 기준(개장 후 30분에 실행, 10분 초과면 실패)은 이
> 피드의 정상적인 지연 폭과 컷오프가 거의 같은 값이어서, 실제 신선도와 무관하게 그 경계선에서
> 통과/실패가 우연히 갈리는 사고가 있었다(같은 ~10분 지연인데 어떤 회차는 통과, 어떤 회차는
> 실패). 실행 시각을 3분 늦추고(30→33분) 컷오프도 3분 늘려(10→13분) 이 피드의 상시 지연에
> 여유를 준 것 — **다만 이게 "더 신선한 데이터"를 만들어주는 건 아니다.** 여전히 ~10분 전
> 시세를 쓰는 것이고, 이 여유는 그 사실을 받아들이고 불필요한 실패만 줄이는 조정이다.

---

당신은 "luckyon" 인스타그램 스토리의 자동 발행 담당입니다. 저장소 `luckyon-briefing` 이 클론된 상태입니다.

이 스토리의 컨셉은 **"주린이가 장 보다가 스마트폰으로 차트를 찍어, 그 위에 펜으로 혼잣말을 갈겨쓴 화면"** 입니다.
분석이 아니라 그 순간의 기분입니다. 스토리는 24시간 뒤 사라지므로 부담 없이 매일 올립니다.

**한국어·영어 두 건을 각각 발행합니다.** (스토리는 캡션이 없어 한 장에 두 언어를 섞을 수 없습니다.)

---

## 이 문서의 범위

**이 문서는 스토리 축이 "무엇을 담고 어떻게 만들지"만 정한다.** 준비·검증·커밋·발행·알림·정정 같은 **모든 축 공통 절차는 `ROUTINE_COMMON.md` 에 있고, 이 세션은 둘 다 따른다.**

- 카드 구조 문서(`FORMAT_BRIEFING.md`)는 **이 축과 무관하다** — 스토리는 렌더러가 다르다.
- **리서치를 시작하기 전에 `ROUTINE_COMMON.md` 를 먼저 읽는다.** 세션이 아는 것은 문서뿐이라, 안 읽으면 그 안의 규칙은 없는 것과 같다.

아래를 순서대로 수행하세요.

---

## 1. 준비

`ROUTINE_COMMON.md` §1 대로 의존성을 확인한다. (스토리는 정지 이미지라 ffmpeg 가 필요 없다.)

브라우저는 이 환경의 `CHROMIUM_PATH` 를 쓴다 — 스크립트가 알아서 처리하므로 `npx playwright install` 은 **실행하지 않는다.**

## 2. 시세 수집

```
node scripts/reels/fetch-window.mjs
```

- 인자 없이 실행하면 **그날 정규장 개장(09:30 ET)부터 지금까지**를 담는다.
- 성공하면 `data/reels/<ET날짜>-<ET시각>.json` 이 생기고 `data/reels/latest.txt` 에 스탬프가 남는다. 이 스탬프를 이후 단계에서 쓴다.

**실패하면(종료코드 1) 여기서 조용히 종료한다.** 아래가 여기 해당한다:
- 미국 공휴일 휴장
- 아직 개장 전 (서머타임 전환 직후 루틴 시각이 안 맞는 경우)
- 시세가 13분 이상 낡음

이 스토리는 **"지금 이 순간"이 전부**이므로, 낡은 값으로 발행하느니 거르는 게 맞다.
종료할 때는 10단계의 알림만 보내고 끝낸다 (실패가 아니라 정상적인 건너뜀이다).

## 3. 상황 문맥 채우기 (선택 — 있으면 문구가 좋아진다)

밤사이~지금 사이에 **시장을 움직인 뉴스**가 있었는지 확인한다. 이미 조사된 자료를 먼저 본다:

- `content/<오늘 KST 날짜>-am.json` 의 `sections[]` (오늘 아침 브리핑 본문 — 특히 'AI · 반도체 기술'·'주요 소식' 글 카드)
- `content/<어제 KST 날짜>-pm.json` 의 `schedule` (어젯밤 예고된 일정)

(예전 `econ`·`ai` 필드는 하위 호환으로만 남아 있고 지금 브리핑은 쓰지 않는다. 오래된 파일을 읽을 때만 나온다.)

시장을 실제로 움직인 뉴스가 뚜렷하면, 2단계에서 만든 JSON 에 `context` 를 추가한다:

```json
"context": {
  "recentNews": { "title_ko": "짧은 한 줄", "title_en": "one short line" }
}
```

또는 아직 발표 전인 큰 지표가 오늘 남아 있으면:

```json
"context": {
  "pendingEvent": { "title_ko": "FOMC", "title_en": "the Fed" }
}
```

- **제목은 아주 짧게** 쓴다(10자 내외). 손글씨로 차트 위에 얹히므로 길면 안 들어간다.
- **확실하지 않으면 넣지 않는다.** 문맥이 없어도 차트 모양만으로 문구가 나온다 — 틀린 인과를 쓰는 것보다 안 쓰는 게 낫다.

## 4. 이미지 생성 (한국어·영어)

```
node scripts/reels/render-reel.mjs <스탬프> ko --still
node scripts/reels/render-reel.mjs <스탬프> en --still
```

`cards/stories/<스탬프>/ko/story.png`, `.../en/story.png` (각 1080x1920) 가 생긴다. 한 장에 3~4초 걸린다. 같은 자리에 `alt.txt`(대체 텍스트)도 함께 생기며 발행 스크립트가 이를 읽어 넘긴다 — 세션이 따로 할 일은 없고, 5단계에서 함께 커밋되기만 하면 된다.

**두 장을 반드시 열어서 눈으로 확인한다.** 아래를 본다:

- 손글씨가 캔들을 가리지 않는가 (스크립트가 자동 배치하지만 실패할 수 있다)
- **손글씨 내용이 차트 모양과 맞는가 — 이게 가장 중요하다.**
  차트가 올랐는데 "계속 밀리네" 라고 쓰여 있으면 안 된다.
  실제로 개발 중에 "초반에 올랐다가 밀린" 흐름을 "내내 밀렸다"로 잘못 쓴 사례가 여러 번 있었다.
- 맞지 않으면 3단계의 `context` 를 손보고 다시 렌더링한다. 그래도 이상하면 **발행하지 말고** 10단계 알림에 사유를 적는다.
- **`context` 에 뉴스·지표를 넣었다면 그 한 건은 원문으로 확인한다** (`ROUTINE_COMMON.md` §3 의 스토리 축 게이트). 문구에 인과가 들어가는 순간 사실 주장이 된다.

## 5. `claude/live` 브랜치에 커밋 & 푸시

`ROUTINE_COMMON.md` §4 절차를 따른다. 이 축이 add 할 경로는 **`data/reels/<스탬프>.json` 과 `cards/stories/<스탬프>`** 뿐이다.

```
git add data/reels/<스탬프>.json data/reels/latest.txt cards/stories/<스탬프>
git commit -m "story: <스탬프> (ko+en)"
```

**시세 JSON 은 `data/reels/` 에 있다** — 이미지만 `cards/stories/` 로 간다. 2단계의 `fetch-window.mjs` 와 4단계의 `render-reel.mjs --still` 이 릴스 축과 같은 데이터를 공유하기 때문이다. 예전 절차서에 `data/stories/<스탬프>.json` 으로 적혀 있었는데 그런 파일은 만들어지지 않는다 — `git add` 는 없는 경로를 만나면 **아무것도 스테이징하지 않고 실패하므로**, 그대로 두면 커밋이 통째로 비고 이미지가 공개되지 않아 발행이 404 로 실패한다.

커밋 후 `git log --oneline -1` 로 **커밋이 실제로 만들어졌는지 확인**한다.

## 6. Pages 배포 대기

푸시 직후에는 아직 공개되지 않는다. GitHub Actions 의 `pages build and deployment` 워크플로가 끝날 때까지 기다린다.

```
curl -s "https://api.github.com/repos/dreamerstar88-ui/luckyon-briefing/actions/runs?branch=claude/live&per_page=1"
```

가장 최근 run 의 `status` 가 `completed` 가 될 때까지 기다린다 (보통 1~2분).

## 7. 토큰 만료 점검

`ROUTINE_COMMON.md` §5 대로 확인하고, 만료가 임박했으면 10단계 알림에 "인스타 토큰 갱신 필요"를 함께 적는다.

## 8. 스토리 발행 (한국어·영어)

```
node scripts/stories/publish-story.mjs <스탬프> ko
node scripts/stories/publish-story.mjs <스탬프> en
```

- **두 언어를 서로 독립적으로 시도한다.** 한쪽이 실패해도 다른 쪽을 건너뛰지 않는다.
- 각각 성공하면 media id 를, 실패하면 에러 메시지를 그대로 기록한다.
- 스토리는 24시간 뒤 자동으로 사라진다. 캡션·해시태그는 없다.

## 9. 결과 확인

각 스크립트가 `✅ 스토리 발행 완료! media id = ...` 를 출력했는지 확인한다.

## 10. 카카오톡 알림 (PlayMCP)

`ROUTINE_COMMON.md` §7 대로 **결과가 무엇이든** 보낸다. 이 축의 메시지 형식만 다르다 (200자 제한):

- 둘 다 성공: `✅ [luckyon] 스토리 <스탬프> — KO <id> / EN <id>` + 손글씨 문구 한 줄
- 일부 성공: `⚠️ [luckyon] 스토리 일부 성공 — KO: <요약> · EN: <요약>. 확인 필요`
- 둘 다 실패: `❌ [luckyon] 스토리 발행 실패 — <사유 한 줄>. 확인 필요`
- **2단계에서 건너뜀**: `⏭️ [luckyon] 스토리 건너뜀 — <사유: 휴장/개장 전/시세 지연>` — 이 축에만 있는 정상 종료 경로다.

## 11. 마무리 보고

무엇을 발행했는지(스탬프, 언어별 media id, 손글씨 문구)와 실패·건너뜀이 있었으면 그 사유를 요약한다.

---

## 참고 — 이 스토리가 담는 것

| | |
|---|---|
| 시각 | 미국 정규장 개장 후 33분 (23:03 KST, 겨울엔 00:03) |
| 구간 | 09:30 ET → 지금 |
| 차트 | 나스닥 선물(NQ) 1분봉, S&P 500 선물은 아래 한 줄 |
| 표시하지 않는 것 | **등락률(%)** — 30분 창 대비 등락이 하루 등락으로 오해되기 때문 |
| 대신 | '어제 종가' 점선을 그려 갭을 눈으로 보이게 함 |
| 문구 | 숫자 없이, 독백체로. 완결된 문장이 아니어도 된다 |

## 참고 — 왜 거르는 게 중요한가

이 컨셉의 유일한 자산은 **"지금 이 순간"** 입니다. 낡은 차트나 사실과 어긋난 문구가 한 번 나가면 그 신뢰가 무너집니다.
2단계에서 멈추는 것은 실패가 아니라 **설계된 동작**입니다. 조용히 건너뛰고 알림만 남기세요.
