# 주간 쇼츠 제작 지침서

**이 문서는 "무엇을 만들고 무엇을 만들지 않는가"를 정한다.** 순서와 명령어는
`ROUTINE_PROMPT_WEEKLY_SHORTS.md` 에 있다. 둘이 어긋나면 이 문서가 우선한다.

2026-09-12, 2회차를 만들다 세 번 갈아엎고 나서 썼다. 갈아엎은 이유가 전부
"기계적으로 만들어서"였기 때문에, 이 문서는 대부분 **하지 말 것**으로 되어 있다.

---

## 1. 누가 보는가

**미국 주식을 실제로 하는 사람이다.** 나스닥 선물 5분봉을 35초에 되돌려 보는
영상을 끝까지 볼 사람은 초보가 아니다. 이 전제가 아래 모든 규칙의 근거다.

이 시청자는 이런 것을 이미 안다. 그러니 이런 것을 알려 주면 무시당한다.

- 미국 정규장이 한국시간 밤 10시 30분~새벽 5시라는 것
- 지수가 오르면 초록, 내리면 빨강이라는 것
- 고용지표·CPI가 중요하다는 것
- 5분봉이 뭔지, 선물이 뭔지

**2회차에서 실제로 저지른 일**: 요약 화면에 "이번 주 바닥도, 꼭대기도 한국 밤에
나왔다"를 발견인 것처럼 적었다. 미국 정규장이 한국 밤이니 이건 발견이 아니라
정의다. 시청자가 보면 우습다. 대표가 잡아냈다.

---

## 2. 문구 원칙

### 2-1. 자명한 말을 발견인 척 쓰지 않는다

문구를 쓴 다음 스스로 물어라. **"이 문장이 거짓일 수 있는 주가 있는가?"**
없으면 그건 정보가 아니라 정의다. 지운다.

| 쓰면 안 되는 말 | 왜 |
|---|---|
| 바닥도 꼭대기도 한국 밤에 나왔다 | 미국 정규장이 한국 밤이라 거의 항상 참이다 |
| 지표 발표 때 크게 움직였다 | 지표 시각을 골라 보여 주고 있으니 당연하다 |
| 변동성이 있었다 | 변동성 없는 주는 없다 |
| 시장은 예상을 반영했다 | 반증이 불가능하다 |

### 2-2. 숫자 없는 형용사를 쓰지 않는다

"크게", "급격히", "많이" 는 숫자로 바꾼다. 숫자를 못 쓰겠으면 그 문장은 뺀다.

### 2-3. 화면·자막·설명란은 한 덩어리다

셋 중 하나를 고치면 **반드시 셋을 다 고친다.** 2회차에서 화면과 자막만 "밤"으로
고치고 설명란을 "저녁"으로 두었다가 검증에서 걸렸다. 이건 실수가 아니라 절차
문제이므로, 문구를 바꿀 때는 세 파일을 한 번에 연다.

- 화면: `render2.mjs` 의 `COPY`, 사건의 `l1`/`l2`/`when`/`en`
- 자막: `srt2.mjs` 의 `KO`/`EN`
- 설명란: `발행문구.md`

### 2-4. 회차 간 연속성을 함부로 주장하지 않는다

"이번 주도", "여전히", "또" 는 **앞 회차 숫자를 실제로 확인한 뒤에만** 쓴다.
2회차에서 "이번 주도 밤에 나왔다"고 쓸 뻔했는데, 1회차는 저녁(20:00·19:45)이고
2회차는 밤(21:55·22:30)이라 같은 구간이 아니었다.

### 2-5. 장중 고가와 종가를 구분해서 쓴다

"시가를 되찾지 못했다"와 "종가가 시가를 넘지 못했다"는 다른 말이다. 2회차에서 앞의
표현을 썼다가 검증에서 걸렸다 — 장중 고가는 화요일 11:40 에 시가를 0.5포인트(2틱)
넘겼고, 넘지 못한 것은 종가였다. 영어는 `closed` 로 맞게 썼는데 한국어만 과장됐다.
**번역이 서로 다른 뜻이 되면 그것도 §2-3 위반이다.**

### 2-6. 분포를 주장하면 그 숫자를 검증 대상으로 만든다

"과거 100주 중앙값 69%" 같은 말은 문장으로만 적어 두면 틀려도 아무도 모른다.
실제로 2회차 초안의 69% 는 재계산값 67.3% 와 달랐다. 매니페스트 `question.dist` 에
수치로 적고, 검증 스크립트에 `--hist <시간봉json>` 을 줘서 다시 세게 한다.

"이번 주만 0%" 처럼 **유일성**을 주장할 때는 특히 조심한다. 105주 중 0% 인 주는
3번이었다. 최솟값인 것은 맞지만 유일하지 않았다.

### 2-7. 대표에게 드리는 숫자도 영상 숫자와 같은 경로로 계산한다

영상에 들어가는 숫자는 검증 스크립트가 원본에서 다시 계산해 대조한다. 그런데
**대화에서 즉석으로 만들어 드리는 표는 그 경로를 안 탄다.** 2026-09-12 에
사건별 변동성 표를 손으로 만들어 드렸다가 "5분" 칸이 실제로는 10분인 채로 나갔다.
발표 시각 T 의 5분 영향은 `T 봉의 종가 ÷ 직전 봉 종가` 인데 한 칸을 밀어 계산했다.

대표는 그 표를 보고 판단한다. 보고용 숫자가 틀리면 영상이 맞아도 소용없다.
**표를 손으로 만들지 않는다.** `scripts/weekly-shorts/event-volatility.mjs` 로 뽑고,
그 스크립트는 매니페스트의 `pct` 와 어긋나면 경고를 낸다.

시각도 마찬가지다. "오늘 18:30 에 올릴까요" 라고 묻기 전에 시각을 확인한다.
같은 날 23:43 에 그 질문을 했다.

**그냥 `date` 를 치면 안 된다. 이 세션 컨테이너는 협정세계시로 돈다.** 2026-09-13 에
`date` 가 준 `00:12` 를 한국시간으로 믿고 "지금 일요일 새벽 0시 20분" 이라고 보고했는데
실제로는 아침 9시 12분이었다. 아홉 시간을 틀렸고, 그 위에서 발행 시각을 의논했다.
**한국시간은 `TZ=Asia/Seoul date` 로 뽑는다.** 표준시 이름까지 찍어 확인한다.

```bash
TZ=Asia/Seoul date '+%Y-%m-%d(%a) %H:%M %Z'   # 2026-09-13(Sun) 15:26 KST
```

### 2-8. 한국시간과 미국 날짜를 섞어 쓸 때는 어느 쪽인지 밝힌다

가로축은 미국 거래일, 큰 시각은 한국시간이다. 둘이 갈리는 사건(미 동부 오후
발표 = 한국 다음날 새벽)에는 "화 장중 · 한국 수 새벽 2시"처럼 둘 다 적는다.
마감 시각을 한국시간으로 쓰면 "9/12 토 마감"이 되어 헷갈린다. 거래일로 쓴다.

---

### 2-8-1. 모든 자료는 출처 두 곳 이상에서 확인한다 (대표 지시, 2026-09-19)

**한 곳에서 받은 값은 아직 자료가 아니다.** 대조를 마쳐야 자료가 된다. 예외 없다.

| 무엇 | 1차 | 대조 |
|---|---|---|
| 5분봉·주가 | 야후 그 주 실제 월물(`NQZ26.CME` 등) | QQQ(ETF)와 `^NDX`(지수) — 일별 등락률이 0.2%p 안에 들어와야 한다 |
| 지표 실제값·예상값 | 트레이딩이코노믹스 캘린더 스냅샷 | 발표 기관 원본(미 센서스국·노동통계국·연준) 또는 주요 통신사 기사 |
| 사람 이름·직함 | **캘린더에 있는 글자만** | 없으면 쓰지 않는다. 쓰려면 기관 공식 발표로 확인한다 |
| 사건 원인·뉴스 | 1차 보도 | 시각이 확정되는 별도 보도 하나 더 |

**기억에서 꺼낸 값은 자료가 아니다.** 특히 사람 이름·직함·기관장은 학습 시점 이후에
바뀐다. 2026-09-19 에 캘린더의 `fed press conference` 를 "파월 의장 기자회견"으로
적어 대표께 보고했는데, 캘린더에는 이름이 없었고 의장은 **2026-05-22 에 케빈 워시로
바뀐 뒤**였다. 학습 자료가 끝나는 달에 교체가 있었고, 확인하지 않고 옛 이름을 썼다.
같은 보고서에서 "뉴스보다 데이터를 먼저 의심하라"고 써 놓고 저지른 일이다.

**"모른다"로 끝내지 않는다.** 확인이 안 되면 ① 기관 공식 사이트 ② 검색 ③ 금융
데이터 커넥터까지 세 경로를 다 밟는다. 그래도 안 되면 무엇을 시도했고 어디서
막혔는지를 함께 적는다. 접속이 막힌 도메인이 있으면 그 사실을 알린다.

**검색 결과는 날짜부터 본다.** 요일이 안 맞거나 다른 해 기사가 섞여 오면 그 결과는
버리고 그 사실을 대표께 말한다. 조용히 넘기지 않는다.

---

### 2-9. 큰 봉을 봤으면 그게 진짜인지부터 확인한다

그 주 가장 큰 5분봉은 영상의 중심이 된다. 그래서 그 봉이 가짜면 회차 전체가 가짜다.

2026-09-19 에 3회차를 준비하다 월요일 11:30(동부)에 `+1.295%` 짜리 봉을 봤다.
2위의 4.4배였고, 그 시각 캘린더에는 단기국채 입찰(★) 둘뿐이었다. 입찰이 나스닥을
1.3% 올리지 않는다. **뉴스를 찾기 전에 데이터를 먼저 의심한 게 맞았다** — QQQ 는
같은 시각 `+0.286%` 였고, 원인은 야후가 9월물에서 12월물로 갈아탄 것이었다.

- 1위 봉이 2위의 두 배를 넘으면 멈추고 **다른 소스와 대조한다**(QQQ 가 가장 쉽다).
- 그 시각에 걸린 지표의 등급·성격이 움직임 크기와 안 맞으면 그것도 신호다.
- 데이터가 맞다고 확인된 뒤에야 뉴스를 찾는다. 순서를 바꾸면 가짜 움직임에
  그럴듯한 뉴스를 붙이게 된다.

---

## 3. 질문 고르기 — 이 시리즈의 핵심

### 3-1. 매주 같은 질문을 묻지 않는다

1·2회차가 둘 다 "그 사이 최대 낙폭은?"이었다. **최대 낙폭은 정의상 늘 음수라
그 주가 오르든 내리든 정답이 매번 하락 숫자로 나온다.** 실제로 1회차는 그 주의
가장 큰 움직임이 상승(+2.69%)이었는데도 낙폭(−2.18%)을 물었다. 데이터가 아니라
틀이 주제를 정한 것이다.

### 3-2. 질문 은행에서 고른다

`scripts/weekly-shorts/questions.mjs` 에 **58개**가 있다. 한 해 52주를 한 번도
겹치지 않고 채울 수 있는 양이다. 분류는 변동·시간·모양·연속·지표·수준·환산·경계·집중.

모든 질문에는 `guard` 가 달려 있다 — **"이 질문의 답이 미리 정해져 있지 않은 이유"**.
guard 를 못 쓰는 질문은 은행에 넣지 않는다. 2-1 의 덫을 코드로 막는 장치다.

### 3-3. 고르는 절차

```bash
node scripts/weekly-shorts/screen-questions.mjs us1h_nqf.json      # 자명한 질문 걸러내기(2년 105주)
node scripts/weekly-shorts/pick-question.mjs us5m_nqf.json us1h_nqf.json <주시작일> --used <최근쓴id들>
```

`pick-question` 은 질문마다 **이번 주 답이 과거 100주 분포에서 얼마나 극단인지**를
점수로 매겨 순위를 낸다. 100점이면 100주 중 가장 극단이라는 뜻이다.

**순위를 그대로 믿지 않는다.** 사람이 세 가지를 더 본다.

1. **해상도 함정**: 30분 구간을 보는 질문(`open30`·`close30`)은 1시간봉으로 재면
   전부 0이 나와 자동으로 100점이 된다. 자명해서가 아니라 자료가 모자라서다.
   `screen-questions` 가 `미검증(5분봉 필요)` 로 표시한다.
2. **답이 말이 되는가**: `backtostart` 처럼 "회복 못 함 = −1" 같은 답은 보기로
   못 만든다. 문장으로 풀 수 있을 때만 쓴다.
3. **시청자가 궁금해하는가**: 이게 첫째 기준이다. 특이도 점수는 "통계적으로 희귀한가"
   만 재지 "사람들이 알고 싶어 하는가" 는 재지 못한다. 2회차에서 그 둘을 같은 것으로
   취급해 "시가 위에서 끝난 5분봉 개수" 를 골랐다가 버렸다. 미국 주식을 하는 사람이
   주말에 실제로 궁금해하는 것 — 이번 주 뭐 때문에 움직였나, 어디까지 빠졌나,
   다음 주에 뭐가 오나 — 에서 먼저 고르고, 특이도는 그 안에서 순위를 매길 때 쓴다.
4. **그림으로 보이는가**: 되감기 화면에서 눈으로 확인되는 질문이 가장 좋다.
   2회차의 `aboveopen`(시가 위에서 끝난 봉 개수)은 차트에 시가 기준선 하나만
   그으면 답이 그대로 보인다. 이게 이 시리즈가 잘하는 것이다.

### 3-4. 고른 질문은 매니페스트에 적는다

```json
"question": { "id": "aboveopen", "ko": "...", "answer_value": 0, "unit": "개",
              "why": "왜 이번 주에 이 질문인가", "guard": "왜 답이 미리 정해져 있지 않은가" }
```

검증 스크립트가 `id` 로 은행을 찾아 **원본 봉으로 답을 다시 계산해** 대조하고,
`guard` 가 비어 있으면 발행을 막는다.

### 3-5. 쓴 질문은 기록한다

매니페스트의 `question.id` 가 곧 기록이다. 다음 회차에 `--used` 로 넘겨 제외한다.
52주가 지나면 다시 쓸 수 있다.

---

## 4. 사건 고르기

대표가 정한 규칙이다. **내 판단으로 바꾸지 않는다.** 바꾸려면 대표에게 묻는다.

1. **그날 ★★★ 이 있으면 전부 넣는다.** 장중이든 프리장이든 상관없다.
   하루에 여러 개면 여러 개 다 들어간다.
2. **그날 ★★★ 이 하나도 없을 때만** 아래 순서로 하나를 고른다.
   - 그날 최고 등급(★★ 또는 ★)만 후보로 둔다
   - 같은 등급이면 **장중 우선**
   - 장중 후보가 둘 이상이면 **등락률**로 고른다
   - 장중 후보가 하나도 없으면 등락률로 고르고, **그 사유를 매니페스트
     `session_exception` 에 적는다.** 안 적으면 검증에서 막힌다
3. 같은 시각에 여러 지표가 걸리면 **한 사건으로 묶는다**(5분봉에서 한 봉이다).
   묶을 때 최고 등급 지표 이름을 대표로 쓴다.
   최고 등급이 여럿이면 아래 순서로 대표를 정한다.
   - **예상을 벗어난 것**을 쓴다. 시장이 반응한 건 예상과 다르게 나온 값이지
     예상대로 나온 값이 아니다. 예상대로 나온 지표를 대표로 쓰면 화면에
     "예상 0.4%, 실제 0.4%" 가 떠서 왜 이 사건이 움직였는지 설명이 안 된다
   - 벗어난 게 여럿이면 **예상에서 가장 많이 벗어난 것**을 쓴다
   - 단위가 서로 달라 벗어난 정도를 비교할 수 없으면(예: % 와 만 채) 사람이
     고르고 그 사유를 매니페스트 `tiebreak` 에 적는다
   - 전부 예상과 같으면 **직전값에서 가장 많이 움직인 것**을 쓴다
   고른 근거는 매니페스트 `tiebreak` 에 남긴다. 안 남기면 검증에서 막힌다.
4. **기업 실적도 후보다.** 경제지표만 보면 안 된다.
   - 그 주에 발표한 기업 중 **나스닥100 구성 종목**을 먼저 본다. 이 영상은
     나스닥100 선물을 돌리는 것이라, 지수에 안 들어간 기업 실적은 지수를
     직접 움직이지 않는다
   - 같은 날 여럿이면 **시가총액이 큰 기업** 우선
   - 실적은 대개 **장 마감 뒤(16:05 전후)** 나온다. 선물은 야간에도 거래되므로
     그 시각에 봉이 있다. 그 봉의 반응을 다른 사건과 같은 방식으로 잰다
   - 발표 시각을 확정하지 못하면 넣지 않는다
5. **뉴스도 후보다.** 다만 **최초 보도 시각을 확정한 것만** 넣는다.
   - 그 주 5분 변동 **상위 10개를 전부 훑어**, 캘린더에도 실적에도 걸리지 않는
     시각이 있으면 그 시각에 무슨 일이 있었는지 찾는다
   - 찾았으면 2-8-1 대로 출처 두 곳에서 확인한다. 시각이 확정 안 되면 **버린다**
   - 큰 움직임에 그럴듯한 뉴스를 갖다 붙이지 않는다. 이게 가장 하기 쉬운 거짓말이다
   - 개장 직후·마감 직전의 움직임은 대개 뉴스가 아니라 거래 그 자체다.
     사건으로 쓰지 않는다
6. **주간 최고·최저 같은 구조점은 사건이 아니다.** 한 주가 끝나야 알 수 있는
   값이라 그 시점의 사건이 아니다. 정규장 개장·마감도 사건이 아니다.
7. 휴장일에는 사건이 없는 게 맞다.

**4·5번이 빠져 있던 일 (2026-09-19).** 원래 작업 매뉴얼에는 기업 실적과 뉴스가
규칙 4번·5번으로 있었는데, 2026-09-12 에 지침서 4장을 다시 쓰면서 경제지표
규칙만 옮겨 적고 이 둘을 떨어뜨렸다. 그래서 3회차 준비에서 경제 캘린더만 보고
실적·뉴스를 확인하지 않은 채 «규칙대로 골랐다»고 보고했다. 규칙이 사라진 자리에서
규칙을 지켰다고 착각한 것이다. 대표가 «이번주 주요기업 실적 발표는 없었어?»,
«변화폭이 제일 클 때 나온 뉴스 확인했냐»고 물어 드러났다.
**규칙을 옮겨 적을 때는 옮기기 전 목록과 옮긴 뒤 목록을 줄 수까지 맞춰 본다.**

**같은 등급 타이브레이크를 넣은 이유 (2026-09-13).** 2회차 금요일 협정세계시
12:30(동부 08:30)에 ★★★ 이 네 건 동시에 걸렸다 — 근원 소비자물가 전월비,
근원 소비자물가 전년비, 소비자물가 전월비, 소비자물가 전년비. 이 중 예상을
벗어난 건 **근원 소비자물가 전월비 하나뿐이었다**(실제 0.3% / 예상 0.2%).
나머지 셋은 전부 예상과 같았다. 그래서 근원 소비자물가 전월비를 대표로 썼는데,
그때는 규칙에 없어서 제작자 판단으로 고른 것이었다. 같은 상황이 다음에 또 오면
다른 걸 고를 수 있으니 규칙으로 박아 둔다.

**왜 바꿨나 (2026-09-12).** 원래는 ★★★ 끼리도 "장중 우선"을 적용했다. 그 결과
2회차에서 **CPI(금)와 PPI(목)가 둘 다 프리장 08:30 이라는 이유로 빠지고**,
기존주택 판매와 미시간 소비자심리가 들어갔다. 그 주 5분 변동 상위 5개 중 선정
사건이 하나도 없었다. 미국에서 가장 많이 보는 지표를 버린 선정이었다.

"동일한 조건이면 장중 우선"은 **등급이 같을 때 등락률만으로 못 고를 때 쓰는
타이브레이크**다. ★★★ 을 탈락시키라는 뜻이 아니다.

---

## 4-1. 사건을 고르기 전에 반드시 훑는 것 (확인 목록)

지침서의 나머지는 «이렇게 판단하라»다. 이 장만 «이건 봤는지»다.
**검증 스크립트가 만든 것은 검사해도 안 만든 것은 검사하지 못한다.** 그래서
훑었다는 사실 자체를 매니페스트에 적고, 안 적혀 있으면 발행을 막는다.

매니페스트 `coverage` 에 아래 네 가지를 적는다. 적지 않으면 검증에서 막힌다.

| 키 | 무엇을 훑었나 | 안 적으면 |
|---|---|---|
| `econ_calendar` | 경제 캘린더 스냅샷 경로와 파싱한 행 수 | 발행 금지 |
| `earnings` | 그 주 발표한 나스닥100 구성 종목. 없으면 «없음»과 확인 경로 | 발행 금지 |
| `news_scan` | 5분 변동 상위 10개를 훑은 결과. 각 시각이 캘린더·실적·뉴스 중 무엇에 걸리는지, 못 밝힌 것은 못 밝혔다고 | 발행 금지 |
| `data_crosscheck` | 주가를 어느 두 소스와 대조했는지와 그 결과 | 발행 금지 |

**«없음»도 결과다.** 실적이 없었으면 «없음»이라고 적는다. 안 적힌 것과
없다고 적힌 것은 다르다. 안 적혀 있으면 확인을 안 한 것이다.

**못 밝힌 것은 못 밝혔다고 적는다.** 상위 10개 중 원인을 못 찾은 시각이 있으면
그대로 적는다. 억지로 뉴스를 붙이는 것보다 낫다 — 4장 5번.

---

## 5. 화면 배치

### 5-1. 1회차 발행본 격자를 쓴다

| 요소 | y |
|---|---|
| 제목 | 186 |
| 현재가(145px) | 330 |
| 시각 | 400 |
| 고점대비(우측) | 300 · 372 |
| 사건 칩 | 510 |
| 사건 문구 2줄 | 600 · 680 |
| 차트 | 790 ~ 1290 (x 60~1020) |
| 요일축 · 축설명 | 1352 · 1414 |
| 영어 줄 | 1524 |
| 브랜드 · 면책 | 1668 · 1716 |

### 5-2. 안전영역을 "비워야 할 땅"으로 읽지 않는다

플랫폼 UI가 덮는 구간이 있다고 해서 거기를 비우면 **화면이 위쪽에 몰려 아래
3분의 1이 통째로 빈다.** 2026-09-12 에 그렇게 만든 3개 안이 전부 폐기됐다
(`docs/samples/weekly-shorts/v8-design-options/`).

맞는 규칙은 **"꽉 채우되 아래쪽 밴드에는 없어도 되는 것만 둔다"** 이다.
발행본은 영어 줄·브랜드·면책처럼 가려져도 손해가 작은 것만 아래에 두고,
현재가·사건 문구·차트는 가운데에 둔다.

### 5-3. 색

| 상황 | 색 |
|---|---|
| 사건 봉의 \|등락률\| 0.05% 미만 | 노랑 `#ffe14d` |
| 상승 | 초록 `#3ddc84` |
| 하락 | 빨강 `#ff4d4d` |

**색을 코드에 박지 않는다.** 2회차에서 훅의 주간 등락률이 `C.up` 으로 박혀 있어
하락 주인데 초록으로 나갔고, 같은 숫자가 34초 루프 카드에서는 빨강이었다.
검증 스크립트에 색 검사가 들어 있다.

사건 문구는 **첫 줄(실제값)에 사건 색, 둘째 줄(예상값)은 회색**이다.

### 5-4. 세로 라벨

주가 선을 라벨 **위로** 그린다(대표 지시). 대신 라벨 상자는 선이 지나가지 않는
쪽에 세운다. 판정은 상자가 덮는 x 구간 전체의 선 최고·최저로 한다 — 사건 봉의
종가 한 점만 보면 바로 뒤 스파이크가 상자를 뚫는다.

### 5-5. 영어 줄은 72자를 넘기지 않는다

`enBand` 는 34px 에서 26px 까지 줄여 보고, 그래도 안 들어가면 **말없이 잘라낸다.**
2회차에서 88자짜리 훅이 "…closed above the ope…" 로 잘린 채 표지에까지 들어갔다.
코드는 설계대로 동작했지만 결과물은 질문이 잘린 영어 훅이었다.
검증 스크립트가 72자를 넘으면 막는다.

### 5-6. 겹침은 자리를 고정하지 말고 피하게 만든다

사건 위치는 회차마다 다르므로, 무엇과 겹칠지 미리 알 수 없다. 2회차의 시가 기준선
이름표는 오른쪽에 뒀다가 ④ 세로 라벨과 겹쳤고, 왼쪽으로 옮겼더니 ① 커서가 1.8초
내내 덮었다. 지금은 **사건 커서와 라벨 상자의 실제 x 구간을 구해 놓고 그 어느 것과도
안 겹치는 자리를 찾는다.** 좌표를 손으로 정하면 다음 회차에 또 겹친다.

루프 카드의 큰 숫자 자리도 같은 문제였다. 원래 "−2.17%" 같은 숫자만 오던 곳이라
250px 였는데, "0개" 처럼 한글이 섞이자 글리프가 숫자보다 높아 위 라벨을 뚫었다.
내용을 보고 크기를 정한다.

### 5-7. 회차마다 달라지는 것은 코드에 두지 않는다

`scene.js` 는 그리는 법만 안다. 문구·숫자·퀴즈 보기는 전부 `render2.mjs` 가
`COPY`/`QUIZ` 로 넘긴다. 1회차 값("5분봉 1,179개", "Peak 29,571 …")이 코드에
남아 2회차에 그대로 나올 뻔했다.

---

## 6. 시간 배분

35초. `HOOK [0,5]` · `REPLAY [5,27.9]` · `ANSWER [27.9,32]` · `SUMM [32,35]`.

> 3회차(2026-09-19) 대표 지시로 요약·루프를 4.7초에서 3.0초로 줄이고 그 1.7초를
> 되감기에 옮겼다. 전체 35초와 정답 4.1초는 그대로다. 이 값이 현재 기준이다.

- 훅은 문구를 다 띄운 뒤 1.95초 세워 두고 **5초 정각에** 넘어간다.
- 사건마다 멈추는 총 시간은 9.0초다. 사건 수로 똑같이 나누지 말고 **물채우기로 배분한다**
  — 앞 사건과 간격이 좁은 쪽에 더 오래 세운다(`scene.js` 참고). 바닥은 0.6초.
  3회차는 사건 7개에 0.6/0.6/1.905/2.613/0.6/0.6/2.082 초가 돌아갔다.
- 페이드: `loudnorm` 을 `afade` **앞에** 건다. 순서를 바꾸면 끝이 −28dBFS 에 머문다.
- 페이드아웃 곡선은 `ipar`.

---

## 7. 검증

발행 전에 **두 겹**을 통과해야 한다. 하나라도 빠뜨리면 발행하지 않는다.

### 7-1. 기계 검사 (114건)

```bash
node scripts/weekly-shorts/verify-shorts.mjs content/weekly-shorts/<날짜>.json \
     --srt <산출물폴더> --hist <시간봉json>
```

원본 5분봉을 **새로 받아 독립적으로 재계산**한다. 만든 세션의 중간 파일은 안 쓴다.

### 7-2. 독립 검증 에이전트

`.claude/agents/shorts-verifier.md`. 프레임을 직접 열어 눈으로 본다. 기계가 못
보는 것(겹침·잘림·색·어색한 문장·자명한 주장)을 잡는다.

**실제로 잡은 것들** — 이 절차가 형식이 아니라는 증거다.

| 회차 | 잡은 것 |
|---|---|
| 1 | 영어 줄이 화면 밖으로 나감, "하루 반 만에"가 29시간을 36시간으로 부풀림 |
| 2 | 사건 색 규칙 위반, 훅 등락률 색이 하락 주에 초록, 루프 전환 때 1.1초 잔상, 설명란만 "저녁"으로 남음 |

### 7-3. 검사 항목이 없어서 놓친 것은 검사 항목을 만든다

2회차의 색 위반은 기계 검사 49건을 통과하고도 남았다. 검사에 색 항목이 아예
없었기 때문이다. **놓친 결함을 고치고 끝내지 말고, 같은 것을 다음에 기계가 잡게
만든다.** 지금은 114건이다.

---

## 8. 발행

- 요일·시각: 매니페스트 `publish.published_at` 에 KST 로 남긴다. 요일 효과를
  비교하려면 기록이 있어야 한다.
- 자막 ko·en 두 벌을 같이 올린다.
- **유튜브 설명란에는 정답을 적어도 되지만, 인스타그램 캡션 첫 줄에는 적지 않는다.**
  인스타는 캡션 첫 줄이 영상과 나란히 보여 유튜브 설명란처럼 접히지 않는다.
  퀴즈가 스크롤 단계에서 소진된다.
- 제목에도 정답을 넣지 않는다. 절반을 보여주고 절반을 가린다.
- **유튜브 설명란에 `<` 와 `>` 를 넣으면 업로드가 통째로 거부된다.**
  돌아오는 말은 "The request metadata specifies an invalid video description" 뿐이라
  어느 글자가 문제인지 알려 주지 않는다. 2회차에서 영어 목록의 `->` 때문에 막혔다.
  화살표는 `→` 를 쓴다.
- **API 로 보낼 한글을 손으로 `\uXXXX` 로 옮겨 적지 않는다.** 2회차에서 "출렁인" 이
  "출렉인" 으로, 고치려다 다시 "출렝인" 으로 나갔다. 한글은 글자 그대로 넣고,
  올린 뒤에는 반드시 다시 받아서 원본 파일과 대조한다.
- **`videos.update` 는 보내지 않은 필드를 지운다.** 설명만 고치려고 `snippet` 을
  보냈다가 `defaultAudioLanguage` 가 `en-US` 로 바뀌었다. 고칠 때도 제목·태그·
  카테고리·언어를 전부 함께 보낸다.

---

## 9. 하지 말 것 (요약)

0. 확인하지 않고 말하는 것 — 시각, 표의 숫자, 규칙의 원문
1. 매주 같은 질문을 묻는 것
2. 반증 불가능한 문장을 발견처럼 쓰는 것
3. 화면만 고치고 자막·설명란을 두는 것
4. 앞 회차를 확인하지 않고 "이번 주도"라고 쓰는 것
5. 회차마다 달라지는 값을 `scene.js` 에 박는 것
6. 색을 코드에 박는 것
7. 안전영역을 이유로 화면 아래를 비우는 것
8. 사건 선정 규칙을 그 주 사정에 맞춰 바꾸는 것
9. 검증 에이전트를 건너뛰는 것
10. 놓친 결함을 고치고 검사 항목은 안 만드는 것
11. 경제 캘린더만 보고 «규칙대로 골랐다»고 하는 것 — 실적과 뉴스도 규칙이다
12. 규칙을 다른 문서로 옮겨 적으면서 항목을 떨어뜨리는 것
13. 훑지 않은 것을 «없었다»고 적는 것
