# luckyon — "투자 3분 노트" 루틴 절차서

> **이 축은 루틴 하나로 운영합니다.** 트리거 두 개를 같은 루틴에 겁니다.
>
> **① 스케줄** — 매주 일요일 **10:30 KST** (발행이 11시 무렵 이뤄지도록 여유를 둔 시각)
> **② GitHub 이벤트** — 프리셋 `이슈 열림` + 필터 `Labels is one of chartnotes`
>
> Instructions 칸에는 한 줄만 넣습니다:
> ```
> 저장소의 ROUTINE_PROMPT_CHARTNOTES.md 파일을 읽고, 그 절차를 순서대로 그대로 수행하세요.
> ```
>
> **트리거가 두 개인데 Instructions 는 하나이므로, 이 문서가 무엇을 할지 스스로 판단합니다** (0단계 디스패처).
> **어떤 트리거로 떴는지 알아내려 하지 마세요.** 세션이 트리거 종류를 알 수 있다는 보장이 없습니다.
> 대신 **상태**(밀린 이슈가 있나 / 이번 회차가 만들어졌나)만 보고 경로를 정하므로, 어느 트리거로 떠도 결과가 같습니다.
>
> **⚠️ 제작 경로는 카드를 만들기만 하고 발행하지 않습니다.** 완성본 16장을 **세션에 올리고**
> 카카오톡으로는 «제작 완료, 세션에서 확인해 주세요» 만 보낸 뒤 끝냅니다 (링크를 넣지 않습니다 — 9단계).
> 사람이 세션에 들어와 확인하고 «발행해»라고 해야 인스타에 올라갑니다. 브리핑과 다른 점입니다 —
> 브리핑은 시의성 때문에 자동 발행하지만, 차트 노트는 교육 콘텐츠라 틀린 설명이 저장·공유되며 오래 남고
> 발행 후 캡션·이미지 수정이 불가능하기 때문입니다.
>
> **웹훅 유실 시**: `이슈 열림` 이벤트가 유실되면 그 승인 건은 **다음 일요일 10:30 실행**이 집어 갑니다
> (0단계가 항상 밀린 이슈부터 확인하기 때문). 같은 날 안에 잡히게 하려면 이 루틴에 스케줄 트리거를
> 하나 더(예: 일요일 19:00) 추가하면 됩니다 — 디스패처가 알아서 처리하므로 절차서는 고칠 필요가 없습니다.
>
> **API 트리거는 쓰지 않습니다.** 검토 페이지는 public GitHub Pages 에 있어 페이지에 넣은 토큰이 그대로
> 노출되고, URL 을 아는 누구나 발행을 트리거할 수 있게 됩니다.

---

## 이 콘텐츠가 무엇인가

**컨셉**: 주식 차트·투자 용어를 한 회차에 하나씩, "스프링 노트에 손으로 정리한 필기" 스타일 카드 8장으로 설명합니다.
분석이나 종목 추천이 아니라 **교육**입니다. 뉴스처럼 시의성에 매이지 않고, 저장(save)·공유되며 오래 도는 것이 목표입니다.

**한국어·영어 두 건을 각각 발행합니다.** (캐러셀 캡션은 하나뿐이라 한 게시물에 두 언어를 섞을 수 없습니다.)

**브리핑과의 차이**

| | 브리핑 (ROUTINE_PROMPT.md) | 차트 노트 (이 문서) |
|---|---|---|
| 성격 | 오늘의 뉴스 — 시의성이 생명 | 교육 — 시의성 없음, 저장형 |
| 주기 | 평일 하루 2회 | 주 1회 (일요일) |
| 카드 | 9장 (훅·시장·본문×5·일정·아웃트로) | 8장 (표지·도입·개념·구조·비교·실제사례·심화·요약) |
| 디자인 | 어두운 배경 + 파란 포인트 | 밝은 모눈 노트 + 손글씨 느낌 |
| 콘텐츠 경로 | `content/<DATE>-<SESSION>.json` | `content/chart-notes/<STAMP>.json` |
| 카드 경로 | `cards/<DATE>/<SESSION>/<LANG>/` | `cards/chart-notes/<STAMP>/<LANG>/` |
| 발행 스크립트 | `scripts/publish-instagram.mjs` | `scripts/chart-notes/publish-chartnotes.mjs` |
| **발행 방식** | **자동** (시의성이 생명, 검토 기다릴 수 없음) | **사람 승인 후** (틀리면 오래 남고 수정 불가) |

`STAMP` 형식은 `<YYYY-MM-DD>-ep<NN>` 입니다 (예: `2026-08-01-ep01`). 날짜가 앞이라 정렬하면 발행 순서가 됩니다.

---

## 이 문서의 범위

**이 문서는 차트 노트 축이 "무엇을 다루고 어떻게 만들지"만 정한다.** 준비·검증·커밋·발행·알림·정정 같은 **모든 축 공통 절차는 `ROUTINE_COMMON.md` 에 있고, 이 세션은 둘 다 따른다.**

- 브리핑 카드 구조 문서(`FORMAT_BRIEFING.md`)는 **이 축과 무관하다** — 차트 노트는 렌더러도 카드 타입도 다르다. 이 축의 카드 구조는 아래 5단계에 있다.
- **작업을 시작하기 전에 `ROUTINE_COMMON.md` 를 먼저 읽는다.** 세션이 아는 것은 문서뿐이라, 안 읽으면 그 안의 규칙은 없는 것과 같다.

---

## 0. 경로 결정 — 무엇을 할지 먼저 정한다

**이 단계를 건너뛰지 않는다.** 아래 순서대로 상태를 확인해 경로를 정한다.

### 0-0. 먼저 `claude/live` 를 가져온다 — 콘텐츠 상태는 거기에만 있다

```
git fetch origin claude/live
git checkout -B claude-live-work FETCH_HEAD
```

**세션은 `main` 에서 시작하는데, 이 축의 콘텐츠(`content/chart-notes/`)는 `claude/live` 에서만 갱신된다.**
따라서 `main` 의 `_series.json`·회차 JSON 은 **몇 회차씩 뒤처져 있는 것이 정상**이다. 이걸 모르고
`main` 의 파일을 읽으면 아래 두 단계가 **조용히 틀린 답**을 낸다:

- **0-2** 가 `main` 의 `content/chart-notes/*.json` 만 보면, 승인 대기 중인 회차가 거기 없으므로
  «대기 중 없음»으로 판정해 제작 경로로 내려간다.
- **2단계**가 `main` 의 `_series.json` 을 읽으면 `next_up` 이 옛 값이라 **이미 발행한 회차를 다시 만든다.**

2026-08-30 점검 시점의 실제 격차: `main` 은 `published: [EP.01]` · `next_up: 이동평균선`,
`claude/live` 는 `published: [EP.01~EP.04]` · `next_up: 지지선과 저항선`. 그대로 믿었다면
**EP.02(이동평균선)를 통째로 다시 만들 상황**이었다.

`_series.json` 을 `main` 에도 복사해 두는 방식은 쓰지 않는다 — 상태가 두 곳에 생기면 반드시 다시 어긋난다.
**정본은 `claude/live` 하나이고, 세션이 그걸 먼저 가져오는 것으로 맞춘다.**

> **⚠️ 대신 이 체크아웃 뒤에는 «절차서·스크립트»가 거꾸로 뒤처진다.** 방향이 정반대인 두 정본이 있다:
> **콘텐츠(`content/`)의 정본은 `claude/live`, 문서·스크립트의 정본은 `main`** 이다(`ROUTINE_COMMON.md` §9 가
> 문서 수정을 `main` 에 올리게 하기 때문). 2026-09-06 실행 시점에 `claude/live` 의
> `ROUTINE_PROMPT_CHARTNOTES.md` 는 `main` 보다 **186줄 뒤처진 «카드 타입 12종» 시절 사본**이었다.
> 따라서 **0-0 체크아웃 뒤에 절차서를 다시 읽지 않는다** — 세션이 처음에 `main` 에서 읽은 내용이 맞는 것이다.
> 그리고 **문서·스크립트를 고쳤으면 그 수정은 `claude/live` 가 아니라 `main` 에 커밋한다**
> (§4 의 이 축 `git add` 목록이 `cards/…`·`content/chart-notes` 뿐인 것도 같은 이유다).
>
> **절차서는 «다시 읽지 않으면» 되지만 렌더러는 «실행해야» 한다.** 그래서 스크립트는 경고만으로는
> 부족하다. 0-0 체크아웃 뒤 **렌더하기 전에 반드시 `main` 의 스크립트를 가져온다**:
>
> ```bash
> git fetch origin main && git checkout FETCH_HEAD -- scripts/chart-notes/
> git reset HEAD -- scripts/chart-notes/     # 인덱스에 올라가 claude/live 로 딸려 가지 않게
> ```
>
> 2026-09-06 EP.06 에서 이걸 빼먹었다. `claude/live` 사본이 190줄 낡아 표지 `overlay:'trend2'` 를
> 모르는 채로 돌았고, **오류 없이** 기본 표지를 그려 「선 두 개」라는 제목에 선이 하나도 없는 카드가
> 사용자에게 갔다. 지금은 렌더러가 모르는 변종 이름을 만나면 멈추므로(5단계) 같은 사고가 나면
> 조용히 넘어가지 않고 위 명령을 안내하는 오류로 뜬다 — **그 오류가 뜨면 렌더러가 낡은 것이다.**

### 0-1. 밀린 승인 요청이 있는가

`mcp__github__list_issues` 로 `dreamerstar88-ui/luckyon-briefing` 의 **열린(OPEN)** 이슈 중
라벨이 `chartnotes` 인 것을 조회한다.

- **있으면** → `ROUTINE_PROMPT_CHARTNOTES_APPROVE.md` 를 읽고 **그 절차를 수행한다.**
  (이 문서의 제작 절차는 읽지 않는다 — 승인 처리와 카드 제작은 전혀 다른 작업이다.)
  처리가 끝나면 **0-2 로 계속 진행한다.** (발행까지 했다면 0-3 이 제작을 건너뛰므로, 발행과 제작이
  같은 실행에서 함께 일어나지 않는다.)
- **없으면** → 바로 0-2 로.

### 0-2. 승인을 기다리는 회차가 이미 있는가

`content/chart-notes/*.json` 을 훑어 **`published` 가 비어 있는 회차**가 있는지 본다
(`_series.json`·`_roadmap.md` 는 회차 파일이 아니므로 제외). 그런 회차는 카드까지 다 만들어 놓고
사람 승인을 기다리는 중이다.

- **있으면** → **새로 만들지 않고 여기서 끝낸다.** 그 회차의 완성본 16장을 세션에 다시 올리고
  카카오톡으로 확인을 다시 요청한다 (주 1회 실행이라 잦지 않다). 형식은 9단계와 같되 앞에
  `⏳ 검토 대기 중` 을 붙인다.
- **없으면** → 0-3 으로.

> **왜 이 확인이 날짜 확인보다 먼저인가.** 예전에는 "오늘 날짜 파일이 있는가"만 봤다. 그러면
> **지난주에 만들어 놓고 아직 승인받지 못한 회차가 있어도, 오늘 날짜 파일이 없다는 이유로 다음 회차를 또 만든다.**
> 게다가 `_series.json` 의 `next_up` 은 **발행돼야** 다음 주제로 넘어가므로, 그렇게 만든 회차는
> 대기 중인 회차와 **주제가 똑같은 복제본**이 된다. 미발행분이 쌓이면 나중에 승인 한 번에 여러 건이
> 한꺼번에 나가 "주 1회" 원칙도 깨진다. (2026-08-09 점검에서 발견 — EP.03 이 승인 대기 중인 상태로
> 다음 일요일을 맞으면 EP.03 이 하나 더 만들어질 상황이었다.)
>
> 승인을 오래 미루면 제작이 멈추는데, 그게 맞는 동작이다. 검토받지 않은 회차를 쌓아 두는 것보다 낫다.

### 0-3. 이번 회차를 만들 차례인가

**0-1 에서 발행을 했다면 이번 실행에서는 제작하지 않는다. 여기서 끝낸다.**
발행한 그 자리에서 다음 편까지 만들면, 방금 나간 회차가 아직 반응도 받기 전에 다음 편이
검토 대기로 쌓인다. **다음 편은 다음 실행에서 만든다.**

발행이 없었다면, 한국시간 기준 **오늘이 일요일인지** 확인하고, `content/chart-notes/` 에
**오늘 날짜로 시작하는 `<오늘>-ep*.json`** 이 있는지 본다.

- **오늘이 일요일 + 파일 없음** → 아래 **제작 경로(1~10단계)** 를 수행한다.
- **그 외 (평일이거나, 오늘 회차가 이미 있음)** → **여기서 끝낸다.**

> **주 1편을 유지하려면 승인을 일요일 실행 전에 한다.** 평일에 승인하면 그날 발행되고,
> 이어지는 일요일 실행은 대기 중인 회차가 없으므로 다음 편을 만든다 — 발행 1편·제작 1편이
> 서로 다른 날에 일어나 주 1편이 유지된다.
> 일요일 실행 중에 승인하면 그 실행은 발행만 하고 끝나므로 제작이 한 주 밀린다(2주에 1편).
> 어느 쪽이든 **미리 만들어 쌓아 두는 일은 없다.**

### 0-4. 아무것도 할 게 없으면 조용히 끝낸다

**알림을 보내지 않는다.** 이벤트 트리거로 떴는데 처리할 이슈가 없는 경우(라벨 없는 이슈가 생겼거나
이미 처리된 건)가 여기 해당하며, 이는 정상이다. 빈손인데 알림을 보내면 쓸모없는 알림이 쌓이고
정작 중요한 알림이 묻힌다.

---

## 제작 경로 (0-3 에서 '제작' 으로 판정된 경우에만)

### 1. 준비

`ROUTINE_COMMON.md` §1 대로 의존성을 확인한다.

브라우저는 이 환경의 `CHROMIUM_PATH` 를 쓴다 — `npx playwright install` 은 **실행하지 않는다.**

### 2. 시리즈 대장 읽기 — 다음 주제 결정

`content/chart-notes/_series.json` 을 **`claude/live` 기준으로**(0-0 참고) 읽는다. 이 파일이 **"무엇을 이미 다뤘고 다음은 무엇인가"의 유일한 근거**다. `main` 쪽 사본은 뒤처져 있으므로 읽지 않는다.
편성 의도와 회차별 기획(훅·사례 후보)은 `content/chart-notes/_roadmap.md` 에 있으니 함께 읽는다.

- `published` 배열 = 이미 발행한 회차. **여기 있는 주제는 최근 30일 내라면 다시 쓰지 않는다.**
- `next_up` = 다음 회차 주제. **이 값이 있으면 무조건 그것을 쓴다.**
  직전 회차의 마지막 카드에 "다음 편 → ○○" 이라고 예고해 두었기 때문에, 다른 주제를 내면 예고를 어기게 된다.
- `backlog` = 예비 주제 목록. `next_up` 이 비어 있을 때만 여기서 맨 위를 고른다.
- 에피소드 번호는 `published` 의 마지막 `episode` 에서 1을 더한다.

`backlog` 가 바닥나면 가장 중요한 주제(캔들·이동평균선·거래량·PER·시가총액 등)로 돌아가되,
**다른 예시 종목·다른 시각적 각도**로 새로 만든다. 같은 카드를 재발행하지 않는다.

### 3. 원고 작성 (카드 8장 구성)

#### 3-0. 원고를 쓰기 전에 — 어떤 상황에서 쓰는 글인가

**아래를 자기 상황으로 놓고 쓴다. 이 절을 건너뛰고 스키마부터 채우면 «틀리지는 않았는데 남는 게 없는» 카드가 나온다.**

> 당신은 시장을 오래 본 사람이고, 지금 **주식을 막 시작한 사람들 앞에서 짧은 강의를 하는 중**이다.
> 청중은 어제 계좌를 텄고 오늘 처음 차트를 열어 봤다. **용어를 모르는 게 아니라 «어디를 봐야 하는지»를 모른다.**
> 손에 쥔 것은 슬라이드 8장, 시간은 3분. 청중은 서서 스크롤하며 넘기고, 마음에 들면 저장해 두었다가 나중에 다시 꺼내 본다.
> 강의가 끝났을 때 **청중이 차트를 다시 열어 전에는 안 보이던 것을 하나 볼 수 있으면 성공**이고, 그러지 못하면 실패다.

이 상황을 잡고 있으면 아래가 저절로 따라온다. 규칙으로 외우는 것보다 상황으로 판단하는 편이 새로운 경우에도 통한다.

- **강의는 «그래서요?»에 답해야 한다.** 수치 두 개를 띄우고 다음 장으로 넘어가는 강사는 없다.
  수강생이 손을 들고 「그래서 그게 뭐가 다른데요?」라고 물을 것을 알기 때문이다. (아래 «문장 원칙» 의 3단이 이것이다.)
- **아는 것을 다 꺼내지 않는다.** 8장뿐이고 청중은 초보다. 강의를 잘하는 사람은 «자기가 아는 것»이 아니라
  «청중이 오늘 가져갈 것» 하나를 고른다. 카드에 넣고 싶은 게 넘치면, 넣을 것을 고르는 게 아니라 **뺄 것을 고른다.**
- **화면에 없는 것을 말하지 않는다.** 슬라이드에 안 띄운 그림을 「여러분 앱에 보면 이렇게 나오죠」라고 말하면
  강의가 아니라 짐작이 된다. 가리킬 것은 **내가 이 장에 직접 그린 것**뿐이다.
- **강사는 종목을 추천하지 않는다.** 설명하는 사람이지 권하는 사람이 아니다.
- **만드는 것은 «강의 슬라이드»가 아니라 «나중에 꺼내 보는 자료»다.** 이 축의 목표는 한 번 읽히는 게 아니라
  **저장됐다가 몇 달 뒤 「그게 뭐였더라」 할 때 다시 열리는 것**이다. 그래서 달라지는 것들:
  - **카드 한 장이 그 자체로 완결돼야 한다.** 반년 뒤 p.06 한 장만 캡처해서 다시 봐도 무슨 말인지 알아야 한다.
    앞 장을 가리킬 때 그 카드에 라벨·주석을 실제로 달아 두는 이유도 이것이다.
  - **시점 표현을 쓰지 않는다.** 「오늘」「최근」「올해」「지난주」는 다시 열었을 때 틀린 말이 된다.
    **날짜를 그대로 적는다** — 「7월 23일」은 반년 뒤에도 맞고, 검증도 된다.
  - **용어를 정확한 이름으로 적는다.** 나중에 검색해서 이 카드를 다시 찾는 경로가 되고,
    다른 자료와 말이 맞아야 이어서 공부할 수 있다.
  - 그 자리에서 말로 때운 것은 남지 않는다. **카드 위 글자만으로 이해되어야 한다.**

이 프레임과 아래 규칙은 하는 일이 다르다. **규칙은 이미 겪은 실패를 거르는 필터고, 이 프레임은 아직 겪지 않은
경우에 판단 기준이 된다.** 규칙에 없다고 해서 괜찮은 것이 아니라, **이 상황에서 그렇게 말할 것인가**로 판단한다.

#### 3-0-2. «가르칠 것» 목록을 먼저 쓰고, 렌더 뒤 커버리지 표로 대조한다

**카드를 먼저 쓰고 나중에 「빠진 게 있나」 보면 늦는다 — 이미 8장이 차 있어 넣을 자리가 없다.**
그래서 원고 전에 **이 회차가 독자에게 남길 것을 2~4개**로 적어 둔다. 4-B-2 §1·§2 에서 나온다.

```
EP.06 추세선이 가르치는 것
  ① 두 점을 이어 긋는다 (세 번째가 «추세»로 만든다)
  ② 선 하나에서 «방향»과 «속도»를 함께 읽는다   ← 축이 둘이다
  ③ 이탈은 결론이 아니라 첫 신호
  ④ 상승·하락 두 방향으로 그어진다
```

**렌더가 끝나면 항목 × 카드 표를 만들어 어디서 다뤘는지 표시한다.**

| | p.01 | p.02 | p.03 | p.04 | p.05 | p.06 | p.07 | p.08 |
|---|---|---|---|---|---|---|---|---|
| ① 긋는 법 | | | ● | | | ● | ● | ● |
| ② 방향+속도 | ● | ● | | ● | | | | ● |

- **표의 한 칸도 못 채운 항목**이 있으면 그 항목은 «가르쳤다고 생각만 한» 것이다.
- **항목의 «절반»만 실린 칸을 특히 본다.** ②처럼 축이 둘인 항목은 «방향만» 또는 «속도만» 적힌 칸이
  나오기 쉽다. EP.06 초안이 그랬다 — p.01·p.02·p.04·p.08 이 전부 «속도»만 말하고 «방향»을 빼먹어,
  사용자가 「추세선은 방향과 기울기를 같이 보는데 한 부분만 표기됐다」고 지적했다.
- 한 항목이 **카드 한 장에만** 있으면 훑어보는 독자에게는 없는 것과 같다. 최소 두 자리에 둔다.

#### 3-1. 카드 8장의 흐름

한 회차는 아래 흐름을 따른다. 이 골격은 EP.01 에서 검증된 것이므로 유지한다.

| 카드 | 역할 |
|---|---|
| p.01 | **표지** — 통념을 깨는 한 문장 훅 + 시리즈명 + "저장하고 넘겨보세요" |
| p.02 | **도입** — 독자의 막막함에 공감 ("차트가 그냥 그림처럼 보이나요?") |
| p.03 | **핵심 개념** — 용어의 정의를 가장 단순한 말로 |
| p.04 | **구조/원리** — 다이어그램으로 분해 |
| p.05 | **비교/구분** — 헷갈리는 둘을 나란히 |
| p.06 | **실제 사례** — **실제 시장 데이터**로 읽어보기 |
| p.07 | **한 걸음 더** — 초보가 흔히 하는 실수와 교정 |
| p.08 | **3줄 요약** + 저장·태그 유도 + **다음 편 예고** |

**p.05·p.06 의 순서는 회차에 따라 바뀐다** (바로 아래 문단 참고). 그래서 이 문서의 4·7단계는 자리 번호가
아니라 **역할 이름**(«실제 사례 카드»)으로 그 카드를 가리킨다 — EP.04 에서 실제 사례는 p.05 다.

**수치를 제시한 카드와 그 수치를 설명하는 카드는 반드시 붙인다.** EP.04 는 p.05(비교)와 p.06(실제 사례)의
자리를 맞바꿔 «공식 → 실제 차트»를 연달아 놓았다. 공식 카드가 「평소의 2.3배 — 무슨 일이 있었을까요?」로
끝나고 **바로 다음 장**이 그 답을 주게 하라는 사용자 요청이었다(2026-08-23 이슈 #22). 사이에 관계없는
카드가 끼면 「이 숫자가 왜 큰가」와 「그래서 무슨 일이 있었나」의 흐름이 끊긴다. 골격의 순서는 이런
이유가 있을 때 바꾼다 — 순서 자체를 지키는 것이 목적이 아니다.

**급증·급락한 날을 예로 들었으면 그날 무슨 일이 있었는지도 적는다.** 숫자만 던지면 「그래서?」로 끝난다.
사건 서술은 반드시 뉴스 원문으로 확인하고 `fact_check` 에 출처를 남긴다(4단계와 같은 기준이다).

**비교 카드의 두 축은 그 회차가 가르치는 개념 안에서 고른다.** EP.04 초안의 비교 카드는 두 종목의
«거래대금(원·달러)»을 나란히 놓았는데, 조 단위 금액이 화면을 채워 「거래량 수업」이 아니라 「누가 더 큰
회사인가」로 읽혔다(같은 이슈 #22). 1차 수정판은 가격·금액을 빼고 «오간 주식 수»와 «각자의 평소 대비 몇 배»를
나란히 놓았지만 **그것도 반려됐다**(이슈 #23) — 거래량과 «주식 수»는 같은 값인데 두 이름으로 부르니
서로 다른 것처럼 보였고, 그 카드의 교훈이 p.07·p.08 에도 있어 카드를 한 장 쓸 이유가 없었다. 2차 수정판은
«거래량(주) vs 거래대금(원)» 으로 축을 갈아 봤지만 **그것도 반려됐다**(이슈 #24) — 두 가지가 겹쳤다.
① 카드가 「증권앱은 거래량과 거래대금을 나란히 보여줍니다」라고 적었는데 **차트의 거래량 패널 옆에
거래대금이 함께 뜨지는 않는다.** ② 5만 원짜리 주식 100주 같은 **가상의 숫자로만 채워** 「뜬 구름」이라는
지적을 받았다. 사용자의 요구는 «거래량에 포커스를 맞춰, 실제 예시로» 였다.

**최종판은 같은 종목의 실제 두 거래일을 맞댔다**: 주가가 거의 같은 폭으로 움직였는데 거래량은 2.5배(영어판
2.0배) 차이 난 날 두 개. **두 날 모두 p.05 차트에 이미 그려져 있는 날로 고르고, p.05 의 x축 라벨을 그 두 날에
맞춰 다시 찍었다** — 「앞 장에 둘 다 있다」고 써 놓고 독자가 못 찾으면 그 문장이 거짓말이 되기 때문이다.
가격만으로는 구별되지 않는 두 날을 거래량이 갈라 보이게 하므로, 카드 한 장이 «거래량을 왜 따로 보는가»에
그대로 답한다. 마지막 행에서 두 날을 «거래가 활발한 3% / 거래가 한산한 3%» 로 이름 붙이고,
«거래가 한산할수록 적은 물량에도 가격이 움직인다» 로 닫는다 — 두 날이 왜 **다른 종류의 날**인지를 말하는
메커니즘이다. 여기까지 와야 «이래서 거래량을 보는구나» 가 생긴다.

여기서 세 가지가 남는다.
- **비교 축을 고를 때는 «이 둘이 정말 다른 것인가»를 먼저 따진다** — 같은 값의 두 이름을 맞대면 설명이 아니라 혼란이 된다.
- **«앱이/증권사가 화면에 무엇을 어떻게 보여준다»는 서술을 카드에 넣지 않는다.** 앱마다 다르고 버전마다
  바뀌어 대조할 원본이 없다. 사실 확인이 불가능한 문장은 검증에서도 안 걸리고 발행 뒤에 틀린 것으로 드러난다.
  화면 이야기가 필요하면 **우리가 그 카드에 직접 그린 그림**만 가리킨다(예: p.02 의 «가격 아래 칸»).
- **비교 카드는 가상의 예시 숫자보다 실제 데이터 두 개가 낫다.** 가상의 숫자는 아무리 단순해도 「그래서
  뭐」로 읽히고, 실제 날짜·수치는 그 자체로 근거가 된다. 4단계 시세 검증도 그대로 적용된다.

#### 3-0-1. 정의만 있으면 교과서다 — «실무자의 선택»을 반드시 하나 넣는다

**이 축이 반복해서 밋밋했던 이유는 검증을 안 해서가 아니라 «정의만 가르쳐서»다.** 갓 수료한 사람과 20년 한
사람의 차이는 아는 정의의 양이 아니라, **실무자들이 실제로 갈리는 지점을 알고 자기 선택을 말할 수 있는가**다.
정의는 검색하면 나오지만 그건 아무도 저장하지 않는다.

**그래서 회차마다 4-B-2 §2(실무자들이 갈리는 지점)에서 최소 하나를 골라 카드에 싣는다.**

- 추세선: 몸통으로 긋나 꼬리로 긋나 / 2점이면 유효한가 3점부터인가 / **가로선보다 주관적이라 사람마다 다르게 그어진다**
- 이동평균선: 20일이냐 60일이냐, 단순이냐 지수냐 — 그리고 **왜 하나로 정해지지 않는가**
- 갭: 갭을 «메운다»는 통념이 실제로 얼마나 믿을 만한가

**이건 어려운 이야기를 넣으라는 뜻이 아니다.** 초보에게 「전문가들도 여기서 갈린다, 그래서 이 선을
남의 말처럼 믿으면 안 된다」를 알려 주는 것이 오히려 가장 실용적이다.

> **규칙을 만족시키려고 문장을 «발명»하지 않는다.** EP.06 p.04 마무리가 그렇게 나왔다 —
> 검증이 「마무리가 표를 되풀이한다」고 하자 그럴듯한 한 줄(«추세선은 어제 값으로는 지켜지지 않습니다»)을
> 지어냈고, 사용자가 「무슨 말인지, 왜 필요한지 모르겠다」고 했다. **채울 말이 없으면 그 자리는 비우거나
> 카드를 다시 짠다.** 마무리 문구는 규칙이 요구해서가 아니라 **할 말이 남아서** 있는 것이다.

**문장 원칙**
- 한 카드에 한 가지 생각만. 카드당 본문 2~3문장을 넘기지 않는다.
- 결론부터 쓴다. "~일 수도 있습니다" 같은 흐린 서술을 피한다.
- **«그래서?» 가 남으면 그 카드는 아직 안 끝났다.** 카드는 «① 사실 → ② 그게 무슨 뜻 → ③ 그래서 뭐가
  달라지나» 세 칸을 다 채워야 한다. **마지막 문장을 읽고 «그래서?» 라고 물었을 때 답할 말이 아직 남아
  있으면, 그 문장은 마지막 문장이 아니다.**
  - 수치를 나열하고 끝내면 ①에서 멈춘 것이다 — 「A는 4,009만 주, B는 1,601만 주」.
  - **그 수치를 말로 옮기기만 한 것도 여전히 ①이다** — 「많이 거래된 3% / 적게 거래된 3%」는
    같은 말을 두 번 한 것이지 의미가 아니다. 이 함정이 제일 잘 안 보인다.
  - ②는 «그래서 이 둘이 어떤 점에서 다른 종류인가», ③은 «그걸 알면 차트에서 무엇이 새로 보이나» 다.
  - EP.04 p.06 이 여기에 세 번 걸렸다(2026-08-23). 1차 「거래량은 다르게 말합니다」는 차이만 되풀이했고,
    2차 「많이 거래되며 오른 3% / 적게 거래되고 오른 3%」는 수치를 말로 바꾸기만 했다. 3차에 와서야
    「거래가 한산할수록 적은 물량에도 가격이 움직인다」로 ②·③에 닿았다.
- **개념을 가르치는 카드는 «이걸 모르면 무엇을 못 보는가»로 끝내면 대개 맞는다.** 그 문장이 곧
  «왜 이 개념을 보는가»의 답이기 때문이다.
- 존댓말·평이한 어휘. 전문용어를 쓸 땐 그 자리에서 풀어준다.
- **투자 권유로 읽힐 문장을 절대 쓰지 않는다.** "지금 사야 합니다", "오를 겁니다" 류는 금지.
  마지막 카드에 "본 콘텐츠는 교육 목적이며 투자 권유가 아닙니다" 를 반드시 넣는다.

#### 3-1-1. 한국어를 «먼저» 끝내고, 영어는 그다음에 따로 쓴다

**규칙에는 「번역이 아니라 각각의 원고」라고 적혀 있었지만 실제로는 두 언어를 나란히 써 왔고, 그래서
한국어가 번역체가 됐다.** 2026-09-06 EP.06 에서 사용자가 「외국인이 작성한 것 같다」고 지적했다.
개념을 영어 구조로 잡아 놓고 한국어를 거기 맞추면 문장이 한국어로 서지 않는다.

**순서를 강제한다: ① 한국어 원고를 끝까지 완성한다 → ② 아래 다섯 가지로 훑는다 → ③ 그다음에 영어를 새로 쓴다.**
영어를 쓰다가 한국어를 고치지 않는다(고치고 싶으면 ①로 돌아간다).

**번역체 다섯 가지 — 이것만 걸러도 대부분 잡힌다** (괄호 안이 EP.06 초안에 실제로 있던 문장):

| 패턴 | 이렇게 나온다 | 이렇게 고친다 |
|---|---|---|
| **목적어 없는 비문** | 「세 번째가 «추세»**로 만듭니다**」 | 「세 번째가 **있어야** «추세»입니다」 |
| **피동 남용** | 「저점 두 개면 선**은 그어집니다**」 | 「저점 두 개**를 이으면 선이 됩니다**」 |
| **무생물 주어 + 직역 술어** | 「선 하나가 …**말해 줍니다**」(tells you) · 「선이 **남기는** 것은」(records) · 「선이 **요구하는** 만큼」(asking) · 「가격은 그 선을 **만납니다**」(meets) | 「선 하나**로** … **봅니다**」 · 「그래서 **알 수 있는 건**」 · 「**선을 지킬 만큼**」 · 「가격이 그 선에 **부딪힙니다**」 |
| **영어 어순** | 「**세 번째로** 그 선에서 멈추는지 봅니다」 | 「가격이 **세 번째로도 그 자리에서** 멈추는지 지켜봅니다」 |
| **딱딱한 한자어·문어체** | 「**요구가 클수록 지켜지기** 어렵습니다」 · 「**확정된** 것은 아니고」 · 「**갈리는 것은** …인가 아닌가입니다」 · 제목 「무엇이 다른가」 | 「**바라는 게** 클수록…」 · 「…**라는 뜻은** 아니고」 · 「**다른 점은** …냐 아니냐입니다」 · 「뭐가 다를까요」 |

**소리 내어 읽어 본다는 기준으로 판단한다.** 강의에서 입으로 말했을 때 걸리는 문장은 카드에서도 걸린다.
카드는 «쓴 글»이 아니라 «말한 것을 받아 적은 것»에 가까워야 한다.

**한국어·영어는 번역이 아니라 각각의 원고다.** 예시 종목도 독자에 맞춰 바꾼다 —
EP.01 은 한국어판이 삼성전자, 영어판이 애플이었다. 영어판에 한글이 한 글자도 남지 않도록 저장 전 점검한다.

### 4. 실제 시세 검증 (필수)

**실제 사례 카드**(골격상 p.06, EP.04 처럼 자리를 맞바꾼 회차는 p.05)에 넣는 시세는 **반드시 원본 데이터와
대조한다.** 기억이나 추정으로 쓰지 않는다.

- 미국 주식: `mcp__PlayMCP__UsStockInfo-get_historical_stock_prices` (ticker 예: `AAPL`)
- 한국 주식: 같은 도구에 `005930.KS` 형식으로 조회
- 시가·고가·저가·종가 네 값을 **자릿수까지** 카드 표기와 대조한다.

**날짜 라벨을 특히 조심한다.** 이 도구가 주는 KRX 일봉은 `15:00Z` 로 찍히는데 이는 **KST 기준 다음 날 00:00** 이다.
즉 `2026-07-20T15:00:00Z` 봉의 실제 거래일은 **7월 21일**이다. 그대로 읽으면 하루가 밀린다.
오프셋이 맞는지 의심되면 휴장일로 교차검증한다 — 공휴일(어린이날·제헌절 등) 자리가 정확히 비어 있고
마지막 봉이 직전 거래일에 떨어지면 해석이 맞는 것이다.

검증 결과는 콘텐츠 JSON 의 `fact_check` 필드에 한 줄로 남긴다.

### 4-B. 개념 검증 (필수) — 원고를 쓰기 «전»에 한다

**이 축은 교육 콘텐츠다. 틀린 수치보다 틀린 «설명»이 더 오래 남고 더 널리 퍼진다.** 그런데 오랫동안 수치에만 정본이 있고 개념에는 없었다 — 세션이 기억으로 쓰고 검증 에이전트도 기억으로 동의해, **둘 다 틀려도 통과하는 구멍**이 있었다(`ROUTINE_COMMON.md` §3 참고). 그래서 이 단계를 둔다.

**3단계(원고 작성) 전에** 이번 회차 용어에 대해 아래 셋을 **외부 출처 최소 2곳**으로 확인한다. 한 곳은 한국어, 한 곳은 영어권 표준 자료를 권한다(용어가 번역되며 뜻이 미끄러지는 것을 잡는다).

0. **그 개념을 이루는 «축»이 몇 개인가** — 먼저 이것부터 적는다. 대부분의 차트 용어는 축이 둘 이상이고,
   **축 하나를 빼먹으면 반쪽짜리 강의가 된다.** 추세선 = «방향» + «기울기(속도)». 이동평균선 = «기간» + «값».
   갭 = «방향» + «크기» + «메우는가». 캔들 = «몸통» + «꼬리». 축을 적어 두면 3-0-2 커버리지 표의 항목이 되고,
   카드에서 한쪽만 말하는 사고를 막는다. EP.06 이 정확히 여기서 뚫렸다 — 축이 둘인 줄 알면서도
   카드 네 장이 «속도»만 말했다.
1. **표준 정의** — 그 용어가 무엇인가. 어떻게 그리고/계산하는가.
2. **그 용어가 주장하는 것과 주장하지 «않는» 것** — 특히 신호·시그널류(이탈·교차·돌파·과매수)는 «이것이 확정하는 것»과 «확정하지 않는 것»의 경계가 이 축이 가장 자주 틀리는 자리다.
3. **표준적으로 지적되는 흔한 오해** — p.07 의 재료가 되고, 우리가 만들어낸 오해를 싣지 않게 한다.
4. **출처보다 센 말을 쓰지 않는다** — 출처가 «갈릴 여지가 있다»고 하면 카드도 «다를 수 있습니다»이지
   «다릅니다»가 아니다. 단정은 «외우기 좋은 한 줄»을 만들 때 슬쩍 세지는데, 강의에서 가능성과 단정을
   섞으면 초보는 단정으로만 가져간다. (EP.06 p.04 「사람마다 다릅니다」 — 사용자 지적)

> **사용자가 준 문장도 이 검증을 통과해야 한다.** 수정 요청에 들어 있던 표현을 «요청대로 반영한다»며
> 그대로 제목에 올리면, 검증을 건너뛴 문장이 가장 눈에 띄는 자리에 박힌다. EP.06 표지
> 「기울어진 선 **두 개**로…」가 그랬다 — §0 의 축이 «선 하나에서 방향과 속도»인데 제목이 선 개수로
> «둘»을 말해 자기 개념과 어긋났고, 사용자가 다시 「왜 두 개냐」고 물었다. **요청의 «뜻»을 반영하되
> 문장은 개념에 맞게 다시 쓴다.**

쓸 수 있는 도구: `mcp__PlayMCP__NaverSearch-search_encyc`(백과사전) · `NaverSearch-search_news` · `WebSearch`/`WebFetch`(ToolSearch 로 불러온다). **검색 결과를 그대로 믿지 말고 출처가 무엇인지 보고 고른다** — 개인 블로그 두 개는 출처 2곳이 아니다.

결과를 `fact_check.concept` 에 **출처와 함께** 남긴다:

```
"fact_check": {
  "ko": "...시세 대조...",
  "en": "...",
  "concept": "추세선 정의: 저점(상승)/고점(하락) 두 개를 이어 오른쪽으로 연장한 직선. 출처 A·B 일치.
              이탈이 확정하는 것 = «그 선이 정의하던 추세의 종료». 확정하지 않는 것 = «반대 방향 추세의 시작».
              두 출처 모두 추세 전환의 «신호»로 다루되 확정으로 쓰지 않음. 흔한 오해: 이탈 = 즉시 하락."
}
```

**이 필드가 비어 있으면 7단계 검증으로 넘어가지 않는다.** 그리고 7단계에서 **이 출처를 검증 에이전트에게 함께 넘겨** «기억이 아니라 이 출처로» 판정시킨다.

#### 4-B-2. 조사한 것을 «버리지 말고 쌓는다» — `_knowledge/<용어>.md`

**조사 결과를 그 회차 JSON 에만 남기면 다음 회차는 다시 백지에서 시작한다.** 그래서 매번 같은 종류로 틀린다.
4-B 의 결과를 `content/chart-notes/_knowledge/<용어>.md` 에 **누적**한다(정본은 `claude/live`, §4 의 `git add content/chart-notes` 에 이미 포함된다).

**시작할 때 먼저 이 디렉터리를 훑는다.** 이번 주제와 인접한 노트가 있으면(추세선 ↔ 채널·박스권·이동평균선)
그것부터 읽는다 — 이미 조사해 둔 것을 다시 조사하지 않고, 그 위에 쌓는다.

파일 구성(정의를 옮겨 적는 곳이 아니다):
1. **정의 — 이견이 없는 데까지** (출처 포함)
2. **실무자들이 갈리는 지점** ← **여기가 강의의 알맹이다.** 아래 3단계 «실무자의 선택» 참고
3. **이 도구의 한계** — 무엇을 확정하고 무엇을 확정하지 않는가
4. **초보가 실제로 틀리는 것** (출처가 지적하는 것. 우리가 지어낸 것이 아니다)
5. **사용자 지적 이력** — 사람이 잡아 준 것을 여기 남긴다. **이게 이 시스템이 실제로 «배우는» 유일한 경로다.**

> **왜 원고 «전»인가.** 뒤에 하면 이미 쓴 문장을 방어하게 된다. EP.06 이 그랬다 — 「기울기 = 속도」라는 프레임을 먼저 잡고 이탈까지 거기 맞춰 「속도가 꺾인 것이지 추세가 바뀐 게 아니다」라고 썼는데, 이는 **우리가 그 카드에 붙인 이름(«추세 이탈»)과 정면으로 모순**됐다. 사용자가 발행 전에 잡았고, 검증 에이전트는 오히려 "통념보다 엄밀하다"며 통과시켰다.

### 5. 카드 이미지 렌더링

**카드는 이 세션이 직접 만든다.** 렌더러가 이미 있으므로 디자인을 새로 고민하지 않는다 — 원고만 스키마에 맞게 쓰면 된다.

```
node scripts/chart-notes/render-chartnotes.mjs <STAMP> ko
node scripts/chart-notes/render-chartnotes.mjs <STAMP> en
```

산출물: `cards/chart-notes/<STAMP>/{ko,en}/card1..8.png` (1080×1350, 4:5)

**렌더러가 스스로 잡는 것 세 가지 — `❌` 가 하나라도 뜨면 그 상태로 다음 단계에 가지 않는다.**

| 검사 | 무엇을 막나 |
|---|---|
| **변종 이름** (`type` · `overlay` · `sketch` · `direction` · `mode`) | 모르는 값이면 멈춘다. 예전에는 조용히 기본 그림으로 떨어져 **본문이 말하는 것과 다른 그림**이 실렸다(0-0 의 EP.06 표지). `direction` 은 오타 하나로 「하락」이라 써 놓고 상승 그림이 그려지던 자리다. 값이 맞는데 이 오류가 나면 **렌더러가 낡은 것**이니 0-0 의 명령으로 `main` 것을 가져온다. |
| **종이 밖 넘침** | 제목·본문이 길어져 글자가 종이 밖으로 잘리는 것. 넘친 요소와 몇 px 인지 찍어 준다. |
| **형광펜 칩 겹침** | 종이 «안»에서 마무리 칩이 다른 요소 위에 올라타는 것. 넘치지는 않아 눈으로 놓치기 쉽다 — EP.06 en p.04 가 그랬다. |

넘침·겹침은 **본문·제목을 줄여서** 푼다. 그림(`pricevol`·`lines`·`versus`)은 자리가 모자라면 비율 그대로
조금 작아지므로 대개 글자 쪽이 원인이다.

**그래도 각 카드를 눈으로 연다.** 자동 검사는 «잘렸나»를 볼 뿐 «그림이 본문과 맞나»는 못 본다 —
이 축에서 실제로 난 사고는 대부분 후자였다.

렌더러가 시리즈 아이덴티티(크림 모눈 노트 · 스프링 제본 · 감청색 헤더 · 붉은 포인트 · 노란 형광펜)를
코드로 들고 있다. **디자인 토큰과 레이아웃을 회차마다 바꾸지 않는다** — 피드에서 한 시리즈로 보여야 한다.
색·여백을 손대고 싶으면 그 회차만 고치지 말고 렌더러를 고쳐 전 회차에 같이 적용한다.

**캔들 색은 JSON 에서 지정하지 않는다.** 방향(`"direction": "up"|"down"`)만 쓰면 렌더러가 언어권 관행을 적용한다
(한국어판 상승=빨강·하락=파랑 / 영어판 상승=초록·하락=빨강). 이 시리즈가 EP.01 에서 가르친 내용이므로
여기서 틀리면 안 된다 — 그래서 코드가 강제한다.

**타입을 고르기 전에 `visual-catalog` 스킬의 `references/chartnotes-types.md` 를 읽는다.**
회차별 실제 제작 기록과 «주제 → 타입» 대응표가 있어 이번 주제에 무엇을 쓸지 바로 나온다.
거래량을 `bars` 로 그렸다가 통째로 다시 만든 EP.04 같은 일을 막는다.
(**타입의 정본은 아래 표다.** 스킬은 표를 복사해 갖지 않는다 — 두 곳에 두면 어긋난다.)

**쓸 수 있는 카드 타입 14종** (`cards[].type`) — 각 타입이 요구하는 필드는 렌더러 소스의 `R` 객체를 보고 맞춘다:

| type | 쓰임 | 주요 필드 |
|---|---|---|
| `cover` | p.01 표지 | `title`, `sub`, `cta`, `annot`(`\|` 로 줄바꿈), `overlay`(선택) |
| `pricevol` | **주가 + 거래량 2단 패널** — 실제 차트가 생긴 그대로 | `title`, `body`, `mode`(`candle`\|`line`), `bars[].{o,h,l,c,v,hi}`, `avg`, `avg_label`, `levels[].{price,to,label,color}`, **`trendline.{from,to,label,label_at}`**, **`marks[].{i,price,n}`**, `callout.{i,text}`, `xlabels[].{i,text}`, `price_label`, `panel_label`, `frame`, `closing` |
| `intro` | p.02 도입 | `title`, `body`, `caption`, **`sketch`**(`zigzag`\|`mystery-levels` — 아래 «도입 스케치» 참고) |
| `checklist` | 용어 여러 개 나열 | `title`, `body`, `items[].term/desc`, `closing`, **`figure`**`.{kind:'levels-map', resistance, support, range, breakout}` |
| `anatomy` | 구조를 화살표로 분해 | `title`, `body`, `labels.upper/body/lower` |
| `compare` | **캔들 둘**을 나란히 비교 (상승 캔들 vs 하락 캔들) | `title`, `legend[]`, `rows[]`, `closing` |
| `versus` | **개념 대 개념 표** — 항목별로 한 줄씩 맞대어 놓는다 | `title`, `body`, `columns[].{name,unit}`(정확히 2개), `rows[].{label,left,right,highlight}`, `closing` |
| `example` | **실제 시세 사례** | `title`, `sub`, `direction`, `values.{high,close,open,low}`, `conclusion`, `note` |
| `numbered` | 번호 매긴 3가지 + 경고 | `title`, `items[].title/desc`, `warn_title`, `warn_body` |
| `recap` | p.08 요약 + 다음 편 예고 | `title`, `points[]`, `ctas[]`, `next_label`, `next`, `disclaimer` |
| `lines` | **선 그래프** — 선 여러 개·교차점·수평선·밴드 | `title`, `body`, `series[].points`, `marker`, `levels[]`, `band`, `closing`, **`touches[].{x,y,n}`**(닿은 자리에 번호 동그라미), **`axis`**(기본 true), **`note`**(그림 아래 붉은 세로줄 문단) |
| `flip` | **역할 반전** — 뚫린 저항선이 지지선이 되는 것을 **선 하나**로 | `title`, `body`, `before`, `after`, `break`, `retest`, `note`, `closing` |
| `bars` | **막대 비교** — 시총·지표 수치, 종목 간 순위 | `title`, `body`, `items[].label/value/display/highlight` 또는 `sections[].{heading,items}`, `closing` |
| `formula` | **공식** — 분수 + 항 설명 + 계산 예시 | `title`, `body`, `formula.{numerator,denominator,result}`, `parts[]`, `example` |

모든 텍스트 필드는 `_ko` / `_en` 접미사로 두 언어를 각각 쓴다. `title` 은 `<br>` 로 줄바꿈할 수 있다.

**도입 스케치(`intro.sketch`) — 회차마다 반드시 고른다.** 예전에는 도입 그림이 렌더러에 하나만 박혀 있어서 **EP.02·EP.03·EP.05 의 p.02 가 픽셀 단위로 같은 그림**이었다. 사용자가 "시각적 자료가 이전 회차와 똑같은 그림도 있고 심심합니다"라고 반려한 직접 원인이다(2026-08-30 이슈 #25). **값을 안 주면 기본값 `zigzag` 로 돌아가 같은 사고가 그대로 재발한다.**

| 값 | 그림 | 쓸 때 |
|---|---|---|
| `zigzag` | 회색 꺾은선 + 물음표 3개 (기본값) | 「이 그래프가 뭘 말하는지 모르겠다」는 막막함을 그리는 회차 |
| `mystery-levels` | 꺾은선 위에 **정체불명의 붉은 가로선 두 줄** + 그 끝을 묻는 물음표 | 「이 선은 누가 왜 그었나」를 묻는 회차 (지지·저항·박스권) |
| `mystery-slope` | 오른쪽 위로 오르는 꺾은선의 **저점들을 따라 그어진 붉은 «비스듬한» 선 한 줄** + 그 끝을 묻는 물음표 | 「이 기울어진 선은 어떻게 그은 건가」를 묻는 회차 (추세선·채널). EP.06 에서 추가했다 — `mystery-levels` 는 가로선이라 «기울어진 선»을 묻는 제목의 대상이 화면에 없다 |

**제목이 묻는 것이 그림에 실제로 있어야 한다.** EP.05 제목은 «가로선»을 묻는데 그림에는 가로선이 한 줄도 없었다 — 표지의 `overlay` 와 같은 종류의 사고다. 마땅한 스케치가 없으면 **새로 하나 추가하고 이 표에 한 줄 적는다.**

**`checklist.figure`** — 용어 목록 아래에 «그 이름들이 차트 어디에 있는지» 한 장으로 보여준다. 용어를 네 줄 늘어놓기만 하면 **카드 아래 절반이 통째로 비어** «공부 못하는 사람이 정리한 노트»가 된다(같은 이슈). `levels-map` 은 저항선·지지선 두 줄, 그 사이 박스권 띠, 오른쪽 끝에서 위로 뚫고 나가는 돌파를 한 그림에 담아 **네 용어가 서로의 관계로 정의되게** 한다. 라벨은 그림 왼쪽 바깥에 세로로 쌓인다 — 오른쪽에 붙이면 돌파해 올라가는 선의 끝과 겹친다.

**`lines.touches`** — 선이 그 높이에서 **몇 번째로 멈췄는지** 번호 동그라미를 찍는다. 「한 번은 우연, 두 번부터 자리」가 이 시리즈의 핵심 문장인데 그림이 세어 주지 않으면 독자는 그 말을 글로만 읽는다. **좌표는 `series[].points` 의 실제 꼭짓점이자 `levels[].y` 와 정확히 같은 값이어야 한다** — 닿지도 않은 곳에 번호를 붙이면 그림이 설명을 배반한다. 반대로 **닿았는데 번호가 없는 자리도 없어야** 독자가 셈을 믿는다.

**`lines.note`** — 그림 아래에 붉은 세로줄 문단을 붙인다(`flip.note` 와 같은 장치이고, 문단이 들어가면 그림 높이가 470 → 372 로 줄어든다). **한 방향만 그리고 «반대 방향도 같다»는 글로 마저 채우는 자리다.** 두 방향을 다 그리면 선이 둘이 되어 그림의 요점이 무너진다.

> **«한쪽 방향만 그린 그림»이 곧 «한쪽 방향만 가르치는 회차»가 되지 않게 한다.** EP.06 초안은 그림도 문장도 전부 «저점을 잇는 상승 추세선»이어서, 카드 8장에 **「저점」이 32번·「고점」이 0번** 나왔다 — 하락 추세선은 고점을 잇고 그 고점이 점점 «낮아지는데», 그 말이 카드 어디에도 없어 사실상 «추세선 = 저점을 잇는 선»으로 가르치는 꼴이었다(사용자 지적). 캡션에만 한 줄 적는 것으로는 부족하다 — **저장·재공유되는 것은 카드 이미지이지 캡션이 아니다.** 최종판은 ① 개념 카드와 이탈 카드에 `note` 로 반대 방향을 적고, ② 비교표·요약·경고문의 «저점/오르는/내려가면»을 «점/옮겨 가는/벗어나면»으로 바꿔 방향 중립으로 만들고, ③ 워크드 예시(표지·도입·실제 사례)만 상승 한 방향으로 두었다. **일반적인 용어를 제목으로 걸었으면(«추세선», «갭», «이동평균선») 그 용어의 반대·대칭 경우가 카드 위에 최소 한 번은 글자로 있어야 한다.**

**`lines.axis`** — 바닥 축선은 «0 이 의미 있는» 그림(VIX·거래대금 등)에서만 쓸모가 있다. 데이터가 0 근처에 가지 않는 그림에서는 화면 한가운데 떠 있는 회색 선이 되어 **정체불명의 세 번째 수평선**으로 읽힌다(EP.05 p.04 에서 실제로 그랬다). 그런 회차는 `"axis": false`.

**`flip`** — «뚫린 저항선이 지지선이 된다»를 **선 하나**로 보여준다. 이 개념은 지지·저항 회차의 핵심인데 `versus` 표의 한 칸이나 `numbered` 의 한 줄로는 전달되지 않는다 — 사용자가 EP.05 검토에서 이 부분을 콕 집어 보강을 요청했다(같은 이슈). 그림의 요점은 **선이 하나뿐**이라는 것이다: 되밀린 자리와 받쳐 준 자리가 같은 높이임을 눈으로 확인해야 「이름만 바뀐다」가 들어온다. 그래서 선을 둘로 나눠 그리지 않고 하나의 가로선 **위아래로 라벨을 갈라** 붙인다(각각 «가격이 아직 닿지 않은 쪽»의 빈 공간이라 겹치지 않는다). **반대 방향(지지선이 무너져 저항선이 되는 것)은 그리지 말고 `note` 로 적는다** — 두 방향을 다 그리면 선이 둘이 되어 «같은 선 하나»라는 요점이 무너진다. 추세선·박스권 이탈·배당락에도 그대로 쓴다.

**표지의 `overlay`** — 캔들 위에 무엇을 얹을지 고른다. **표지 주석(`annot`)이 가리키는 대상이 실제로 그려지는지 반드시 확인한다.**

| 값 | 그림 | 쓸 때 |
|---|---|---|
| (없음) | 캔들만 | EP.01 처럼 주석이 캔들을 가리키는 회차 |
| `ma` | 이동평균선 곡선 **1개** | 주석이 '구불구불한 선 하나'를 가리키는 회차 (이동평균선). **추세선은 `trend` 를 쓴다** — `ma` 는 곡선이라 «두 점을 이어 그은 직선»이 아니다 |
| `cross` | 교차점에서 만나는 곡선 **2개** + 붉은 원 | 주석이 '두 선이 만나는 순간'을 말하는 회차 (골든크로스·MACD·볼린저밴드) |
| `volume` | 캔들 **아래에 거래량 막대 칸**을 붙인 2단 그림 | 주석이 '가격 아래 칸'을 가리키는 회차 (거래량·거래대금) |
| `levels` | 캔들 9개가 **위아래 붉은 가로선 두 개 사이**를 오가는 그림 | 주석이 '가격이 자꾸 멈춘 높이'를 가리키는 회차 (지지선·저항선·박스권) |
| `trend` | 캔들 8개가 계단처럼 오르고 그 **저가들을 따라 붉은 비스듬한 직선 하나**가 지나는 그림 | 주석이 '저점을 이어 그은 기울어진 선'을 가리키는 회차 (추세선·채널) |

`levels` 는 EP.05(지지선과 저항선)에서, `trend` 는 EP.06(추세선)에서 추가했다. `ma`·`cross` 는 비스듬한 곡선이라 **수평선을 가리킬 수 없고**, 거꾸로 `levels` 는 가로선이라 **기울어진 선을 가리킬 수 없다** — 어느 쪽이든 잘못 고르면 바로 아래 EP.03 과 같은 사고가 난다.

EP.03 을 `ma` 로 두었더니 주석은 "선 두 개가 만나는 순간"인데 화면에는 선이 하나뿐이라 **가리킬 교차점이 없었다**(검증에서 적발). 주석 문구를 바꾸기 전에 `overlay` 가 맞는지 먼저 본다.

**렌더 후 반드시 눈으로 확인한다.** 8장을 모두 열어 텍스트가 카드 밖으로 넘치거나 겹치지 않는지 본다.
특히 **영어는 같은 내용도 한국어보다 길어져 넘치기 쉽다.** 넘치면 그 언어의 문구만 줄여 다시 렌더한다
(사실관계나 다른 언어 필드는 건드리지 않는다). 겹침이 없어질 때까지 반복한다.

**타입을 새로 만들 일은 거의 없다.** 위 13종이 로드맵 24회차의 그림을 사실상 전부 덮는다 —
`lines` 하나가 골든크로스·지지저항·추세선·VIX·볼린저밴드·배당락을 다 그리고,
`bars` 하나가 거래량·시가총액·PER·PBR·EPS·배당수익률·ROE 를 다 그린다.
**주제가 바뀌어도 바뀌는 것은 숫자와 라벨뿐이지 타입이 아니다.**

`pricevol` 은 **실제 시세를 그대로** 넣는다(`lines` 와 달리 정규화하지 않는다). `bars[]` 에 세션 순서대로
`{o,h,l,c,v}` 를 주면 축은 렌더러가 잡는다. 급증일에는 `hi: true` — **색은 그대로 두고 검은 테두리로만** 짚는다
(붉게 칠하면 한국어판 «빨강 = 상승» 과 어긋난다. 하락일 급증에서 실제로 어긋났다). 언어별로 다른 종목을 실으려면
`bars_ko` / `bars_en`, `avg_ko` / `avg_en` 처럼 갈라 쓴다. `mode: "line"` + `frame: true` 는 숫자 없이
"여기가 거래량 칸"만 보여주는 도식용이다.

**`pricevol` 의 `levels`** 는 «실제 가격 값»에 가로 기준선을 긋는다(지지선·저항선·목표가·배당락 기준가). `lines` 의 `levels` 가 0~100 정규화 좌표를 받는 것과 달리 **진짜 가격을 그대로** 주므로, 4단계에서 대조한 수치와 그림이 어긋날 수 없다. `to` 를 함께 주면 두 값 사이를 **구간(띠)** 으로 칠한다 — 지지·저항은 한 값이 아니라 폭이 있는 띠이고 카드도 그렇게 가르치므로, 선 하나로 그리면 그림이 설명을 배반한다.

**`pricevol` 의 `trendline`** 은 같은 패널에 «비스듬한» 기준선을 긋는다(추세선·채널·목표선). `levels` 가 가로선뿐이라 EP.06 에서 추가했다. 두 끝점을 `{ from:{i,price}, to:{i,price} }` 로 — **봉 번호와 진짜 가격**으로 — 받으므로, 4단계에서 대조한 저점이 그대로 선의 끝이 되어 그림이 설명을 배반할 수 없다. **오른쪽 끝점에는 선을 늘려 얻은 «그 선 위의 값»을 넣는다**(EP.06 한국어판은 7/30 저가 202,000원 → 8/24 자리 253,000원). `label_at`(0~1, 기본 0.72)으로 라벨을 선 위 어디에 붙일지 정한다 — 기본값 자리에 `marks` 의 번호 동그라미가 있으면 정확히 겹치므로 **비어 있는 구간을 골라 준다**(EP.06 영어판은 0.36).

**`pricevol` 의 `marks`** 는 `[{ i, price, n, label }]` 로 «그 선에서 몇 번째로 멈춘 날인지» 번호 동그라미를 찍는다. `lines.touches` 와 같은 역할이되 좌표가 아니라 **실제 가격**을 받는다. 카드가 "세 번 닿았다"고 써 놓고 그림이 세어 주지 않으면 독자는 그 말을 글로만 읽는다. **`n` 을 빼면 빈 동그라미**가 되어 «세는 자리»가 아니라 «끊긴 자리»를 짚는 데 쓴다(EP.06 한국어판의 8/28 종가 이탈). `label` 은 동그라미 아래에 붙고, 오른쪽 절반이면 글자가 왼쪽으로 뻗어 카드 밖으로 넘치지 않는다. 거래량 막대를 가리키는 `callout` 과 혼동하지 말 것 — **`callout` 은 아래 칸(거래량)을, `marks` 는 위 칸(주가)을** 짚는다. **본문이 날짜로 짚은 날은 `xlabels` 에도 반드시 넣는다** — 안 그러면 7단계 7번(«앞 장을 보라»의 검증 가능성)에 걸린다.

> **실제 사례 카드의 창(窓)을 «선이 아직 살아 있는 데»에서 끊지 않는다.** EP.06 한국어판 초안은 7/30 ~ 8/24 로 잘라 「선을 긋고 세 번 확인했다」에서 끝냈는데, **화면 밖 나흘 뒤인 8/28 에 그 선이 종가로 끊겨** 있었다(검증에서 적발). 카드가 「그어 두면 다음에 어디를 봐야 하는지가 정해집니다」라고 해 놓고 그 «다음»을 프레임 밖에 둔 셈이라, 오늘 이 카드를 보고 차트를 여는 독자가 **강의가 안 보여준 것을 먼저 보게 된다.** 최종판은 21봉(~8/28)으로 넓혀 «꼬리만 걸친 사흘 → 종가로 끊긴 날»을 그대로 실었고, 그 덕에 p.05(이탈)와 p.07 의 «꼬리 vs 종가»가 도식이 아니라 실제 시세로 증명된다. **창을 정할 때는 마지막 봉 이후까지 원본을 훑어, 카드가 하는 말이 화면 밖에서 뒤집히지 않는지 먼저 본다.**

> **다만 띠의 두께는 그 종목의 가격 범위에 좌우된다.** EP.05 에서 영어판(AAPL, 범위 $19.71 · 띠 $2.37)은 띠가 29px 로 뚜렷했지만, 한국어판(삼성전자, 범위 33,000원 · 띠 1,500원)은 11px 라 «두꺼운 점선 한 줄»처럼 보였다. 축척을 억지로 부풀리면 그림이 거짓말을 하게 되므로, **띠의 폭을 본문에 숫자로 적어** 그림이 못 보여주는 것을 글이 채우게 한다(EP.05 p.06 본문의 «1,500원 폭입니다»). **대체 텍스트는 언어판마다 실제로 그려진 모양대로 쓴다** — 영어판을 보고 쓴 «붉은 띠로 칠해져» 를 한국어판 대체 텍스트에 그대로 옮기면 사실과 어긋난다(EP.05 검증에서 적발).

**거래량·거래대금처럼 «가격과 나란히 봐야 하는» 것은 `bars` 가 아니라 `pricevol` 로 그린다.** EP.04 초안이
거래량을 가로 막대(`bars`)로 설명했다가 "이게 막대그래프 얘기인지 거래량 얘기인지 모르겠다"는 지적을 받고
통째로 다시 만들었다(2026-08-23 이슈 #21). 가로 막대는 순위 비교용이라 «가격 아래 붙은 세로 막대»라는
거래량의 정체를 못 보여준다. `bars` 는 종목 간 비교(거래대금 순위 등)에만 쓴다.

**«개념 A 와 개념 B 는 어떻게 다른가»는 `versus` 로 그린다.** `compare` 는 캔들 두 개가 렌더러 코드에
박혀 있어 «상승 캔들 vs 하락 캔들» 밖에 못 그리고, `bars` 는 길이로 크기를 견주는 그림이라 «세는 단위가
다르다»는 말을 못 한다. EP.04 의 p.06 이 `versus` 없이 `bars` 로 만들어졌다가 **"거래량을 강조하는 건지
주식 수를 강조하는 건지, 왜 썼는지 모르겠다"** 는 지적을 받고 통째로 교체됐다(2026-08-23 이슈 #23).
그 카드는 같은 값(거래량)을 제목에서는 «주식 수», 섹션 머리에서는 «거래량» 으로 불러 두 개가 서로 다른
것처럼 보였고, 담고 있던 교훈(«종목끼리 주식 수로 견주지 마라»)이 p.07·p.08 에도 있어 **한 회차에 세 번**
나왔다. **한 카드에 한 가지 생각만** 이라는 원칙은 «그 생각이 이 회차에서 그 카드에만 있는가»까지
포함한다 — 다른 카드가 이미 말한 것을 한 장 더 쓰면 독자는 "왜 이게 있지"로 읽는다.

`versus` 는 **왼쪽 칸이 그 회차의 주제**이며 감청색으로 칠해진다. 오른쪽은 «구별해야 할 다른 것»이라
눌러 둔다. 순서를 뒤집으면 강조가 엉뚱한 개념에 붙으므로 바꾸지 않는다. 로드맵에서 이 그림을 쓸 회차는
EP.08(시가총액 vs 주가)·EP.09(PER)·EP.10(PBR)·EP.19(선물 vs 현물)·EP.22(액면분할 전 vs 후) 등이다.

**개념 쌍 말고 «같은 종목의 두 날»을 맞대는 데에도 쓴다.** EP.04 최종판이 그 예다 — 열 머리에 날짜와
요일을 넣고, 행에 «그날 주가 / 그날 거래량 / 평소와 견주면» 을 놓았다. 이때 왼쪽 감청색은 «주제 개념»이
아니라 그냥 첫 번째 날이 되므로, **어느 날을 왼쪽에 둘지는 «놀라운 쪽을 오른쪽에»** 로 정한다:
`highlight: true` 는 **오른쪽 값만** 붉게 짚기 때문이다(렌더러 `hot = r.highlight && i === 1`).
강조하고 싶은 숫자가 왼쪽에 있으면 붉은 테두리가 엉뚱한 칸에 붙는다.

`lines` 의 `series[].points` 는 **0~100 정규화 좌표**다 (x 왼→오, y 아래→위). 실제 픽셀은 렌더러가 계산하므로
화면 크기를 신경 쓸 필요 없이 "대략 이런 모양"으로만 찍으면 된다. 교차점을 강조하려면 `marker` 에 그 좌표를 준다.

그래도 정말 안 되는 그림을 만났다고 판단되면 **먼저 `visual-catalog` 스킬을 호출한다.**
정말 새로 만들 필요가 있는지 가려 준다 — ETF 구성 비율은 `Treemap`·`Donut`, 자금 흐름은 `Sankey`,
실적 증감 분해는 `Waterfall` 처럼 **밖에 이미 있는 서식**이 후보로 나온다(`references/catalog-sites.md`).
새 도구·서식을 알게 됐을 때 쌓아 두는 자리도 그 스킬이다.

그러고도 안 되는 그림(예: 호가창 표, ETF 구성 비율)이면 억지로 끼워 넣지 말고
**렌더러에 새 타입을 추가**하고 위 표에 한 줄 적어 둔다. 그 타입은 다음 회차부터 재사용된다.
**그리고 `visual-catalog` 스킬의 `references/chartnotes-types.md` «주제 → 타입» 표에도 한 줄 적는다** —
어떤 주제에 쓰는 타입인지가 거기 없으면 다음 회차가 또 새로 만든다.

> 예외: 사람이 이미 만든 카드를 넘겨받는 경우(EP.01 이 그랬다)에는 다시 만들지 말고 받은 파일을 그대로 쓴다.
> 디자인이 이미 확정된 것이므로 재현하려 들면 오히려 훼손된다. 이때는 5단계를 건너뛰고 파일만 배치한다.

### 6. 콘텐츠 JSON 작성

`content/chart-notes/<STAMP>.json` 을 아래 스키마로 저장한다.

```
{
  "type": "chart-notes",
  "stamp", "date", "series", "series_en", "episode", "byline",
  "term_ko", "term_en",                  // 이 회차가 다루는 용어
  "fact_check": { "ko": "...", "en": "..." },   // 4단계 검증 결과
  "cards": [ 8개 ],                       // 5단계 렌더러가 읽는 카드 원고 (타입별 필드는 위 표 참고)
  "caption_ko", "caption_en",
  "alt_ko": [ 8개 ], "alt_en": [ 8개 ],  // 슬라이드별 대체 텍스트
  "published": { ... }                    // 8단계에서 채운다
}
```

`cards` 를 먼저 쓰고 렌더한 뒤, 완성된 카드를 보면서 `alt_*` 를 쓰면 설명이 실제 그림과 어긋나지 않는다.

- `alt_*` 는 카드 수와 **정확히 같은 개수**여야 한다. 시각장애인 접근성과 노출 모두에 쓰인다.
- 캡션은 요약 + 저장/태그 유도 + 해시태그 10개 내외 + 교육 목적 고지로 구성한다.
- 캡션 끝에 다음 편 예고를 한 줄 넣으면 시리즈 이탈이 줄어든다.

### 7. 내용 검증 (필수) — 반드시 별도 에이전트에게 시킨다

**`ROUTINE_COMMON.md` §3 의 발행 전 통합 검증(A~E)을 거친다.** 원고를 쓴 세션이 스스로 검토하면 확증편향으로 놓치므로 별도 검증 에이전트를 띄우는 것이 핵심이며, 이 축은 **카드 8장의 원고 전문(ko·en 모두)** 과 4단계에서 조회한 원본 시세를 함께 넘긴다.

**에이전트를 «검사관»이 아니라 «반박하는 동료»로 세운다.** 체크리스트 대조만 시키면 체크리스트에 있는 것만
잡힌다 — 그리고 그 목록은 우리가 이미 겪은 실패로만 되어 있다. 그래서 **항목표보다 먼저** 아래를 쓰게 한다:

> 당신은 이 도구를 **20년 써 온 실무자**다. 후배가 만든 이 8장을 받았다. **틀린 곳을 찾는 게 아니라,
> 「나는 그렇게 안 본다」고 말할 곳을 찾아라.** 세 가지를 답한다:
> ① 이 강의에서 **당신의 경험과 다른 서술**은 어디인가. 왜 다른가.
> ② 초보에게 이렇게 말하면 **실전에서 다치는 곳**은 어디인가.
> ③ 당신이라면 8장 중 **어느 한 장을 무엇으로 바꾸겠는가.** 그 자리에 무엇이 빠져 있나.

**이 답을 먼저 받고, 그다음에 아래 항목표를 대조시킨다.** 순서가 중요하다 — 항목표를 먼저 주면
에이전트가 항목 안에서만 생각한다. EP.06 에서 항목표는 전부 pass 였는데 사람이 두 가지를 잡았다.

**3-0 의 상황 프레임(초보자 앞에서 하는 3분 강의)도 그대로 넘겨, 검증 에이전트가 그 자리에 앉아 보게 한다.**
아래 판정 항목은 이미 겪은 실패를 거르는 목록이라 새로운 종류의 허술함은 못 잡는다. 그래서 마지막에
**「이 강의를 들은 초보자가 차트를 다시 열었을 때 전에 없던 무엇을 볼 수 있는가. 못 본다면 어느 카드가
제 몫을 안 했는가」** 를 한 문단으로 답하게 한다 — 항목에 없는 문제는 여기서 걸린다.

공통 다섯 범주에 더해 **이 축 고유의 판정 항목**을 함께 보고하게 한다:

1. **개념이 사실로 맞는가** — 용어의 정의, 계산식, 인과관계. 초보자용으로 단순화한 것과 **틀린 것**은 다르다.
   ("RSI 70 이상은 무조건 하락 신호" 처럼 단정하면 fail. "과매수 구간으로 본다" 는 pass.)
   **판정은 기억이 아니라 4-B 의 `fact_check.concept` 출처로 한다** — 에이전트에게 그 출처를 넘기고,
   필요하면 직접 다시 조회해 대조시킨다. 수치를 원본에서 다시 뽑게 하는 것과 같은 이유다:
   **세션과 에이전트의 기억이 같으면 둘 다 틀려도 통과한다.**
   특히 **신호류(이탈·교차·돌파·과매수)는 «이 신호가 확정하는 것»과 «확정하지 않는 것»의 경계**를
   출처와 한 줄씩 대조시킨다. EP.06 이 정확히 여기서 뚫렸다.
1-A. **카드가 쓴 «용어 이름»과 본문의 주장이 서로 모순되지 않는가** — 이름이 곧 주장이다.
   EP.06 은 그림에 «추세 이탈»이라 이름 붙여 놓고 본문은 「추세가 바뀐 것은 아니다」라고 했다.
   그 자체로 자기모순이라 외부 지식 없이도 잡혔어야 하는 것인데 항목이 없어 통과했다.
   **회차의 용어·라벨을 뽑아 각각이 본문·요약과 같은 말을 하고 있는지 하나씩 대조하게 한다.**
1-B. **«외우기 좋은 한 줄»에 사실을 끼워 맞춘 곳이 있는가** — 이 회차의 프레임(한 문장 요약)을
   먼저 뽑게 한 다음, 회차의 주요 주장 각각에 그 프레임이 **실제로 성립하는지** 하나씩 대 보게 한다.
   안 맞는데 맞다고 쓴 문장이 하나라도 있으면 fail 이고, 그 문장은 프레임 밖으로 빼야 한다.
   (EP.06: 프레임 「기울기 = 속도」는 기울기에는 맞지만 **이탈에는 맞지 않는데** 그대로 밀었다.)
2. **실제 사례 카드의 시세** — 4단계에서 조회한 원본과 숫자·날짜가 자릿수까지 일치하는가. (공통 B 를 이 축에 적용한 것)
3. **투자 권유 표현** — "사야 한다 / 오른다 / 수익이 난다" 류가 없는가. 교육 고지가 마지막 장에 있는가.
4. **다음 편 예고**가 `_series.json` 의 로드맵과 맞는가.
5. **확인할 원본이 없는 주장이 섞였는가** — 특히 «증권앱·HTS·차트 화면이 무엇을 어떻게 보여준다»류.
   앱마다 다르고 버전마다 바뀌어 대조할 원본이 없으므로 **카드·캡션·대체텍스트 어디에도 넣지 않는다.**
   EP.04 가 「증권앱은 거래량과 거래대금을 나란히 보여줍니다」로 반려됐다(2026-08-23 이슈 #24) —
   숫자가 아니라서 시세 대조에 안 걸렸고, 그래서 이 항목을 따로 둔다. 우리가 그 카드에 직접 그린
   그림을 가리키는 것은 괜찮다(자기 증명이 된다).
6. **각 카드가 «그래서?» 에 답하고 끝나는가** — 3단계 «문장 원칙» 의 ①②③ 을 기준으로 본다.
   마지막 문장이 앞 내용을 되풀이하거나 수치를 말로 옮긴 데 그치면 fail 이고, 무엇을 보태야 하는지까지
   적게 한다. 비교 카드·수치 카드에서 특히 잘 생긴다.
7. **카드가 «앞 장을 보라»고 하면 독자가 실제로 찾을 수 있는가** — 앞 카드의 특정 날짜·지점을 가리켰다면
   그 카드에 라벨·주석이 실제로 붙어 있는지 렌더된 이미지로 확인한다. EP.04 p.06 이 「앞 장 차트에 둘 다
   있는 날」이라고 썼는데 p.05 의 x축 라벨에 그 두 날이 없어 확인이 불가능했다(같은 회차 검증에서 적발).

공통 C(최신성)는 이 축에서 **거의 적용되지 않는다** — 교육 콘텐츠라 시의성이 없다. 다만 실제 사례 카드에 "최근"·"올해" 같은 시점 표현을 썼다면 그것만은 지금 기준으로 사실인지 본다.

fail 은 그대로 두지 않는다. 고친 뒤 **다시 렌더**한다(5단계). 검증 결과는 짧게 로그에 남긴다.

### 8. 고정 브랜치 `claude/live` 에 커밋 & 푸시

`ROUTINE_COMMON.md` §4 절차를 따른다. **커밋 전에 검토 페이지를 먼저 만든다** — 이래야 카드와 같은 커밋에 실려 한 번의 Pages 빌드로 함께 공개된다.

```
node scripts/chart-notes/make-preview.mjs <STAMP>

# 그다음 ROUTINE_COMMON.md §4 의 fetch / checkout -B FETCH_HEAD 를 수행하고
git add cards/chart-notes/<STAMP> content/chart-notes
git commit -m "chart-notes: <STAMP> <term> (ko+en)"
```

`FETCH_HEAD` 재기준을 특히 지킨다 — `origin/main` 기준으로 새로 만들면 그동안 쌓인 발행 이력이 통째로 날아간다.

### 9. 세션에 완성본 올리기 & 카카오톡으로 확인 요청 — **여기서 루틴은 멈춘다**

**이 루틴은 발행하지 않는다.** 사람이 카드를 눈으로 확인한 뒤에 발행한다.
차트 노트는 교육 콘텐츠라 틀린 설명이 저장·공유되며 오래 남고, 인스타 캡션과 카드는 발행 후 수정이 불가능하다.
주 1회이고 마감 시각도 없으므로 검토 한 번 거치는 비용이 사실상 없다.

**검토는 이 세션 안에서 이뤄진다** (2026-08-30 사용자 결정 — 아래 «왜 이렇게 바뀌었나» 참고).

**9-1. 완성본 16장을 세션에 올린다.** `SendUserFile` 로 **ko 1~8 → en 1~8 순서**로 넘긴다.
경로는 `cards/chart-notes/<STAMP>/{ko,en}/card1..8.png`. 캡션에 회차·용어와 «아직 발행 전»임을 한 줄로 적는다.
사용자가 세션에 들어왔을 때 스크롤만으로 전부 넘겨볼 수 있어야 하므로 **일부만 올리지 않는다.**

**9-2. 마무리 보고를 같은 턴에 쓴다.** 10단계 형식대로 — 회차·용어, 8단계 검증 결과 요약,
이번 회차에서 판단이 갈렸던 지점(있으면). 사용자는 이걸 읽고 무엇을 볼지 정한다.

**9-3. 카카오톡으로 «제작 완료» 만 알린다.** PlayMCP 카카오톡 도구(`KakaotalkChat-MemoChat`, 나와의 채팅).
**링크를 넣지 않는다** — 사용자는 이 알림을 받고 세션으로 들어온다.

```
📝 [luckyon] 차트노트 <EP> <용어> 제작 완료 — 세션에서 확인해 주세요
카드 16장(ko 8 · en 8) 올려뒀습니다. 아직 발행 전입니다.
```

카카오톡 도구를 쓸 수 없는 세션이면 **건너뛰지 말고 PushNotification 으로 같은 내용을 보낸다.**

여기까지 하고 **루틴을 끝낸다.** 승인을 기다리며 세션을 붙잡고 있지 않는다.
사용자가 세션에 들어와 수정을 지시하면 그 자리에서 고치고, «발행해»라고 하면 **P1~P4** 로 간다.

> **왜 이렇게 바뀌었나 (2026-08-30).** 원래는 검토 페이지(`preview.html`) 링크를 카카오톡으로 보내고,
> 사용자가 폰에서 `✅ 발행 승인` / `✏️ 수정 요청` 버튼을 눌러 GitHub 이슈를 남기면 루틴이 그걸 잡아
> 처리하는 방식이었다. **몇 회차 돌려 보니 실제로는 매번 세션에 들어와 직접 고치게 되더라는 것이
> 사용자 판단이었다** — 수정 요청은 한 줄로 적기 어렵고(EP.04 는 이슈 #21~#24 네 번, EP.05 는 #25),
> 이슈 → 루틴 재실행 → 다시 검토 요청의 왕복보다 세션에서 바로 고치는 편이 빨랐다.
>
> **검토 페이지와 이슈 경로는 그대로 살려 둔다.** 8단계는 계속 `preview.html` 을 만들어 푸시하고,
> 그 페이지의 버튼도 그대로 동작하며, 0-1 디스패처와 `ROUTINE_PROMPT_CHARTNOTES_APPROVE.md` 도
> 그대로 둔다. **밖에 있어 세션을 열 수 없을 때 쓰는 폴백**이기 때문이다. 달라진 것은 **기본 경로**뿐이다 —
> 카카오톡에 링크를 넣지 않으므로 평소에는 이슈가 생기지 않고, 0-1 은 조용히 지나간다.

### 10. 마무리 보고

회차·용어, 렌더한 카드 수, 7단계 검증 결과를 요약한다.
**아직 발행되지 않았다는 점을 분명히 적는다.** (검토 페이지 URL 은 폴백 경로를 쓸 때만 언급한다.)

---

## 발행 절차 (P1~P4) — 사람이 승인한 뒤에만

**제작 경로에서 이어서 수행하지 않는다.** 승인이 확인된 뒤에만 여기로 온다.
불려 오는 경로는 두 가지이고, **어느 쪽이든 아래 P1~P4 는 동일하다.**

1. **GitHub 이슈** — 사용자가 검토 페이지의 `✅ 발행 승인` 버튼으로 이슈를 남기면,
   0-1 디스패처가 이를 발견해 `ROUTINE_PROMPT_CHARTNOTES_APPROVE.md` 를 수행하고, 그 3단계가 여기를 부른다.
   사용자가 밖에 있어도 폰만으로 되는 경로다.
2. **직접 지시** — 사용자가 Claude 에게 `차트노트 <STAMP> 발행해` 라고 하는 경우 (루틴이 아닌 대화).

**고칠 곳을 알려준 경우**에는 먼저 그 부분만 고쳐 다시 렌더하고(5단계), 검토 페이지를 갱신해
다시 확인받은 뒤에 발행한다. **승인 없이 발행하지 않는다.**

### P1. 발행 전 확인

- `content/chart-notes/<STAMP>.json` 의 `published` 가 비어 있는지 본다. **이미 채워져 있으면 발행된 회차다** — 중복 발행하지 말고 사용자에게 확인한다.
- 카드가 Pages 에 실제로 공개돼 있는지 URL 로 확인한다 (16장 전부 200).

### P2. Instagram 발행

발행 전에 카드가 Pages 에 실제로 올라갔는지 확인해야 한다. 아직 404 인 URL 을 인스타가 가져가면 실패한다.
**확인 방법은 환경에 따라 다르므로 아래 순서로 판단한다.**

**① 먼저 세션이 Pages 에 접근할 수 있는지 본다.**

```
node -e "fetch('$PAGES_BASE_URL/index.html',{method:'HEAD'}).then(r=>console.log(r.status)).catch(e=>console.log('ERR',e.message))"
```

- **200 이 나오면** → 세션이 Pages 를 볼 수 있다. `SKIP_PAGES_WAIT=0` 으로 발행해 스크립트가 직접 기다리게 한다.
- **403 이 나오면** → **이 컨테이너의 egress 프록시가 `github.io` 를 막고 있다** (2026-08-09 확인).
  이때 `SKIP_PAGES_WAIT=0` 을 주면 스크립트가 이미지 1장당 6초씩 40번 재시도한 뒤
  "이미지 URL이 아직 공개되지 않음" 으로 죽는다 — **Pages 는 멀쩡한데 발행만 실패한다.**
  대신 아래 ② 로 확인하고 `SKIP_PAGES_WAIT=1` 로 발행한다.

**② 프록시가 막힌 환경에서의 확인** — `mcp__github__actions_list` 로 `pages-deploy.yml` 의
최신 실행이 **자기 커밋 SHA 에 대해 success** 인지 본다. 그러면 그 커밋의 트리가 Pages 에 반영된 것이다.
(커밋 메시지만 고친 amend 라면 트리가 같으므로 직전 성공 배포로도 충분하다.)

**이미지를 가져가는 것은 인스타그램 서버이지 이 컨테이너가 아니다.** 인스타는 우리 프록시를 거치지 않으므로,
세션에서 403 이 나도 인스타는 정상적으로 받아 간다 (EP.02 가 실제로 이 경로로 발행됐다).

```
# ① 이 200 이면
SKIP_PAGES_WAIT=0 node scripts/chart-notes/publish-chartnotes.mjs <STAMP> ko
SKIP_PAGES_WAIT=0 node scripts/chart-notes/publish-chartnotes.mjs <STAMP> en

# ① 이 403 이고 ② 로 배포 성공을 확인했으면
SKIP_PAGES_WAIT=1 node scripts/chart-notes/publish-chartnotes.mjs <STAMP> ko
SKIP_PAGES_WAIT=1 node scripts/chart-notes/publish-chartnotes.mjs <STAMP> en
```

- 두 언어는 **서로 독립적으로** 시도한다 — 한쪽이 실패해도 다른 쪽을 건너뛰지 않는다.
- 컨테이너가 `FINISHED` 여도 `media_publish` 가 일시적으로 `9007 media is not ready` 를 뱉는 일이 있다.
  스크립트가 15초 간격으로 5회까지 재시도하므로 **직접 다시 실행하지 않는다** (중복 게시물이 생긴다).
- 발행 후 `GET /{media-id}?fields=permalink` 로 실제 링크를 확인한다.

### P3. 발행 기록 남기기

`content/chart-notes/_series.json` 을 갱신하고 커밋·푸시한다. **이 단계를 빠뜨리면 다음 회차가 주제를 모른다.**

- `published` 에 이번 회차(`stamp`·`episode`·`term_ko`·`date`·`ko`/`en` permalink)를 추가
- `next_up` 을 **이번 회차 마지막 카드에서 예고한 주제**로 교체
- 그 주제를 `backlog` 에서 제거

같은 내용을 `content/chart-notes/<STAMP>.json` 의 `published` 에도 넣는다 (media id 포함).

### P4. 발행 결과 알림

`ROUTINE_COMMON.md` §7 대로 **결과가 무엇이든** 알린다. 이 축의 메시지 형식만 다르다:

- 둘 다 성공: `✅ [luckyon] 차트노트 <EP> <용어> 발행 완료 — KO <permalink> / EN <permalink>`
- 일부 성공: `⚠️ [luckyon] 차트노트 <EP> 일부 성공 — KO: <요약> · EN: <요약>. 수동 확인 필요`
- 둘 다 실패: `❌ [luckyon] 차트노트 <EP> 발행 실패 — <사유 한 줄>. 수동 확인 필요`

---

## 과거 발행분의 오류를 발견했다면

`ROUTINE_COMMON.md` §8 절차를 따른다. 이 축에서 정정 커밋할 파일은 `content/chart-notes/<STAMP>.json` 이다.

**차트 노트는 이 절차의 비용이 가장 큰 축이다** — 교육 콘텐츠라 저장·검색으로 오래 돌기 때문에, 틀린 설명은 정정 댓글을 달아도 카드 이미지째로 계속 공유된다. 그래서 이 축만 **발행 전 사람 승인**을 둔다.

---

## 발행 슬롯 — 왜 일요일 오전인가

**계정 전체 축·스케줄 지도는 `ROUTINE_COMMON.md` §0 이 정본이다.** 새 축을 넣을 때는 거기서 기존 슬롯과 겹치지 않는 시각을 고른다. 아래는 이 축이 **왜 일요일 오전인지**에 대한 기록이다.

1. **주말 브리핑을 나중에 켜도 안 겹친다.** 주말 루틴이 예약해 둔 자리는 토 08:00 과 일 21:00 이다.
   일요일 오전 11시는 양쪽에서 각각 27시간·10시간 떨어져 있어, 주말 루틴을 활성화해도 이 슬롯을 비켜 간다.
   토요일 오후를 골랐다면 토 08:00 과 7시간 간격이라 같은 날 두 번 밀어 넣는 모양이 된다.
2. **평일 슬롯과 완전히 분리된다.** 평일은 아침·저녁·심야가 모두 뉴스성 콘텐츠로 차 있다.
   교육 콘텐츠를 그 사이에 끼우면 서로 노출을 갉아먹는다.
3. **콘텐츠 성격과 시간대가 맞는다.** 8장짜리 학습 카드는 출근길에 훑는 뉴스와 달리 앉아서 읽어야 한다.
   주말 늦은 오전은 그 여유가 있는 시간대다.
4. **시의성이 없어 시각에 덜 민감하다.** 브리핑은 장 시작 전에 나가야 의미가 있지만,
   차트 노트는 저장·검색으로 오래 도는 콘텐츠라 특정 분(分)에 매일 이유가 없다. 그래서 비어 있는 슬롯을 우선했다.

**바꾸고 싶다면** 토요일 15:00 KST 가 차선이다. 단 주말 브리핑을 활성화할 계획이라면 토요일은 이미 오전이 차 있으므로,
그날 두 번째 게시물이 되는 것을 감수해야 한다.

**주 1회를 넘기지 말 것.** EP.01 이 "주말 콘텐츠"로 예고됐고, 8장짜리 카드는 제작 부담이 크다.
주 2회로 늘리면 품질이 먼저 무너진다. 반응이 확인된 뒤에 늘려도 늦지 않다.
