# 공통 실행 절차 (COMMON)

> **이 문서가 정의하는 것**: 모든 콘텐츠 축이 **똑같이** 따르는 실행 절차 — 준비, 발행 전 검증, 글쓰기 기준, 커밋·푸시, Instagram 발행, 알림, 마무리 보고, 발행 후 정정.
>
> **이 문서가 정의하지 않는 것**
> - 무엇을 조사하고 무엇을 쓸지 → 축별 절차서
> - 브리핑 카드의 구조·스키마 → `FORMAT_BRIEFING.md` (평일·주말 전용)
>
> **왜 따로 뺐나**: 예전에는 이 내용이 축마다 복사돼 있었고, 2026-08-04 점검에서 이미 어긋나 있는 것이 확인됐다 — 언어 무관 필드 규칙은 평일에만, 카드 분량 확인은 평일·주말에만, alt_text 는 평일에만, 발행 후 정정 절차는 평일·차트노트에만 있었다. 축과 무관한 기준을 한 곳에 두어 한 번 고치면 모든 축에 적용되게 한다.

---

## 0. 콘텐츠 축 지도 (교통정리)

이 계정은 여러 축을 함께 운영한다. **자기 축의 경로만 건드린다.**

| 축 | 절차서 | 콘텐츠 경로 | 카드 경로 | 발행 스크립트 | 스케줄(KST) |
|---|---|---|---|---|---|
| 평일 브리핑 | `ROUTINE_PROMPT.md` | `content/<DATE>-<SESSION>.json` | `cards/<DATE>/<SESSION>/<LANG>/` | `scripts/publish-instagram.mjs` | 월~금 07:35 / 20:35 |
| 주말 브리핑 | `ROUTINE_PROMPT_WEEKEND.md` | 〃 | 〃 | 〃 | 토 07:35 / 일 20:30 |
| 스토리 | `ROUTINE_PROMPT_STORY.md` | `data/reels/<STAMP>.json` | `cards/stories/<STAMP>/<LANG>/` | `scripts/stories/publish-story.mjs` | 월~금 23:01 (11~2월 00:01 — 미 서머타임) |
| 차트 노트 | `ROUTINE_PROMPT_CHARTNOTES.md` | `content/chart-notes/<STAMP>.json` | `cards/chart-notes/<STAMP>/<LANG>/` | `scripts/chart-notes/publish-chartnotes.mjs` | 일 10:30 |
| 릴스 | (문서 없음 · 보류) | `data/reels/<STAMP>.json` | `cards/reels/<STAMP>/<LANG>/` | `scripts/reels/publish-reel.mjs` | — |
| 숫자로 보는 기업 공시 | `ROUTINE_PROMPT_FILINGS.md` | `content/filings/<STAMP>.json` | `cards/filings/<STAMP>/<LANG>/` | `scripts/filings/publish-filings.mjs` | 스케줄 없음 · 수동 |

- **스토리와 릴스는 시세 JSON(`data/reels/`)을 공유한다.** 같은 `fetch-window.mjs` 출력을 스토리는 정지 이미지로, 릴스는 영상으로 렌더할 뿐이다. 이미지 산출 경로만 다르다.
- `content/` **루트**의 json 은 브리핑 전용이다 (세션 없는 구버전 파일 포함). 다른 축의 콘텐츠를 여기 두면 렌더러·발행 스크립트가 서로의 파일을 집어 든다.
- **모든 축이 `claude/live` 브랜치 하나와 GitHub Pages 를 공유한다.**
- **데이터 소스가 되는지/안 되는지는 `DATA_SOURCES.md` 를 먼저 본다.** 어떤 소스가 세션에서
  막히고 러너에서 되는지(그 반대도 있다), 무엇을 이미 시도해 실패했는지가 거기 있다.
  **새 소스를 시험했으면 성공이든 실패든 그 문서에 적는다** — 같은 벽에 매번 부딪히지 않도록.
- 스케줄은 요일·시각으로 완전히 분리돼 있어 동시 실행이 없다. 그래도 아래 §4 의 `FETCH_HEAD` 재기준 가드는 반드시 지킨다.

---

## 1. 준비

**의존성 확인**: `npm ls playwright` 로 playwright 가 있는지 보고, 없으면 `npm install`. (setup 스크립트에서 이미 설치됐다면 건너뛴다.)
- `npm install` 이 403 등으로 실패하는 환경이면 전역 설치본을 심볼릭 링크로 연결한다:
  `mkdir -p node_modules && ln -sf /opt/node22/lib/node_modules/playwright node_modules/playwright`

**날짜 확인**: 한국시간(KST) 기준 오늘 날짜를 `YYYY-MM-DD` 로 정해 `DATE` 로 쓴다. 세션 구분이 있는 축은 루틴 지시문의 `SESSION` 을 확인한다.

- **시스템 컨텍스트의 `currentDate` 를 그대로 믿지 않는다.** 세션에 주어지는 `currentDate` 는 세션 시작 시점에 주입된 스냅샷이라, 실제 시각과 하루 어긋나 있을 수 있다. `DATE` 를 정하기 전에 반드시 쉘로 실제 시각을 **두 가지 독립된 방식으로** 재확인한다 — `TZ` 존파일 조회 하나만 쓰면, 그 컨테이너 이미지에 `Asia/Seoul` 존파일이 없을 때 일부 `date` 구현이 에러 없이 UTC로 조용히 폴백해 하루가 밀릴 수 있다(하필 이 폴백이 위험한 시간대는 00:00~08:59 KST 로, `am` 루틴이 도는 시각과 정확히 겹친다):
  ```
  TZ=Asia/Seoul date "+%Y-%m-%d %H:%M %a %Z"      # 방식 1: 존파일 조회
  date -u -d "+9 hours" "+%Y-%m-%d %H:%M %a"      # 방식 2: UTC + 9시간 산술(존파일 무관)
  ```
  두 값이 서로 다르면 그 자체가 이상 신호이므로(존파일 누락 등) 방식 2(산술) 결과를 우선한다. 이렇게 나온 값이 시스템 컨텍스트의 `currentDate` 와도 다르면 **쉘의 실제 시각을 따르고 `currentDate` 는 버린다** — 컨테이너의 시스템 클록은 실시간 UTC를 그대로 반영하지만 `currentDate` 는 스냅샷이라 어긋날 수 있다. 두 값이 다르면 그 사실을 마무리 보고에 남기고, 반복되면 사용자에게 플랫폼(세션 컨텍스트 주입) 쪽 확인을 요청한다.
  - **이 정도로도 못 잡는 남은 위험**: 컨테이너의 시스템 클록 자체가 (NTP 미동기화 등으로) 실시간과 어긋나 있으면 위 두 방식 모두 똑같이 틀린 값을 준다 — 세션 안에서 자체적으로 검증할 방법이 없는 더 근본적인 문제다. 의심되면 그날 세션 안에서 어차피 조회하는 외부 시각 소스(예: `scripts/fetch-fear-greed.mjs` 가 CNN API에서 받아오는 `timestamp` 필드)와 대조해 볼 수 있다.
  (2026-08-27 아침 세션에서 실제로 이 문제가 있었다 — `currentDate` 는 "2026-08-26"이었지만 실제 KST는 이미 "2026-08-27" 아침이었다. 세션이 이를 그대로 믿고, 어제 이미 정상 발행된 8/26 콘텐츠를 "오늘 회차의 중복 발행"으로 오판해 8/27 아침 브리핑 리서치를 멈췄다 — 사용자가 지적하고서야 바로잡았다.)

**중복 실행 가드**: `claude/live` 에 이번 회차의 콘텐츠 파일이 이미 있으면(사람이 수동으로 다시 돌린 경우 등) 이미 발행됐을 가능성이 있다. 발행 직전 판단이 애매하면 무리해서 추측하지 말고 PushNotification 으로 "같은 회차 파일이 이미 존재 — 중복 발행 여부 확인 필요"를 알린 뒤 진행 여부를 신중히 결정한다. 정상적인 스케줄 실행이라면 파일이 없는 것이 기본값이다.

- **파일이 있든 없든, 그 `DATE` 근처에 실제로 발행된 기록(커밋 시각)을 함께 확인한다** — 존재 여부만 보지 않는다. `git log -1 --format=%cI content/<DATE>-<SESSION>.json` (`origin/claude/live` 기준)로 그 파일이 **언제 커밋됐는지**를 KST로 환산해 보고, 위에서 재확인한 "지금"과 견줘 앞뒤가 맞는지 확인한다:
  - 그 회차의 정상 발행 시각대(am 이면 07~08시대, pm 이면 20~21시대) 근처에 커밋돼 있으면 **진짜 중복**이니 위 알림 절차를 따른다.
  - 반대로 파일이 존재하는 게 **어제 이하 날짜**의 정상 커밋 시각이라면(예: `<DATE>` 를 하루 잘못 짚어 어제 날짜의 이미 끝난 회차를 "오늘 중복"으로 착각한 경우), 그건 중복이 아니라 `DATE` 판단 자체가 틀렸다는 신호다 — §1 위쪽의 실시간 재확인으로 돌아가 `DATE` 를 다시 잡는다.
  - (2026-08-27 아침 세션이 정확히 이 실수를 했다 — `content/2026-08-26-am.json`·`-pm.json` 이 **각각 정상적인 어제 발행 시각**(07:47 KST·21:14 KST)에 커밋돼 있었는데도, 틀린 `DATE`("오늘"=8/26으로 오인) 때문에 이를 "오늘 회차의 중복"으로 잘못 판단해 정상적인 8/27 리서치를 멈췄다. 커밋 시각을 그 자리에서 확인했다면 "어제 이미 끝난 회차"라는 것과 "그러면 오늘 날짜 판단이 틀렸다"는 것을 동시에 알아챘을 것이다.)

**API 키**: 루틴 환경 설정에 키가 들어 있으면 **그쪽이 우선**이다. 그게 안 되는 상황일 때만 로컬 `SJ PARK Project/api-keys/keys.env` 를 쓴다(경로는 사람이 쓰는 노트북 기준이라 클라우드에는 없다). 이미 다른 곳에 있는 키는 **옮기지 않고** `keys.env` 에 "현재 위치" 주석만 남긴다. 새로 받는 키는 그 파일에 모은다.
- **공공데이터포털은 계정당 인증키가 하나다.** API 마다 키가 다른 게 아니라 같은 키로 **활용신청한 서비스만** 열린다. 신청 안 된 API 는 `등록되지 않은 서비스키`(코드 30) 를 준다.
- 워크플로(GitHub Actions)가 쓰는 키는 저장소 시크릿에 **개별로** 넣는다. 파일을 통째로 시크릿 하나에 담지 않는다 — 워크플로 하나가 뚫리면 전부 나간다. 2026-08-23 기준 들어 있는 것: `APIFY_TOKEN`·`DATABENTO_API_KEY`·`KRX_ID`·`KRX_PW`·`ECOS_API_KEY`·`KOSIS_API_KEY`·`DATA_GO_KR_SERVICE_KEY`·`GEMINI_API_KEY`.
- **저장소 시크릿과 이 세션의 환경은 다른 것이다.** 시크릿은 워크플로만 본다. 세션이 직접 돌리는 스크립트(`fetch-kr-trade.mjs` 등)는 **루틴 환경 설정**의 값을 읽는다. 시크릿에 넣었다고 세션에서 되는 것이 아니다.
- **GitHub 시크릿은 넣은 뒤 값을 다시 읽을 수 없다.** 저장소 주인도, 웹에서도, API 로도. 동기화는 **로컬 → 깃허브 한 방향뿐**이다.


---

## 2. 글쓰기 기준 (모든 축 공통)

- **뉘앙스가 그날 국면과 맞는지 본다.** 관용구를 반사적으로 쓰면 방향이 거꾸로 된 문장이 나온다. 대표적으로 유가에 **"80달러선을 겨우 지켰다 / 방어했다"** 는 유가가 높아야 좋다는 프레임인데, 고유가를 경계하는 국면에서는 정반대다 (2026-08-04 사고). **가격이 오르는 게 좋은지 나쁜지는 대상마다·국면마다 다르다** — 유가·금리·환율은 특히 그렇다. 방어/사수/버텼다 류를 쓰기 전에 "지금 독자에게 이게 좋은 소식인가"를 따지고, 애매하면 방향과 수치만 적는다.
- **동사 자체가 방향성을 내포하는 경우가 있다.** "흐르다/흘러내리다"는 뜻만 보면 방향이 없어 보이지만, 실제 쓰임은 "아래로 흐르는 물"처럼 소극적·하방 뉘앙스에 굳어 있다 — "위로 흐르는 중"처럼 상승에 붙이면 어색하다(2026-08-10, 스토리 축 `comment.mjs` 문구 풀에서 발견). 상승엔 "오르다/올라가다/치솟다" 계열을, 하락엔 "흐르다/새다/흘러내리다" 계열을 쓴다. 관용구를 새로 쓸 때는 그 동사가 평소 어느 방향으로만 쓰이는지 먼저 떠올려본다.
- **코멘트를 엉뚱한 대상에 붙이지 않는다.** 지수·종목별 한 줄 해석은 **그 대상에 실제로 해당하는 사실**이어야 한다. 예: 아마존은 나스닥 상장사이므로 "아마존 시총 3조달러 돌파가 견인"은 나스닥에 붙고, S&P 500 에는 S&P 자체 사실(섹터 폭 등)을 쓴다 (2026-08-04 에 둘이 뒤바뀌어 있었다).
- **고유명사에는 «왜 중요한지»를 한 덩어리로 붙인다.** 이름만 적으면 이미 아는 독자에게만 정보가 된다. 괄호가 가장 싸다 — 두세 글자면 충분하다.
  - **연준 인사는 성향과 투표권을 병기한다**: `해맥 총재(매파·투표권 O)` · `바킨 총재(중립·투표권 X)`. "연준 인사 발언(해맥·바킨)"만 적으면 독자는 이게 왜 실린 일정인지 알 수 없다. 같은 발언도 투표권 유무에 따라 무게가 다르다.
  - **기업·기관은 정체를 밝힌다**: `사모펀드 실버레이크` · `행동주의 펀드 퍼싱스퀘어`. 2026-08-14 am 에서 카드 분량을 줄이려다 "사모펀드"를 지워 «실버레이크»만 남은 적이 있다 — **분량이 모자라면 수식어가 아니라 문장 뒤쪽을 줄인다.**
  - 이름이 곧 설명인 것(«소매판매», «실업률»)에는 붙이지 않는다. 군더더기가 된다.
- **사건성 항목은 1차 발표문을 쓴다.** 금통위·FOMC·고용지표·규제 시행 등은 기사 대신 한국은행·연준·통계청·백악관의 **발표 원문**을 근거로 삼는다. 그래야 "누가 주장했는지"가 흐려지지 않는다.
- **주장은 주체를 밝혀 쓴다.** 한쪽 당사자의 주장을 양측 합의로 단정하지 않는다. `트럼프 "이란과 합의"` 처럼 쓴다.
- **영어판 100% 영어**: `_en` 필드는 물론, 언어 무관 공용 필드에도 한글이 한 글자도 남지 않았는지 점검한다. 한국어 표기가 필요해 보이면 그 필드에 넣지 말고 문구 자체를 언어 중립적으로 다시 쓴다.
  - **예외 — 브랜드 로고·푸터**: 영어 카드에도 상단 로고와 하단 푸터가 `luckyon 브리핑` 으로 **한글 그대로** 나간다. 버그가 아니라 브랜드 정체성을 위한 의도된 선택이므로 **영어로 바꾸지 않는다.** 검증에서 "영어 카드에 한글이 있다"고 잡아내 고치는 일이 없어야 한다.

---

## 3. 발행 전 통합 검증 (필수 · 통과 못 하면 발행 금지)

**모든 축·모든 회차에 예외 없이 적용되는 단일 QA 게이트다.** 건너뛰거나 줄일 수 없다. **검증 에이전트를 실제로 띄워 결과를 받기 전에는 콘텐츠 저장 단계로 넘어가지 않는다.** 시간·자원에 쫓기더라도 항목 수를 줄일지언정 이 검증은 생략하지 않는다 (항목이 준 것은 독자가 모르지만, 검증을 건너뛰면 틀린 사실이 발행된다).

**검증의 성격**: 항목 대부분은 우리가 직접 계산한 값이라 **대조할 원문이 없다.** 따라서 검증 에이전트에게 **원본 데이터를 다시 조회해 독립적으로 재계산**시킨다 — "이 값이 맞는지 기사에서 확인해라"가 아니라 "같은 소스에서 다시 뽑아 같은 숫자가 나오는지 확인해라"로 지시한다. 계산이 들어간 항목("N일 연속", "N개월래 최고", "올해 N번째")은 기간·기준을 명시해 재계산시킨다.

**반드시 별도의 검증 에이전트**(그 세션에 로드된 서브에이전트 실행 도구 — `Agent`/`Task` 등, 범용 타입)를 최소 1개 띄운다. 만든 세션이 스스로 재검토하면 확증편향으로 놓친다 — 실제로 2026-07-21 아침 브리핑에서 2주 전 기사를 "오늘 발표"로 잘못 표기한 채 발행한 사고가 있었다. (세션당 한 번이면 ko·en 모두에 적용된다.)

**검증 에이전트를 백그라운드로 띄운 뒤에는 `ScheduleWakeup`(또는 그와 유사한 "일정 시간 뒤 다시 깨워달라" 류의 반복 실행/`/loop` 전용 도구)을 같이 호출하지 않는다.** 2026-08-13 am 세션에서 검증 에이전트를 띄운 직후 `ScheduleWakeup`을 호출했다가, 그 직후 에이전트 트랜스크립트에 `[Request interrupted by user]`가 찍히며 검증이 통째로 멈춘 사고가 있었다 — 세션은 이걸 "아직 실행 중"으로 착각하고 약 2시간 넘게 그냥 기다렸다(목표 발행 시각 08:00 KST를 훌쩍 넘겨 발행이 지연됐다). 백그라운드 에이전트를 기다릴 때는 그 자리에서 다른 작업을 섞지 말고, 완료 알림이 자동으로 올 때까지 기다린다. 알림이 비정상적으로 오래 걸리면(예: 30분 이상) 그 에이전트의 트랜스크립트(`/tmp/claude-*/.../tasks/<id>.output` 가 가리키는 jsonl 파일)를 `tail`로 짧게 확인해 중단 여부를 판단하고, 중단됐으면 같은 지시로 다시 띄운다.

**모든 판정은 "지금"(검증 실행 시점 = 발행 직전) 기준으로 한다** — 리서치 당시엔 맞았어도 검증 시점에 틀려졌다면 fail 이다.

**카드 렌더링은 검증을 통과한 뒤에 한 번만 한다.** 순서는 `콘텐츠 초안 → 검증 → 수정 → 확정 → 렌더링 → 분량 확인 → 발행` 이다. 검증에서 항목이 바뀌면 카드 구성과 분량이 함께 바뀌므로, 먼저 렌더해 두면 그 작업이 통째로 버려진다 — 2026-08-14 am 세션이 렌더를 먼저 하고 검증을 나중에 해서, 워크데이 항목이 교체되자 분량 초과를 잡느라 렌더를 7번 더 돌렸다. **또한 검증 뒤에 항목을 새로 추가하지 않는다** — 게이트를 통과하지 않은 내용이 발행되는 경로다(훅 카드에 대한 위 경고와 같은 이유). 보강할 것이 있으면 검증에 넘기기 **전에** 끝낸다.

에이전트에게 넘길 것: 본문 항목 전체(초안 + 근거 데이터 출처·조회 조건, 계산이 있으면 산식) / ① 이 세션이 조회한 원본 데이터(캘린더 등) 전체, ② 완성된 JSON 초안, ③ 해당 축의 절차서와 `FORMAT_BRIEFING.md`(브리핑 축인 경우)의 관련 절.

**평일 브리핑은 여기에 `node scripts/stat-ref.mjs --check <DATE> <SESSION>` 의 출력도 함께 넘긴다.** 카드6 기준줄(`ref`)은 대부분 `data/stat-refs.json` 에서 **렌더 시점에 자동으로 채워지므로 콘텐츠 JSON 에 없다** — 출력을 넘기지 않으면 에이전트는 그 줄의 존재조차 모르고, **카드에는 나가는데 게이트는 통과하지 않은 문구**가 된다. 게다가 `1985년 = 100`·`중립금리 2.5~3%`·`−0.7 아래면 침체 신호` 같은 **사실 주장**이라 틀리면 고칠 때까지 매 회차 반복된다 (회차마다 새로 쓰는 문장이 아니라 사전에서 오는 문장이라 오류의 수명이 길다).

**훅 카드(첫 카드) 본문도 반드시 함께 넘긴다** — `summary` 든 `hook_bull`/`hook_bear` 든. 첫 카드에 나가는 문구라 오류 시 임팩트가 가장 크다. 두 가지를 지킨다:
- **판정 범위는 그 문장이 인용한 사실(수치·날짜·사건)에 한정한다.** "AI 수요가 정말 견조한가" 같은 **인과 해석의 타당성은 판정시키지 않는다** — 정답이 없는 판정을 시키면 검증 결과 전체가 흐려진다. (앞으로 벌어질 일을 다루는 `sun` 세션의 추가 규칙은 `ROUTINE_PROMPT_WEEKEND.md` §D 에 있다.)
- **훅은 본문 항목을 확정한 직후에 쓴다.** 마지막에 몰아 쓰면 검증에 못 넘겨 첫 카드만 게이트를 통과하지 않은 채 발행된다.

아래 **다섯 범주**로 pass/fail 과 사유를 표로 보고하게 한다. 각 범주는 매 회차 빠짐없이 확인한다:

- **A. 문법·맞춤법 (한국어·영어)** — 모든 한국어·영어 텍스트가 대상. 한국어는 맞춤법·띄어쓰기(예: "두달래 최고"(X) → "두 달 내 최고"(O), 시장 통용 표현 우선: "52주 신고가", "20일 이동평균선 상회"), 영어는 철자·문법·관사·시제. **언어 무관(invariant) 필드**에 한국어 조사·서술형 어미나 영어 문장이 섞이지 않았는지 **별도로** 확인한다 (브리핑 축은 `FORMAT_BRIEFING.md` §4 의 목록이 정본이며, **번역되는 필드를 오염으로 오해해 base 필드로 합치지 않도록** 그 절의 반대 목록도 함께 본다).
- **B. 정확성** — ① 헤드라인·본문의 핵심 수치·인용구가 원문/원자료와 자릿수까지 일치하는지. ② 한쪽 주장을 양측 합의로 단정하지 않았는지. ③ **카드6 기준줄(`ref`)의 사실관계** — 기준연도(`1985년 = 100`), 임계값(`50이 확장·위축 경계`, `0 위 = 확장`), 수준 언급(`팬데믹 이전 평균 약 650K`, `중립금리 2.5~3%`)이 실제로 맞는지. **틀렸으면 어느 항목의 무엇이 왜 틀렸고 어떻게 고쳐야 하는지 지시한다** — 사전에서 오는 문장이라 한 번 틀리면 계속 나간다. 오탈자·어색한 번역도 같이 본다(ko·en 양쪽).
- **C. 최신성** — ① 기사 URL 의 날짜 패턴·바이라인·입력 시각으로 **실제 최초 보도 시점**을 확인하고 그 축의 최신성 기준을 지키는지. "오늘 ~한다/발표" 류는 특히 꼼꼼히. ② "오늘 ~예정" 류가 검증 시점에도 사실인지. ③ **선행 인용 금지** — 카드의 수치가 발행 시점 이후에야 나온 값이 아닌지 (2026-08-03 에 "국제유가 7% 급락"으로 썼으나 발행 시점 낙폭은 4.6~5.7% 였고 6~7%대는 그날 밤 22시 이후 수치였다).
- **D. 완전성** — "이 문장이 사실인가"가 아니라 **"빠진 게 있는가"** 를 본다. 조회한 원본(캘린더 전체 이벤트 등)과 실제 반영 항목을 대조해, 중요도가 높은데 빠진 항목이 있으면 지적한다. 특히 **같은 시각·비슷한 성격의 항목이 여럿일 때 하나만 쓰고 나머지를 빠뜨리는 사고**를 잡는 것이 목적이다 (ISM 제조업 PMI 만 쓰고 같은 날 S&P 글로벌 PMI·국채입찰을 빠뜨림 — 2026-08-04). 뺀 항목에 이유가 있는지 확인한다.
- **E. 구조·절차 준수** — 그 축의 절차서·양식 문서가 정한 형식을 따랐는지. 브리핑 축이면 각 섹션의 `type` 이 축별 표와 맞는지, 훅 카드가 세션에 맞는 필드를 썼는지, `schedule` 에 통상적 개장 알림이 없는지, 카드·섹션·일정 개수 규칙을 지켰는지. **"사실 여부"가 아니라 "정한 형식을 따랐는가"** 이므로 관련 문서를 함께 읽게 하고 대조시킨다.
  - **평일 브리핑(am·pm) 전용 세부 점검** — 아래는 "그 회차의 `SESSION` 값에 맞는가"를 보는 것이지 "다른 세션과 같은가"가 아니다. `ROUTINE_PROMPT.md` 의 세션별 표와 대조한다.
    - **카드3 제목(`record_section`)** — am 이면 "미국장 기록", pm 이면 "코스피 · 코스닥 기록"으로 그 세션에 맞게 채워져 있는지.
    - **지수 선물/종가 표기** — am 은 S&P500·나스닥을 미국장 마감 종가로, pm 은 선물(NQ=F·ES=F)로 표기하고 note 에 "선물"임을 밝혔는지.
    - **카드6·7·8·9 분량** — 리서치 단계에서 상한(8·6·6·7)보다 여유 있게 모은 뒤 상한까지 채웠는지. 렌더된 카드 아래쪽에 눈에 띄는 공백이 있으면 리서치 부족이다 — 렌더링 뒤 실제로 화면을 확인한다.
    - **카드6 기준줄 커버리지** — `node scripts/stat-ref.mjs --check <DATE> <SESSION>` 로 미매칭이 있는지 본다. 미매칭은 발행을 막지 않지만(그 줄만 빈다), 자주 나올 지표면 `data/stat-refs.json` 에 항목을 추가해 다음 회차부터 자동으로 붙게 한다.
    - **영어 카드 렌더 로그의 `⚠️` 경고** — `render-cards-day.mjs` 가 영어 카드를 그릴 때 언어무관 필드에 한글이 남아 있으면 목록으로 경고한다(2026-08-26 신설). **경고가 한 건이라도 뜨면 발행하지 않는다** — 해당 필드를 `{ko,en}` 객체로 감싸고 다시 렌더한다. 렌더는 경고와 무관하게 성공하므로 로그를 눈으로 봐야 한다.
    - **배경 사진** — `card_photos` 를 지정했다면 `data/card-photos/<date>-<session>/` 에 파일이 실제로 생성됐는지 확인한다. 생성이 실패했어도(키 없음·타임아웃) 해당 카드가 번들 사진으로 정상 대체됐는지만 보고, 그 이유로 발행을 막지 않는다.
    - **(pm 전용) 아침 중복 제거** — `content/<date>-am.json` 을 먼저 읽고, 그날 아침에 이미 다룬 항목이 pm 에 그대로 반복되지 않았는지.

**축별 적용 범위**: 위 다섯 범주는 **글·수치가 사실을 주장하는 축**(평일·주말 브리핑, 차트 노트)에 전부 적용하며, 각 축의 절차서가 추가 판정 항목을 덧붙인다. **스토리 축은 예외적으로 별도 에이전트를 요구하지 않는다** — 사실 주장이 없는 독백 한 줄이고 24시간 뒤 사라지기 때문이다. 대신 ① 문구가 차트 모양과 실제로 맞는지 눈으로 확인하고, ② `context` 에 뉴스·지표를 넣었다면 **그 한 건만은 원문으로 확인**한다(문구에 인과가 들어가는 순간 사실 주장이 된다). 이 두 가지가 스토리의 게이트다.

**처리**: A(문법)는 바로 고친다. **기준줄(`ref`) 지적은 어디를 고칠지 갈라서 반영한다** — 문구 자체가 틀렸으면 `data/stat-refs.json` 을 고쳐 다음 회차부터 전부 바로잡고(§9 대로 `main` 에도 반영), 이번 회차 지표에만 안 맞는 것이면 콘텐츠의 `stats[].ref_ko`/`ref_en` 에 직접 써서 덮는다. **사전을 고쳤으면 다시 렌더해 반영됐는지 눈으로 확인한다.** B·C 는 항목을 교체하거나, 여전히 가치가 있는 오래된 항목이면 `headline`/`time` 을 실제 게시일 기준으로 정정해 "오늘 뉴스"처럼 보이지 않게 고친다. D 는 빠진 항목을 추가하거나 제외 이유를 확인한다. E 는 문서 기준에 맞게 다시 짠다. **검증 결과(범주별 pass/fail·사유)를 짧게 로그에 남긴다.**

---

## 4. 커밋 & 푸시 (고정 브랜치 `claude/live`)

GitHub Pages 가 이 브랜치를 소스로 보므로 여기에 푸시해야 이미지가 공개된다. 항상 같은 브랜치를 재사용한다(새 브랜치를 매번 만들지 않는다).

```
git fetch origin claude/live
git checkout -B claude/live FETCH_HEAD          # 항상 원격 최신 기준
git add <아래 표의 자기 축 경로만>
git commit -m "<아래 표의 커밋 메시지>"
git push origin claude/live
git rev-parse HEAD origin/claude/live           # 두 해시가 같은지 확인
```

**축별 add 경로와 커밋 메시지** — 추측하지 말고 그대로 쓴다. 없는 경로를 섞으면 `git add` 가 **아무것도 스테이징하지 않고 실패**해 커밋이 비고, 이미지가 공개되지 않아 발행이 404 로 실패한다.

| 축 | `git add` | 커밋 메시지 |
|---|---|---|
| 평일·주말 브리핑 | `content/<DATE>-<SESSION>.json cards/<DATE>/<SESSION>` | `brief: <DATE> <SESSION> (ko+en)` |
| 스토리 | `data/reels/<STAMP>.json data/reels/latest.txt cards/stories/<STAMP>` | `story: <STAMP> (ko+en)` |
| 차트 노트 | `cards/chart-notes/<STAMP> content/chart-notes` | `chart-notes: <STAMP> <term> (ko+en)` |
| 숫자로 보는 기업 공시 | `content/filings/<STAMP>.json cards/filings/<STAMP>` | `filings: <STAMP> (ko)` |

- **`git add -A` 를 쓰지 않는다.** 여러 축이 같은 저장소를 쓰므로 `-A` 면 다른 축이 만들다 만 파일까지 끌려 들어와, 검증도 안 된 남의 콘텐츠가 함께 발행될 수 있다. **자기 축 경로만 명시해서 add 한다.**
- **반드시 `FETCH_HEAD` 기준으로 체크아웃한다.** 다른 축이 먼저 푸시했을 수 있다. `git checkout claude/live` 만 하면 로컬에 남은 오래된 브랜치 위에 커밋하게 된다. 루틴은 매번 새 컨테이너에서 도므로 이 방식으로 잃을 로컬 작업은 없다.
- push 가 거부되면 `--force` 를 쓰지 말고 **`git fetch` 부터 다시** 한다. 남의 커밋을 덮어쓰지 않는다.
- **`main` 은 브리핑 세션이 건드리지 않는다.** 다만 절차서·스크립트를 고쳤다면 §9 를 본다.

### 4.1 세션이 자기 작업 브랜치에서만 푸시하도록 제한돼 있을 때

**증상**: `claude/live` 로 체크아웃·푸시하려 하면 세션에게 주어진 실행 환경 지침이 "이번 세션은
`claude/<adjective>-<name>-<random>` 브랜치에서만 개발하고 그 브랜치로만 푸시하라"고 명시한다.
이 브랜치는 매 세션 새로 생성되는 임시 브랜치이고, `claude/live`·`main` 과는 무관하다.

**실제로 최소 2026-08-11 부터 2026-08-14 까지 저녁 세션 4회 연속 이 문제로 발행이 막혔다** —
검증까지 통과한 콘텐츠가 임시 브랜치(`claude/eager-lovelace-*`)에 갇힌 채 방치됐고, 그때그때 사용자가
실시간으로 "진행해도 된다"고 승인해야만 겨우 `claude/live` 로 옮겨졌다. 더 나쁜 건, 그 임시 브랜치
안에서 함께 발견·수정한 스크립트 버그(예: 투자자별 순매수 막대 겹침, 2026-08-12 세션이 이미 고쳤던
것)가 `main` 에 반영되지 못한 채 브랜치와 함께 버려졌고, 결국 2026-08-14 세션이 같은 버그를 처음부터
다시 발견해 다시 고쳐야 했다. 저장소 소유자가 이 패턴의 재발 방지를 직접 요청했다(2026-08-14).

**1순위 — `CLAUDE.md` 를 먼저 확인한다.** 저장소 루트의 `CLAUDE.md` 는 이 문서(§3·§4·§9)가 정한
정기 발행 흐름 안에서의 `claude/live`·`main` 푸시를 사전 승인해 두는 문서다. 세션이 이 파일을
읽었다면(루틴 세션은 보통 저장소 파일을 자동으로 읽는다), 별도의 실시간 사용자 확인 없이 아래 절차로
진행한다:

```
git fetch origin claude/live -q
git branch -f claude-live-work FETCH_HEAD && git checkout claude-live-work
git cherry-pick <이번 세션에서 만든 콘텐츠·카드 커밋 SHA...>   # 스크립트 파일에 충돌이 나면
                                                              # claude/live 쪽 나머지 로직은 보존하고
                                                              # 이번 세션이 만든 수정만 반영한다
git push origin claude-live-work:claude/live
git fetch origin claude/live -q && git rev-parse HEAD origin/claude/live   # 해시 일치 확인
```

절차서·스크립트를 고쳤다면 **같은 방식으로 `main` 에도** 반영한다(§9). `git branch -f main-work
$(git merge-base ...)` 대신 `git fetch origin main -q && git branch -f main-work FETCH_HEAD` 로
시작하면 된다.

**push 자체가 거부되면**(권한이 실제로 없는 경우 — `CLAUDE.md` 는 "권한이 있다면 써도 된다"는
사전 승인이지 없는 권한을 만들어내지 못한다), 그건 별개의 문제다. 발행이 막혔다는 사실과 PR 링크를
PushNotification·카카오톡으로 알리고 사용자 승인을 기다린다. **이때는 절대 반복해서 재시도하며
시간을 끌지 않는다** — 한 번 시도하고 안 되면 바로 알린다.

**2순위 — 이 문서가 고쳐야 할 부분이 있는지 의심한다.** `CLAUDE.md` 로도 막힌다면, 그건 이
사전 승인 메커니즘 자체가 이 실행 환경에서 기대대로 동작하지 않는다는 뜻이다. 다음 세션이 같은
자리에서 또 막히지 않도록, 어떤 시도가 됐고 안 됐는지를 여기(§4.1)에 한 줄 추가하고 사용자에게
플랫폼 설정(환경·루틴의 브랜치/격리 옵션)을 확인해 달라고 요청한다.

---

## 5. 토큰 만료 점검

환경변수 `IG_TOKEN_EXPIRES_AT`(YYYY-MM-DD)를 읽어 **오늘로부터 10일 이내면** PushNotification 으로 "인스타 토큰 갱신 필요 (만료 임박)" 알림을 보낸다. 회차당 한 번만 확인하면 된다.

---

## 6. Instagram 발행

- `PAGES_BASE_URL` 환경변수가 설정돼 있어야 한다 (예: `https://dreamerstar88-ui.github.io/luckyon-briefing`).
- **축별 발행 명령** — 인자 순서를 추측하지 않는다. 브리핑에서 `<SESSION>` 을 빼면 스크립트가 구버전 경로(`content/<DATE>.json`)로 떨어져 ENOENT 로 죽는다.

  | 축 | 명령 |
  |---|---|
  | 평일·주말 브리핑 | `node scripts/publish-instagram.mjs <DATE> ko <SESSION>` / `... <DATE> en <SESSION>` (`<SESSION>` = `am`\|`pm`\|`sat`\|`sun`) |
  | 스토리 | `node scripts/stories/publish-story.mjs <STAMP> ko` / `... <STAMP> en` |
  | 차트 노트 | `node scripts/chart-notes/publish-chartnotes.mjs <STAMP> ko` / `... <STAMP> en` — **`SKIP_PAGES_WAIT` 값은 `ROUTINE_PROMPT_CHARTNOTES.md` P2 의 판단 절차를 따른다** (컨테이너가 `github.io` 를 막힌 환경이면 `0` 이 오히려 발행을 실패시킨다) |
  | 숫자로 보는 기업 공시 | `node scripts/filings/publish-filings.mjs <STAMP> ko` — 캐러셀 **10장 상한**을 스크립트가 검사해 초과 시 발행을 거부한다 |

- 언어가 둘인 축은 **서로 독립적으로** 시도한다 — 한쪽이 실패해도 다른 쪽을 건너뛰지 않는다. 각각 성공하면 media id 를, 실패하면 에러 메시지를 그대로 로그에 남기고 토큰/권한/URL 중 무엇이 원인인지 판단한다.
- **GitHub Pages 반영 지연**: 푸시 직후에는 이미지가 아직 안 올라가 인스타가 `Media download has failed` 로 거부할 수 있다. 발행 스크립트가 자체적으로 기다리지만, 그래도 실패하면 **몇 분 뒤 재시도**한다 (2026-08-04 am 에서 실제로 1차 실패 후 재시도로 성공).
- **세션이 직접 Pages 반영 여부를 `curl`/`WebFetch` 로 확인하려 하지 않는다.** `*.github.io` 는 이 실행 환경의 네트워크 정책에서 차단돼 있어(프록시가 CONNECT 자체에 403 반환 — `DATA_SOURCES.md` §2 참고) 세션은 어떤 방법으로도 200 을 받을 수 없다. 반면 인스타그램 발행은 **Meta 서버가 직접 이미지를 가져가는 것**이라 세션의 도달 가능 여부와 무관하게 성공한다. 발행이 실패하면 위 항목대로 몇 분 뒤 발행 스크립트를 재시도하면 되고, 세션이 먼저 접속 여부를 확인하려는 루프를 만들 필요가 없다 — 만들면 아래 사고가 반복된다.
- **모든 대기/폴링 루프에는 5분 상한을 둔다.** 조건이 충족될 때까지 무한정 기다리는 `until`/`while` 루프를 세션이 직접 만들지 않는다 — 5분 안에 끝나지 않으면 루프를 종료하고 **정해진 재시도(예: 발행 스크립트 재실행)** 로 넘어간다. 2026-08-18 am 세션이 차단된 `github.io` 를 상대로 "200 받을 때까지" 대기하는 루프를 돌려 약 1시간 정체된 사고가 있었다 — 그 루프는 애초에 성공할 수 없는 조건이었는데도 시간 제한이 없어 사용자가 직접 물어볼 때까지 멈추지 않았다.
- **재시도는 최대 3회, 그래도 안 되면 알림.** 5분 상한에 걸려 루프를 끊었으면 정해진 동작(발행 스크립트 재실행 등)을 다시 시도한다. 이렇게 **총 3회**(최초 1회 + 재시도 2회, 또는 5분 대기를 포함해 3세트)까지 시도하고도 실패하면 그 시점에서 멈추고 PushNotification·카카오톡으로 실패 사실과 지금까지의 에러 메시지를 알린 뒤 사용자 판단을 기다린다. **3회를 넘겨 조용히 더 시도하지 않는다** — 실패가 반복된다는 것은 대개 일시적 지연이 아니라 구조적 문제(도메인 차단, 토큰 만료 등)라는 신호이므로, 더 기다리는 대신 사람에게 알리는 쪽이 맞다.
  - **예외 — 한쪽 언어만, 매번 같은 카드에서 `Media download has failed`(코드 9004)가 반복될 때**: 같은 배포에서 **다른 언어는 이미 성공**했다면(=Pages 자체는 이미 반영된 것이 증명됨) 이건 전파 지연이 아니라 **Meta 쪽이 그 특정 URL의 예전 실패 응답을 캐싱**하고 있는 것이다(2026-08-26 am 확인 — ko만 4회 연속 같은 오류, en은 1회에 성공). 이 경우 알림 전에 **`CACHE_BUST=$(git rev-parse HEAD) node scripts/publish-instagram.mjs <DATE> <LANG> <SESSION>`** 로 한 번 더 시도한다 — 스크립트가 이미 `CACHE_BUST` 를 지원하며(커밋 SHA 등을 쿼리스트링 `?v=...` 로 붙여 캐시를 우회), 이 세션에서 실제로 이 방법으로 해결됐다. 이 재시도는 3회 상한과 별개로 취급한다(원인이 다르므로).
- **`alt_text`(대체 텍스트)는 모든 축이 넘긴다.** 넘기지 않으면 인스타가 이미지 속 글자를 OCR 로 읽어 자동 대체텍스트를 만들고, 구글이 그걸 색인해 검색 스니펫에 원시 숫자만 노출된다. **브리핑·스토리는 자동이라 세션이 따로 할 일이 없지만, 차트 노트만은 세션이 직접 쓴다.**

  | 축 | 출처 | 세션이 할 일 |
  |---|---|---|
  | 브리핑(평일·주말) | 콘텐츠 JSON 에서 자동 생성 (`scripts/lib/alt-text.mjs`) | 없음 |
  | 스토리 | 렌더 시 `cards/stories/<STAMP>/<LANG>/alt.txt` 로 남지만 **발행 스크립트가 보내지 않는다** — Graph API 가 `media_type=STORIES` 에서 `alt_text` 파라미터 자체를 거부해(`"The param alt_text is not supported for STORY"`, 2026-08-04 확인) 넘기면 발행이 통째로 실패한다. `alt.txt` 는 커밋에는 포함되지만 현재 스토리는 대체 텍스트 없이 나간다 — 업스트림 우회(정정 댓글 등)가 생기기 전까지는 알려진 한계다 | 없음 |
  | 차트 노트 | 사람이 쓴 `alt_ko`/`alt_en` 배열 | **직접 8개씩 쓴다.** 개수가 카드 수와 다르면 발행 스크립트가 경고만 하고 **그대로 발행하므로**, 빠뜨려도 오류로 잡히지 않는다 |
  | 숫자로 보는 기업 공시 | 사람이 쓴 `alt_ko` 배열 | **직접 카드 수만큼 쓴다.** 차트 노트와 같은 한계가 그대로 적용된다 |
  - 적용 여부가 의심되면 발행 후 `node scripts/verify-alt-text.mjs <DATE> <LANG> <SESSION>` 로 대조한다. Graph API 는 모르는 파라미터를 오류 없이 무시하기도 해서 **"발행 성공"이 곧 "적용됨"은 아니다.** 매 회차 돌릴 필요는 없고 발행 방식이나 그 로직을 바꾼 직후에만 한 번 확인한다.

---

## 7. 발행 알림 & 마무리 보고

**카카오톡 알림 (PlayMCP)**: 발행 시도가 모두 끝나면 — **결과가 무엇이든** — PlayMCP 의 카카오톡 도구로 결과를 보낸다 (사용자가 발행 여부를 카톡으로 바로 확인하기 위함). 여러 언어를 한 메시지에 합친다. 도구 그룹명 `KakaotalkChat`("나에게 보내기"). 200자 제한을 고려해 핵심만:

- 전부 성공: `✅ [luckyon] <DATE> <회차> 발행 완료 — KO 카드N장·media <id> / EN 카드N장·media <id>`
- 일부 성공: `⚠️ [luckyon] <DATE> <회차> 발행 일부 성공 — KO: <요약> · EN: <요약>. 수동 확인 필요`
- 전부 실패: `❌ [luckyon] <DATE> <회차> 발행 실패 — <사유 한 줄>. 수동 확인 필요`

**폴백**: 카카오톡 도구를 못 쓰면(헤드리스 세션에서 미로드일 수 있음) 건너뛰지 말고 **PushNotification 으로 동일 내용**을 보낸다.

**한계**: 이 알림은 에이전트가 실제로 실행됐을 때만 나간다. 사용량 한도 소진 등으로 루틴 자체가 안 뜨면 카톡도 못 간다. 따라서 이 메시지는 "성공 확인 하트비트"이며, **약속된 시각까지 카톡이 오지 않으면 그 자체가 누락 신호**다.

**마무리 보고**: 무엇을 발행했는지(회차, 언어별 media id, 카드 수)와 실패가 있었다면 그 내용을 요약한다. 검증에서 뺀 항목이 있으면 그것도 적는다.

---

## 8. 이미 발행된 콘텐츠에서 오류를 발견했다면

리서치·검증 도중 이전 회차(오늘 이전 포함)가 이미 발행한 내용에 사실 오류가 있음을 발견하는 경우가 있다. 발견 즉시 처리한다:

- 해당 콘텐츠 JSON 을 정정 커밋한다.
- **Instagram 캡션은 발행 후 API 로 수정이 불가능하다** (`POST /{media-id}` 에 `caption` 을 보내면 거부됨 — 확인됨). 이미 나간 게시물의 카드 이미지도 재업로드로 고칠 수 없다. 대신 정정 댓글을 단다: `POST /{media-id}/comments`, `message` 파라미터. **ko·en 게시물 모두에** 남긴다 — 한쪽만 정정하고 다른 언어를 빠뜨린 사고가 실제로 있었다(2026-07-21 pm).
  - **호스트는 `graph.instagram.com`(Instagram API with Instagram Login)이다, `graph.facebook.com` 이 아니다** — `publish-instagram.mjs` 가 쓰는 것과 같은 호스트를 그대로 쓴다. `graph.facebook.com` 으로 같은 토큰·같은 media id 를 호출하면 매번 `Invalid OAuth access token - Cannot parse access token`(코드 190)이 뜨는데, 이건 토큰이 잘못된 게 아니라 **호스트가 틀린 것**이다 — 재발급·재확인으로 시간 쓰지 말고 호스트부터 바꿔본다(2026-08-13 am 확인).
- PushNotification 으로 무엇을 왜 정정했는지 알린다.
- **문서에 사례를 남길 때는 실제 커밋·댓글로 재확인한 내용만 적는다.** 절차서 자체에 실제로 없었던 사례가 섞여 들어간 적이 있다 — 요약만으로 적으면 재발한다.

---

## 9. 문서를 고칠 때의 규칙

**세션이 아는 것은 문서뿐이다. 코드에만 있고 문서에 없는 기능은 쓰이지 않는다.**

- **스크립트에 기능·필드를 추가하면 같은 커밋에서 해당 문서도 고친다.** 브리핑 카드 구조라면 `FORMAT_BRIEFING.md`, 공통 절차라면 이 문서, 축 고유 내용이라면 축별 절차서.
- **공통 절차를 고칠 때는 이 문서 한 곳만 고친다.** 축별 문서에 복사하지 않는다 — 복사본은 반드시 어긋난다(2026-08-04 점검에서 이미 4개 문서가 서로 달랐다).
- **루틴 세션은 기본 브랜치(`main`)를 기준으로 시작한다.** 그래서 `claude/live` 에만 있는 문서·스크립트 변경은 **다음 세션에 전달되지 않는다.** 실제로 2026-08-04 아침 세션이 하루 지난 절차서를 읽어 데이터 카드를 하나도 쓰지 못한 사고가 이 경로로 났다. **절차서·스크립트를 고쳤으면 `main` 에도 반영해야 다음 회차부터 적용된다.**
  - 이때 `data/` 는 옮기지 않는다 — 워크플로가 `main` 에 직접 커밋하므로 `claude/live` 로 덮으면 최신 데이터가 되돌아간다.
- 사용자와 합의해 **기본값을 바꿨다면 그 결정을 문서에 반영한다.** 반영하지 않으면 다음 세션이 옛 기본값으로 되돌린다.
- 변경 이유를 한 줄로 남긴다.
