# 주간 쇼츠 인수인계 — 3회차 발행 직후 (2026-09-20 01:00 KST 작성)

새 세션이 이 일을 이어받을 때 **맨 먼저 읽는 문서**다. 지금 무엇이 끝났고, 무엇이
남았고, 무엇을 조심해야 하는지만 적는다. 만드는 방법 자체는 `GUIDE_WEEKLY_SHORTS.md`
와 `ROUTINE_PROMPT_WEEKLY_SHORTS.md` 에 있다.

---

## 1. 이 일이 무엇인가

유튜브 쇼츠 채널 **「luckyon 넘버뷰」**(`UCdqD5SpqpXZJUzCZILyeRAA` · `@luckyon_77`)에
**주 1회 35초짜리 나스닥100 선물 되감기 영상**을 올린다. 한 주의 5분봉을 처음부터
끝까지 다시 그리면서, 그 주에 예정돼 있던 경제 이벤트마다 잠깐 멈춰 수치를 보여 주고,
마지막에 퀴즈 정답을 공개하는 형식이다. 같은 영상을 인스타그램 릴스로도 올린다.

저장소는 `dreamerstar88-ui/luckyon-briefing`, 작업 브랜치는
`claude/youtube-luckyon-analysis-fi90r8` 이다.

---

## 2. 지금까지 나간 회차

| 회차 | 대상 주 | 유튜브 | 인스타그램 | 발행 |
|---|---|---|---|---|
| 1회차 | 2026-08-31 주 | `dIfR_J4TrAA` | `DdBTl_OjfI4` | 09-08 17:54 KST |
| 2회차 | 2026-09-08 주 | `OSLaKuhQNtg` | `DdN6dSoD6sH` | 09-13 15:18 KST |
| 3회차 | 2026-09-14 주 | `ZfVRdo2eL08` | `DdeVQqEEcu3` | 09-20 00:28 KST |

각 회차의 자세한 기록은 `content/weekly-shorts/<주 시작일>.json` 의 `publish` 항목에
있다. **다만 1회차와 2·3회차의 기록 모양이 다르다.** 1회차는 `youtube_video_id` ·
`instagram_permalink` 처럼 평평하게 적혀 있고, 2회차부터 `youtube` · `instagram` 으로
묶여 있다. 회차를 가로질러 읽는 코드를 쓸 때 주의한다.

카테고리도 1회차는 25(뉴스·정치), 2·3회차는 27(교육)이다. 어느 쪽이 나은지 아직
비교해 보지 않았다. 4회차도 27 로 두고, 시청 지표가 쌓이면 그때 본다.

발행 요일과 시각이 아직 정해지지 않았다. 1회차는 화요일 저녁, 2회차는 일요일 오후,
3회차는 토요일 자정 직후였다. 2회차 때 「토요일 18:30 KST 로 시험한다」고 적어 두었는데
아직 그대로 해 본 적이 없다.

---

## 3. 3회차에서 확정된 것 (다음 회차도 이대로 간다)

**타임라인**은 35초 고정이고 구간은 `HOOK [0,5]` · `REPLAY [5,27.9]` ·
`ANSWER [27.9,32]` · `SUMM [32,35]` 다. 요약을 3.0초로 줄이고 그만큼 되감기에 준 것이
3회차 대표 지시였다. 사건마다 멈추는 총 시간은 9.0초이고, 사건 수로 똑같이 나누지 않고
**물채우기 방식**으로 배분한다 — 앞 사건과 간격이 좁은 쪽에 더 오래 세운다.

**데이터 심볼**은 연속 심볼 `NQ=F` 를 쓰지 않는다. 그 주의 실제 월물을 쓴다(3회차는
`NQZ26.CME`). 연속 심볼은 만기 때 월물을 말없이 갈아타서 가격이 통째로 뛴다. 3회차
검증에서 `NQ=F` 는 09-14 11:30 에 배율이 1.005% 튀는 것이 확인됐다. 월물 글자는
H=3월, M=6월, U=9월, Z=12월이고 만기는 해당 월 셋째 금요일이다.

**주간 창** — **2026-09-26 에 바뀌었다.** 4회차(2026-09-21 주)부터는 **월요일 프리장 시작
(04:00 ET, 한국 월 오후 5시) ~ 금요일 16:00 ET** 다. 대표 지시는 «주가 변화 프리장부터»,
«이번 회차부터 포함시켜, 앞에건 놔둬» 였다. 1~3회차는 월요일 09:30 ET 부터였고 **그대로 둔다**.

> **한때 이 지시를 «일요일 18:10 선물 개장부터» 로 바꿔 적고 그 기준으로 영상까지 만들었다.**
> 대표가 한 말이 아니었고, 18:10 은 야후 5분봉의 첫 봉일 뿐 실제 선물 개장(18:00 ET)도
> 아니었다 — 야후는 CME 종목의 일요일 첫 10분(18:00·18:05 봉)을 주지 않는다. 독립 검증과
> 대표 지적으로 바로잡았다. **대표가 쓴 말을 내 해석으로 바꾸지 않는다.**

넓힌 이유는 월요일 정규장 전에 나온 발표가 옛 기준으로는 통째로 빠지기 때문이다.
4회차 주가 그랬다 — 월요일 ★★ 두 건이 전부 개장 전이라 쓸 사건이 ★ 하나뿐이었다.

**기준이 달라졌으니 회차끼리 숫자를 그대로 비교하면 안 된다.** 같은 주(2026-09-21)를
두 기준으로 재면 옛 기준 1,179봉 · +2.231%, 새 기준(프리장부터) 1,245봉 · +2.377% 다.
3회차는 옛 기준으로 1,179봉이었다.

끝의 16:00 봉(16:00~16:05)까지 들어가는 것은 1회차부터 이어온 관례라 그대로 둔다.
매니페스트 `window.end_bar_note` 에 적어 두었다. 「하루에 최소 1개」의 요일 수는
그대로 5일이다 — 검증 스크립트가 정규장 시간대 봉이 있는 날만 요일로 센다.

**최대 낙폭**은 봉의 고가에서 저가까지로 잰다. 종가끼리 재면 다른 값이 나온다
(3회차: 고가→저가 −1.86%, 종가→종가 −1.64%).

---

## 4. 반드시 지키는 절차

### 4-1. 자료는 출처 두 곳 이상에서 확인한다

대표 지시(2026-09-19)다. 지침서 2-8-1 에 표로 정리돼 있다. 주가는 실제 월물에
QQQ·^NDX 를 겹쳐 보고, 지표는 트레이딩이코노믹스와 발표 기관 원본을 대조하고,
사람 이름은 캘린더에 적힌 글자만 쓴다.

**사람 이름을 기억에서 꺼내 쓰지 않는다.** 3회차 준비 중에 캘린더에 `fed press
conference` 라고만 적힌 것을 보고 「파월 의장」이라고 썼다가 걸렸다. 2026년 5월
22일부터 연준 의장은 **케빈 워시**다. 모델의 학습이 끝나는 시점과 겹쳐서 특히
위험한 자리다.

**오늘 날짜를 컨테이너 시계로 읽지 않는다.** 이 환경은 UTC 다. 한국 시각이 필요하면
`TZ=Asia/Seoul date` 를 쓴다.

### 4-2. 사건을 고르기 전에 네 가지를 훑는다

경제 캘린더, 기업 실적, 뉴스, 데이터 교차확인. 매니페스트 `coverage` 에 각각의 결과를
남긴다. **«없음»도 결과다.** 훑지 않은 것을 «없었다»고 적으면 안 된다. 검증 [3-1]
이 이걸 본다.

기업 실적은 나스닥100 구성종목을 먼저 본다. 뉴스는 시각이 확정된 1차 보도만 쓰고,
그 주 5분봉 변동 상위 10개 시점을 훑어 거기서 무슨 일이 있었는지 본다.

### 4-3. 발행 전에 두 겹을 통과한다

1. **기계 검사** — `node scripts/weekly-shorts/verify-shorts.mjs <매니페스트> --srt <산출물폴더>`
   현재 117건이다. 하나라도 실패하면 발행하지 않는다.
2. ~~독립 검증 에이전트~~ — **2026-09-26 대표 지시로 없앴다.** 4회차에 두 번 56분 · 94만 토큰을 썼다.
   그 일(두 번째 출처 대조·영상 파일·발행문구·과거 1시간봉 재수집)은 기계 검사 [1-2]·[10]~[12] 로 옮겼다.
   이제 `make-episode.mjs` 한 번이 렌더부터 검사까지 약 70초에 끝낸다(`WEEKLY_SHORTS_QUICK.md`).

**지적이 나오면 전부 모아 한 번에 고치고 한 번만 다시 돌린다.** 3회차에서 하나씩
고치고 매번 다시 돌리다가 검증이 네 바퀴 돌았다. 새로 생긴 지적이 아니라 처음부터
목록에 있던 것을 한 번에 안 본 탓이었다.

에이전트를 띄웠으면 **실제로 돌고 있는지 확인한다.** 3회차에서 돌지 않는 에이전트를
기다리며 시간을 버렸다.

### 4-4. 발행 순서

1. `claude/live` 브랜치에 산출물을 올린다.
   경로는 `cards/reels/nasdaq-week-<주 시작일>/ko/` 이고 파일은 다섯 개다 —
   `reel.mp4` · `cover.png` · `caption.txt` · `subtitles.ko.srt` · `subtitles.en.srt`.
   `CLAUDE.md` 가 이 브랜치 직접 푸시를 사전 승인해 두었다. 다만 **`git add -A` 를
   쓰지 않는다.** 자기 경로만 add 하고, `origin/claude/live` 의 `FETCH_HEAD` 기준으로
   따로 워크트리를 만들어 작업하는 게 안전하다.
2. GitHub Pages 배포를 기다린다. **이 환경에서는 `github.io` 가 프록시에 막혀
   직접 확인이 안 된다.** 환경변수 `SKIP_PAGES_WAIT=1` 이 그래서 설정돼 있다.
   인스타그램은 자기 서버가 URL 을 받아가므로 우리가 못 봐도 상관없다.
3. 유튜브에 올린다 (§5 참고).
4. 인스타그램에 올린다 — `node scripts/reels/publish-reel.mjs nasdaq-week-<주 시작일> ko`
   `claude/live` 체크아웃에서 실행한다. 먼저 `--dry-run` 으로 경로와 문구를 본다.
5. **유튜브 고정 댓글을 단다** (지침서 8-1). 문구는 발행문구 문서의 「5-1. 고정 댓글」
   절에 미리 적어 둔다. 정답은 넣지 않고 보기를 다시 제시한다.
6. 매니페스트 `publish` 에 전부 기록하고 작업 브랜치에 커밋·푸시한다.

---

## 5. 발행 경로에서 조심할 것

### 유튜브

> **2026-09-26 — 한국 채널 토큰은 7일마다 만료된다.** 구글 동의 화면이 «테스트 중» 이라서다.
> 2026-09-26 에 만료(`invalid_grant`)를 확인하고 다시 받았다(발급 2026-09-26 → **10-03 만료**).
> 다시 받는 법: `backtest-reels` 폴더에서
> `node --env-file="C:/Users/PSJ_1/.claude/SJ PARK Project/api-keys/keys.env" scripts/authorize-youtube.mjs`
> (기본이 한국 채널) → 뜬 주소를 **대표님이 직접** 열어 dreamerstar88@gmail.com 으로 로그인 →
> 채널 «luckyon 넘버뷰» 선택 → 허용. 새 토큰과 발급일이 `keys.env` 에 자동 저장된다.
> 이미 떠 있는 인증 도구가 기다리고 있으면 8917 포트가 막혀 새로 못 띄운다 — 그 도구가
> 받으면 되므로 주소만 다시 드린다. 받은 뒤 `publish-youtube.mjs` 를 `--confirm` 없이 돌려
> 채널이 맞는지 미리보기로 확인한다.

> **2026-09-20 갱신 — 우회가 더 이상 필요 없다.**
> 대표님 컴퓨터의 인증 파일 `C:\Users\PSJ_1\.claude\SJ PARK Project\api-keys\keys.env` 에
> 구글 인증이 들어 있고, 실제로 갱신해 확인했더니 **두 채널 모두 살아 있다.**
>
> | 항목 | 채널 | 권한 |
> |---|---|---|
> | `GOOGLE_REFRESH_TOKEN` | `UCdqD5SpqpXZJUzCZILyeRAA` — luckyon 넘버뷰 | youtube.upload · youtube.force-ssl · youtube |
> | `GOOGLE_REFRESH_TOKEN_EN` | `UC1vnIy-A4QET44nDBVe18kQ` — luckyon numberview (영어) | 같음 |
>
> **토큰 자체가 채널이라 엉뚱한 채널로 갈 수가 없다.** 아래의 「올리기 전에 채널 확인」
> 절차도 필요 없어진다. 영상은 **로컬 파일 그대로** 올라가므로 Pages URL 을 먼저
> 만들지 않아도 된다(인스타그램은 여전히 공개 URL 이 필요하다).
>
> ```bash
> node scripts/weekly-shorts/render/publish-youtube.mjs --video=... --title=... --ko=... --en=...
> ```
>
> `--confirm` 을 붙이기 전까지는 **미리보기만 하고 아무것도 올리지 않는다.** 채널·제목
> 형식·자막 유무·설명란 길이를 먼저 보여 준다. 자막도 같은 도구가 이어서 올린다.
>
> 아래 Zapier 방식은 그 인증 파일을 못 쓰는 환경(클라우드 세션 등)에서의 대비책으로 남긴다.

- 앱 이름 `YouTubeV4CLIAPI`, 도구는 `youtube_upload_video` 와
  `youtube_make_api_mutating_request` / `youtube_make_api_get_request`.
- **연결이 두 개다.** 기본값 「YouTube #2」(`0203372c-9660-8960-96e7-ae9c1300cc3d`)가
  luckyon 넘버뷰 채널이다. **올리기 전에 반드시 `channels?part=snippet&mine=true` 로
  채널 ID 가 `UCdqD5SpqpXZJUzCZILyeRAA` 인지 확인한다.** 엉뚱한 채널에 올라가면
  되돌리기 어렵다.
- 영상 파일은 Pages URL 로 넘긴다. 로컬 경로는 Zapier 가 읽지 못한다.
- 자막은 `commentThreads` 가 아니라 `upload/youtube/v3/captions` 에 `multipart/related`
  로 올린다. 3회차 호출 형태가 매니페스트 기록과 이 세션 로그에 남아 있다.
- **설명란에 `<` 나 `>` 가 있으면 업로드가 통째로 거부된다.** 화살표는 `→` 를 쓴다.
- **`videos.update` 는 보내지 않은 필드를 지운다.** 고칠 때도 제목·태그·카테고리·
  언어를 전부 함께 보낸다.
- 카테고리는 27(교육), 언어는 ko, 공개로 올린다.
- **고정(pin) 은 API 에 없다.** 댓글은 API 로 달고, 고정은 사람이 유튜브 스튜디오나
  앱에서 한 번 눌러야 한다. 매니페스트에 `pinned: false` 로 남기고 누른 뒤 고친다.

### 인스타그램

저장소 스크립트가 **`graph.instagram.com`** 을 쓴다(Instagram Login 방식). 토큰이
안 먹는 것 같으면 **호스트부터 확인한다.** 3회차에서 `graph.facebook.com` 으로
시험해 놓고 「토큰이 가려졌다」고 잘못 보고했다. 환경변수 `IG_TOKEN_EXPIRES_AT` 이
지난 날짜여도 토큰은 살아 있을 수 있다.

vidIQ 커넥터에도 `luckyon_77` 이 발행 권한까지 연결돼 있다
(`platformAccountId` `38087698064179081`). 저장소 스크립트가 막히면 그쪽이 대안이다.

---

## 6. 지금 열려 있는 것

**사람이 해야 할 일 하나.** 3회차 댓글(`UgwE-5IPBX67FVK_NSN4AaABAg`)을 유튜브
스튜디오나 앱에서 **고정**해야 한다. API 로는 안 된다. 누른 뒤 매니페스트
`publish.youtube.pinned_comment.pinned` 를 `true` 로 고친다.

**다음 주에 확인할 것.** 1·2회차 모두 평균 시청이 정확히 12초에서 끊겼다. 정답은
27.9초에 나오니 대부분 답을 못 보고 떠난 것이다. 3회차는 **길이를 35초 그대로 두고
훅만** 숫자(+2.58%)에서 사건(3년 만의 첫 금리 인상)으로 바꿨다. 다른 걸 같이 바꾸지
않았으므로 이 한 가지 변경의 효과를 따로 볼 수 있다. 4회차를 만들기 전에 3회차의
평균 시청 시간을 확인하고, 올랐으면 이 방향을 유지하고 아니면 다른 수를 쓴다.

**개선하기로 한 것 두 가지** (3회차 발행 후 대표께 보고, 아직 착수 전).

1. 날짜 하나만 주면 데이터 수집부터 매니페스트 작성, 렌더, 검증까지 한 번에 도는
   스크립트로 묶는다. 지금은 단계마다 손으로 이어 붙여서 실수가 끼어들 자리가 많다.
2. 발행문구도 매니페스트에서 생성하게 바꾼다. 지금 같은 문장이 렌더러·매니페스트·
   발행문구 세 곳에 있어서, 한 곳만 고치는 사고가 3회차에만 두 번 났다. 자막은
   이미 매니페스트에서 생성하도록 바꿔 한 군데를 없앴다.

**재현성 문제 하나 — 2026-09-20 해결.** `v10-ep3/render2.mjs` 와 `srt2.mjs` 에 클라우드
세션의 절대경로(`/home/user/...` · `/tmp/claude-0/...`)가 박혀 있었고, 브라우저 실행
파일까지 `/opt/pw-browsers/...` 로 고정돼 있었다. 입력 `week3_5m.json` 도 저장소에
없었다. **다른 컴퓨터에서는 실행 즉시 멈췄다.**

고친 내용:

- 두 파일 모두 **저장소 뿌리를 위로 올라가며 스스로 찾는다.** 어느 폴더로 복사해도 돈다
- 브라우저 경로를 없앴다 — playwright 가 그 컴퓨터 캐시에서 알아서 찾는다
- 5분봉 원자료를 `data/weekly-shorts/<주 시작일>.5m.json` 으로 저장소에 넣었다.
  **야후는 약 1개월치만 준다** — 넣어 두지 않으면 한 달 뒤에는 그 회차를 다시 못 그린다
- 다시 쓸 수 있는 도구 한 벌을 `scripts/weekly-shorts/render/` 에 두었다 (§9)

고친 뒤 3회차 발행본 폴더로 검증을 돌려 **117건 전부 통과 · 종료코드 0** 을 확인했다.

**검증 관문이 윈도우에서 헛되이 막히던 것 — 같이 고쳤다.** 이 저장소는 줄바꿈 자동
변환이 켜져 있어(`core.autocrlf=true`) 윈도우에서 받으면 글자 파일의 줄 끝이 CRLF 로
바뀐다. 그러면 `[9] 고정 댓글` 의 정규식이 안 맞고 자막 바이트 대조도 어긋나,
**내용이 멀쩡한데 「발행 금지」가 난다.** `verify-shorts.mjs` 가 글자 파일을 읽을 때
줄바꿈을 맞추도록 바꿨다(`readText`). 자막 내용 대조는 그대로 엄격하다.

---

## 7. 3회차에서 배운 것 (같은 실수를 반복하지 않기 위해)

지침서 9장에 「하지 말 것」으로 16개가 정리돼 있다. 그중 3회차에 새로 추가된 것은
이것들이다.

- **영상만 올리고 고정 댓글을 안 다는 것.** 1·2회차가 댓글 없이 나갔다.
- **자격증명이 안 먹을 때, 그걸 실제로 쓰는 스크립트가 어느 주소를 부르는지 읽지
  않고 토큰·환경 탓을 하는 것.**
- **검증 지적을 하나씩 고치고 그때마다 다시 돌리는 것.**

그리고 이것들은 이전 회차부터 있던 것인데 3회차에도 걸렸다.

- **확인하지 않고 말하는 것** — 시각, 표의 숫자, 규칙의 원문. 9장 0번이다.
- **훑지 않은 것을 «없었다»고 적는 것.**
- **놓친 결함을 고치고 검사 항목은 안 만드는 것.** 결함을 고쳤으면 같은 것을 다음에
  기계가 잡게 만든다. 검사 항목을 새로 만들었으면 **일부러 틀린 입력을 넣어 실제로
  걸리는지 확인한다.** 항상 통과만 하는 검사는 검사가 아니다.

---

## 8. 파일이 어디 있나

| 무엇 | 어디 |
|---|---|
| 제작 지침 (규칙의 원본) | `GUIDE_WEEKLY_SHORTS.md` |
| 루틴 절차서 | `ROUTINE_PROMPT_WEEKLY_SHORTS.md` |
| 회차 매니페스트 | `content/weekly-shorts/<주 시작일>.json` |
| 검증 스크립트 | `scripts/weekly-shorts/verify-shorts.mjs` |
| 질문 목록 (61개) | `scripts/weekly-shorts/questions.mjs` |
| 인스타 발행 | `scripts/reels/publish-reel.mjs` |
| 3회차 작업 폴더 | `docs/samples/weekly-shorts/v10-ep3/` |
| 발행 산출물 | `claude/live` 브랜치의 `cards/reels/` |

음악은 `lyria-build-v1.mp3` 를 쓴다. **2026-09-20 부터 저장소 `assets/bgm/` 에 들어 있다**
(드라이브 원본과 바이트 단위로 같다). 매번 받지 않아도 된다. 드라이브에서 다시 받아야
한다면 「콘텐츠 제작 / 음악자료」 폴더에 있고, 커넥터가 「한도 초과」를 돌려주더라도
**실패가 아니다** — 파일을 디스크에 저장하고 경로를 알려준다.

> **2026-09-20 실측 — 지금까지 적혀 있던 값이 틀렸다.**
> 길이는 154초가 아니라 **59.14초**다(320kbps, ffmpeg 로 읽음).
> 1초 단위로 음량을 재 보니 **25초 주기로 반복하지 않는다.** 구조는 이렇다.
>
> | 구간 | 음량(RMS) | 무엇 |
> |---|---|---|
> | 0~31초 | 약 −16 ~ −20 dB | 낮게 깔림 |
> | **32~52초** | 약 −12.4 ~ −14.8 dB | **힘이 붙어 유지되는 구간** |
> | 53~59초 | −17 → −76 dB | 잦아듦 |
>
> 공식대로 「첫 정점 29초」를 쓰면 3회차 시작점은 1.1초가 되고, 실측(32초)대로면
> 4.1초가 된다. **지금은 바꾸지 않기를 권한다.** 이번에 보려는 것은 훅을 숫자에서
> 사건으로 바꾼 효과 하나인데, 음악까지 같이 바꾸면 무엇 때문에 달라졌는지 못 가린다.
> 그 관찰이 끝난 뒤에 따로 손대는 편이 낫다.

---

## 9. 대표님 컴퓨터에서 이어받을 때 (2026-09-20 추가)

클라우드 세션의 작업을 대표님 윈도우 컴퓨터로 옮기고, **다음 회차를 바로 시작할 수 있는
상태까지** 맞춰 놓았다.

### 작업 폴더

`C:\Users\PSJ_1\SJ PARK Project\luckyon-weekly-shorts` 에 이 브랜치를 꺼내 두었다.
같은 저장소를 공유하는 별도 작업 폴더라 커밋·푸시는 평소대로 되고, 옆 폴더
(`..\luckyon-briefing`, 발행용 `claude/live`)는 건드리지 않는다.

> 옆 폴더의 `claude/live` 가 원격보다 **커밋 4개 뒤처져 있다.** 3회차 발행 자산
> 커밋(`102bb2c`)도 그 안에 있다. 발행 산출물을 만질 일이 생기면 먼저 받는다.

### 새로 만든 도구 — `scripts/weekly-shorts/render/`

| 파일 | 하는 일 |
|---|---|
| `fetch-week.mjs` | 대상 주 5분봉을 받아 저장. 봉 개수가 평소와 다르면 경고한다 |
| `parse-calendar.mjs` | 캘린더 HTML 에서 기간·별표로 발표를 뽑는다. 장중/장밖도 표시 |
| `render.mjs` · `scene.js` | 영상 프레임 그리기 (경로 이식형) |
| `srt2.mjs` | 한국어·영어 자막. 기본값이 **자기 폴더에 다시 쓰기**라 검증의 재현 검사를 통과한다 |
| `publish-youtube.mjs` | 유튜브 영상+자막 업로드. `--confirm` 없이는 미리보기만 한다 |

새 회차 폴더는 이 다섯을 복사해 쓴다. 회차마다 바꿀 곳은 파일 안에 주석으로 표시해 두었다.

### 이 컴퓨터에서 실제로 돌려 본 것

**전부 실행해서 확인했다. 「된다더라」가 아니다.**

| 무엇 | 결과 |
|---|---|
| 5분봉 받기 (야후) | 통과 — `NQZ26.CME` 1개월치 4,732봉 |
| 경제 캘린더 받기 | 통과 — 677KB · 발표 72건 파싱 |
| 영상 그리기 (헤드리스 크로미움) | 통과 — 폰트·색·배경사진 정상 |
| 자막 만들기 | 통과 — 발행본과 내용 완전히 같음 |
| **발행 전 검증** | 통과 — **117건 전부 · 종료코드 0** |
| 배경음악 | 통과 — 드라이브 원본과 바이트 단위로 같음 |
| 유튜브 발행 인증 | 통과 — 두 채널 모두 살아 있음 (§5) |
| 인스타그램 발행 경로 | 통과 — vidIQ 로 가능 (아래) |

**재현 검사**: 3회차를 답을 아는 시험 문제로 썼다. 새로 받은 자료로 다시 계산했더니
**봉 1,179개 · 주간 +2.577%** 로 발행본과 같은 값이 나왔다.

### 인스타그램 — 토큰은 `~/.secrets/luckyon-ig.env` 에 있다 (2026-09-26 정정)

`IG_ACCESS_TOKEN` · `IG_USER_ID` · `PAGES_BASE_URL` 세 가지가 **`C:/Users/PSJ_1/.secrets/luckyon-ig.env`**
에 다 있다. 2026-09-26 읽기 전용 호출(`graph.instagram.com/me`)로 **살아 있음을 확인했다**
(계정 `luckyon_77`). `IG_USER_ID` 는 `/me` 가 주는 `id`(앱용 번호)이고, 그 번호로 조회해도
`luckyon_77` 이 나온다. `user_id`(계정 번호)와 다른 것은 정상이다.

> 예전에 여기 적혀 있던 «환경변수에도 `.env` 에도 `keys.env` 에도 없다, 이 컴퓨터에는 토큰이 없다» 는
> **틀렸다.** `keys.env` 한 곳만 보고 쓴 것이다. 인증 파일은 두 곳이다 — `keys.env` 와 `~/.secrets/`.

vidIQ 경로(계정 `luckyon_77` · `38087698064179081`)는 대안으로 남긴다.

> vidIQ 크레딧은 지금 **0 이고 2026-09-30 에 150 으로 충전된다.** 분석 기능(채널 분석·
> 영상 통계)은 1회 5크레딧이라 **4회차 제작 시점에는 못 쓴다.** 평균 시청 시간은
> 유튜브 스튜디오에서 직접 봐야 한다.

### 4회차 — 미리 해 둔 것

| 항목 | 값 | 근거 |
|---|---|---|
| 대상 주 | **2026-09-21(월) ~ 09-25(금)** 미 동부 | 3회차 다음 주. 휴장 없는 5거래일 |
| 심볼 | **`NQZ26.CME`** (12월물) | 9월물은 09-18 셋째 금요일 만기 |

- `content/weekly-shorts/2026-09-21.json` — 매니페스트 뼈대. 지금 알 수 있는 것은 채웠고
  그 주가 끝나야 아는 자리는 「채울 것」으로 표시했다
- `content/weekly-shorts/2026-09-21.calendar-preview.html` — **다음 주 일정 예고편**
- `docs/samples/weekly-shorts/v11-ep4/` — 작업 폴더. 스크립트 세 벌을 4회차 값으로 바꿔 두었다

**그 주에 예정된 발표 (★★ 이상, 미 동부)**

| 요일 | 예정 |
|---|---|
| 월 09-21 | ★★ 굴스비 연설 06:30 · 시카고 연준 전미활동지수 08:30 |
| 화 09-22 | ★★ ADP 주간 고용 08:15 · 윌리엄스 10:05(장중) · 제퍼슨 10:20(장중) · 바킨 13:00(장중) |
| 수 09-23 | ★★ S&P글로벌 PMI 속보치 3건 09:45(장중) · 바 10:05(장중) · EIA 원유재고 10:30(장중) |
| 목 09-24 | ★★ 신규 실업수당청구 08:30 · 경상수지 08:30 · 신규주택판매 10:00(장중) |
| 금 09-25 | **★★★ 내구재 주문 08:30** · ★★ 미시간대 소비자심리 확정치 10:00(장중) |

**그 주 ★★★ 는 금요일 내구재 주문 하나뿐이다.** 규칙상 나머지 나흘은 ★★ 로 내려간다.
예고편이라 발표가 더 붙거나 빠질 수 있고, **기업 실적은 이 캘린더에 안 들어 있다.**

> **새로 알아낸 것 — 캘린더 쿠키 `calendar-range=4` 가 「다음 주」다.**
> 지금까지 1(오늘)·3(이번 주)·−1(어제)·−2(지난 주)·−3(지난 달)만 적혀 있었다.
> 4 를 쓰면 **아직 오지 않은 주의 예정 일정을 미리 받을 수 있다.** 실제값은 당연히 없다.
> 발행용 스냅샷은 그 주가 끝난 뒤 `calendar-range=-2` 로 다시 받아야 한다.

### 회차 실행 순서 — 명령 그대로 (4회차에서 실제로 돈 순서, 2026-09-26)

그 주가 끝난 뒤(토요일)부터. 모든 명령은 작업 폴더에서 실행한다. 아래 날짜·회차 번호만 바꾼다.

**0. 새 회차 폴더** — 틀 세 개를 복사하고 파일 맨 위 «← 회차마다 바꾼다» 줄(날짜 `STAMP`, 자막 이름 `BASE`)만 고친다.
창·사건·문구·보기·정지 시간은 전부 매니페스트(`content/weekly-shorts/<그 주 월요일>.json`)에서 읽는다.

```bash
mkdir -p docs/samples/weekly-shorts/v12-ep5 && cp scripts/weekly-shorts/render/{render.mjs,scene.js,srt2.mjs} docs/samples/weekly-shorts/v12-ep5/
```

**1. 자료** — 5분봉(그 주 월물)과 과거 분포용 1시간봉(연속 심볼 2년). 둘 다 미 동부 시각을 서머타임에 맞게 적는다.

```bash
node scripts/weekly-shorts/render/fetch-week.mjs --symbol=NQZ26.CME --from=2026-09-21 --to=2026-09-25
```

```bash
node scripts/weekly-shorts/render/fetch-week.mjs --symbol=NQ=F --interval=1h --range=2y --from=2026-09-21 --to=2026-09-25 --out=data/weekly-shorts/us1h_nqf.json
```

> **야후 자료의 빈칸 (2026-09-26 확인)** — 야후는 CME 종목의 ① 일요일 첫 10분(18:00·18:05 봉)과
> ② 매일 자정 첫 5분(00:00 봉, 1분봉에도 없음)을 주지 않는다. 창이 월요일 04:00 부터라 ①은 창 밖이지만
> ②는 화~금 4개가 창 안에 빠진다. CNBC 5분봉과 대조해 정답·숫자가 안 바뀌는지 확인하고 매니페스트
> `window.data_gap_note` 에 적는다. CNBC 는 이렇게 받는다(2026-09-26 실제로 받아 확인, 시각은 미 동부):
> `https://ts-api.cnbc.com/harmony/app/bars/@ND.1/5M/20260921040000/20260925160500/unadjusted/EST5EDT.json`

**2. 캘린더** — 스냅샷을 받아 ★★ 이상을 뽑는다.

```bash
curl -sSL -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0 Safari/537.36" -H "Cookie: calendar-range=-2" https://tradingeconomics.com/united-states/calendar -o content/weekly-shorts/2026-09-21.calendar.html
```

```bash
node scripts/weekly-shorts/render/parse-calendar.mjs --html=content/weekly-shorts/2026-09-21.calendar.html --from=2026-09-21 --to=2026-09-25 --minstars=2
```

**3. 표본 → 전체 렌더** — 렌더러가 글꼴·겹침·줄 폭·정지 시간을 먼저 재고, 어긋나면 멈춘다.
표본 시각은 로그의 «사건 멈춤(초)» 구간 안에서, 글이 다 뜬 뒤(멈춘 뒤 0.3초 이후)로 고른다.

```bash
node docs/samples/weekly-shorts/v11-ep4/render.mjs --out=out/ep4/sample --sample=0,4,30,33.7,34.95
```

```bash
node docs/samples/weekly-shorts/v11-ep4/render.mjs --out=out/ep4/frames
```

**4. 인코딩 · 음악 · 표지** — 음악은 3회차와 같은 값이다(곡 1.1초부터, 페이드아웃 32.0초부터 3.0초 —
3회차 완성본 소리 곡선과 대조해 확인한 값). 표지는 훅 글이 다 뜬 뒤 멈춘 4.0초 프레임이다(3회차 표지와 같은 방식).

```bash
node_modules/ffmpeg-static/ffmpeg.exe -y -framerate 30 -i out/ep4/frames/f%05d.jpg -c:v libx264 -preset slow -crf 18 -pix_fmt yuv420p -movflags +faststart out/ep4/silent.mp4
```

```bash
node_modules/ffmpeg-static/ffmpeg.exe -y -ss 1.1 -i assets/bgm/lyria-build-v1.mp3 -t 35 -af "loudnorm=I=-14:TP=-1.5:LRA=11,afade=t=in:st=0:d=3.0,afade=t=out:st=32.0:d=3.0:curve=ipar" -ar 48000 -ac 2 out/ep4/bgm.wav
```

```bash
node_modules/ffmpeg-static/ffmpeg.exe -y -i out/ep4/silent.mp4 -i out/ep4/bgm.wav -map 0:v:0 -map 1:a:0 -c:v copy -c:a aac -b:a 192k -ar 48000 -ac 2 -shortest -movflags +faststart docs/samples/weekly-shorts/v11-ep4/reel4_35s.mp4
```

```bash
node_modules/ffmpeg-static/ffmpeg.exe -y -i docs/samples/weekly-shorts/v11-ep4/reel4_35s.mp4 -ss 4.0 -frames:v 1 docs/samples/weekly-shorts/v11-ep4/cover_ep4.png
```

**5. 자막 · 발행문구 · 기계 검사**

```bash
node docs/samples/weekly-shorts/v11-ep4/srt2.mjs
```

```bash
node scripts/weekly-shorts/verify-shorts.mjs content/weekly-shorts/2026-09-21.json --srt docs/samples/weekly-shorts/v11-ep4 --hist data/weekly-shorts/us1h_nqf.json
```

종료코드 0 이 아니면 발행하지 않는다. **위 1~5 단계는 이제 `make-episode.mjs` 명령 하나가 한다**(검증 에이전트는 없앴다).
올리기는 `publish-episode.mjs --confirm` 하나가 한다(`WEEKLY_SHORTS_QUICK.md`).

### 클라우드 세션의 대화 기록을 가져오려면

파일은 다 왔지만 **대화 기록**은 이 컴퓨터에 없다. 데스크톱 앱 창에서는 못 가져오고,
**터미널에서 연 Claude Code 안에서만** 된다.

```bash
cd "C:\Users\PSJ_1\SJ PARK Project\luckyon-weekly-shorts" && claude
```

그다음 세션 안에서 `/teleport` 를 친다. 같은 저장소가 아니면 막는 장치가 들어 있다.

**주의:** `claude --teleport` 라는 실행 옵션은 **없다.** 설치된 2.1.205 의 도움말
226줄을 전부 확인했다. 세션 안에서 치는 `/teleport` 만 있다.
